---
phase: 06-cv-contact-photo-discoverability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - proxy.ts
  - lib/locales.ts
  - components/not-found-body.tsx
  - app/(en)/writing/not-found.tsx
  - app/(de)/texte/not-found.tsx
  - app/(en)/writing/not-found-page/page.tsx
  - app/(de)/texte/nicht-gefunden/page.tsx
  - tests/unit/proxy-slugs.test.ts
  - tests/writing-not-found.spec.ts
  - tests/global-setup.ts
autonomous: true
requirements: [FIND-02]

must_haves:
  truths:
    - "A visitor with JavaScript disabled who requests /writing/does-not-exist receives HTTP 404 and a document declaring lang=en whose h1 reads 'Not found'"
    - "A visitor with JavaScript disabled who requests /texte/gibt-es-nicht receives HTTP 404 and a document declaring lang=de whose h1 reads 'Nicht gefunden'"
    - "A German slug requested under /writing/ 404s, and an English slug requested under /texte/ 404s — the locale filter is real"
    - "Both published post URLs still return 200 with their own content"
    - "A direct visit to either reserved 404 route returns 404, not 200 — no crawlable soft-404 URL is created"
  artifacts:
    - path: "proxy.ts"
      provides: "Node-runtime request interception for /writing/:slug and /texte/:slug; rewrites unpublished slugs to a per-locale 404 route with status 404"
      contains: "export function proxy"
    - path: "components/not-found-body.tsx"
      provides: "The single localised 404 body shared by both not-found.tsx boundaries and both reserved pages"
      exports: ["NotFoundBody"]
    - path: "app/(en)/writing/not-found-page/page.tsx"
      provides: "The EN proxy rewrite target; exports its own metadata.title"
    - path: "app/(de)/texte/nicht-gefunden/page.tsx"
      provides: "The DE proxy rewrite target; exports its own metadata.title"
    - path: "tests/unit/proxy-slugs.test.ts"
      provides: "The binding test that stops the proxy predicate and publishedFor() from silently diverging"
  key_links:
    - from: "proxy.ts"
      to: "lib/locales.ts NOT_FOUND_SLUG"
      via: "fixed rewrite target constant, never request-derived"
      pattern: "NOT_FOUND_SLUG"
    - from: "tests/unit/proxy-slugs.test.ts"
      to: "lib/content.ts publishedFor"
      via: "set equality between the two predicates, per locale"
      pattern: "publishedFor"
---

<objective>
Close CR-01, the accessibility defect carried out of Phase 2: `/writing/<unknown>` and
`/texte/<unbekannt>` currently return `<html id="__next_error__">` with no `lang` and an empty
body when JavaScript is disabled — a WCAG 3.1.1 Level A failure on the surface a mistyped link
reaches first.

The fix is measured and proven in `06-RESEARCH.md` § "CR-01 — SOLVED AND MEASURED". Do not
re-derive it and do not evaluate alternatives: the self-rewrite variant was measured and does
**not** work, and `dynamicParams = false` drops the German copy `02-UI-SPEC.md` requires.

Purpose: a localised, server-rendered, genuinely-404 error document for unmatched post slugs,
plus the locale filter that stops a German slug resolving under the English layout.
Output: `proxy.ts`, two reserved 404 routes, one shared body component, and the unit test that
binds the proxy's slug predicate to `publishedFor()`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cv-contact-photo-discoverability/06-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. Do not go looking for these in the codebase. -->

From lib/content.ts (existing, unchanged by this plan):
```
export const LOCALES = ["en", "de"] as const;
export type Locale = (typeof LOCALES)[number];
export type PostFrontmatter = { title, standfirst, date, lang: Locale, translationKey, draft?: boolean, type?: "post" | "case-study" };
export type PostEntry = { slug: string; frontmatter: PostFrontmatter };
export async function publishedFor(lang: Locale): Promise<PostEntry[]>;   // async; loads MDX modules
const CONTENT_DIR = path.join(process.cwd(), "content");                  // module-scope constant, ASVS V12
const SAFE_SLUG = /^[a-z0-9]+(?:-[a-z0-9]+)*$/;
```

From lib/locales.ts (existing):
```
export const PATH_TOKEN: Record<Locale, string> = { en: "writing", de: "texte" };
export function indexPath(locale: Locale): string;   // "/writing" | "/texte"
export function postPath(locale: Locale, slug: string): string;
export const UI: Record<Locale, UiCopy>;             // .notFoundHeading .notFoundBody .backLink
```

Content directory on disk today (front-matter is a `---` fenced YAML block at the top of each file):
```
content/the-chart-therefore-changes.mdx    lang: en   draft: false
content/die-darstellung-aendert-sich.mdx   lang: de   draft: false
content/fixture.mdx                        lang: en   draft: true
content/musterseite.mdx                    lang: de   draft: true
content/nur-auf-deutsch.md                 lang: de   draft: true
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: proxy.ts, the two reserved 404 routes, and the one shared 404 body</name>
  <files>proxy.ts, lib/locales.ts, components/not-found-body.tsx, app/(en)/writing/not-found.tsx, app/(de)/texte/not-found.tsx, app/(en)/writing/not-found-page/page.tsx, app/(de)/texte/nicht-gefunden/page.tsx</files>
  <read_first>
    - `.planning/phases/06-cv-contact-photo-discoverability/06-RESEARCH.md` § "1. CR-01 — SOLVED AND MEASURED" (lines 211-266) — the measured result table, the six things the planner must know, and Pattern 1 at lines 701-731
    - `.planning/phases/02-content-pipeline/deferred-items.md` — CR-01's original root-cause isolation
    - `app/(en)/writing/not-found.tsx` and `app/(de)/texte/not-found.tsx` (23 lines each) — the exact JSX to extract
    - `lib/locales.ts:1-33` — `PATH_TOKEN`, `indexPath`, `postPath`, and the `UI` map at `:63-88`
    - `components/smear-title.tsx` — the server/client boundary the 404 heading uses
    - `lib/content.ts:25` (`CONTENT_DIR`), `:122` (`SAFE_SLUG`), `:236` (`publishedFor`)
  </read_first>
  <action>
Create the file as `proxy.ts` at the repository root, beside `app/`. It is NOT `middleware.ts` —
that name is deprecated in Next 16.3.3 and prints `⚠ The "middleware" file convention is
deprecated` on every build. Export a function named `proxy`. Do NOT add a `runtime` key: `proxy`'s
runtime is nodejs and is not configurable.

Export `config` with `matcher: ["/writing/:slug", "/texte/:slug"]` — one segment only, so
`/writing/a/b` falls through to the global 404 as it does today.

The proxy body: split `request.nextUrl.pathname`, derive the locale from the first segment
(`texte` maps to `de`, otherwise `en`), and call a published-slug predicate. On a hit return
`NextResponse.next()`. On a miss, clone `request.nextUrl`, set `pathname` to the reserved
constant for that locale, and return `NextResponse.rewrite(url, { status: 404 })`. The status is
the entire point — it reaches the wire and Next injects `<meta name="robots" content="noindex">`
for any response at or above 400, so these pages stay unindexed after FIND-02 with no extra code.

ASVS V5/V12: the rewrite target is built from a fixed constant, never by interpolating any part of
the request path. State that in a comment. Do not add an error boundary, do not log the requested
path, and do not echo the slug into the rendered document.

The predicate reads `content/` with `node:fs` at request time (this is the measured-working
option; Next's file tracing adds `content/*.mdx` to the proxy's `.nft.json` automatically). It
must: use a module-scope directory constant derived from `process.cwd()` exactly as
`lib/content.ts:25` does, never a request-derived path component; reject any slug failing the same
`SAFE_SLUG` shape; read each `.md`/`.mdx` file's leading `---` front-matter block; and admit a
slug only when its `lang` equals the requested locale AND its `draft` is not `true`. The locale
filter is not optional — without it `/writing/die-darstellung-aendert-sich` would serve German
content under an English layout with a 200. Export the predicate so Task 2 can test it.

In `lib/locales.ts` add `NOT_FOUND_SLUG: Record<Locale, string>` = `{ en: "not-found-page", de:
"nicht-gefunden" }` and a `notFoundPath(locale)` helper returning `/writing/not-found-page` and
`/texte/nicht-gefunden`. This is a path constant beside `PATH_TOKEN`, not UI copy — do not add any
string to the `UI` map (D-1.5 forbids growing `Record<Locale, UiCopy>`). The proxy and the sitemap
(plan 06-05) both read these constants so the two cannot drift.

Create `components/not-found-body.tsx` exporting `NotFoundBody({ locale })`, containing exactly
the JSX currently duplicated in the two `not-found.tsx` files: the `main` with
`flex min-h-screen flex-col justify-center gap-md px-lg`, the `SmearTitle as="h1"
className="text-heading"` carrying `UI[locale].notFoundHeading`, the `p.text-body` carrying
`UI[locale].notFoundBody`, and the `Link` to `indexPath(locale)` with
`text-label link-quiet inline-block py-xs` carrying `UI[locale].backLink`. Rewrite both existing
`not-found.tsx` files to render `<NotFoundBody locale="en" />` / `locale="de"` and nothing else.
Four copies of two locales is how the German drifts.

Create the two reserved pages rendering the same component. Each MUST export its own
`metadata.title` — `"Not found — Guillem Gelabert"` and `"Nicht gefunden — Guillem Gelabert"`. The
research prototype measured the served 404 falling back to the layout default `<title>Guillem
Gelabert</title>` without this, which is a WCAG 2.4.2 regression against what
`app/global-not-found.tsx` already achieves. Neither reserved page declares `robots` — Next
injects noindex from the 404 status, and that field stays confined to the two root layouts.
  </action>
  <verify>
    <automated>rm -rf .next && npm run build 2>&1 | tee /tmp/b6.log; /usr/bin/grep -c 'middleware.*deprecated' /tmp/b6.log; npx next start -p 3199 & sleep 8; for p in /writing/does-not-exist /texte/gibt-es-nicht /writing/die-darstellung-aendert-sich /texte/the-chart-therefore-changes /writing/not-found-page /texte/nicht-gefunden /writing/the-chart-therefore-changes /texte/die-darstellung-aendert-sich /nope; do curl -s -o /tmp/b.html -w "$p %{http_code} " "http://localhost:3199$p"; /usr/bin/grep -o '<html lang="[a-z]*"' /tmp/b.html | head -1; done; kill %1</automated>
  </verify>
  <acceptance_criteria>
    - `next build` output contains zero occurrences of the string `middleware` deprecation warning (the grep -c above returns 0)
    - `/writing/does-not-exist` returns status 404 with `<html lang="en"` and `<h1 ...>Not found` present in the curl body (no JavaScript involved)
    - `/texte/gibt-es-nicht` returns status 404 with `<html lang="de"` and `<h1 ...>Nicht gefunden` present in the curl body
    - `/writing/die-darstellung-aendert-sich` returns 404 (cross-locale rejected) and `/texte/the-chart-therefore-changes` returns 404
    - `/writing/the-chart-therefore-changes` and `/texte/die-darstellung-aendert-sich` both return 200
    - `/writing/not-found-page` and `/texte/nicht-gefunden` each return 404 on a direct visit, not 200
    - `/nope` still returns 404 and still renders the global not-found copy
    - Both 404 responses carry a non-empty `<title>` that is not the bare string `Guillem Gelabert`
    - `proxy.ts` contains `export function proxy` and contains no `runtime` key
    - `git grep -c 'min-h-screen flex-col justify-center' -- 'app/**/not-found.tsx' 'app/**/not-found-page/**' 'app/**/nicht-gefunden/**'` returns 0 — the JSX lives only in `components/not-found-body.tsx`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Unmatched and cross-locale post slugs return a real 404 carrying the correct language and localised copy in the server HTML with no JavaScript, on both locales; published routes and the global 404 are unaffected; the 404 body exists in exactly one source file.</done>
</task>

<task type="auto">
  <name>Task 2: the binding unit test — the proxy predicate and publishedFor() must agree</name>
  <files>tests/unit/proxy-slugs.test.ts</files>
  <read_first>
    - `proxy.ts` as written in Task 1 — the exported predicate's signature
    - `lib/content.ts:170-250` — `showDrafts`, `isVisible`, `selectForLocale`, `publishedFor`
    - `tests/unit/content.test.ts` — the house style for `node:test` against `lib/content.ts`
    - `.planning/phases/06-cv-contact-photo-discoverability/06-RESEARCH.md` lines 253-262 (the four options table and why the binding test is not optional) and A3 at line 1011
  </read_first>
  <action>
The proxy restates the draft rule a second time, which is precisely what
`tests/build/prerender.test.ts`'s own closing forward note warns against. This test is what makes
that duplication safe, and it is the reason the `fs` option was chosen over the three untested
alternatives.

Assert, for each of `en` and `de` independently, that the set of slugs the proxy predicate admits
is exactly equal to the set of slugs `publishedFor(locale)` returns. Compare as sorted arrays so a
failure names the differing slug rather than reporting a boolean.

`publishedFor` is async and gated by `showDrafts()`, which reads `process.env.NODE_ENV`. Set
`NODE_ENV` to `production` for the comparison so both sides agree on draft visibility, and assert
in a second case that under `development` the proxy predicate and `publishedFor` still agree —
whichever way the flag falls, the two must not diverge.

Add three explicit regression cases with named expectations rather than only the set comparison:
the English case-study slug is admitted for `en` and rejected for `de`; the German case-study slug
is admitted for `de` and rejected for `en`; and every draft fixture slug (`fixture`,
`musterseite`, `nur-auf-deutsch`) is rejected for both locales in production.

Add a fourth case asserting the predicate rejects slug shapes that fail `SAFE_SLUG` — at minimum
`../secret`, `a/b`, `UPPER`, and the empty string — so a path-traversal attempt never reaches the
`next()` branch.

Write a comment at the top stating in one sentence that this file is the sole guarantee against
the two predicates drifting, and that deleting it re-opens the failure mode where a published post
404s or a draft becomes reachable.
  </action>
  <verify>
    <automated>npm run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:unit` passes with the new file included and the total test count is strictly greater than 102
    - The file asserts set equality between the proxy predicate and `publishedFor()` for both `en` and `de`
    - The file contains explicit cases for the cross-locale rejection in both directions, named by slug
    - The file contains a case rejecting `../secret`, `a/b`, `UPPER` and `""`
    - Temporarily adding `draft: true` to `content/the-chart-therefore-changes.mdx` makes this file fail and restoring it makes it pass (run once, then restore; record the observed failure message in the summary)
  </acceptance_criteria>
  <done>A single `node --test` file proves the proxy's slug predicate and `lib/content.ts`'s `publishedFor()` return identical sets per locale, and the drift it prevents has been demonstrated red once.</done>
</task>

<task type="auto">
  <name>Task 3: replace the KNOWN GAP block with no-JS and cross-locale assertions</name>
  <files>tests/writing-not-found.spec.ts, tests/global-setup.ts</files>
  <read_first>
    - `tests/writing-not-found.spec.ts` in full (133 lines) — especially `LOCALE_CASES` at `:23-38`, the `UNMATCHED_PATHS` no-JS loop at `:44-91` (the shape to copy), the two `LOCALE_CASES` tests at `:93-121`, and the 14-line KNOWN GAP comment block at `:123-133`
    - `tests/global-setup.ts:16-25` — the `ROUTES` warm-up list
    - `.planning/phases/06-cv-contact-photo-discoverability/06-RESEARCH.md` lines 586-592 — the four required additions
  </read_first>
  <action>
Delete the KNOWN GAP block at `:123-133` and replace it with a short comment recording the
measured resolution: the boundary now renders because `proxy.ts` rewrites to a real page rather
than the segment throwing `notFound()`, the status is set at the proxy tier because App Router
pages cannot set one, and the German copy is preserved — which `dynamicParams = false` could not
do. Do not delete the file's existing tests.

Convert the two `LOCALE_CASES` tests at `:93-121` to run in a `javaScriptEnabled: false` context,
using the same `browser.newContext(...)` / `try` / `finally { await context.close() }` shape the
`UNMATCHED_PATHS` loop at `:46-91` already uses. A context with JavaScript on waits for hydration
and would pass against a blank shell, which is exactly the defect these assertions exist to
detect. Per case assert: status 404; `document.documentElement.lang` equals `en` / `de`;
`page.title()` is non-empty AND is not the bare string `Guillem Gelabert`; the `h1` text equals
the localised heading; the localised body text is visible; and the back link's `href` equals
`/writing` / `/texte`.

Keep the existing `link-quiet` + 24px target-floor test per locale as-is (it needs layout, so it
stays JavaScript-enabled).

Add a cross-locale table with two rows — `/writing/die-darstellung-aendert-sich` expecting the
English 404 document, and `/texte/the-chart-therefore-changes` expecting the German one — asserting
404, the correct `lang`, and the correct localised `h1`. This is the case that catches a slug
allowlist that forgot the locale filter, and a name-only match passes every other test in this
file.

Add a third case per locale: a direct request to the reserved rewrite target
(`/writing/not-found-page`, `/texte/nicht-gefunden`) must return 404, not 200 — the reserved routes
are self-guarding and must stay that way.

In `tests/global-setup.ts` add `/texte/gibt-es-nicht`, `/writing/not-found-page` and
`/texte/nicht-gefunden` to `ROUTES`. The dev server compiles routes lazily and a cold route under
`fullyParallel` blows a 5s locator timeout — a measured 1-in-3 flake. Every new route this phase
adds goes in this list.
  </action>
  <verify>
    <automated>npx playwright test tests/writing-not-found.spec.ts --repeat-each=2</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test tests/writing-not-found.spec.ts --repeat-each=2` passes with zero failures and zero flakes
    - The string `KNOWN GAP` no longer appears in `tests/writing-not-found.spec.ts`
    - Both `LOCALE_CASES` status/lang/title/h1 tests run inside a context created with `javaScriptEnabled: false`
    - Cross-locale cases exist for `/writing/die-darstellung-aendert-sich` and `/texte/the-chart-therefore-changes`, each asserting 404 plus the opposite locale's document language
    - Direct-hit cases exist for `/writing/not-found-page` and `/texte/nicht-gefunden`, each asserting 404
    - `tests/global-setup.ts` `ROUTES` contains all three new paths
    - `npm test` passes in full (127 pre-existing Playwright tests plus the additions)
  </acceptance_criteria>
  <done>The CR-01 known gap is closed in the test suite as well as in the code: the localised 404s are asserted with JavaScript disabled, cross-locale slugs are asserted to 404 in the right language, and the reserved targets are asserted non-crawlable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crawler/visitor → `proxy.ts` | Attacker-controlled URL path segments reach a new server-tier component for the first time in this milestone |
| `proxy.ts` → filesystem (`content/`) | The predicate reads a directory at request time |
| `proxy.ts` → App Router render | A rewrite target path is chosen server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Elevation of Privilege | `proxy.ts` rewrite target | mitigate | Rewrite path is built from `NOT_FOUND_SLUG` in `lib/locales.ts`, a fixed constant. No request-derived component is ever interpolated into `url.pathname`. Asserted by reading the source in task 1's acceptance criteria. |
| T-06-02 | Elevation of Privilege | `proxy.ts` slug predicate → `content/` read | mitigate | Directory is a module-scope constant from `process.cwd()` (the `lib/content.ts:25` posture); the slug is rejected unless it matches `SAFE_SLUG` before any filesystem access. Test case in task 2 covers `../secret`, `a/b`, `UPPER`, `""`. |
| T-06-03 | Elevation of Privilege | `[slug]/page.tsx` allowlist | accept | ASVS V4: the proxy is defence in depth only. `findBySlug(publishedFor(locale), slug)` before `loadPostModule` stays in place unchanged — this plan modifies neither `[slug]/page.tsx` nor `lib/content.ts`. |
| T-06-04 | Tampering | Open redirect via the proxy | mitigate | `NextResponse.rewrite` only; `NextResponse.redirect` is not used anywhere in `proxy.ts`. A rewrite cannot send a visitor to an attacker-chosen origin. |
| T-06-05 | Information Disclosure | The 404 document | mitigate | The reserved pages render `NotFoundBody`, which contains only static localised copy. The requested path is never echoed into the response and nothing is logged. |
| T-06-06 | Spoofing | Draft content reachable via a forgotten locale filter | mitigate | Task 2's set-equality test per locale, plus the two named cross-locale regression cases. |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs nothing. `06-RESEARCH.md` § Package Legitimacy Audit records zero packages for this phase. If any install becomes necessary, stop and gate it behind a blocking human checkpoint. |
</threat_model>

<verification>
1. `rm -rf .next && npm run build` — build succeeds and prints no `middleware` deprecation warning.
2. `npx next start` + the nine-path curl loop in Task 1 — every status and every `<html lang>` matches.
3. `npm run test:unit` — passes, count above 102.
4. `npx playwright test tests/writing-not-found.spec.ts --repeat-each=2` — passes, no flakes.
5. `npx tsc --noEmit` — exits 0.
6. `npm run lint` — exactly one error, `use-prefers-reduced-motion.ts:23` (the known deferred item). No new errors.
7. `git diff --stat components/smear-heading/` — empty. This plan must not touch that module.
</verification>

<success_criteria>
- Both localised post-slug 404s server-render with the correct `lang`, the correct localised heading and body, a non-default `<title>`, and HTTP 404, with JavaScript disabled.
- Cross-locale slugs 404 in the requesting URL's language.
- Both reserved rewrite targets 404 on a direct visit.
- The 404 body exists in exactly one component; the two `not-found.tsx` files and the two reserved pages all render it.
- A unit test binds the proxy's predicate to `publishedFor()` per locale and has been shown to go red on an induced drift.
- `robots: { index: false }` in both root layouts is untouched by this plan.
</success_criteria>

<output>
Create `.planning/phases/06-cv-contact-photo-discoverability/06-01-SUMMARY.md` when done.
Record: the reserved route names as shipped, the measured curl table (all nine paths), the observed
red message from Task 2's induced drift, and the proxy latency if measured.
</output>
</content>
</invoke>
