---
phase: 01-deploy-foundation-design-system
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - package.json
  - package-lock.json
  - app/fonts/Humane-VF.ttf
  - app/fonts/humane.ts
  - app/fonts/newsreader.ts
  - app/globals.css
  - text_trail_demo/assets/Humane-VF.ttf
autonomous: true
requirements: [BUILD-06, HOME-05]

must_haves:
  truths:
    - "Any page that imports app/globals.css has a self-hosted Humane display face (no external font-fetch) and a self-hosted-via-next/font Newsreader body face available to it"
  artifacts:
    - path: "app/fonts/humane.ts"
      provides: "next/font/local Humane loader, weight '100 900', display:optional"
      exports: ["humane"]
    - path: "app/fonts/newsreader.ts"
      provides: "next/font/google Newsreader loader, display:swap"
      exports: ["newsreader"]
    - path: "app/globals.css"
      provides: "Tailwind v4 @theme tokens + clamp()-based type-scale classes"
      contains: "@theme"
  key_links:
    - from: "app/globals.css .text-display rule"
      to: "app/fonts/humane.ts --font-humane CSS variable"
      via: "--font-display: var(--font-humane), Impact, sans-serif;"
      pattern: "--font-display: var\\(--font-humane\\)"
---

<objective>
Wire the two locked typefaces (Humane display, Newsreader body) through `next/font`, and lay down
the Tailwind v4 `@theme` design tokens plus the `clamp()`-based type-scale rules that make the
typographic system real CSS, not framework-default styling.

Purpose: This is the mechanism half of HOME-05/BUILD-06 — the font loaders and design tokens that
Plan 03's routes will render, and that Plan 04's heading trail will run on top of. Nothing here is
visible to a visitor yet (no route consumes these tokens until Plan 03); this plan produces the
contracts, not the checkable artifact.
Output: `app/fonts/humane.ts`, `app/fonts/newsreader.ts`, and a rewritten `app/globals.css` carrying
the locked palette, spacing scale, and four type-scale roles (Display/Heading/Body/Label).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-deploy-foundation-design-system/01-CONTEXT.md
@.planning/phases/01-deploy-foundation-design-system/01-RESEARCH.md
@.planning/phases/01-deploy-foundation-design-system/01-UI-SPEC.md
@.planning/phases/01-deploy-foundation-design-system/01-01-SUMMARY.md
</context>

<interfaces>
<!-- This plan CREATES the font/token contracts Plan 03's routes and Plan 04's trail consume. -->

**app/fonts/humane.ts contract:**
```ts
import localFont from 'next/font/local'
export const humane = localFont({
  src: './Humane-VF.ttf',
  weight: '100 900',
  display: 'optional',
  variable: '--font-humane',
})
```

**app/fonts/newsreader.ts contract:**
```ts
import { Newsreader } from 'next/font/google'
export const newsreader = Newsreader({
  subsets: ['latin'],
  display: 'swap',
  variable: '--font-newsreader',
})
```

**app/globals.css class contract (consumed verbatim by Plan 03's `app/page.tsx`/`app/type/page.tsx`):**
- `.text-display` — Humane, `clamp(3.5rem, 1.5rem + 8vw, 11.25rem)`, weight 800, line-height 0.82
- `.text-heading` — Humane, `clamp(2rem, 1rem + 4vw, 4.5rem)`, weight 800, line-height 0.9
- `.text-body` — Newsreader, 18px fixed, weight 400, line-height 1.6
- `.text-label` — Newsreader, 14px fixed, weight 400, line-height 1.3, uppercase, letter-spacing 0.04em
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install the typography plugin, move the Humane asset, create both font loaders</name>
  <files>package.json, package-lock.json, app/fonts/Humane-VF.ttf, app/fonts/humane.ts, app/fonts/newsreader.ts, text_trail_demo/assets/Humane-VF.ttf</files>
  <read_first>text_trail_demo/index.html (lines 8-14 @font-face declaration, lines 16-24 root palette vars — for value cross-reference only), .planning/phases/01-deploy-foundation-design-system/01-CONTEXT.md (D-01, D-02), .planning/phases/01-deploy-foundation-design-system/01-UI-SPEC.md (Design System table, "Why Newsreader", "Why font-display: optional for Humane, swap for Newsreader"), .planning/phases/01-deploy-foundation-design-system/01-RESEARCH.md (Architecture Pattern 2, Code Examples "next/font/local"/"next/font/google", Pitfall 8, Pitfall 10, Assumption A1)</read_first>
  <action>
    Move the font asset, do not copy it: `git mv text_trail_demo/assets/Humane-VF.ttf app/fonts/Humane-VF.ttf`. Do not run any subsetting/optimization tool (`glyphhanger`, `fonttools`, or similar) against it at any point — the licence forbids modification without written permission (D-01, Pitfall 8).

    Install the one new dependency this plan needs: `npm install @tailwindcss/typography@0.5.20`. `tailwindcss@4.3.3` and `@tailwindcss/postcss@4.3.3` were already installed by `create-next-app --tailwind` in Plan 01 — confirm both are present in `package.json` before running this install; do not reinstall them.

    Create `app/fonts/humane.ts` exactly per the Interfaces contract above: `weight: '100 900'` (the full variable axis range), `display: 'optional'` — this is a deliberate deviation from the demo's `font-display: swap` (UI-SPEC's resolution: Humane is self-hosted/same-origin/85KB, and a fallback-sans substitution at poster scale would look broken, not just different).

    Create `app/fonts/newsreader.ts` exactly per the Interfaces contract above: `display: 'swap'` (Newsreader must always end up in the correct face even at the cost of a brief FOUT — it carries the real reading load in later phases). Confirm at this step that `Newsreader` is a valid named export of `next/font/google` in the installed Next.js version (Pitfall 10 / Assumption A1 — the exact `opsz`/`wght`/`ital` axis ranges were not directly confirmed against the live typed export); if the typed import rejects a supplied option, adjust to match the actual exported axis range rather than guessing.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `test -f app/fonts/Humane-VF.ttf` succeeds and `test -f text_trail_demo/assets/Humane-VF.ttf` fails (asset moved, not duplicated)
    - `app/fonts/humane.ts` exports `humane` from `localFont({ src: './Humane-VF.ttf', weight: '100 900', display: 'optional', variable: '--font-humane' })`
    - `app/fonts/newsreader.ts` exports `newsreader` from `Newsreader({ ..., display: 'swap', variable: '--font-newsreader' })`
    - `package.json` lists `@tailwindcss/typography` at `0.5.20`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Both font loader modules exist, compile cleanly, and the Humane binary exists only at its new `app/fonts/` path.</done>
</task>

<task type="auto">
  <name>Task 2: Build the design-token stylesheet — Tailwind v4 @theme + clamp() type scale + palette + spacing</name>
  <files>app/globals.css</files>
  <read_first>app/globals.css (current create-next-app scaffold output, to know exactly what is being replaced), .planning/phases/01-deploy-foundation-design-system/01-UI-SPEC.md (Typography table, Spacing Scale table, Color table, "Weight discipline — exactly 2 weights in the system", "Letter-spacing cap"), .planning/phases/01-deploy-foundation-design-system/01-RESEARCH.md (Code Examples "Tailwind v4 @theme wired to next/font CSS variables")</read_first>
  <action>
    Replace `app/globals.css` entirely. Keep `@import "tailwindcss";` as the first line; add `@plugin "@tailwindcss/typography";` immediately beneath it (registers the plugin Phase 2's `prose` classes will consume — install and register now per D-04, even though nothing in this phase renders a `prose` block).

    Add a `@theme` block defining, verbatim: `--font-display: var(--font-humane), Impact, sans-serif;`, `--font-body: var(--font-newsreader), serif;`, `--color-paper: #f2eee5;`, `--color-ink: #171714;`, `--color-accent: #C1272D;`, and the seven spacing tokens `--spacing-xs: 4px;` through `--spacing-3xl: 64px;` per UI-SPEC's Spacing Scale table (`xs`=4, `sm`=8, `md`=16, `lg`=24, `xl`=32, `2xl`=48, `3xl`=64, all px).

    Below `@theme`, add the four plain-CSS type-scale classes (D-04's split — these are `clamp()` formulas, not static `@theme` tokens): `.text-display` and `.text-heading` per the Interfaces contract's exact `clamp()` values, weight 800, letter-spacing `0em`; `.text-body` and `.text-label` at fixed pixel sizes per the Interfaces contract, weight 400, `.text-label` additionally `text-transform: uppercase; letter-spacing: 0.04em;`. Do not exceed `0.04em` tracking on any multi-word text (UI-SPEC's documented VoiceOver/NVDA risk threshold) and do not introduce a second weight for either face (UI-SPEC's "exactly 2 weights in the system" budget: Newsreader 400 only, Humane 800 only).

    Set `body { background: var(--color-paper); color: var(--color-ink); }`. The benchmark's ornamental horizontal-rule background (`text_trail_demo/index.html:38-40`) is explicitly optional polish per UI-SPEC ("ship only after the five Phase 1 success criteria are met") — do not add it in this task.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `app/globals.css` contains `@theme` and, within it, `--color-ink: #171714`, `--color-paper: #f2eee5`, `--color-accent: #C1272D`
    - `grep -c 'clamp(3.5rem, 1.5rem + 8vw, 11.25rem)' app/globals.css` returns `1` (`.text-display` rule present with the exact locked curve)
    - `grep -c 'clamp(2rem, 1rem + 4vw, 4.5rem)' app/globals.css` returns `1` (`.text-heading` rule present with the exact locked curve)
    - `grep -c 'font-weight: 800' app/globals.css` and no occurrence of any other Humane weight value in the file
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>`app/globals.css` defines the `@theme` tokens and all four type-scale classes exactly per UI-SPEC's Typography/Spacing/Color tables; `npm run build` succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| npm registry → local install | `@tailwindcss/typography` crosses from a public registry into the build |
| Licensed asset → build pipeline | `Humane-VF.ttf` carries a no-modification licence term; the build pipeline must not alter the binary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-01-04 | Tampering | npm install (`@tailwindcss/typography`) | mitigate | Already in 01-RESEARCH.md's Package Legitimacy Audit (slopcheck OK, Approved) — install the exact audited version `0.5.20`, no substitution. |
| T-01-05 | Tampering | Humane font asset modified/subset in violation of its licence | mitigate | `git mv` only, no subsetting/optimization tool invoked anywhere in this plan's tasks — verified by the acceptance criterion that the source path no longer exists and no build step touches the binary. |
| T-01-06 | Information Disclosure | N/A this plan (no HTTP-serving change) | accept | Real security response headers remain Phase 6's job (BUILD-04) — noted here only for consistency across this phase's threat models. |
</threat_model>

<verification>
- `npm run build` exits 0 after both tasks
- `app/fonts/humane.ts` and `app/fonts/newsreader.ts` export typed font objects consumable by `variable` className
- `app/globals.css` contains the exact locked `clamp()` curves and palette values
</verification>

<success_criteria>
The typographic system's mechanism exists as real CSS/font-loader code — self-hosted Humane via `next/font/local`, self-hosted-via-next/font Newsreader, and Tailwind v4 `@theme` tokens plus `clamp()` type-scale rules — ready for Plan 03's routes to render.
</success_criteria>

<output>
Create `.planning/phases/01-deploy-foundation-design-system/01-02-SUMMARY.md` when done
</output>
