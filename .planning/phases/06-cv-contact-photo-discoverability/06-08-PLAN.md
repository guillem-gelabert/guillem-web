---
phase: 06-cv-contact-photo-discoverability
plan: 08
type: execute
wave: 3
depends_on: [06-04]
files_modified:
  - tests/contact.spec.ts
  - tests/cv.spec.ts
  - tests/landing.spec.ts
  - tests/landing-trail.spec.ts
autonomous: true
requirements: [PROF-01, PROF-02, PROF-03, PROF-04, PROF-05]

must_haves:
  truths:
    - "The email link is reachable by keyboard, carries the address as its accessible name, and its textContent equals the real address so it can be selected and copied"
    - "The portrait decodes rather than merely returning 200, and loading it shifts nothing on the page"
    - "The /cv heading's smear origin is unchanged before and after the portrait loads"
    - "Absent channels render as nothing at all — no empty list item, no disabled control"
  artifacts:
    - path: "tests/contact.spec.ts"
      provides: "PROF-03's three-part accessibility test in the browser, plus channel presence on both surfaces"
    - path: "tests/cv.spec.ts"
      provides: "Extended with the portrait, the CLS guard, and the sections' heading level"
    - path: "tests/landing-trail.spec.ts"
      provides: "Extended with D-2.6's smear-origin invariance across image load"
  key_links:
    - from: "tests/contact.spec.ts"
      to: "the rendered mailto anchor on / and /cv"
      via: "getByRole('link') accessible name plus textContent equality"
      pattern: "mailto:"
    - from: "tests/cv.spec.ts"
      to: "PerformanceObserver layout-shift entries"
      via: "cumulative shift score measured across the portrait's load"
      pattern: "layout-shift"
---

<objective>
The browser tier for PROF-01 through PROF-05. Every Playwright spec in this repo runs against
`npm run dev`, so this tier proves what a *browser does* with the page; what production *emits* is
plan 06-09's job in `tests/build/`. Keep that division — a `textContent` check here reads the decoded
DOM and would pass even if the served bytes carried `&amp;#64;`, which is exactly the failure mode
Pitfall 3 describes.

Two of these assertions exist because a source-level gate structurally cannot catch what they catch.
`<strong>` outside `.prose-site` resolves to 700 under Tailwind preflight — a third weight on screen
that the CSS budget tests cannot see, because they read `app/globals.css` only. And an arbitrary
Tailwind value in a `.tsx` file passes every existing gate silently. This plan owns the `/cv` and
contact half of that; plan 06-10's `tests/design-budget.spec.ts` owns the site-wide sweep.

Purpose: prove the surfaces plan 06-04 built, including with the user-supplied values present, not
only in their absent state.
Output: one new spec and three extended ones.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cv-contact-photo-discoverability/06-VALIDATION.md

<interfaces>
Assertion rules this repo already follows, all three load-bearing here:

  1. Assert MEASURED computed values, never values derived from a plan's arithmetic. Phase 1's scar
     tissue is a computed value assumed from a plan that was wrong by 40px.
  2. Call page.emulateMedia({ reducedMotion: "reduce" }) BEFORE page.goto(). Playwright's
     reducedMotion context/test option does not reliably affect matchMedia in this
     environment (1.62.1/Chromium) — recorded in STATE.md.
  3. Production truth lives in tests/build/; dev truth lives in Playwright. npm run dev always
     shows drafts, so a spec structurally cannot prove what a production build ships.

Shipped state this plan asserts against:

  lib/contact.ts   EMAIL null, LINKEDIN null, GITHUB "https://github.com/guillem-gelabert"
  lib/cv.ts        EXPERIENCE [], PORTRAIT null, CV_STUB_BODY "The CV is being written up as a page."
  components/contact-block.tsx   rendered on both / (#contact) and /cv (foot)
  components/portrait.tsx        returns null while PORTRAIT is null

Existing assertions that constrain this plan:

  tests/cv.spec.ts:19-23    h1 toHaveCount(1) — CV sections must be h2, never a second h1
  tests/cv.spec.ts:57-68    six banned marker words in body text: todo, placeholder, coming soon,
                            under construction, lorem, tbd
  tests/landing.spec.ts:125 meta[name=description] equals the rendered positioning <p>
  tests/landing.spec.ts:455-465  a comment still describing SectionStub as serving #contact —
                            stale after plan 06-04 deleted the component
  tests/landing.spec.ts:700-710  the existing note about <strong> rendering 700
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: tests/contact.spec.ts — PROF-03's three-part test, and channel presence on both surfaces</name>
  <files>tests/contact.spec.ts</files>
  <read_first>
    - `.planning/phases/06-cv-contact-photo-discoverability/06-CONTEXT.md:121-136` — D-2.3 and the three-part acceptance test PITFALLS #5 names
    - `.planning/research/PITFALLS.md` #5 — the three failure modes and why each is rejected
    - `.planning/phases/06-cv-contact-photo-discoverability/06-RESEARCH.md:733-779` — Patterns 2 and 3, including the measured decoded output
    - `components/contact-block.tsx` as written by plan 06-04
    - `tests/landing.spec.ts` — the house style for locating landing sections by `#id`
    - `lib/contact.ts`
  </read_first>
  <action>
PITFALLS #5's acceptance is three things and this file owns two of them. Tab to it with a keyboard;
read it with a screen reader; select and copy it. The screen-reader pass is a recorded manual check
owned by plan 06-11; the other two are automated here.

The problem: `EMAIL` is `null` today, so there is nothing to assert against. Do not skip the tests
and do not assert only the absent state — write them so they exercise a **real address** without
inventing a public one. Use a fixture address that is obviously a fixture, injected by rendering the
contact block with a passed-in value or by a route the spec controls, and state in a comment that
the fixture is not and must never become the shipped value. Then assert the absent state separately.

With a fixture address present, assert all three legs:
- **Keyboard.** The link is reachable by `Tab` from the section heading — walk focus and assert the
  mailto anchor receives it — and it shows a visible focus ring. The site's focus treatment is a 2px
  accent outline with 2px offset; assert the computed `outline-color` is non-transparent when
  focused, measured rather than assumed.
- **Accessible name.** `getByRole("link", { name: <the address> })` resolves to exactly one element.
  This is the assertion that fails if the entities leak into the accessible name.
- **Copyable.** The anchor's `textContent` equals the real address exactly, and its `href` equals
  `mailto:` plus the real address. The browser decodes entities at parse time, so both come out
  clean — and if they do not, the obfuscation is broken in the way that matters.

Also assert the target size: the channel links are Label-role and standalone, so each must clear the
WCAG 2.5.8 24px floor. Assert the measured `getBoundingClientRect().height` is at least 24, after
`document.fonts.ready`.

Then the absent state, against the real shipped module: with `EMAIL` and `LINKEDIN` null, the contact
block on `/` renders exactly one channel row (GitHub), zero elements whose accessible name contains
`mailto`, zero `[disabled]` attributes and zero elements with `aria-disabled`. A dead affordance is
worse than no affordance, and this is where that is provable.

Assert the same block renders on both `/` (inside `#contact`) and `/cv` (at the foot), and that the
GitHub link is same-tab: no `target` attribute at all.

Add `/cv` and `/` to the spec via the existing `baseURL`; both are already warmed in
`tests/global-setup.ts`, so no change there.
  </action>
  <verify>
    <automated>npx playwright test tests/contact.spec.ts --repeat-each=3</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test tests/contact.spec.ts --repeat-each=3` passes with zero failures and zero flakes
    - With the fixture address, the spec asserts keyboard focus reaches the mailto anchor, `getByRole("link", { name: <address> })` resolves to exactly one element, `textContent` equals the address, and `href` equals `mailto:` plus the address
    - The focus-ring assertion reads a computed value, not a class name
    - With the shipped null state, the spec asserts `/` renders exactly one channel row and zero `mailto` links
    - The spec asserts zero `[disabled]` and zero `[aria-disabled]` inside the contact block
    - The spec asserts the contact block is present on both `/` and `/cv`
    - The spec asserts the GitHub link carries no `target` attribute
    - Every channel link's measured height is at least 24px
    - A comment names the fixture address as a fixture and states it must never become the shipped value
    - `/usr/bin/grep -c '@liip' tests/contact.spec.ts` returns 0
  </acceptance_criteria>
  <done>The email's keyboard and copyable legs are proven against a real address, absence is proven against the real shipped module, and both surfaces are proven to render one block from one data module.</done>
</task>

<task type="auto">
  <name>Task 2: tests/cv.spec.ts — the portrait, the CLS guard, and the sections</name>
  <files>tests/cv.spec.ts</files>
  <read_first>
    - `tests/cv.spec.ts` in full (69 lines) — all six existing assertions, which must keep passing
    - `.planning/phases/06-cv-contact-photo-discoverability/06-CONTEXT.md:137-167` — D-2.4, D-2.5, D-2.6
    - `.planning/phases/06-cv-contact-photo-discoverability/06-RESEARCH.md:407-436` — the CLS mechanism, the flex-stretch gotcha, the LCP recommendation, and the note that the budget gates do not read JSX
    - `.planning/phases/06-cv-contact-photo-discoverability/06-RESEARCH.md:916-918` — Pitfall 11, `<strong>` at 700
    - `components/portrait.tsx` and `components/cv/cv-sections.tsx` as written by plan 06-04
    - `tests/font-cls.spec.ts` — the existing `PerformanceObserver` layout-shift pattern to reuse rather than reinvent
  </read_first>
  <action>
Extend the file; do not restructure it. All six existing assertions (200, exactly one `h1` reading
`CV`, the Humane stack, the back link, the 24px floor, the six banned marker words) stay.

The portrait needs a real file to be asserted meaningfully, and `PORTRAIT` is `null` today. Structure
the spec the way `tests/landing.spec.ts` already handles the featured slot's derived state: assert
the **shipped** state unconditionally, and assert the **populated** state against a fixture asset the
spec controls. State in a comment that the fixture is a fixture and that plan 06-11's hand-off names
the real file as user-supplied.

Shipped state, unconditional: with `PORTRAIT` null, `/cv` renders zero `<img>` elements — no empty
slot, no frame, no placeholder box.

Populated state, against the fixture:
- Exactly one `<img>`, and its `naturalWidth` is greater than zero. A 200 response proves the route;
  `naturalWidth` proves the browser actually decoded the bytes.
- Its `width` and `height` attributes are present and equal the file's real intrinsic dimensions.
- Its computed width is strictly less than the `<main>` element's content width. This is Pitfall 10:
  `/cv`'s `<main>` is `flex flex-col` with `align-items: stretch`, which would balloon a replaced
  element to the full column. Measure both, do not assume.
- Its computed `border-radius` is `0px` on all four corners.
- It sits after the `<h1>` in document order — assert with `compareDocumentPosition`, not by
  coordinates, so the assertion survives a layout change.

CLS: reuse `tests/font-cls.spec.ts`'s `PerformanceObserver` pattern. Register the observer before
navigation, load `/cv` with the fixture portrait, wait for the image's `decode()`, and assert the
cumulative layout-shift score is at or below the same threshold `font-cls.spec.ts` already uses.
Reuse its threshold rather than inventing one. This is BUILD-06's requirement and D-2.6's real
justification.

Sections: with a fixture two-row `EXPERIENCE`, assert `/cv` still renders exactly **one** `h1`, that
each section heading is an `h2` carrying `section-head`, and that each experience row renders one
Label-role line and one Body-role line. Assert every element inside `<main>` has a computed
`font-weight` in `{400, 530}` — this is the only tier that catches a `<strong>` resolving to 700, and
the source-level gates read `app/globals.css` only. Assert every computed `border-radius` inside
`<main>` is `0px`.

Restore `lib/cv.ts` to its shipped null/empty state at the end, and assert `git diff lib/cv.ts` is
empty as part of the task's completion.
  </action>
  <verify>
    <automated>npx playwright test tests/cv.spec.ts --repeat-each=3 && git diff --stat lib/cv.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test tests/cv.spec.ts --repeat-each=3` passes with zero failures and zero flakes
    - The six pre-existing assertions are unmodified and still pass
    - The spec asserts zero `<img>` on `/cv` in the shipped null-portrait state
    - The populated-state assertions cover `naturalWidth > 0`, intrinsic `width`/`height` attributes, computed width strictly less than `<main>`'s content width, `border-radius: 0px`, and document order after the `h1`
    - The CLS assertion uses `PerformanceObserver` layout-shift entries and reuses `tests/font-cls.spec.ts`'s threshold constant rather than a new number
    - With a fixture `EXPERIENCE`, the spec asserts exactly one `h1`, `h2` section headings carrying `section-head`, and every computed `font-weight` inside `<main>` in `{400, 530}`
    - `git diff --stat lib/cv.ts` is empty after the run
    - A comment names the fixture asset and the fixture experience rows as fixtures that must never become shipped values
  </acceptance_criteria>
  <done>`/cv` is proven in both its absent and populated states: no empty slot when the portrait is missing, a decoded and correctly-sized image when it exists, a layout-shift score at the existing threshold, exactly one `h1`, and no third font weight on screen.</done>
</task>

<task type="auto">
  <name>Task 3: the landing's real #contact, and the smear-origin invariance across image load</name>
  <files>tests/landing.spec.ts, tests/landing-trail.spec.ts</files>
  <read_first>
    - `tests/landing.spec.ts` — the `#contact` assertions, the stale `SectionStub` comment at `:455-465`, the meta-description equality test at `:125`, and the existing `<strong>` note at `:700-710`
    - `tests/landing-trail.spec.ts` in full — the existing smear-origin assertions and how it reads trail state
    - `.planning/phases/06-cv-contact-photo-discoverability/06-CONTEXT.md:158-167` — D-2.6 as written
    - `.planning/phases/06-cv-contact-photo-discoverability/06-RESEARCH.md:407-421` — the correction: `documentTop` is gauge-invariant, the UI-SPEC's stated reason is wrong, and no `ResizeObserver` may be added
    - `.planning/phases/03-work-list-landing-skeleton/03-UI-SPEC.md:232` — the forward note being superseded
    - `components/smear-heading/smear-heading-provider.tsx:110,137,145,271` — the four lines that consume `documentTop`
  </read_first>
  <action>
In `tests/landing.spec.ts`, update the `#contact` assertions for the real block: the section renders
the channel rows plan 06-04 ships, contains zero stub copy (`No contact details here yet.` and
`Email, GitHub and LinkedIn are being added.` must both be absent from the rendered page), and
contains a link to `https://github.com/guillem-gelabert`. Assert no `target="_blank"` anywhere on the
landing — the existing production assertion at `tests/build/prerender.test.ts:542` covers the built
HTML; this is the dev-tier companion.

Correct the stale comment at `:455-465`: `SectionStub` no longer exists — plan 06-04 deleted it after
`#contact` became its last call site, following Phase 5's D-13 precedent that an ended interim state
means the component is deleted, not orphaned as an unreachable branch. Rewrite the comment to say
what is true now. A comment describing a deleted component is how the next reader is misled.

Leave `:125`'s meta-description equality test alone. Plan 06-07 keeps `/`'s description bound to
`POSITIONING_PLACEHOLDER`, so it still holds, and it is HOME-01's tripwire.

In `tests/landing-trail.spec.ts`, add D-2.6's assertion: the `/cv` `<h1>`'s smear origin is unchanged
before and after the portrait loads. Measure the heading's trail origin with the fixture portrait
present, once before the image has decoded and once after `decode()` resolves, and assert the two are
equal.

Write the reasoning honestly in the comment, because the record it supersedes is wrong.
`03-UI-SPEC.md:232` says a post-mount layout change above a trail-carrying heading leaves it smearing
from a stale origin. It does not: `documentTop` enters the trail maths only as `lagY − targetY`
(provider lines 110, 137, 145, 271), where it cancels — shifting it by Δ shifts both terms by Δ and
`draw()` never sees an absolute document offset. The requirement to reserve the portrait's space
stands, but its real justification is BUILD-06/CLS, and the portrait sitting *below* the `<h1>` makes
even that a non-event. This assertion therefore proves invariance rather than guarding a desync —
say so, so nobody later "fixes" the trail with a `ResizeObserver`.

Call `page.emulateMedia({ reducedMotion: "reduce" })` **before** `page.goto()` in any case that needs
it — Playwright's `reducedMotion` context and test options do not reliably affect `matchMedia` in
this environment, which is recorded in STATE.md as a standing rule for this repo.

Do not modify `components/smear-heading/` in any way. That module must come out of this phase with an
empty diff.
  </action>
  <verify>
    <automated>npx playwright test tests/landing.spec.ts tests/landing-trail.spec.ts --repeat-each=2 && git diff --stat components/smear-heading/</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test tests/landing.spec.ts tests/landing-trail.spec.ts --repeat-each=2` passes with zero failures and zero flakes
    - `tests/landing.spec.ts` asserts both stub strings are absent from the rendered landing page
    - `tests/landing.spec.ts` asserts a link to `https://github.com/guillem-gelabert` inside `#contact` and zero `target="_blank"` on the page
    - `/usr/bin/grep -c SectionStub tests/landing.spec.ts` returns 0
    - `tests/landing.spec.ts:125`'s meta-description equality test is unmodified and passes
    - `tests/landing-trail.spec.ts` asserts the `/cv` `h1` smear origin is equal before and after the fixture portrait decodes
    - The new comment states `documentTop` is gauge-invariant, cites the four provider line numbers, and says the reservation requirement is CLS-driven
    - `/usr/bin/grep -c ResizeObserver tests/landing-trail.spec.ts components/smear-heading/*.ts*` returns 0 for every file
    - `git diff --stat components/smear-heading/` is empty
    - Any reduced-motion case calls `emulateMedia` before `goto`
  </acceptance_criteria>
  <done>The landing's contact section is asserted against real channels with the stale comment corrected, and the `/cv` heading's smear origin is proven invariant across the portrait's load — with the superseded UI-SPEC reasoning corrected in place rather than silently inherited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture values → shipped source | A fixture email or a fixture employment row committed into `lib/` would be a fabricated personal fact |
| dev-tier DOM → production bytes | A decoded-DOM assertion cannot prove what the server emitted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-45 | Spoofing | A test fixture address or employment row leaking into `lib/contact.ts` or `lib/cv.ts` | mitigate | Fixtures live in the spec files, each labelled in a comment as a fixture that must never become the shipped value. Tasks 2 and 3 assert `git diff` on `lib/cv.ts` is empty after the run, and task 1 greps that the employer domain appears nowhere in the spec. |
| T-06-46 | Repudiation | A decoded-DOM `textContent` check passing while the served bytes carry `&amp;#64;` | mitigate | Stated explicitly in the objective; the served-bytes assertion is plan 06-09's, in `tests/build/prerender.test.ts`. This plan's comments name the division. |
| T-06-47 | Tampering | A `ResizeObserver` added to `components/smear-heading/` to "fix" a non-existent desync | mitigate | Task 3 asserts zero `ResizeObserver` in the trail module and an empty `git diff` on the directory, and records the gauge-invariance derivation with provider line numbers. |
| T-06-48 | Tampering | A third font weight reaching the screen via `<strong>` or an arbitrary Tailwind value | mitigate | Task 2's computed `font-weight` sweep over `/cv`'s `<main>`; plan 06-10 extends the sweep site-wide. Source-level gates read `app/globals.css` only and cannot catch either. |
| T-06-49 | Tampering | Reverse tabnabbing via a contact link | mitigate | Tasks 1 and 3 assert no `target` attribute on outbound channel links and no `target="_blank"` on the landing. |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs nothing. `06-RESEARCH.md` § Package Legitimacy Audit records zero packages for this phase. |
</threat_model>

<verification>
1. `npx playwright test tests/contact.spec.ts tests/cv.spec.ts tests/landing.spec.ts tests/landing-trail.spec.ts --repeat-each=3` — passes, zero flakes.
2. `npm test` — the full Playwright suite passes.
3. `git diff --stat lib/cv.ts lib/contact.ts components/smear-heading/` — empty.
4. `npm run test:unit` — passes.
5. `npm run lint` — no new errors. `npx tsc --noEmit` — exits 0.
6. `git diff app/(en)/layout.tsx app/(de)/layout.tsx` — empty.
</verification>

<success_criteria>
- The email's keyboard reachability, accessible name and copyable text are proven against a real address; the screen-reader leg is handed to plan 06-11.
- Absent channels and an absent portrait are proven to render nothing at all.
- The portrait decodes, is correctly sized against its container, has square corners, sits below the `h1`, and its load produces a layout-shift score at the existing threshold.
- The `/cv` heading's smear origin is proven invariant across image load, with the superseded reasoning corrected in place.
- No fixture value reaches any `lib/` module; `components/smear-heading/` diff is empty.
</success_criteria>

<output>
Create `.planning/phases/06-cv-contact-photo-discoverability/06-08-SUMMARY.md` when done.
Record: the measured layout-shift score for `/cv` with the fixture portrait and the threshold reused
from `font-cls.spec.ts`; the measured `<img>` computed width against `<main>`'s content width; the
measured smear-origin values before and after decode; and confirmation that `git diff` on `lib/cv.ts`,
`lib/contact.ts` and `components/smear-heading/` is empty.
</output>
</content>
