---
phase: 03-work-list-landing-skeleton
plan: 08
type: execute
wave: 4
depends_on: ["03-03", "03-04", "03-05", "03-06", "03-07"]
files_modified:
  - tests/build/prerender.test.ts
  - .planning/phases/03-work-list-landing-skeleton/deferred-items.md
  - .planning/STATE.md
autonomous: true
requirements: [WORK-01, WORK-02, HOME-01, HOME-03, HOME-04]
must_haves:
  truths:
    - "The production build prerenders / and /cv, and both carry the inherited noindex — no route restated robots"
    - "/ emits a rel=canonical it did not have before this phase"
    - "/'s meta description equals POSITIONING_PLACEHOLDER in production, so the user's real sentence updates the page and the share preview at once"
    - "The featured slot ships in its interim state, and the interim headline carries no link"
    - "A clean-.next build proves the de-clienting and the new route, not a warm cache"
    - "The live Railway URL serves the new landing view, /cv returns 200, and the site is still noindexed"
    - "HOME-01 is recorded by name as deferred-by-decision and carries into every subsequent phase's state until the user supplies the sentence"
  artifacts:
    - path: "tests/build/prerender.test.ts"
      provides: "Production-tier assertions for /, /cv, the interim slot copy and the inherited noindex"
      contains: "POSITIONING_PLACEHOLDER"
    - path: ".planning/phases/03-work-list-landing-skeleton/deferred-items.md"
      provides: "The four UI-SPEC phase-completion checklist items, HOME-01 first"
      contains: "HOME-01"
    - path: ".planning/STATE.md"
      provides: "The HOME-01 tripwire in the Deferred Items table so it survives phase transitions"
      contains: "HOME-01"
  key_links:
    - from: "tests/build/prerender.test.ts"
      to: "lib/work.ts POSITIONING_PLACEHOLDER"
      via: "relative .ts import, compared against the built index.html's meta description"
      pattern: "POSITIONING_PLACEHOLDER"
    - from: ".planning/STATE.md Deferred Items"
      to: "Phase 6 FIND-02"
      via: "HOME-01 must never reach the robots flag flip still holding Developer."
      pattern: "FIND-02"
---

<objective>
Assert what actually ships — production HTML, not the dev server — run the clean-`.next` phase
gate, confirm the deploy, and record the four phase-completion items so the `HOME-01` tripwire
survives into every subsequent phase.

Purpose: every Playwright spec in this repo runs against `npm run dev`, where `showDrafts()` is
`true`. That environment structurally cannot prove what a production build omits, what its
metadata says, or which state the featured slot ships in. And `HOME-01` ships unwritten behind a
source-only marker, so the landing *looks finished* while the site's central sentence is
outstanding — no optical pass can catch that, which is exactly why it needs a written record
rather than a note.
Output: extended build-tier assertions, a green clean-build phase gate, a confirmed deploy, and
two records.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-work-list-landing-skeleton/03-VALIDATION.md
@.planning/phases/03-work-list-landing-skeleton/03-UI-SPEC.md
@.planning/phases/02-content-pipeline/deferred-items.md
</context>

<interfaces>
<!-- The build-output harness already exists. New assertions just call getRoutes(). -->

tests/build/prerender.test.ts (shipped):
  const APP_DIR = path.join(process.cwd(), ".next", "server", "app")          // :21
  async function walkHtmlRoutes(dir, base): Promise<Map<string, string>>      // :36-65
    // normalises index.html -> route key "", and "x/index.html" -> "x"
  function getRoutes(): Promise<Map<string, string>>                          // :67-71  (memoised)
  function draftMarkerLine(marker: string): RegExp                            // :137-151
  test("Phase 1's routes still prerender after the route-group restructure")  // :190-194

<!-- Production content state, VERIFIED: content/ holds three files, ALL draft: true.  -->
<!--   fixture.mdx (en), musterseite.mdx (de), nur-auf-deutsch.md (de)                  -->
<!-- Therefore in a production build publishedFor("en") === [] and                      -->
<!-- findBySlug([], CASE_STUDY_SLUG) === null → the featured slot ships INTERIM.         -->
<!-- In next dev, publishedFor("en") === [fixture] but findBySlug still returns null.    -->

<!-- The substring-check trap (prerender.test.ts:137-151): a bare html.includes("Draft") -->
<!-- would fire on any post whose prose contained "Drafting" or "Draftsman". Match the    -->
<!-- rendered SHAPE, not a bare word, whenever the needle is short or generic.            -->

<!-- React SSR emits DOM property spellings (hrefLang, not hreflang) — match             -->
<!-- case-insensitively in any attribute assertion (prerender.test.ts:184-187).          -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend tests/build/prerender.test.ts with the production-tier assertions for / and /cv</name>
  <files>tests/build/prerender.test.ts</files>
  <read_first>
    - `tests/build/prerender.test.ts:1-35` — the header comment stating the tier rule (this file reads real HTML from `.next/server/app`; Playwright covers the dev half), and the `NO_BUILD_MESSAGE` guard.
    - `tests/build/prerender.test.ts:101-112` — the interim-copy assertion shape for the `/writing` empty state, which is exactly what the featured slot needs.
    - `tests/build/prerender.test.ts:128-135` — the noindex assertion shape.
    - `tests/build/prerender.test.ts:137-151` — the substring-check trap and `draftMarkerLine`.
    - `tests/build/prerender.test.ts:177-194` — the canonical / `hrefLang` assertion shape and the route-key test to extend.
    - `tests/unit/dates.test.ts:1-12` — the relative `../../lib/*.ts` import form Node needs; from `tests/build/` the path to the work module is `../../lib/work.ts`.
    - `.planning/phases/03-work-list-landing-skeleton/03-VALIDATION.md` — the five ⚠️-extend rows this task owns.
  </read_first>
  <action>
    Extend `tests/build/prerender.test.ts`. Add tests; modify existing ones only where explicitly
    instructed. Import `POSITIONING_PLACEHOLDER` from `../../lib/work.ts` at the top — relative,
    with the `.ts` extension, matching the form `tests/unit/dates.test.ts` uses.

    First, extend the existing test at `:190-194` ("Phase 1's routes still prerender after the
    route-group restructure"): add an assertion that `routes.has("cv")` alongside the existing
    `""` and `"type"` checks, and update the title to name the Phase 3 change too. A page
    converted from Client to Server Component plus a brand-new route is exactly the class of
    change that stale generated route types mask.

    Then add these tests:

    (a) **`/`'s canonical.** Take `routes.get("")`, assert it contains `rel="canonical"`, extract
    the canonical `href` with a regex and assert `new URL(href).pathname === "/"`. Comment that
    this is the measured gap: before this phase `/` inherited a title and a description from the
    layout but emitted no canonical and could not declare one, because `"use client"` makes the
    `metadata` export illegal. **Do not write an assertion of the form "a title appears on `/`"**
    — that passed before the change and proves nothing (`03-RESEARCH.md` C-2).

    (b) **One source for `HOME-01`.** Extract the `content` of `<meta name="description">` from
    `routes.get("")` and assert it equals `POSITIONING_PLACEHOLDER` exactly. Comment Pitfall 6 at
    length: the failure this prevents is the user writing the positioning sentence into the
    rendered `<p>` while the meta description still says `Developer.` — which is what Slack,
    LinkedIn and eventually Google quote once Phase 6 flips `FIND-02`. Comparing against the
    imported constant rather than a literal means this keeps passing when the real sentence lands
    and fails the moment the two drift.

    (c) **The inherited noindex, on both new surfaces.** Assert `routes.get("")` and
    `routes.get("cv")` each carry a robots meta tag containing `noindex`, matched
    case-insensitively. Comment that neither route declares `robots` in its own source: Next
    merges metadata parent→child, the two root layouts are the only declarations site-wide, and
    Phase 6's `FIND-02` flips it in exactly those two places (Pitfall 5).

    (d) **`/cv`'s metadata.** Assert `routes.get("cv")`'s `<title>` matches
    `/CV\s*(?:—|&#x2014;|&mdash;)\s*Guillem Gelabert/` — tolerate the literal em dash and both
    entity forms rather than assuming which React emits. Assert it contains `rel="canonical"`
    resolving to pathname `/cv`.

    (e) **The featured slot ships interim.** Assert `routes.get("")` contains
    `The case study is being written.` and
    `On the Mallorca piece: what was expected, what the data showed, and how the visual form changed in response.`
    Comment that this assertion belongs HERE and not in Playwright: the moment Phase 4 creates
    `content/the-chart-therefore-changes.mdx` with `draft: true`, `findBySlug` starts returning an
    entry in dev only, so a Playwright copy assertion would go red during Phase 4 authoring with
    no Phase 3 file changed (Pitfall 2). Add a forward note in the same comment — when Phase 4
    publishes the case study this test is expected to be updated to assert the published state,
    and that is a real change in what ships, not a flaky test.

    (f) **The interim headline is not a link.** Extract the `<h3 …class="…text-heading…">…</h3>`
    block from `routes.get("")` with a shape-matching regex and assert its inner HTML contains no
    `<a`. Use the extracted block, not a whole-document anchor count, which would fail on the nav.
    Comment why the rule exists: there is nowhere honest for the interim headline to point — the
    case study does not exist and `/writing` is at n=0 — so a link would be a circular dead end
    into an empty index.

    (g) **The stubs ship their real copy.** Assert `routes.get("")` contains all four stub strings:
    `Nothing listed here yet.`, `The current work is being written up.`,
    `No contact details here yet.`, `Email, GitHub and LinkedIn are being added.` Then assert the
    same document contains none of `TODO`, `Coming soon`, `Under construction`, `Lorem`, matched
    case-insensitively. Comment that `D-02` requires placeholder content to be deliberately
    typeset because the site is on a live URL during a job hunt, and Pitfall 7's warning sign is
    exactly one of those words reaching rendered output.

    (h) **The private repo stays private in production.** Assert `routes.get("")` contains both
    absolute D-06 URLs, contains no `github.com` inside an `href` attribute, contains the string
    `ib-gdp-evolution` zero times, and contains no `target="_blank"`.

    (i) **The launch gate, recorded as an executable note.** Add a test whose name reads as a gate,
    asserting that `/`'s production HTML currently holds the interim featured copy AND both stub
    state lines. Comment the rule plainly: **if the featured slot, the backlog stub, the contact
    stub or `/cv` is still in its interim state when Phase 6 goes to flip the robots flag, Phase 6
    is blocked.** This test passing today is the record that all four are interim; when Phases 4,
    5 and 6 fill them, this test is the thing that must be updated, which is where the gate gets
    noticed rather than forgotten.
  </action>
  <verify>
    <automated>rm -rf .next && npm run build && npm run test:build</automated>
  </verify>
  <acceptance_criteria>
    - `rm -rf .next && npm run build && npm run test:build` exits 0 with 0 failing tests.
    - The route-key test asserts all three of `""`, `"type"` and `"cv"`.
    - Test (b) compares the built `<meta name="description">` content against the imported `POSITIONING_PLACEHOLDER` by equality — `grep -c '"Developer\."' tests/build/prerender.test.ts` returns 0.
    - Test (c) asserts a `noindex` robots meta on both `""` and `"cv"`, case-insensitively.
    - Test (f) extracts the `h3.text-heading` block by regex and asserts no `<a` inside that block only.
    - Test (h) asserts zero occurrences of `ib-gdp-evolution` and zero `target="_blank"` in `/`'s production HTML.
    - No existing test was deleted or weakened; `git diff tests/build/prerender.test.ts` shows additions plus the one route-key extension.
  </acceptance_criteria>
  <done>Production HTML for `/` and `/cv` is asserted for canonical, description, inherited noindex, interim slot copy, stub copy and the private-repo rule.</done>
</task>

<task type="auto">
  <name>Task 2: Run the clean-build phase gate and confirm the deploy</name>
  <files>tests/build/prerender.test.ts</files>
  <read_first>
    - `package.json` `scripts` — `test:all` is `npm run test:unit && rm -rf .next && npm run build && npm run test:build && npm test`, which is exactly the phase gate.
    - `tests/deploy-smoke.spec.ts:1-29` — it asserts a 200, a `/_next/static/` script and the `__next_f` RSC marker, and it runs against local `next dev` by default and against the live deployed URL when `PLAYWRIGHT_BASE_URL` is set.
    - `.planning/phases/02-content-pipeline/deferred-items.md` § the lint item — `npm run lint` exits 1 for exactly one known reason, `components/smear-heading/use-prefers-reduced-motion.ts:23`. **Never write a criterion of "0 errors".**
    - `.planning/phases/03-work-list-landing-skeleton/03-RESEARCH.md` § *Pitfall 3* — why a warm `.next` hides the de-clienting, and why the clean build is not optional for this phase specifically.
    - `.planning/phases/03-work-list-landing-skeleton/03-VALIDATION.md` § *Sampling rate* — the phase-gate command list.
  </read_first>
  <action>
    This task runs gates and records their output in the plan summary. It writes no application
    code. If a gate fails, fix the cause in the file that owns it and re-run — do not weaken a
    gate, do not add a `test.skip`, and do not loosen a threshold.

    Run, in this order, capturing the output of each:

    1. `npx tsc --noEmit` — must exit 0.
    2. `npm run test:unit` — 0 failures across `content`, `dates`, `link-contract`,
       `prose-contract`, `work`.
    3. `rm -rf .next && npm run build` — must complete clean. **The clean `.next` is not
       optional.** Converting `/` from Client to Server Component and adding
       `app/(en)/cv/page.tsx` are exactly the change class that stale generated route types mask;
       `02-VALIDATION.md` records the same lesson from Phase 2's route-group move. Read the
       build's route table in the output and confirm `/` and `/cv` both appear as prerendered
       static routes rather than dynamic ones.
    4. `npm run test:build` — 0 failures.
    5. `npx playwright test` — the whole suite, 0 failures.
    6. `npm run lint` — record the exact error count and the file each error names. The criterion
       is **no NEW errors**: exactly one is expected, in
       `components/smear-heading/use-prefers-reduced-motion.ts` at line 23
       (`react-hooks/set-state-in-effect`), a Phase 2 deferred item. If a second error appears,
       fix it in the file that introduced it. If the count drops to zero, note it and check that
       the deferred item was not silently edited — this phase must not touch
       `components/smear-heading/`.

    Then confirm the deploy. Commit and push the phase's work so Railway's zero-config Node
    builder picks it up, wait for the deploy to finish, and run the smoke spec against the live
    origin with `PLAYWRIGHT_BASE_URL` set to `https://web-production-9cedb.up.railway.app`.

    Then check the live origin directly and record three results in the summary: `/cv` returns 200
    rather than the 404 it returned before this phase; `/` carries a `<link rel="canonical">`; and
    both `/` and `/cv` carry `<meta name="robots" content="noindex">`.

    Do **not** set or unset `NEXT_PUBLIC_SITE_URL` in the Railway environment. `lib/site.ts:16`
    falls back to the Railway origin either way, so both states are correct and neither needs
    touching — that is Phase 6 territory.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run test:unit && rm -rf .next && npm run build && npm run test:build && npx playwright test && npm run lint 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `npm run test:unit` reports 0 failures across five files.
    - `rm -rf .next && npm run build` completes and its route table lists `/` and `/cv` as prerendered static routes.
    - `npm run test:build` reports 0 failures.
    - `npx playwright test` reports 0 failures across every spec in `tests/`.
    - `npm run lint` reports **no NEW errors**: exactly one error, naming `components/smear-heading/use-prefers-reduced-motion.ts`. A criterion of "0 errors" is wrong and must not be written.
    - `git diff --stat HEAD -- components/smear-heading/` is empty.
    - The live Railway origin serves the new landing view: `PLAYWRIGHT_BASE_URL=https://web-production-9cedb.up.railway.app npx playwright test tests/deploy-smoke.spec.ts` passes.
    - Live `/cv` returns HTTP 200; live `/` emits `<link rel="canonical">`; live `/` and `/cv` both emit `<meta name="robots" content="noindex">`. All three recorded in the summary.
    - `NEXT_PUBLIC_SITE_URL` was neither set nor unset in the Railway environment.
  </acceptance_criteria>
  <done>The full gate is green on a clean build and the live URL serves the new landing view with `/cv` reachable and the site still noindexed.</done>
</task>

<task type="auto">
  <name>Task 3: Write the phase-completion record and carry the HOME-01 tripwire into STATE.md</name>
  <files>.planning/phases/03-work-list-landing-skeleton/deferred-items.md, .planning/STATE.md</files>
  <read_first>
    - `.planning/phases/02-content-pipeline/deferred-items.md` in full — the established format for this project: a heading per item, then **Found during / What / Why out of scope / Root cause / Impact / Recommendation**, and for CR-01 a **Status / Severity / Measured behaviour / Root cause / Why not the obvious fixes / Chosen disposition** structure. Match that register.
    - `.planning/STATE.md` § *Deferred Items* and § *Blockers/Concerns* — the tables to update. The Deferred Items table is currently a single "none" placeholder row.
    - `.planning/phases/03-work-list-landing-skeleton/03-UI-SPEC.md` § *Phase-completion checklist* — the four items, verbatim. All four go in the record.
    - `.planning/phases/03-work-list-landing-skeleton/03-VALIDATION.md` § *Manual-only verifications* — the three manual rows, including the tripwire's own instructions.
    - `.planning/phases/03-work-list-landing-skeleton/03-CONTEXT.md` `D-08` and `D-09` — `HOME-01` is deferred **by decision**, not by omission, and the two `WORK-02` annotations are drafts for the user to edit.
  </read_first>
  <action>
    Create `.planning/phases/03-work-list-landing-skeleton/deferred-items.md` following the Phase 2
    file's format. It carries five entries.

    **1. `HOME-01` — the positioning sentence is unwritten. THIS IS THE TRIPWIRE. Put it first
    and mark it as such in the heading.** Record: it ships as `Developer.` behind the
    `POSITIONING_PLACEHOLDER` constant in `lib/work.ts`. Status is **deferred by decision (`D-08`)**,
    not a gap — the user writes this sentence and nothing blocks on it. State the reason it needs
    a written record rather than a note: the placeholder is marked in source and never on screen,
    so the landing *looks finished* while the site's single most important sentence is
    outstanding, and no 375px or 1440px visual pass can catch it. State the two places it is
    consumed — the rendered `<p>` in `app/(en)/page.tsx` and that page's `metadata.description` —
    and that both come from the one constant, so supplying the sentence is a one-line edit
    (Pitfall 6). State the carry-forward rule explicitly: **it must be re-asserted at the top of
    every subsequent phase's carried-forward state until the user supplies the sentence, and it
    must appear in Phase 6's pre-launch check alongside the robots flag. `HOME-01` must never
    reach the `FIND-02` flag flip still holding `Developer.`** Name the gate that will catch a
    partial fix: `tests/build/prerender.test.ts` asserts the built meta description equals
    `POSITIONING_PLACEHOLDER`, and `tests/landing.spec.ts` asserts the rendered `<p>` equals the
    meta description, so editing one without the other fails.

    **2. The two `WORK-02` annotations are drafts awaiting the user's edit (`D-09`).** They satisfy
    the requirement as written and are not final copy. Quote both lines verbatim so the user can
    edit them without opening the source, name their file (`lib/work.ts`), and record the
    constraint that governs any edit: each says what the piece is *about*, never what it was built
    with, and `tests/unit/work.test.ts` enforces that with a banned-tool-word list — note that the
    list is a heuristic that could false-positive on a legitimate future annotation, in which case
    the fix is a ten-second edit to the list, not deleting the test.

    **3. Four surfaces ship in an interim state, and all four are blocking pre-conditions on
    `FIND-02`.** The featured slot, the backlog stub, the contact stub and `/cv`. Each is
    deliberately typeset per `D-02` and safe to ship because `robots: { index: false }` stays on
    until Phase 6. Record the gate: **none of the four may be the public launch condition.** Name
    which phase fills each — Phase 4 the slot, Phase 5 the backlog, Phase 6 the contact block and
    the CV — and name the test that records the current state
    (`tests/build/prerender.test.ts`'s launch-gate test).

    **4. `SmearTitle`'s `as` union was widened to include `"h3"` and the featured headline renders
    as a real `<h3>`.** Record it as done rather than deferred, with the reason it was on the
    checklist: an `<h2>` there would put two `<h2>`s inside `section#case-study`, one of them the
    section head, silently breaking the heading outline `aria-labelledby` depends on.
    `tests/landing.spec.ts`'s heading-outline test is the standing gate.

    **5. The two `03-RESEARCH.md` corrections to the approved UI-SPEC, recorded so a later phase
    does not act on the superseded reasoning.** (i) The UI-SPEC's smear-system constraint #2 says
    a post-mount layout change above a registered heading leaves its trail anchored to a stale
    `documentTop`; measured, it does not — `documentTop` is gauge-invariant, cancelling in every
    expression `draw()` consumes. **Phase 6's photograph must still reserve its space, but for
    `BUILD-06` (cumulative layout shift), not for trail correctness.** (ii) The UI-SPEC's
    constraint #1 marks an off-screen `draw()` viewport guard as `planner may override`; measured,
    the cost is 0.085ms per heading per frame against an 8.33ms budget and the shipped 5-heading
    `/type` holds 120fps, so the guard was deliberately not added and should not be added at this
    heading count.

    Also record, in the same file, that **CR-01 remains open and deferred to Phase 6** — Phase 3
    did not attempt it, added no middleware, did not set `dynamicParams = false`, and added no
    no-JS assertion for `/writing/<unknown>` or `/texte/<unbekannt>` because such an assertion
    would fail today.

    Then update `.planning/STATE.md`:
    - Replace the placeholder row in the **Deferred Items** table with rows for items 1, 2 and 3
      above plus CR-01, each with a Category, the Item, a Status and "Phase 03" (CR-01 keeps
      "Phase 02") as the Deferred At value. `HOME-01`'s row must name it explicitly and say
      **blocks `FIND-02`**.
    - Add a **Blockers/Concerns** entry for `HOME-01` worded as the tripwire, pointing at
      `.planning/phases/03-work-list-landing-skeleton/deferred-items.md` for the full record.
    - Add the Phase 3 decisions to **Accumulated Context → Decisions**: the landing is English-only
      for v1 (`I18N-01` is writing-scoped and complete; `/startseite` is the recorded future
      shape, not built); the featured slot's state is derived from
      `findBySlug(await publishedFor("en"), CASE_STUDY_SLUG)` and there is no boolean to flip; and
      amendment A3 was extended beyond the UI-SPEC's written list to `app/not-found.tsx` and the
      two `[slug]` post templates, recorded rather than made silently.

    Do not mark the phase complete in `ROADMAP.md` here — the optical sign-off in Plan 03-09 is the
    last gate, and `/gsd:verify-phase` owns the completion record.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const d=fs.readFileSync('.planning/phases/03-work-list-landing-skeleton/deferred-items.md','utf8'); for (const k of ['HOME-01','TRIPWIRE','D-08','WORK-02','D-09','FIND-02','CR-01','documentTop','POSITIONING_PLACEHOLDER']) if(!new RegExp(k,'i').test(d)) throw new Error('deferred-items.md missing: '+k); const s=fs.readFileSync('.planning/STATE.md','utf8'); if(!/HOME-01/.test(s)) throw new Error('STATE.md does not carry HOME-01'); if(!/FIND-02/.test(s)) throw new Error('STATE.md does not link HOME-01 to FIND-02'); if(/\(none — this is the first executed milestone/.test(s)) throw new Error('STATE.md Deferred Items placeholder row still present'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/03-work-list-landing-skeleton/deferred-items.md` exists and contains all five entries plus the CR-01 note, in the Phase 2 file's format.
    - Entry 1 is `HOME-01`, is first in the file, is labelled as the tripwire, names `D-08`, names `POSITIONING_PLACEHOLDER` and `lib/work.ts`, and states that it must never reach the `FIND-02` flag flip still holding `Developer.`
    - Entry 2 quotes both `WORK-02` annotations verbatim and names `lib/work.ts` as the file to edit.
    - Entry 3 names all four interim surfaces and states that none may be the public launch condition.
    - Entry 5 records both `03-RESEARCH.md` corrections, including that Phase 6's photograph reserves space for `BUILD-06`, not for trail correctness.
    - `.planning/STATE.md`'s Deferred Items table no longer contains the "none — this is the first executed milestone" placeholder row and now carries `HOME-01` with a note that it blocks `FIND-02`.
    - `.planning/STATE.md` Blockers/Concerns carries the `HOME-01` tripwire entry pointing at the deferred-items file.
    - `.planning/STATE.md` Decisions records the English-only landing, the derived featured-slot state, and the A3 scope extension.
  </acceptance_criteria>
  <done>The tripwire is written down where every subsequent phase reads it, not left as a note in a plan nobody reopens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build output → public internet | The boundary this plan is entirely about. `.next/server/app/*.html` is what a visitor and a crawler actually receive. Every dev-tier assertion in this phase is a proxy for this one. |
| local repo → Railway deploy | `git push` to master triggers Railway's zero-config Node builder. This plan pushes and then verifies the live origin rather than assuming a green build means a correct deploy. |
| phase → phase | `.planning/STATE.md` and `deferred-items.md` are the only durable channel between phases. An item recorded only in a plan is an item nobody reopens. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-30 | Information disclosure | `robots: { index: false }` on `/` and `/cv` | mitigate | Premature indexing of an in-progress site whose featured slot reads "The case study is being written." is the realistic harm during a job hunt. Task 1 (c) asserts the inherited `noindex` against real production HTML for both new routes; Task 2 confirms it on the live origin. `FIND-02` is Phase 6 and **must not** be attempted here. |
| T-03-31 | Information disclosure | `/`'s production HTML | mitigate | Task 1 (h) asserts the private `ib-gdp-evolution` repo name appears zero times, no `href` contains `github.com`, and no `target="_blank"` exists — the production-tier repeat of what Plans 01, 03 and 06 assert at the data, source and dev tiers. |
| T-03-32 | Information disclosure | `/`'s `<meta name="description">` | mitigate | Once `FIND-02` flips, this string is what Slack, LinkedIn and Google quote. Task 1 (b) binds it to `POSITIONING_PLACEHOLDER` by equality so it cannot silently diverge from the sentence on the page (Pitfall 6). |
| T-03-33 | Tampering | Stale `.next` masking the de-clienting | mitigate | A warm cache lets `npm run build` succeed and `/` prerender while generated route types are stale, which then fails on Railway. Task 2 mandates `rm -rf .next` before the build and reads the route table to confirm `/` and `/cv` are prerendered static (Pitfall 3). |
| T-03-34 | Repudiation | The `HOME-01` tripwire | mitigate | An unwritten central sentence behind an invisible marker is precisely the defect that survives every review because everything looks finished. Task 3 writes it into `deferred-items.md` and `STATE.md`, and two automated gates make a partial fix fail: the build test binds the meta description to the constant, and `tests/landing.spec.ts` binds the rendered `<p>` to the meta description. |
| T-03-35 | Tampering | Railway environment configuration | mitigate | `NEXT_PUBLIC_SITE_URL` must be neither set nor unset in this phase — `lib/site.ts:16` falls back to the Railway origin either way, both states are correct, and changing it mid-phase would move every `rel="canonical"` and every `hreflang` alternate on the site with no test asserting the move was intended. Recorded as an acceptance criterion. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | Phase 3 installs zero packages; `03-RESEARCH.md` § Package Legitimacy Audit records none to vet, so no `[ASSUMED]`/`[SUS]` entry and no blocking legitimacy checkpoint applies. A clean `npm run build` from the committed `package-lock.json` is the only dependency action in this plan. |

**ASVS L1 chapters explicitly not applicable**: V2 Authentication, V3 Session Management, V5
Input Validation (the built site accepts no visitor input), V6 Cryptography, V13 API. V4 Access
Control and V12 File & Resource are applicable but inherited and unchanged — this plan calls no
content-loader function. **V14 Configuration is the live chapter here** and is covered by T-03-30
and T-03-35. Real security response headers are `BUILD-04`, Phase 6, and are deliberately not
attempted now.
</threat_model>

<verification>
- `rm -rf .next && npm run build && npm run test:build && npx playwright test` — all green.
- `npm run test:unit` reports 0 failures; `npx tsc --noEmit` exits 0.
- `npm run lint` shows no NEW errors beyond `components/smear-heading/use-prefers-reduced-motion.ts:23`.
- `git diff --stat HEAD -- components/smear-heading/` is empty.
- The live Railway origin passes `tests/deploy-smoke.spec.ts` under `PLAYWRIGHT_BASE_URL`; live `/cv` is 200; live `/` carries a canonical; live `/` and `/cv` are both noindexed.
- `.planning/STATE.md` names `HOME-01` in both its Deferred Items table and its Blockers/Concerns section.
- CR-01 is untouched and still deferred to Phase 6.
</verification>

<success_criteria>
- Production HTML for `/` proves the canonical, the `POSITIONING_PLACEHOLDER`-sourced description, the inherited noindex, the interim featured copy with no link in its `<h3>`, both stub copy sets, and the private-repo rule.
- Production HTML for `/cv` proves the route exists, carries its own title and canonical, and inherits noindex.
- The clean-`.next` build, the full unit suite, the full build suite and the full Playwright suite are green, and the deploy serves them.
- The `HOME-01` tripwire and the other three phase-completion items are written into `deferred-items.md` and carried into `STATE.md`.
</success_criteria>

<output>
Create `.planning/phases/03-work-list-landing-skeleton/03-08-SUMMARY.md` when done.
</output>
