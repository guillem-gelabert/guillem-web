---
phase: 04-the-case-study
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - content/the-chart-therefore-changes.mdx
autonomous: true
requirements: [CASE-01, CASE-02, CASE-03, HOME-02]

must_haves:
  truths:
    - "A visitor can read a published case study about the ib-gdp-evolution piece at /writing/the-chart-therefore-changes."
    - "A visitor can follow it through six parts in CASE-02's order: the question, the stated prior expectation, what the data showed, how the visual form changed, the shipped result, and a methodology note."
    - "A visitor who reads only the first two paragraphs still has the whole argument."
    - "Every number and quoted sentence in the piece traces to the author's own published work — nothing is invented."
    - "Publishing this file with draft: false makes findBySlug(publishedFor('en'), CASE_STUDY_SLUG) resolve, which is the only thing the featured slot needs to flip."
  artifacts:
    - path: "content/the-chart-therefore-changes.mdx"
      provides: "The English case study: front-matter, six h2 sections, three Figures, one Aside, one blockquote, one outbound link"
      min_lines: 90
      contains: "type: case-study"
  key_links:
    - from: "content/the-chart-therefore-changes.mdx"
      to: "public/case-study/f1-constant-dollars.png"
      via: "<Figure src width height>"
      pattern: "/case-study/f1-constant-dollars.png"
    - from: "content/the-chart-therefore-changes.mdx filename"
      to: "lib/work.ts CASE_STUDY_SLUG"
      via: "the filename stem IS the slug the featured slot resolves"
      pattern: "the-chart-therefore-changes"
    - from: "content/the-chart-therefore-changes.mdx"
      to: "https://ib-gdp.guillemgelabert.com/everyone-in-mallorca-agrees-on-one-thing"
      via: "one plain inline prose link in What shipped (D-20)"
      pattern: "ib-gdp.guillemgelabert.com"
---

<objective>
Write the case study. This is the phase's long pole and the milestone's — per the evidence audit
it is the only artifact that can close the "talented developer, no editorial judgment" gap.

The subject is **not** the Balearic economics. The live piece already carries that, and showing
the same project twice reads as duplication. The subject is the moment the visual form had to
change because the data turned out to be something other than expected: *here is what I expected,
here is what the data actually was, here is how the visual form changed because of it.* That
sentence from `BRIEF` §2 is the brief. Roughly 60% of the words go to that decision moment; 40% to
subject and outcome.

**Nothing in this piece is invented.** The complete source substrate exists and has been verified
byte for byte: the shipped English text, the abandoned earlier thesis (real, titled *"The Balearic
Paradox: Is Tourism Mallorca's Dutch Disease?"*), the author's own published English methodology,
and Plan 01's verbatim snapshot of the live pages. Every number and every quoted sentence traces
to one of those. Where a vault draft and the live page disagree, the live page wins — Plan 01's
`live-text/en-story.txt` is the authority, and `en_with_charts.md` is a working copy.

Two tasks, split at the piece's own hinge. Task 1 writes the setup — the question, the expectation
and the reversal. Task 2 writes the pivot, the result and the receipts, which is where the
editorial claim actually lands.

Purpose: CASE-01, CASE-02. Publishing it with `draft: false` also flips the featured slot
(CASE-03, HOME-02) with zero code change, and closes Phase 2's `/writing` n=0 launch gate.
Output: one MDX file, about 1,200–1,800 English words.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-the-case-study/04-CONTEXT.md
@.planning/phases/04-the-case-study/04-RESEARCH.md
@.planning/phases/04-the-case-study/04-02-SUMMARY.md
@content/fixture.mdx

<interfaces>
<!-- Locked by Plan 02. tests/unit/case-study-content.test.ts and tests/case-study.spec.ts
     already assert every value below. Do not reword, reorder or renumber. -->

Filename: content/the-chart-therefore-changes.mdx
  The stem IS lib/work.ts's CASE_STUDY_SLUG. It is already correct in that file; no code changes.

Front-matter, exactly:
  title: "The Chart Therefore Changes"
  standfirst: <plain text, your discretion — no bold, no links, no inline markup, no em dash
              styling tricks; it is already weight 530>
  date: "2026-08-31"
  lang: en
  translationKey: ib-gdp-case-study
  draft: false
  type: case-study

The six `##` section marks, in this order, and no other heading level anywhere:
  ## The question              → id the-question
  ## What I expected           → id what-i-expected
  ## What the data showed      → id what-the-data-showed
  ## Where the chart changed   → id where-the-chart-changed
  ## What shipped              → id what-shipped
  ## Methodology               → id methodology

Figures — three, all 2400 × 1640, only the third `wide`:
  <Figure src="/case-study/f1-constant-dollars.png" width={2400} height={1640}>  in beat 3
  <Figure src="/case-study/f2-eu-average.png"       width={2400} height={1640}>  in beat 4
  <Figure src="/case-study/f3-arrivals-diverge.png" width={2400} height={1640} wide>  in beat 5

  `alt` is at least 120 characters and 20 words, describes shape and finding, and does not open
  with "Chart of" / "A chart" / "Screenshot". The caption is the element's children, renders in
  the Label role, and must name at least one data source from: Rosés-Wolf, Maddison, Eurostat,
  AETIB, FRONTUR, Funcas, Cirer-Costa, Barceló Pons, Valdivielso.

  Import only `Figure`, from `@/components/mdx/figure`, exactly as content/fixture.mdx does.
  `Aside` needs no import — it is in the shipped MDX component map. Bare Markdown `![]()` throws
  at prerender by design; there is no second way to place an image.

Components: `Figure` and `Aside` only. Exactly one `<Aside>`, in Methodology. No fenced code
blocks. No `###` and no `h4`–`h6`.

Word count, as tests/unit/case-study-content.test.ts computes it (front-matter stripped, whole
`<Figure>` blocks removed, remaining tags stripped, Markdown links reduced to their labels):
between 1200 and 1800 inclusive.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Front-matter, the standfirst, and beats 1–3 — the question, the expectation, the reversal</name>
  <files>content/the-chart-therefore-changes.mdx</files>
  <read_first>
    - .planning/phases/04-the-case-study/live-text/en-story.txt — THE AUTHORITY for every English
      quotation. Read it before writing a sentence.
    - .planning/phases/04-the-case-study/live-text/en-methodology.txt — the published prior
      ("This project began with a widely shared suspicion…") and the author's own English wording
    - /Users/guillem/vault/projects/personal/data-story-ib-gdp/ARTICLE_PLAN.md — the abandoned
      Dutch-disease thesis in full; this is beat 2's material
    - .planning/phases/04-the-case-study/04-RESEARCH.md § "The Six-Part Spine" beats 1–3, and
      § "Corrections to CONTEXT" corrections C-2 and C-3
    - .planning/phases/02-content-pipeline/02-UI-SPEC.md § Prose Contract and § Copywriting
      Contract (the voice paragraph: engineering is demonstrated, never claimed; no adjectives
      about the work)
    - content/fixture.mdx (the front-matter fence shape and the exact `<Figure>` call form)
  </read_first>
  <action>
    Create `content/the-chart-therefore-changes.mdx` with the locked front-matter, then the single
    `Figure` import, then beats 1 through 3. First person singular throughout. The implied reader
    is a graphics editor who reads charts for a living: do not explain what a log scale, an index
    to 100 or a relative measure is, and do not assume any knowledge of Balearic or Spanish
    regional economics. Explain the subject, never the craft.

    Write the standfirst last, once the piece exists. It describes THIS case study, not the live
    piece — it must not echo the live standfirst (*"Tourism made the islands rich—or so the story
    goes. The data tells a more complicated story."*) and must not restate the work-list
    annotation (*"The Balearics stopped gaining on Europe in 1993 — while tourist arrivals went on
    tripling"*), which describes the piece rather than the case study. Plain text, one or two
    sentences, subject and judgment.

    **The first two paragraphs must carry the whole argument on their own.** The audience is a
    ninety-second scanner with forty tabs open. State, inside those two paragraphs: what the piece
    set out to test, that the expected answer did not survive the data, and that the response was
    to change what the chart measured mid-scroll. Someone who reads no further should still have
    the claim.

    `## The question` — around 200–250 words. Tourism accounts for 45.5% of the Balearic economy.
    The myth in one line: *"From developers to campaigners, left and right agree that tourism
    rescued the islands from poverty."* And the reason it is hard to dislodge: *"This is Mallorca's
    accepted economic history. Politicians repeat it. Hoteliers rely on it. Even critics of mass
    tourism, while condemning the damage it causes, tend to accept its central claim."* Quote from
    `live-text/en-story.txt`, not from memory and not from the vault draft.

    `## What I expected` — around 250–300 words, and this is CASE-02's hardest beat. It has two
    distinct records and conflating them misrepresents both. First, the published prior, verbatim
    from the live methodology page: *"This project began with a widely shared suspicion: tourist
    arrivals keep breaking records, but life for local people stopped improving long ago and may
    be getting worse. I wanted to know whether that impression would survive contact with the
    data."* Second, the shape the answer was expected to take: an article titled *"The Balearic
    Paradox: Is Tourism Mallorca's Dutch Disease?"*, structured as five symptoms — wages (about
    €23,100 average salary against about €31,600 needed for basic expenses), Catalan language
    decline, early school leaving at 20.1% and the worst in Spain despite the region ranking
    fourth or fifth by GDP, housing at 60.8 years to buy against 29.7 nationally, and the
    environment. Name it as an abandoned plan; `BRIEF` §5.5 licenses saying plainly that the first
    thesis was dropped. No self-deprecation, no hedging.

    Two traps in this beat, both load-bearing. **Do not claim the relative measure was invented
    mid-project** — the abandoned plan's own Chart 1 was already *"GDP per capita (relative to EU
    regional average) vs tourist arrivals over time"*. The honest and stronger claim is promotion:
    what was the discarded plan's opening exhibit became the shipped piece's closing reveal. And
    **the richest form-change material in that file is written entirely in library names.** Naming
    a library, framework or build tool is a scope violation. The abandoned plan's own library-free
    phrasing is the model: *"merge 4 stepped intro charts into one scroll-driven animated
    timeline."* Describe the form, never the tooling.

    `## What the data showed` — around 250–300 words. The reversal is sharper than "the thesis
    failed": the suspicion survived, the popular explanation for it did not. Two inversions.
    Extremadura is the control case — Spain's poorest region, landlocked, no resorts — and *"With
    no Magaluf, no s'Arenal, no beach resorts and not even a coast, GDP per head in Extremadura
    also increased tenfold within two generations."* Be precise here: only Extremadura is
    described as growing tenfold. Of Andalusia, Portugal, France and Ireland the shipped text says
    only that *"The same pattern appears"*. Writing "all grew tenfold" is an overstatement the
    source does not support. And: *"Tourism was the Balearics' route into that wider boom, but it
    was not the boom's underlying cause."* Second inversion: measured against Europe, *"the
    islands were not exceptionally poor throughout the first half of the century"* — the pre-1960
    line *"rises and falls, moving above and below the European average."* The first half of the
    myth dissolves; what survives is narrower and stranger.

    Preserve the causal posture exactly. `act2.md:64` records it: *"At no point does the piece
    claim tourism caused the climb, or caused the fall."* The defensible claim is that the climb
    was common and the fall is ours. Overstating it undermines the very judgement this case study
    exists to demonstrate — the words "proves", "caused" and "debunks" do not belong in it.

    Place F1 in this beat, after the Extremadura paragraph. Its alt text should describe six
    income series in constant dollars whose levels differ but whose shape is shared — a relatively
    flat first half of the century followed by sustained growth after 1950 — because that shared
    shape is exactly what the figure proves. The caption states what it proves and names the data
    source. **The unit is 2011 international PPP dollars.** The chart displays no currency mark of
    its own, so the caption must supply the unit; a euro sign anywhere near a figure caption is
    wrong by a conversion factor, and the test suite fails on it.
  </action>
  <verify>
    <automated>node -e "const fs=require('node:fs');const t=fs.readFileSync('content/the-chart-therefore-changes.mdx','utf8');const [,fm,body]=t.split(/^---$/m);for(const k of ['title:','standfirst:','date: \"2026-08-31\"','lang: en','translationKey: ib-gdp-case-study','draft: false','type: case-study'])if(!fm.includes(k))throw new Error('front-matter missing '+k);const h2=[...body.matchAll(/^## (.+)$/gm)].map(m=>m[1].trim());if(JSON.stringify(h2)!==JSON.stringify(['The question','What I expected','What the data showed']))throw new Error('beats 1-3 wrong: '+JSON.stringify(h2));if(/^#{3,6} /m.test(body))throw new Error('h3-h6 present');if(body.includes('!['))throw new Error('bare markdown image');const figs=[...body.matchAll(/<Figure[^>]*src=\"([^\"]+)\"/g)].map(m=>m[1]);if(JSON.stringify(figs)!==JSON.stringify(['/case-study/f1-constant-dollars.png']))throw new Error('F1 only, got '+JSON.stringify(figs));const prose=body.replace(/<Figure[\s\S]*?<\/Figure>/g,'').replace(/<[^>]+>/g,'').replace(/\[([^\]]*)\]\([^)]*\)/g,'\$1');const words=prose.split(/\s+/).filter(Boolean).length;if(words<550||words>950)throw new Error('beats 1-3 word count '+words+', expected 550-950');const live=fs.readFileSync('.planning/phases/04-the-case-study/live-text/en-story.txt','utf8');for(const q of ['From developers to campaigners','GDP per head in Extremadura also increased tenfold','was not the boom','rises and falls'])if(!live.includes(q.slice(0,30)))throw new Error('anchor absent from live text: '+q);console.log('beats 1-3 ok,',words,'words');"</automated>
  </verify>
  <acceptance_criteria>
    - The file exists with all seven front-matter fields at the locked values; `draft: false` and `type: case-study` are literal.
    - Exactly three `##` headings so far, reading `The question`, `What I expected`, `What the data showed` in that order.
    - Zero `###`–`######` headings; zero `![` bare Markdown images; zero fenced code blocks.
    - Exactly one `<Figure>`, `src="/case-study/f1-constant-dollars.png"`, `width={2400}`, `height={1640}`, no `wide`.
    - F1's `alt` is at least 120 characters and at least 20 words, and does not begin with `Chart of`, `A chart` or `Screenshot`.
    - F1's caption names at least one of Rosés-Wolf, Maddison, Eurostat, Funcas, Cirer-Costa.
    - No `€` occurs inside the `<Figure>` block.
    - Word count of beats 1–3, computed as in `<interfaces>`, is between 550 and 950.
    - `/usr/bin/grep -c "International Baccalaureate\|World Bank\|EU-15\|ib-gdp-evolution\|Observable Plot\|built with" content/the-chart-therefore-changes.mdx` returns 0.
    - Every sentence rendered as a quotation appears verbatim in `.planning/phases/04-the-case-study/live-text/en-story.txt` or `live-text/en-methodology.txt` — verified by string search, one search per quotation, results recorded in the SUMMARY.
  </acceptance_criteria>
  <done>Beats 1–3 exist, sourced sentence by sentence to the live pages and the abandoned plan, with F1 in place and the causal posture intact.</done>
</task>

<task type="auto">
  <name>Task 2: Beats 4–6 — the pivot, the result, the receipts</name>
  <files>content/the-chart-therefore-changes.mdx</files>
  <read_first>
    - .planning/phases/04-the-case-study/live-text/en-story.txt (the pivot sentence, the 1993
      finding, the 25-year and out-migration coda, the rendered headline)
    - .planning/phases/04-the-case-study/live-text/en-methodology.txt (the chaining approaches,
      the 20% swing, the honest sting, the exclusions, the sensitivity test, the citations)
    - .planning/phases/04-the-case-study/04-RESEARCH.md § "The Six-Part Spine" beats 4–6, § its
      corrections C-5, C-6 and C-7, and § "Common Pitfalls" 5, 6 and 7
    - /Users/guillem/vault/projects/personal/ib-gdp-evolution/act2.md — read for REASONING ONLY.
      Its numbers are draft numbers and disagree with what shipped (EU-15 for EU-27, "around 1990"
      for 1993, a different step count). Never take a number from it.
    - .planning/phases/02-content-pipeline/02-UI-SPEC.md § Prose Contract — the blockquote row
      (italic between two hairlines) and the `<Aside>` row (4px left bar, optional Label kicker)
  </read_first>
  <action>
    Append beats 4, 5 and 6 to the same file, then write the standfirst into the front-matter.

    `## Where the chart changed` — around 300 words, and this is the piece's centre. Open the beat
    with the pivot as a Markdown blockquote, verbatim: *"The chart therefore changes. Instead of
    plotting income in dollars, it expresses each economy as a percentage of the EU average."*
    Quote it rather than paraphrasing — it is the author's own published prose, it names the
    editorial move exactly, it is where the title comes from, and the prose contract gives a
    blockquote the pull-quote treatment. If you set an emphasised phrase inside that quote, note
    that `blockquote em` resets to upright; the spec names this case study as where that reset
    first matters.

    Then the mechanics, precisely and without naming a tool. The axis stops plotting income in
    constant 2011 international PPP dollars and starts plotting each economy as a percentage of
    the EU-27 average with 100 as the baseline. The new scale is calibrated on screen against two
    anchors the reader can hold: *"In 2022, for instance, Ireland stood at 158%, while Bulgaria,
    the EU's poorest country, stood at 54%."* Say why the change had to happen mid-scroll rather
    than as a second chart at the bottom: the absolute view is not wrong, it is answering a
    different question, and the argument only becomes visible when the measure changes under the
    same reader.

    One precision the caption and alt text must respect: this is not the same data replotted.
    Switching the measure also changed the comparator set — the six absolute comparators give way
    to the Balearics, Ireland, Bulgaria and the EU average, because the new scale needs
    calibrating rather than populating. Say the *measure* changed; do not say "the same data".

    Place F2 here. Its alt describes an axis benchmarked on the EU average, the 100 line as the
    average, Ireland well above it and Bulgaria far below. Same width and framing as F1 — they are
    a before/after of the measure and a width change between them would destroy the comparison.

    `## What shipped` — around 250 words. The finding, verbatim: *"The Balearics stopped gaining
    ground on Europe in 1993 and have fallen behind ever since, even as arrivals have tripled."*
    The coda: *"It took 25 years from the end of the Balearics' relative economic rise to the
    first mass protests against tourism"*, and *"More young people are leaving the islands for
    work—three times as many as in 2009"*. Answer the obvious objection before it is raised —
    Ireland climbed after 1990 on the same axis, so Balearic flatness is not a continental
    ceiling, it is Balearic. That anticipated objection is itself the editorial judgement on
    display.

    The live piece's rendered headline is **"Everyone in Mallorca Knows It"**. That is what to
    quote; the slug says something else and is not the headline. The piece ships in several
    languages — say "in several languages", or name only the ones you have checked. Do not assert
    a count.

    This beat carries the phase's single outbound link: one plain inline link, in a sentence, to
    `https://ib-gdp.guillemgelabert.com/everyone-in-mallorca-agrees-on-one-thing`. Not a labelled
    affordance, not a button, not a "view the piece" block, no `target="_blank"`. It must fall
    inside this section and nowhere else.

    Place F3 here, with `wide`. Its alt describes two series on a shared x-axis: Balearic income
    relative to Europe rising through the 1960s, 1970s and 1980s, stopping gaining ground around
    1993, then stagnating or falling, while tourist arrivals continue to rise — two lines that once
    moved together and now diverge. That divergence is the whole thesis rendered in two lines,
    which is why this is the wide one.

    `## Methodology` — around 250–300 words, and the piece **ends here**. Receipts last. No call to
    action of any kind, no "get in touch", no closing paragraph that restates the argument. The
    last sentence of the file is a methodology sentence.

    The material, all of it the author's own published English. The GDP series is chained from
    sources that do not agree on units — Rosés-Wolf and Maddison report constant 2011 international
    PPP dollars, Eurostat reports current PPS per person, which is not inflation-adjusted — and
    chaining them is the hard problem. Three approaches, two rejected. The single-year anchor:
    *"This approach assumes that the relationship between PPP dollars and PPS never changes.
    Because PPS is not adjusted for inflation, that would amount to assuming zero inflation—clearly
    unrealistic."* And the cost of that assumption, which is the sharpest number available:
    *"Using 2000 rather than 2022 changes the estimate by more than 20%."* The two-point
    log-linear: works only if the relationship changes steadily. The adopted method keeps every
    Rosés-Wolf observation as an anchor and rescales between them.

    Then the sting, and it is the most credible sentence the whole project has: *"Up to 2020, the
    simpler single-year method is actually closer to Rosés-Wolf's real GDP figures"* — the more
    sophisticated method does not win outright, and the published methodology says so in public.
    Report what the page says. Do not write that the author "did not anticipate" it or was
    surprised by it; the page never claims surprise, and inventing an emotional state is a
    fabrication like any other.

    Name two more visible judgement calls in running prose: 2020 and 2021 are excluded because
    *"The pandemic was a large but temporary external shock, and plotting those years would obscure
    rather than clarify the 125-year trend"*; and no regional deflator is used, because Balearic
    CPI stayed within 1% of Spain's between 2002 and 2024, stated with the author's own caveat that
    this is an approximation and living costs are higher in the islands. Include here, in running
    prose rather than in the aside, the caveat that the EU-27 average is computed rather than
    published — population-weighted across today's 27 members, with coverage incomplete before
    1985, so the benchmark's own composition shifts under the early half of the series. That is
    the losing aside candidate, and the coordinator's decision is that it goes into prose.

    The single `<Aside>` carries the chained arrivals series, with a Label-role kicker. No
    continuous series of Balearic tourist arrivals exists, so four sources are chained, preferring
    the official series wherever years overlap: Cirer-Costa for 1900–1936, Barceló Pons (1966) for
    1925–1965, Valdivielso and Moranta for 1959–2019, and the AETIB yearbooks with FRONTUR for
    1998–2025. That is a real editorial decision with a visible cost. If you want a sensitivity
    figure, the published one is that even if the 1920 estimate were wrong by a factor of ten — so
    2,000 or 200,000 tourists rather than roughly 20,000 — the angle of the rise from 1960 would
    not change and the article's conclusion would hold.

    On citations: three variants of the Valdivielso and Moranta reference exist across the author's
    own surfaces. Give author names and year, or author names and the DOI. **Do not print a volume
    or issue number** — every available choice contradicts one of the author's own pages.

    Finally, write the standfirst into the front-matter. Then read the whole file once, end to end,
    for the ninety-second test: do the first two paragraphs carry the argument alone?
  </action>
  <verify>
    <automated>npm run test:unit &amp;&amp; npx playwright test tests/case-study.spec.ts --grep-invert "die-darstellung|German|Texte" --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/unit/case-study-content.test.ts` — every English assertion passes. The only remaining failures in that file name the missing German file.
    - `node --test tests/unit/case-study-figures.test.ts` — every English assertion passes; three Figures, dimensions matching the PNG bytes, alt and caption thresholds met.
    - The English half of `tests/case-study.spec.ts` passes: six `h2` ids in exactly the order `the-question`, `what-i-expected`, `what-the-data-showed`, `where-the-chart-changed`, `what-shipped`, `methodology`; zero `h3`–`h6`; one `aside` positioned after `h2#methodology`; three `figure`, one `figure[data-wide]` and it is the third; three `img` each measuring `naturalWidth` 2400; exactly one link to `ib-gdp.guillemgelabert.com`; one `blockquote` computing to `font-style: italic`.
    - Total word count, computed as in `<interfaces>`, is between 1200 and 1800.
    - The outbound link's character offset falls between the fifth and sixth `##` headings.
    - `/usr/bin/grep -c "€" content/the-chart-therefore-changes.mdx` may be non-zero (beat 2's salary figures are legitimately euros), but zero `€` occurs inside any `<Figure>` block.
    - The file's last non-blank line is prose in the Methodology section — not a link, not a heading, not a call to action.
    - Every quoted sentence verified verbatim against `live-text/en-story.txt` or `live-text/en-methodology.txt` by string search; the count of quotations checked and matched is recorded in the SUMMARY.
  </acceptance_criteria>
  <done>The English case study is complete: six beats in CASE-02's order, three figures, one aside, one pull quote, one outbound link, ending on the methodology note with no pitch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → published prose | This file ships without human proofreading. The mechanical gates from Plan 02 and the verbatim check against Plan 01's snapshots are the entire barrier. |
| repo content → build-time MDX compile | `assertFrontmatter` validates this file's front-matter during `next build`; malformed fields fail the build rather than shipping. |
| this file → visitor | Publishing with `draft: false` is a deliberate access-control change: `isVisible` starts returning true in production, and the piece becomes reachable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Spoofing | A quotation that was never published, taken from a vault working copy | mitigate | Every quotation is string-searched against `live-text/en-story.txt` / `en-methodology.txt` before the task closes, and the result count is recorded. The vault drafts are explicitly not authorities; `act2.md` is read for reasoning only. |
| T-04-10 | Repudiation | Overstating the live piece's causal claim, which the author never made | mitigate | The action text fixes the causal posture verbatim from `act2.md:64` and bans "proves", "caused", "debunks". Only Extremadura is described as growing tenfold; the others share the pattern. |
| T-04-11 | Information Disclosure | Naming or linking the private `ib-gdp-evolution` repository | mitigate | `tests/unit/case-study-content.test.ts` asserts both the repo name and any `github.com` URL are absent. The single outbound link points at the public deployment. |
| T-04-12 | Tampering | Reverse tabnabbing on the outbound link | accept | Not applicable, and deliberately not "fixed". The site uses no `target="_blank"` anywhere, so there is no new-window context to hijack and `rel="noopener"` would be the only instance on the site. The gate asserts `target="_blank"` is absent. |
| T-04-13 | Elevation of Privilege | MDX importing an arbitrary component | mitigate | One import, of `Figure`, from `@/components/mdx/figure`. The component allowlist assertion enforces it. |
| T-04-SC | Tampering | npm / pip / cargo installs | accept | No package is installed. This plan writes one Markdown-family file. |

ASVS V5 is satisfied by `assertFrontmatter`, already built, which this plan supplies conforming
input to; V12 by `CONTENT_DIR` and `SAFE_SLUG`, both unchanged. V2, V3, V4-beyond-draft-visibility
and V6 remain inapplicable: static, read-only, form-free, auth-free.
</threat_model>

<verification>
- `npm run test:unit` — `case-study-content` and `case-study-figures` pass every English assertion; the only failures remaining name `content/die-darstellung-aendert-sich.mdx`, which Plan 04 creates.
- `npx playwright test tests/case-study.spec.ts` — the English block passes; the German block still fails.
- `npx playwright test tests/writing-index.spec.ts tests/build` — **expected to fail**, and owned by Plan 05. Publishing the English file makes `/writing` list two entries in dev and changes what the production build emits for the featured slot. Five of the six known breaking assertions go red here. Do not "fix" them in this plan; Plan 05 rewrites them to the published state.
- `npm run lint` — still exactly one error, the known deferred `use-prefers-reduced-motion.ts:23`.
</verification>

<success_criteria>
- `content/the-chart-therefore-changes.mdx` exists, validates against `assertFrontmatter`, and carries `draft: false` and `type: case-study`.
- Six `##` sections in CASE-02's order; zero `###`–`######`; three Figures at 2400×1640 with the third `wide`; one Aside in Methodology; one blockquote carrying the pivot sentence verbatim; one inline outbound link inside "What shipped".
- 1,200–1,800 words, with the first two paragraphs carrying the argument alone.
- Zero engineering claims, zero fabricated sources, zero euro signs in a figure caption.
- The piece ends on the methodology note. Nothing after it.
</success_criteria>

<output>
Create `.planning/phases/04-the-case-study/04-03-SUMMARY.md` when done. Record: the final word
count; the standfirst as shipped; the number of quotations checked and the number matched verbatim
against the live-text snapshots; and the exact list of assertions that are now red and owned by
Plan 05.
</output>
