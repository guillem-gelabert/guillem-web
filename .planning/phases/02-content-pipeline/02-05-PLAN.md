---
phase: 02-content-pipeline
plan: 05
type: execute
wave: 5
depends_on: ["02-03", "02-04"]
files_modified:
  - tests/prose-typography.spec.ts
  - tests/prose-code.spec.ts
  - tests/fixture-viewport.spec.ts
  - tests/font-cls.spec.ts
  - app/globals.css
  - mdx-components.tsx
autonomous: true
requirements: [WRIT-01]

must_haves:
  truths:
    - "Rendered prose computes to Phase 1's tokens, not `@tailwindcss/typography`'s defaults"
    - "Every code block carries Shiki's class, an accessible name, keyboard focus and no inline theme background"
    - "Inline code is tinted and full-ink, never token-coloured"
    - "The fixture renders every Prose Contract element at 375px and at 1440px with no horizontal page overflow"
    - "Adding the Newsreader italic and IBM Plex Mono did not regress the no-layout-shift posture"
  artifacts:
    - path: "tests/prose-typography.spec.ts"
      provides: "SC3 coverage — measured computed values for p, h2, h3, strong, em, blockquote, table cells"
    - path: "tests/prose-code.spec.ts"
      provides: "SC4 coverage — Shiki class, stripped background, accessible name, keyboard focus, unhighlighted inline code"
    - path: "tests/fixture-viewport.spec.ts"
      provides: "SC5 coverage — every Prose Contract element present at both viewports, no page-level horizontal overflow"
    - path: "tests/font-cls.spec.ts"
      provides: "BUILD-06 regression guard extended to the post route"
      contains: "/writing/fixture"
  key_links:
    - from: "tests/prose-typography.spec.ts"
      to: "app/globals.css .prose-site"
      via: "getComputedStyle on rendered prose elements"
      pattern: "getComputedStyle"
    - from: "tests/prose-code.spec.ts"
      to: "mdx-components.tsx pre override"
      via: "asserting role, aria-label and the absence of an inline background-color"
      pattern: "aria-label"
---

<objective>
Prove success criteria 3, 4 and 5 against a real render of the fixture post, and re-point Phase 1's
CLS guard at the new route.

Purpose: Plans 03 and 04 wrote the prose layer and the content; nothing has measured either. This
plan is where "prose reads Phase 1's system, not plugin defaults" stops being a claim. It is also
where RESEARCH's one unexercised assumption (A1 — that destructuring `style` off the `pre` props
actually removes Shiki's inline background from the final markup) gets confirmed or repaired.

Output: three new Playwright specs, an extended `tests/font-cls.spec.ts`, and any corrective edits
to `app/globals.css` or `mdx-components.tsx` the measurements turn out to require.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-content-pipeline/02-RESEARCH.md
@.planning/phases/02-content-pipeline/02-PATTERNS.md
@.planning/phases/02-content-pipeline/02-UI-SPEC.md
@.planning/phases/02-content-pipeline/02-VALIDATION.md
</context>

<interfaces>
<!-- The two assertion-style rules Phase 1 paid for, recorded in STATE.md. Both bind here. -->

1. **Assert measured computed values, never values assumed from a plan.** Phase 1's
   `tests/viewport.spec.ts` had to assert the real `clamp()` output (139.2px at 1440px) rather than
   the plan's near-ceiling guess. Run the page, read the value, write the observed number into the
   assertion, and record any surprise as a comment in the spec.
2. **`page.emulateMedia({ reducedMotion: "reduce" })` before `page.goto()`.** Playwright's
   `reducedMotion` context/test option did not affect `matchMedia` in this environment
   (1.62.1 / Chromium), which has not changed.

Corrective-edit budget: `app/globals.css` and `mdx-components.tsx` are in this plan's
`files_modified` **only** so a measurement that disagrees with the Prose Contract can be fixed at
its source rather than papered over in an assertion. Any edit must be corrective, must keep
`tests/unit/prose-contract.test.ts` green, and must be recorded in the SUMMARY.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Measure the prose typography against Phase 1's tokens</name>
  <files>tests/prose-typography.spec.ts, app/globals.css</files>
  <read_first>
    - tests/viewport.spec.ts (lines 1-72: the deviation-note convention, the `readSizes` helper shape that reads `getComputedStyle(...).fontSize` inside `page.evaluate`, the `TOLERANCE_PX = 4` idiom, and the parameterised-viewport loop)
    - tests/smear-heading.spec.ts (lines 30-45: the `await page.evaluate(() => document.fonts.ready)` + short settle wait that must precede any measurement)
    - app/globals.css (the `.prose-site` block just written — the values under test)
    - .planning/phases/02-content-pipeline/02-UI-SPEC.md § Prose Contract and § Typography (the authored values these computed values must resolve to)
    - content/fixture.mdx (the elements available to measure)
  </read_first>
  <action>
Create `tests/prose-typography.spec.ts`, opening with a `// Covers WRIT-01 (SC3): …` header block
stating the behaviour: rendered prose computes to Phase 1's four type roles rather than
`@tailwindcss/typography`'s defaults.

Navigate to `/writing/fixture`, await `document.fonts.ready` and a short settle, then read computed
values in a single `page.evaluate` that returns a structured object — one round trip, not one per
assertion. Measure at least: a `.prose-site p`, the first `.prose-site h2`, the first
`.prose-site h3`, a `.prose-site strong`, a `.prose-site em`, a `.prose-site blockquote`, a
`.prose-site blockquote em`, a `.prose-site th` and a `.prose-site td`.

Assert against **authored intent expressed as computed output**, not against the authored strings:

- `p` — `fontFamily` contains `Newsreader`, `fontSize` is `18px`, `fontWeight` is `400`, and
  `lineHeight` is the computed pixel value of `1.6` at 18px (`28.8px`), not the string `1.6`
- `h2` — `fontSize` `14px`, `textTransform` `uppercase`, `letterSpacing` the computed pixel value of
  `0.04em` at 14px (`0.56px`), a non-zero `borderBottomWidth`, and `marginTop` `48px`
- `h3` — the same type metrics as `h2`, `borderBottomWidth` `0px`, and `marginTop` `32px`. Assert
  the pair together in one test named for the hierarchy signal they carry: identical type, split
  only by the rule and the margin
- `strong` — `fontWeight` `530` (not 600, not 700)
- `em` — `fontStyle` `italic` with `fontWeight` `400`, and `fontFamily` still Newsreader — this is
  the proof the loader's added italic actually loaded
- `blockquote` — `fontStyle` `italic`, non-zero `borderTopWidth` and `borderBottomWidth`,
  `borderLeftWidth` `0px`
- `blockquote em` — `fontStyle` `normal`. Without the reset this is invisible, and Phase 4 is where
  it would first bite
- `th` — `14px`, uppercase, `0.56px` tracking; `td` — `14px`, `textTransform` `none`,
  `letterSpacing` `normal` or `0px`, and `fontVariantNumeric` containing `tabular-nums`

Then assert the **negative** that makes this success criterion meaningful: the typography plugin's
own scale is not in force. Read `getComputedStyle` on the `.prose` wrapper's `--tw-prose-body`
custom property or, more robustly, assert that no `.prose-site` element computes to a font size
outside `{14px, 18px}` — walk the rendered prose subtree in the same `evaluate` and collect the
distinct computed `fontSize` values, then assert the set is a subset of those two. Exclude
`pre code` spans, which inherit.

Record the two Phase 1 lessons as a comment at the top of the file, and if any measured value
surprises you, write the surprise and the reasoning into the file rather than silently loosening
the assertion. If a value genuinely disagrees with the Prose Contract, fix `app/globals.css` — the
contract is the source of truth, the assertion follows it — and note the fix in the SUMMARY.
  </action>
  <verify>
    <automated>npx playwright test tests/prose-typography.spec.ts && npm run test:unit && npx playwright test</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test tests/prose-typography.spec.ts` passes
    - `grep -c '28.8' tests/prose-typography.spec.ts` is at least 1 (the computed line-height, not the authored `1.6`)
    - `grep -c '0.56' tests/prose-typography.spec.ts` is at least 1 (the computed tracking, not the authored `0.04em`)
    - `grep -c '530' tests/prose-typography.spec.ts` is at least 1
    - `grep -c 'document.fonts.ready' tests/prose-typography.spec.ts` is at least 1
    - `grep -c 'Covers WRIT-01' tests/prose-typography.spec.ts` is 1
    - `npm run test:unit` still exits 0 (the `.prose-site` conformance gate survived any corrective edit)
    - `npx playwright test` reports the full suite green
  </acceptance_criteria>
  <done>Success criterion 3 is measured: every rendered prose element resolves to one of Phase 1's two fixed sizes and two weights, and the set of computed font sizes in the prose subtree contains nothing else.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the code blocks — Shiki, accessibility, and the stripped background</name>
  <files>tests/prose-code.spec.ts, mdx-components.tsx</files>
  <read_first>
    - mdx-components.tsx (the `pre` override under test)
    - .planning/phases/02-content-pipeline/02-RESEARCH.md § Pattern 9 (the verified output shape, the documented CSS fallback, and the note that the `style` destructure specifically was NOT exercised in the spike — Assumption A1), § Pitfall 1 (why the theme is `github-light-high-contrast`), § Pitfall 3 (why every `pre` must carry the `shiki` class) and § Code Examples → "Verified Shiki output shape"
    - .planning/phases/02-content-pipeline/02-UI-SPEC.md § Prose Contract, the `<pre>` and `<code>` rows, and § Color → "Syntax highlighting palette — a scoped exemption"
    - tests/type-specimen.spec.ts (the spec shape and header-block convention)
    - content/fixture.mdx (the two fenced blocks: their languages, which one overflows, which one contains a brace)
  </read_first>
  <action>
Create `tests/prose-code.spec.ts`, opening with a `// Covers WRIT-01 (SC4): …` header block.

Navigate to `/writing/fixture`, await `document.fonts.ready`, then in one `page.evaluate` collect,
for **every** `pre` inside `.prose-site`: its `className`, its inline `style` attribute text, its
`tabindex`, `role` and `aria-label` attributes, its computed `fontFamily`, `fontSize`,
`lineHeight`, `backgroundColor`, `borderRadius` and `whiteSpace`, and its `scrollWidth` versus
`clientWidth`.

Assert:
- every `pre` has a class list containing `shiki` and `github-light-high-contrast`. An unknown
  fenced language degrades silently to a bare unhighlighted block that still picks up the surface
  tint, so this assertion is what makes a typo'd language loud
- every `pre`'s inline `style` attribute contains no `background-color`. This is RESEARCH
  Assumption A1, the one mechanism in the phase that was reasoned about rather than built. If it
  fails, apply the documented fallback — a single unlayered rule
  `.prose-site pre.shiki { background-color: transparent !important; color: var(--color-ink) !important; }`
  in `app/globals.css` (this is the one place `!important` is legitimate, because it is fighting an
  inline attribute) — and record in the spec and the SUMMARY that A1 did not hold
- every `pre` has `tabindex` `0` exactly once (Shiki emits it; the override must not add a second),
  `role` `region`, and a non-empty `aria-label`. A horizontally scrolling `pre` without both fails
  WCAG 2.1.1 and 4.1.2
- computed `fontFamily` on `pre` contains `IBM Plex Mono`; `fontSize` is `18px`; `lineHeight` is the
  computed pixel value of `1.5` at 18px (`27px`); `borderRadius` is `0px`; `whiteSpace` is `pre`
- computed `backgroundColor` on `pre` is the resolved ink tint, measured from the render rather
  than assumed — read it once and write the observed `rgba(...)` string into the assertion
- at least one `pre` has `scrollWidth` greater than `clientWidth` at the default viewport, proving
  the overflow case exists and scrolls internally
- token colouring is present: at least one `span` inside a `pre` carries an inline `color` style
- inline code is **not** highlighted: for every `code` that is not a descendant of a `pre`, no
  descendant `span` carries an inline `color` style, and its computed `color` is the full ink value
  while its computed `backgroundColor` is the tint

Add one keyboard assertion outside the `evaluate`: focus the overflowing `pre` with
`page.keyboard` or `locator.focus()` and assert `document.activeElement` is that element — the
scroll region must be reachable without a mouse.

Finally assert the brace case: the fenced block containing `{` renders the character literally in
the DOM text, proving MDX did not try to evaluate it as an expression.
  </action>
  <verify>
    <automated>npx playwright test tests/prose-code.spec.ts && npm run test:unit && npx playwright test</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test tests/prose-code.spec.ts` passes
    - `grep -c 'github-light-high-contrast' tests/prose-code.spec.ts` is at least 1
    - `grep -c 'background-color' tests/prose-code.spec.ts` is at least 1 (the A1 assertion)
    - `grep -c 'aria-label' tests/prose-code.spec.ts` is at least 1 and `grep -c 'tabindex' tests/prose-code.spec.ts` is at least 1
    - `grep -c 'scrollWidth' tests/prose-code.spec.ts` is at least 1
    - `grep -c '27px' tests/prose-code.spec.ts` is at least 1 (computed line-height, not the authored `1.5`)
    - `npx playwright test` reports the full suite green
    - If the A1 fallback was needed, `app/globals.css` contains exactly one `!important` rule and the SUMMARY records why
  </acceptance_criteria>
  <done>Success criterion 4 is measured: every code block is Shiki-highlighted at the high-contrast theme, sits on the ink tint with no theme background of its own, is keyboard-reachable with an accessible name, and inline code is deliberately left uncoloured.</done>
</task>

<task type="auto">
  <name>Task 3: The fixture at 375px and 1440px, and the CLS regression guard</name>
  <files>tests/fixture-viewport.spec.ts, tests/font-cls.spec.ts</files>
  <read_first>
    - tests/viewport.spec.ts (lines 50-72: the parameterised `VIEWPORTS` loop with `page.setViewportSize` — copy this shape exactly)
    - tests/font-cls.spec.ts (the whole file: the `addInitScript` PerformanceObserver installed **before** navigation, the `document.fonts.ready` + `requestAnimationFrame` settle, the `hadRecentInput` filter, and the `< 0.1` threshold)
    - content/fixture.mdx (the exhaustive element list this spec checks for)
    - .planning/phases/02-content-pipeline/02-UI-SPEC.md § The fixture post (success criterion 5) — in particular that the 375px pass is the one that catches real failures, and that internal scrolling of code and tables is acceptable behaviour while page-level horizontal scrolling is not
    - .planning/phases/02-content-pipeline/02-RESEARCH.md § Pitfall 8 and § Assumptions Log A5 (two added font files are this phase's most plausible silent regression)
  </read_first>
  <action>
Create `tests/fixture-viewport.spec.ts`, opening with a `// Covers WRIT-01 (SC5): …` header block.
Use the parameterised viewport loop from `tests/viewport.spec.ts` over exactly two sizes:
375×812 and 1440×900. In each iteration set the viewport, navigate to `/writing/fixture`, await
`document.fonts.ready` and settle, then assert in one `evaluate`:

- **Every** Prose Contract element is present in the rendered prose: at least one each of `h2`,
  `h3`, `p`, `strong`, `em`, `blockquote`, `blockquote em`, `code` outside a `pre`, `ul`, `ol`,
  a nested `ol ol` or `ol ul`, `table`, `th`, `td`, `figcaption`, `aside`, `hr`, an `a[href^="/"]`
  and an `a[href^="http"]`; exactly two `pre`; exactly two `figure`, one of which carries
  `data-wide`. Return the counts and assert them by name so a failure says which element is missing
- **No page-level horizontal overflow**: `document.documentElement.scrollWidth` is not greater than
  `window.innerWidth` (allow a 1px rounding tolerance). Internal scrolling is fine and expected —
  a `pre` or a `.prose-table` whose `scrollWidth` exceeds its own `clientWidth` is correct
  behaviour, and 18px mono forcing that at 375px is accepted, not a bug. Assert the distinction
  explicitly so the two cannot be confused
- the `h2`/`h3` pair is still measurable at 375px: read both `marginTop` values and both
  `borderBottomWidth` values and assert `48px`/non-zero versus `32px`/`0px`. UI-SPEC designates
  this the thinnest hierarchy signal in the contract, and the 375px pass is where it is judged.
  Write into the file that if it fails optically the fix is more space above `h2`, never a fifth
  type size

Then extend `tests/font-cls.spec.ts` by **parameterising its existing body over
`["/", "/writing/fixture"]`** rather than duplicating the 48-line block. Keep the observer
installation before navigation, keep the `hadRecentInput` filter, keep the `< 0.1` threshold, and
keep the existing `/` case passing unchanged. Add a comment naming why the second case exists:
this phase adds a second Newsreader file (the italic) and IBM Plex Mono to a route that also
renders a fixture full of `pre` blocks, which RESEARCH flags as the phase's most plausible silent
regression against BUILD-06.
  </action>
  <verify>
    <automated>npx playwright test tests/fixture-viewport.spec.ts tests/font-cls.spec.ts && npx playwright test && npm run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test tests/fixture-viewport.spec.ts` passes at both viewports
    - `grep -c '375' tests/fixture-viewport.spec.ts` is at least 1 and `grep -c '1440' tests/fixture-viewport.spec.ts` is at least 1
    - `grep -c 'scrollWidth' tests/fixture-viewport.spec.ts` is at least 2 (the page-level assertion and the internal-scroll distinction)
    - `grep -c 'data-wide' tests/fixture-viewport.spec.ts` is at least 1
    - `grep -c '/writing/fixture' tests/font-cls.spec.ts` is at least 1
    - `tests/font-cls.spec.ts` still asserts `< 0.1` and still installs its observer via `addInitScript` before `page.goto`
    - `npx playwright test` reports the full suite green, including the original `/` CLS case
  </acceptance_criteria>
  <done>Success criterion 5 is measured at both widths — every Prose Contract element renders, code and tables scroll inside themselves while the page does not — and the no-layout-shift posture survives the third face and the added italic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan adds no new runtime surface: it reads a rendered page and asserts against it. The
boundary it *guards* is the one Plan 03 created — the `pre` component override standing between
Shiki's output and the DOM.

| Boundary | Description |
|----------|-------------|
| Shiki output → prerendered `<pre>` | Inline attributes and the accessible name are decided here |

ASVS V2, V3, V4, V6 and V13 are inapplicable to this plan. V14 (configuration) applies to the
forward note only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-21 | Availability (of the feature) | Shiki's inline `background-color` surviving the `pre` override (RESEARCH Assumption A1) | mitigate | Task 2 asserts the absence of `background-color` in every `pre`'s inline `style` attribute. Documented fallback if it fails: one unlayered `!important` rule in `app/globals.css`, which is the only legitimate use of `!important` in this phase because it is fighting an inline attribute. |
| T-02-22 | Denial of service (accessibility) | A horizontally scrolling code block unreachable by keyboard or unnamed to assistive tech | mitigate | Task 2 asserts `tabindex="0"` (exactly once — Shiki already emits it), `role="region"` and a non-empty `aria-label`, plus a real focus assertion. WCAG 2.1.1 and 4.1.2. |
| T-02-23 | Availability | CLS regression from the third face and the added Newsreader italic | mitigate | `tests/font-cls.spec.ts` extended to `/writing/fixture` with the same `< 0.1` threshold. RESEARCH Assumption A5 names this the phase's most plausible silent regression. |
| T-02-24 | Availability | Phase 6 CSP `style-src` vs Shiki's per-token inline `style` attributes and `remark-gfm`'s inline table alignment | transfer | Both confirmed present by Task 2's own assertions. Recorded for Phase 6 BUILD-04; the pragmatic resolution is `style-src 'self' 'unsafe-inline'`, and the visible symptom if it is missed is monochrome code blocks. |
</threat_model>

<verification>
- `npx playwright test` reports the full suite green, including the 9 Phase 1 specs
- `npm run test:unit` exits 0, including the `.prose-site` conformance gate
- `npx tsc --noEmit` exits 0
- Any corrective edit to `app/globals.css` or `mdx-components.tsx` is recorded in the SUMMARY with
  the measurement that prompted it
</verification>

<success_criteria>
Success criteria 3, 4 and 5 are proven by measurement rather than assertion: prose computes to
Phase 1's two fixed sizes and two weights and nothing else, code blocks are Shiki-highlighted,
tinted, keyboard-reachable and named, the fixture renders every contracted element at 375px and
1440px without the page scrolling sideways, and cumulative layout shift on the post route stays
below 0.1.
</success_criteria>

<output>
Create `.planning/phases/02-content-pipeline/02-05-SUMMARY.md` when done
</output>
</content>
