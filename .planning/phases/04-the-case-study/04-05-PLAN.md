---
phase: 04-the-case-study
plan: 05
type: execute
wave: 5
depends_on: ["04-03", "04-04"]
files_modified:
  - tests/build/prerender.test.ts
  - tests/writing-index.spec.ts
autonomous: true
requirements: [CASE-01, CASE-03, HOME-02]

must_haves:
  truths:
    - "A visitor on the landing view can reach the case study: the featured headline is a link to /writing/the-chart-therefore-changes and it is the slot's only link."
    - "The featured slot renders the post's own title and standfirst — the work-list annotation for the featured entry links into the case study rather than duplicating it."
    - "The case study prerenders as real HTML in a production build, not only in development."
    - "The launch gate narrows from three interim surfaces to two: the featured slot's third is closed, backlog and contact remain."
    - "The full suite — units, build tier and Playwright — is green again."
  artifacts:
    - path: "tests/build/prerender.test.ts"
      provides: "Production-tier proof of the published featured slot, the non-empty index, and the prerendered post route"
      contains: "the-chart-therefore-changes"
    - path: "tests/writing-index.spec.ts"
      provides: "Dev-tier index assertions updated for two English entries and three German ones"
      contains: "The Chart Therefore Changes"
  key_links:
    - from: "tests/build/prerender.test.ts"
      to: ".next/server/app/index.html"
      via: "reading real prerendered HTML for the featured slot's <a>"
      pattern: "href=\"/writing/the-chart-therefore-changes\""
    - from: "tests/build/prerender.test.ts"
      to: "the launch gate"
      via: "narrowed assertion covering only the backlog and contact stubs"
      pattern: "Nothing listed here yet."
---

<objective>
Close the six assertions publishing broke, and turn the published state into proof.

Research located every one of these by line number and called them the single most
under-estimated item in the phase. They are **not** flaky tests and they are **not** regressions:
each one asserts an interim state that this phase's whole purpose was to end. Four live in
`tests/build/prerender.test.ts`, two in `tests/writing-index.spec.ts`, and
`prerender.test.ts:278` already carries a forward note saying this update was expected.

This plan is also where CASE-03 and HOME-02 are actually *proven*. Publishing the MDX file caused
the featured slot to flip — there is no boolean anywhere, the slot is derived from
`findBySlug(await publishedFor("en"), CASE_STUDY_SLUG)` and `CASE_STUDY_SLUG` was already correct
before this phase began. But "it flipped" is only a claim until something reads the real
prerendered HTML and finds the link. Playwright cannot do it: every spec runs against
`npm run dev`, where `showDrafts()` is always true, so it structurally cannot prove what a
production build emits. The build tier can.

Net production code change for the whole phase remains zero. This plan touches two test files.

Purpose: CASE-03, HOME-02, and the production half of CASE-01.
Output: two updated test files and a fully green suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-the-case-study/04-VALIDATION.md
@.planning/phases/04-the-case-study/04-04-SUMMARY.md
@.planning/phases/03-work-list-landing-skeleton/03-UI-SPEC.md
@components/landing/featured-slot.tsx

<interfaces>
<!-- The six breaking assertions, located by research, with the change each needs. -->

tests/build/prerender.test.ts
  :102  "both /writing and /texte render their empty state"  — /writing gains a published entry.
        The /texte half changes ONLY if Plan 04 shipped the German draft: false.
  :268  "the featured slot ships its interim copy in production" — asserts
        "The case study is being written."; the slot now resolves.
  :289  "the interim featured headline carries no link" — assert.doesNotMatch(match[0], /<a/);
        the published headline IS a link.
  :342  "launch gate: the featured slot, the backlog stub and the contact stub are all still
        interim" — the featured slot is no longer interim.

tests/writing-index.spec.ts
  :21   "/writing renders exactly one article whose h2 is the sole link" — toHaveCount(1), and
        articles.first() is asserted to be the fixture. The case study is dated 2026-08-31, newer
        than all three fixtures (2026-08-29/30), so it sorts FIRST.
  :85   "/texte renders two articles separated by hr, reverse-chronological…" — toHaveCount(2),
        hr toHaveCount(1), and an exact title list. Breaks EITHER WAY, because Playwright runs
        against npm run dev where showDrafts() is always true.

Sort order is reverse-chronological then alphabetical by slug (byDateThenSlug in lib/content.ts):
  /writing  → The Chart Therefore Changes (2026-08-31), then the fixture (2026-08-30)
  /texte    → Die Darstellung ändert sich (2026-08-31), then Eine Musterseite… , then
              Nur auf Deutsch…

One trap research flagged explicitly: with two `<hr>` elements on /texte,
`page.locator("main > hr").evaluate(…)` throws a strict-mode violation. Scope it to `.first()`.

Expected to keep passing, worth an explicit re-check rather than an assumption:
`tests/landing.spec.ts:428` asserts `page.locator("img")` toHaveCount(0) on the landing page. The
three figures live on `/writing/{slug}`, not on `/`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the four production-tier assertions to the published state</name>
  <files>tests/build/prerender.test.ts</files>
  <read_first>
    - tests/build/prerender.test.ts in full — particularly lines 76-115 (the draft sweep and the
      empty-state test), 250-300 (the featured-slot block and its forward note) and 336-352 (the
      launch gate)
    - .planning/phases/04-the-case-study/04-04-SUMMARY.md — READ THE DRAFT BRANCH LINE FIRST. It
      decides two of the edits below.
    - content/the-chart-therefore-changes.mdx and content/die-darstellung-aendert-sich.mdx — the
      shipped titles and standfirsts these assertions quote
    - components/landing/featured-slot.tsx — the published branch's markup, so the assertions
      match what actually renders
  </read_first>
  <action>
    Open with the draft branch. Read the one-line statement in `04-04-SUMMARY.md` saying whether
    the German shipped `draft: false` or `draft: true`, and record it at the top of your work. Two
    of the edits below fork on it, and inferring it from the file rather than reading the recorded
    decision is how the two get out of step.

    **The empty-state test (line 102).** Rename it — the current name asserts a fact that is no
    longer true. The English half inverts: `/writing` must now contain the string
    `The Chart Therefore Changes` and must NOT contain `Nothing published here yet.` or
    `The first piece is being written.` The German half forks. If the German shipped
    `draft: false`, `/texte` must contain `Die Darstellung ändert sich` and must not contain
    `Hier ist noch nichts veröffentlicht.` or `Der erste Text entsteht gerade.` If it shipped
    `draft: true`, leave the German half exactly as it is — an empty German index is the correct
    and honest production state under the escape hatch — and add a comment saying so, naming the
    branch, so the next reader does not think it was missed.

    **The draft sweep (the test above it, around line 79).** This is the edit most easily missed
    and it also forks. `DRAFT_ROUTE_KEYS` and `DRAFT_TITLES` enumerate the three draft fixtures
    and assert that none of them prerenders and that no draft title appears anywhere in the build
    output. If the German shipped `draft: true`, add `texte/die-darstellung-aendert-sich` to
    `DRAFT_ROUTE_KEYS` and `Die Darstellung ändert sich` to `DRAFT_TITLES` — otherwise the escape
    hatch has no production-tier proof at all. If it shipped `draft: false`, leave both arrays
    alone and add a comment recording that the German is published, not excluded.

    **The featured slot (line 268).** Rename from "ships its interim copy" to what it now proves.
    Assert that the landing page's production HTML contains the case study's `title` and its
    `standfirst`, both read from the front-matter rather than retyped — import them if that is
    clean, or state them once as consts at the top of the test with a comment pointing at the
    content file. Assert it no longer contains `The case study is being written.` or the interim
    body sentence beginning `On the Mallorca piece:`. This is the assertion that discharges Roadmap
    SC4: the featured entry's annotation copy *is* the post's standfirst, so it links into the case
    study instead of duplicating it, and no edit to `lib/work.ts` was needed.

    **The featured headline link (line 289).** Invert it. Keep the existing technique of extracting
    the `h3.text-heading` block specifically rather than counting anchors document-wide, which
    would be defeated by the nav. Then assert the block contains exactly one `<a>` and that its
    `href` is `/writing/the-chart-therefore-changes` — the value produced by
    `postPath("en", entry.slug)`. Assert exactly one, not at least one: the featured slot contract
    is that the headline is the only link in the slot, and a second would be the "Read the case
    study" affordance the contract exists to prevent. Rename the test accordingly. This assertion
    is CASE-03.

    **The launch gate (line 342).** Narrow it, do not delete it. Remove the
    `The case study is being written.` line and keep the backlog and contact stub assertions. Add a
    comment recording that Phase 4 closed the featured-slot third of this gate on 2026-08-31, that
    `/writing` is no longer at n=0, and that two interim surfaces plus the unwritten `HOME-01`
    positioning sentence still block Phase 6's `FIND-02` robots flip. The whole point of this test
    is to fail when an interim state ends, so that the gate gets noticed rather than forgotten;
    narrowing it is that mechanism working.

    **Add two new tests** while you are in this file, both covering validation-map rows that have
    no home yet. First, CASE-01's production half: `writing/the-chart-therefore-changes` exists as
    a prerendered route key, its HTML contains the six section-mark strings, and it contains all
    three `/case-study/*.png` `src` values. Second, I18N-01's production half, which forks on the
    draft branch: if the German shipped `draft: false`, `texte/die-darstellung-aendert-sich` is
    also a prerendered route and the English post's HTML carries an `hreflang` alternate pointing
    at it; if `draft: true`, the German route is absent and the English post carries no German
    alternate. Both branches are correct behaviour — assert whichever one shipped.
  </action>
  <verify>
    <automated>rm -rf .next &amp;&amp; npm run build &amp;&amp; npm run test:build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:build` is fully green after a clean `rm -rf .next && npm run build`.
    - `/usr/bin/grep -c "The case study is being written." tests/build/prerender.test.ts` returns 0 — the interim string is asserted nowhere.
    - `/usr/bin/grep -c "href=\"/writing/the-chart-therefore-changes\"" tests/build/prerender.test.ts` returns at least 1.
    - The launch-gate test still exists, still asserts `Nothing listed here yet.` and `No contact details here yet.`, and carries a comment naming the date the featured-slot third was closed.
    - Two new tests exist: one asserting the prerendered EN post route and its six section marks and three figure `src` values; one asserting the I18N-01 branch that actually shipped.
    - The draft sweep matches the recorded branch: if `draft: true`, `DRAFT_ROUTE_KEYS` and `DRAFT_TITLES` each gained the German entry; if `draft: false`, both arrays are unchanged and carry a comment saying why.
    - No file outside `tests/build/prerender.test.ts` changed in this task.
  </acceptance_criteria>
  <done>The production build proves the published state: the index is not empty, the featured headline links to the case study and is the slot's only link, the post prerenders with its six sections and three figures, and the launch gate is narrowed to the two surfaces that remain interim.</done>
</task>

<task type="auto">
  <name>Task 2: Update the two writing-index specs for the new entry counts</name>
  <files>tests/writing-index.spec.ts</files>
  <read_first>
    - tests/writing-index.spec.ts in full
    - app/(en)/writing/page.tsx — the index renders `{index > 0 ? <hr /> : null}` between
      articles, so n entries produce n−1 hrs
    - lib/content.ts `byDateThenSlug` — reverse-chronological, then alphabetical by slug
    - .planning/phases/04-the-case-study/04-04-SUMMARY.md — the German title as shipped
  </read_first>
  <action>
    Two edits, plus one guard.

    **The English index test (line 21).** Rename it: `/writing` now renders two articles. Assert
    `toHaveCount(2)` and `page.locator("hr")` `toHaveCount(1)` — a second entry brings the
    separator with it, and asserting the hr here is what proves the n≥2 index shape costs no
    second render mode, which is what this file was written to prove in the first place. The FIRST
    article is now the case study, because it is dated 2026-08-31 and the fixture 2026-08-30:
    assert its `h2` carries `text-display`, reads `The Chart Therefore Changes`, and contains
    exactly one link whose `href` is `/writing/the-chart-therefore-changes`. Keep the existing
    "the h2 is the sole link" property — it is the editorial rule the file exists for — and add
    the same assertion for the second article, so both entries are proven to share it rather than
    only the first.

    Check the tests between them that use `page.locator("article").first()`: the not-a-card
    computed-style test and the weight-530 standfirst test now measure the case study rather than
    the fixture. That is fine and arguably better, but say so in a comment so the change of subject
    is deliberate rather than accidental.

    **The German index test (line 85).** Rename it: `/texte` now renders three articles separated
    by two hrs. Assert `articles` `toHaveCount(3)` and `page.locator("hr")` `toHaveCount(2)`.
    Prepend the German title to the expected title list, giving, in order: the German case study
    title as shipped, `Eine Musterseite für die Textvorlage`, `Nur auf Deutsch: ein Text ohne
    Übersetzung`. Extend the shared-class-list assertion from a pairwise comparison to all three,
    since the point is that every entry renders through one code path.

    Fix the strict-mode violation research flagged: `page.locator("main > hr").evaluate(…)` now
    matches two elements and throws. Scope it to `.first()`. Do not weaken the computed-style
    assertions it makes — the `rgba(0, 0, 0, 0.12)` colour, the 1px width and the solid style are
    what caught the separator falling back to full-ink black, and they must survive this edit
    intact.

    Add a comment on this test recording that it passes whichever way the German `draft` flag was
    set, because Playwright runs against `npm run dev` where `showDrafts()` is always true — the
    German file is visible here either way, and its production visibility is proven in
    `tests/build/prerender.test.ts` instead.

    **The guard.** Re-run `tests/landing.spec.ts` explicitly rather than assuming. Its line 428
    asserts `page.locator("img")` `toHaveCount(0)` on the landing page; the three figures live on
    the post route, so it is expected to pass, but "expected to pass" is an assumption until it
    runs. Its featured-slot test (p) is deliberately state-agnostic and should also still pass now
    that the slot renders its published branch. If either fails, the failure is real and belongs to
    this task.
  </action>
  <verify>
    <automated>npx playwright test tests/writing-index.spec.ts tests/landing.spec.ts tests/draft-visibility.spec.ts tests/writing-routing.spec.ts tests/i18n-routing.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test tests/writing-index.spec.ts` is fully green.
    - `tests/landing.spec.ts` is fully green with no edit — its img count of 0 and its state-agnostic featured-slot test both still hold. If an edit was needed, the SUMMARY explains why research's prediction was wrong.
    - `tests/draft-visibility.spec.ts`, `tests/writing-routing.spec.ts` and `tests/i18n-routing.spec.ts` are green with no edit.
    - `/usr/bin/grep -c "main > hr" tests/writing-index.spec.ts` returns at least 1, and every such locator is followed by `.first()`.
    - The `rgba(0, 0, 0, 0.12)` / `1px` / `solid` computed-style assertions on the separator are unchanged.
    - The German expected-title list has three entries with the shipped German case-study title first.
    - No file outside `tests/writing-index.spec.ts` changed in this task.
  </acceptance_criteria>
  <done>Both index specs assert the real published shape — two English entries, three German — and the strict-mode trap on the second `<hr>` is closed.</done>
</task>

<task type="auto">
  <name>Task 3: Full suite green from a clean build</name>
  <files>tests/build/prerender.test.ts, tests/writing-index.spec.ts</files>
  <read_first>
    - package.json — `test:all` is `test:unit` → `rm -rf .next` → `build` → `test:build` → `test`,
      and that ordering is not optional: `tests/build/prerender.test.ts` reads real prerendered
      HTML from `.next/server/app`, so a stale `.next` proves the previous build, not this one
    - .planning/phases/04-the-case-study/04-VALIDATION.md § "Assertions that WILL break" — the
      checklist this task closes
  </read_first>
  <action>
    Run `npm run test:all` end to end and drive it to green. Do not shortcut the `rm -rf .next`:
    the build-tier file reads bytes off disk, and a stale build directory will happily prove
    yesterday's HTML.

    Two rules for anything that fails. If a failure is an interim assertion this phase was supposed
    to end, update it and record it in the SUMMARY as a seventh, eighth — whatever number —
    breaking assertion research did not find, with its file, line and reason. If a failure is
    anything else, it is a real regression: fix the cause, not the assertion.

    Then run `npm run lint`. The criterion is **no NEW errors**, never zero: this repo carries
    exactly one known deferred error at `components/smear-heading/use-prefers-reduced-motion.ts:23`
    and it must not be fixed here. Report the exact count and confirm the one error is that one.

    Record the final suite totals — unit tests, build-tier assertions and Playwright specs, all
    three counts — against the pre-phase baseline of 52 unit tests, 19 build-tier assertions and
    115 Playwright specs, so the phase's real test-surface delta is on the record rather than
    inferred.

    Commit the two test files.
  </action>
  <verify>
    <automated>npm run test:all &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:all` exits 0: units, a clean production build, build-tier assertions and every Playwright spec all pass.
    - `npm run lint` reports exactly one error and it is `components/smear-heading/use-prefers-reduced-motion.ts:23`. That file is unchanged.
    - The SUMMARY records the three suite counts after the phase against the 52 / 19 / 115 baseline.
    - The SUMMARY lists every assertion updated, with file and reason, including any beyond the six research predicted.
    - `git status --porcelain` shows no uncommitted changes.
  </acceptance_criteria>
  <done>The whole suite is green from a clean build, lint is unchanged, and the full list of assertions this phase changed is on the record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build output → assertions | `.next/server/app/*.html` is the only place production truth is observable. A stale build directory silently moves this boundary to the previous commit. |
| draft flag → production visibility | The one access-control decision this phase makes. Dev cannot observe it; the build tier can. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-18 | Information Disclosure | A file intended as a draft reaching a production build | mitigate | The draft sweep in `tests/build/prerender.test.ts` asserts no draft route and no draft title appears anywhere in build output. Task 1 requires that sweep to be updated to match the branch Plan 04 actually recorded — the escape hatch is only real if something proves it worked. |
| T-04-19 | Tampering | Weakening an assertion to make the suite green | mitigate | Task 3 states the rule explicitly: an interim assertion gets updated and recorded; anything else gets its cause fixed. The separator's computed-style assertions are named as must-survive-intact, because they previously caught a real full-ink regression. |
| T-04-20 | Repudiation | Proving the published state against a stale build | mitigate | `rm -rf .next` is required before `npm run test:build`, is already the documented ordering in `test:all`, and is restated in the action text and the verify command. |
| T-04-21 | Information Disclosure | The private repository or a `target="_blank"` reaching production HTML | mitigate | `prerender.test.ts`'s existing "the private repository stays private in production" test already asserts no `github.com` href, no `ib-gdp-evolution` string and no `target="_blank"` anywhere in the landing page's production HTML. It must stay green; do not weaken it. |
| T-04-SC | Tampering | npm / pip / cargo installs | accept | No package is installed. This plan edits two existing test files. |

ASVS V4 (Access Control) is the chapter this plan verifies rather than implements: the
`isVisible` / `showDrafts` boundary and the `findBySlug`-before-`loadPostModule` ordering are
unchanged, and the build-tier assertions are what prove they behaved. V2, V3, V6 and V7 remain
inapplicable — no auth, no session, no cryptography, no server-side request forgery surface exists
anywhere on this site.
</threat_model>

<verification>
- `npm run test:all` exits 0 from a clean `.next`.
- `npm run lint` reports exactly one error, the known deferred one.
- `git diff --stat` for this plan touches exactly two files, both under `tests/`. No file under `app/`, `lib/`, `components/`, `content/` or `public/` changed.
</verification>

<success_criteria>
- All six assertions research located are updated to the published state, plus any further ones found, each recorded with file and reason.
- CASE-03 is proven in production HTML: the featured `h3` contains exactly one `<a>` and its href is `/writing/the-chart-therefore-changes`.
- HOME-02 is proven: the slot renders the post's own title and standfirst, in its published branch, on the landing view.
- CASE-01's production half is proven: `/writing/the-chart-therefore-changes` prerenders, carrying its six section marks and three figures.
- The launch gate is narrowed rather than deleted, and names what still blocks Phase 6.
</success_criteria>

<output>
Create `.planning/phases/04-the-case-study/04-05-SUMMARY.md` when done. Record: every assertion
changed with file, old expectation and new; the draft branch it was written against; the three
suite counts against the 52 / 19 / 115 baseline; and the exact lint error count.
</output>
