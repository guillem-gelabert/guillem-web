---
phase: 03-work-list-landing-skeleton
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - components/landing/contents-nav.tsx
  - components/landing/section-stub.tsx
  - components/landing/work-list.tsx
  - components/landing/featured-slot.tsx
  - app/(en)/page.tsx
  - tests/unit/link-contract.test.ts
autonomous: true
requirements: [WORK-01, WORK-02, HOME-01, HOME-03, HOME-04]
must_haves:
  truths:
    - "A visitor loading / reads one positioning sentence directly beneath the nameplate"
    - "A visitor sees a single-column vertical list of exactly two projects, each with an ordinal, a linked title, a one-line annotation and its destination host"
    - "A visitor can reach the work list, writing index, backlog, CV and contact from five links in the page header"
    - "A visitor sees a Case study section whose structure does not change when Phase 4 lands real content — only copy and one anchor element do"
    - "The landing view declares its own route metadata, including a rel=canonical it does not have today"
    - "/ is a Server Component: no \"use client\", no useSmearHeading import, and the trail still runs on both Humane headings"
  artifacts:
    - path: "app/(en)/page.tsx"
      provides: "The landing view — async Server Component with a static metadata export"
      min_lines: 60
      contains: "export const metadata"
    - path: "components/landing/work-list.tsx"
      provides: "ol role=list over lib/work.ts WORK, hairline separator, host marker"
      contains: "role=\"list\""
    - path: "components/landing/featured-slot.tsx"
      provides: "The two-state CASE-03 slot, derived from PostEntry | null"
      contains: "SmearTitle"
    - path: "components/landing/contents-nav.tsx"
      provides: "nav aria-label=Sections with exactly five links"
      contains: "aria-label=\"Sections\""
    - path: "components/landing/section-stub.tsx"
      provides: "The backlog and contact placeholders — one component, two copy sets"
  key_links:
    - from: "app/(en)/page.tsx"
      to: "lib/content.ts publishedFor + findBySlug"
      via: "findBySlug(await publishedFor(\"en\"), CASE_STUDY_SLUG) — derived state, never a flag"
      pattern: "findBySlug\\(await publishedFor"
    - from: "app/(en)/page.tsx"
      to: "lib/work.ts POSITIONING_PLACEHOLDER"
      via: "one export rendered in the <p> AND used as metadata.description (Pitfall 6)"
      pattern: "POSITIONING_PLACEHOLDER"
    - from: "components/landing/work-list.tsx"
      to: "the two independently-hosted live pieces"
      via: "plain <a href> from WORK[i].href, same tab, no target attribute"
      pattern: "href=\\{entry\\.href\\}"
---

<objective>
Replace Phase 1's holding page with the real landing view: de-client `app/(en)/page.tsx` into
an async Server Component that exports route metadata, and build the four Server Components it
composes — the contents nav, the featured slot, the work list and the section stub.

Purpose: this is the phase's structural task. `app/(en)/page.tsx` is `"use client"` today,
which makes it the one page in the repo that cannot export metadata and cannot `await` the
content pipeline. Phase 2 shipped `SmearTitle` precisely so a page can keep its trail without a
client boundary; this plan adopts it for `/`.
Output: four new components, one replaced route, one appended source-fact assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-work-list-landing-skeleton/03-UI-SPEC.md
@.planning/phases/03-work-list-landing-skeleton/03-PATTERNS.md
@.planning/phases/03-work-list-landing-skeleton/03-CONTEXT.md
</context>

<interfaces>
<!-- Contracts consumed. All shipped or created in wave 1 — no codebase exploration needed. -->

lib/work.ts (Plan 03-01):
  export type WorkEntry = { title: string; annotation: string; href: string; host: string }
  export const WORK: readonly [WorkEntry, WorkEntry]
  export const CASE_STUDY_SLUG: string
  export const POSITIONING_PLACEHOLDER: string

lib/content.ts (shipped):
  export type Locale = "en" | "de"
  export type PostFrontmatter = { title: string; standfirst: string; date: string;
                                  lang: Locale; translationKey: string;
                                  draft?: boolean; type?: "post" | "case-study" }
  export type PostEntry = { slug: string; frontmatter: PostFrontmatter; ... }
  export function publishedFor(locale: Locale): Promise<PostEntry[]>   // :236
  export function findBySlug(entries: PostEntry[], slug: string): PostEntry | null   // :220
  export function showDrafts(): boolean       // :171 — do NOT re-derive NODE_ENV
  export function isVisible(entry: PostEntry): boolean   // :176

lib/locales.ts (shipped, + homeLink from Plan 03-01):
  export function indexPath(locale: Locale): string    // "/writing" | "/texte"
  export function postPath(locale: Locale, slug: string): string
  export const UI: Record<Locale, UiCopy>

components/smear-title.tsx (widened in Plan 03-01):
  export function SmearTitle(props: { as?: "h1" | "h2" | "h3"; className?: string;
                                      children: React.ReactNode }): JSX.Element

components/post-meta.tsx (shipped):
  export function PostMeta(props: { locale: Locale; date: string;
                                    switchHref: string | null; draft?: boolean }): JSX.Element

app/globals.css (Plan 03-02): .section-head, .link, .link-quiet

<!-- Verified Tailwind v4 output for every utility this plan writes (direct compile, 2026-08-31): -->
<!-- .scroll-mt-xl → scroll-margin-top: var(--spacing-xl) /*32px*/ ; .py-xs → padding-block: 4px -->
<!-- .gap-x-lg → column-gap: 24px ; .gap-y-md → row-gap: 16px ; .gap-3xl → gap: 64px            -->
<!-- .pt-xl → padding-top: 32px ; .px-lg → padding-inline: 24px ; .max-w-prose → max-width: 65ch -->
<!-- .border-t → border-top-width: 1px (NO COLOUR) ; .border-rule → border-color: var(--color-rule) -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build components/landing/contents-nav.tsx and components/landing/section-stub.tsx</name>
  <files>components/landing/contents-nav.tsx, components/landing/section-stub.tsx</files>
  <read_first>
    - `components/language-switch.tsx` (all 24 lines) — the closest shipped analog: a small Server Component rendering a `<Link>` with a `className`, with its props type above and an accessibility-motivated comment register. Copy its shape; the `<nav>`/`<ul>`/`<li>` skeleton is new to this repo.
    - `app/(en)/writing/page.tsx:36-39` — the four-line empty-state block the UI-SPEC says `SectionStub` must reproduce exactly: one `flex flex-col gap-md` wrapper, a `max-w-prose text-standfirst` line, a `max-w-prose text-body` line.
    - `app/(en)/writing/page.tsx:1-8` — the import-order house rule: `import type { Metadata } from "next"` → react → `next/link` → `@/lib/*` → `@/components/*`.
    - `.planning/phases/03-work-list-landing-skeleton/03-UI-SPEC.md` § *The contents list* and § *Stub sections and `/cv`* — the normative copy and ordering.
    - `.planning/phases/03-work-list-landing-skeleton/03-PATTERNS.md` § *No Analog Found* — records that `grep -rn 'aria-'` over `app`/`components` returns zero hits today; take the landmark attributes from the UI-SPEC, not from a repo analog.
  </read_first>
  <action>
    Both files are Server Components. Neither carries `"use client"`. Neither takes state.

    **`components/landing/contents-nav.tsx`** — export `function ContentsNav()`, no props.

    Declare a module-local `NAV` array of five entries in this exact order, each with a `label`,
    an `href` and a `kind`: `Work`/`#work`/anchor, `Writing`/`indexPath("en")`/route,
    `Backlog`/`#backlog`/anchor, `CV`/`/cv`/route, `Contact`/`#contact`/anchor. The order is
    reader-importance, not page order — `Writing` sits second because it is the only route in the
    list that already holds shipped content. Comment that.

    Render `<nav aria-label="Sections">` wrapping `<ul className="flex flex-wrap gap-x-lg gap-y-md">`
    wrapping one `<li>` per entry. Every link carries `className="text-label link-quiet inline-block py-xs"`.
    The `inline-block py-xs` is not decoration: a Label-role link's line box measures 18.2px, under
    WCAG 2.5.8's 24px floor; `padding-block: 4px` takes it to 26.2px. Put that in a comment naming
    WCAG 2.5.8, in the "comment the failure, not the code" register.

    Fragment targets render as a plain `<a href="#work">` — native fragment navigation, no
    `next/link`, and explicitly no `scroll-behavior: smooth` anywhere (it is motion, and a smooth
    scroll fires a long burst of `scroll` events, each of which advances the trail's `trailHue`).
    Route targets render as `next/link` `<Link>`. Use `indexPath("en")` for `/writing`; for `/cv`
    write the literal `"/cv"` and comment the deviation from the house rule — `indexPath`/`postPath`
    exist because they are locale-parameterised, and inventing a `cvPath()` for one static route
    is worse than the literal.

    Exactly five links. Do NOT add a sixth link to `#case-study`: `HOME-03` names five
    destinations and a link to the section immediately beneath the nav points at what the reader
    is already looking at. Comment that too. No separator glyph — the 24px column gap and 16px
    row gap are the separation. No sticky, no fixed, no footer copy of this nav.

    **`components/landing/section-stub.tsx`** — export `function SectionStub({ state, body }: { state: string; body: string })`.

    Render exactly the shipped `/writing` empty-state shape: `<div className="flex flex-col gap-md">`
    containing `<p className="max-w-prose text-standfirst">{state}</p>` then
    `<p className="max-w-prose text-body">{body}</p>`. Note the class order — `max-w-prose` before
    `text-*`, matching every shipped call site.

    Copy strings live at the call site in `app/(en)/page.tsx`, NOT in `lib/locales.ts`: the
    landing is English-only for v1 and only `homeLink` crosses into the `UI` map. Comment that.
    Nothing in either file renders the words `TODO`, `placeholder`, `TBD`, `Coming soon`,
    `Under construction` or `Lorem` — `D-02` requires placeholder content to be deliberately
    typeset on a live URL, and the marker for `HOME-01` lives in `lib/work.ts`'s source comment.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint components/landing/contents-nav.tsx components/landing/section-stub.tsx && node -e "const fs=require('fs'); const n=fs.readFileSync('components/landing/contents-nav.tsx','utf8'); if(n.includes('\"use client\"')) throw new Error('nav must be a Server Component'); if(!n.includes('aria-label=\"Sections\"')) throw new Error('missing nav landmark'); for (const h of ['#work','#backlog','#contact','/cv']) if(!n.includes(h)) throw new Error('missing nav target '+h); if(!n.includes('indexPath')) throw new Error('writing link must use indexPath'); if(/scroll-behavior|smooth/.test(n)) throw new Error('smooth scroll is banned'); const s=fs.readFileSync('components/landing/section-stub.tsx','utf8'); if(s.includes('\"use client\"')) throw new Error('stub must be a Server Component'); for (const w of ['TODO','placeholder','Coming soon','Under construction','Lorem']) { if (n.replace(/\/\/.*|\/\\*[\s\S]*?\\*\//g,'').includes(w) || s.replace(/\/\/.*|\/\\*[\s\S]*?\\*\//g,'').includes(w)) throw new Error('rendered placeholder marker: '+w); } console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0; `npx eslint` on both files reports 0 errors (scoped to these files only — the whole-repo `npm run lint` criterion is **no NEW errors**, never 0, because of the one deferred `use-prefers-reduced-motion.ts:23` finding).
    - Neither file contains `"use client"`.
    - `contents-nav.tsx` renders `<nav aria-label="Sections">` and exactly five `<li>` entries whose targets are, in order: `#work`, `indexPath("en")`, `#backlog`, `/cv`, `#contact`.
    - Every one of the five links carries all four of `text-label`, `link-quiet`, `inline-block`, `py-xs` in its className.
    - `contents-nav.tsx` contains no `#case-study` href and no separator glyph character (`·`, `|`, `/` used as a divider).
    - Neither file contains the string `scroll-behavior` or `smooth`.
    - `section-stub.tsx` exports a component taking exactly two string props and renders exactly two `<p>` elements inside one `flex flex-col gap-md` wrapper, with `max-w-prose text-standfirst` then `max-w-prose text-body`.
    - Outside of source comments, neither file contains `TODO`, `placeholder`, `TBD`, `Coming soon`, `Under construction` or `Lorem`.
  </acceptance_criteria>
  <done>The navigation surface and the stub shape exist as Server Components and type-check.</done>
</task>

<task type="auto">
  <name>Task 2: Build components/landing/work-list.tsx and components/landing/featured-slot.tsx</name>
  <files>components/landing/work-list.tsx, components/landing/featured-slot.tsx</files>
  <read_first>
    - `app/(en)/writing/page.tsx:35-66` — the shipped two-state branch (`entries.length === 0 ? … : …`) and the `.map()` with an interposed separator and a headline-wrapping-the-link. Four properties carry over: the `index > 0 ? … : null` separator, headline-wraps-link (never link-wraps-headline), the `key` on the outer element, and the in-JSX comment stating why the headline is the only link in the row (`:50-55`).
    - `app/globals.css:294-304` — the `hr` rule and its comment. **Read this before writing any border.** Tailwind v4 preflight emits `border: 0 solid` with NO colour, so `border-t` alone falls through to `currentColor` = full ink. This is WR-06 and Pitfall 1.
    - `components/language-switch.tsx:10-23` — the guard-first, comment-the-absence register for a component with a `| null` prop.
    - `components/post-meta.tsx:5-19` — the `PostMetaProps` type and the `switchHref: string | null` convention. The published branch passes `null`.
    - `lib/work.ts` — `WorkEntry` and `WORK` (Plan 03-01).
    - `.planning/phases/03-work-list-landing-skeleton/03-UI-SPEC.md` § *Work list contract* and § *Featured slot contract* — normative markup, both slot states, and the copy.
  </read_first>
  <action>
    Both files are Server Components. Neither carries `"use client"`.

    **`components/landing/work-list.tsx`** — export `function WorkList()`, no props; it imports
    `WORK` directly. Render `<ol role="list" className="flex list-none flex-col gap-xl">` mapping
    over `WORK`. `role="list"` is required and not redundant: Safari drops list semantics when
    `list-style: none` is applied. Comment that.

    Each `<li>` takes `key={entry.href}` and a className composed by string concatenation —
    `"flex flex-col gap-sm"` plus, when the index is greater than 0, `" border-t border-rule pt-xl"`.
    **`border-rule` is not optional**: without it Tailwind v4 renders the hairline full-ink black,
    an 8× darker line than `--color-rule` and a fourth rule weight the Prose Contract forbids.
    Comment it naming Pitfall 1 / WR-06. Do not add `clsx`, `cn` or any class-composition helper —
    the repo has none and must not gain one.

    Inside each `<li>`, in this order and nothing else: `<p className="text-label" aria-hidden="true">`
    holding the ordinal as `String(i + 1).padStart(2, "0")`; `<h3 className="text-standfirst">`
    wrapping `<a className="link-quiet" href={entry.href}>{entry.title}</a>`;
    `<p className="max-w-prose text-body">{entry.annotation}</p>`;
    `<p className="text-label">{entry.host}</p>`.

    The ordinals are `aria-hidden` because the `<ol>` already conveys order and count, and a
    visible `01` read aloud as "zero one" is noise — comment it. The title is the ONLY link in
    the row: the annotation is not linked and the host label is not linked; carry the
    `app/(en)/writing/page.tsx:50-55` comment register for why.

    The outbound links are plain `<a>`, not `next/link` — the hrefs are absolute URLs to
    independently-hosted pieces. **Same tab: no `target="_blank"`, and therefore no `rel`
    attribute.** With no `target`, no `window.opener` is created, so there is no reverse-tabnabbing
    surface to close. State that in a comment so nobody "hardens" it by adding `target="_blank"`
    and re-opening the problem. The destination is announced by the host line beneath the
    annotation — that is the outbound marker, and it is deliberately not an arrow glyph or an icon
    (this site ships zero icons, and U+2197 is not a character Newsreader can be relied on to carry).

    No card, no border box, no fill, no `border-radius`, no grid, no column layout, no image, no
    "Read more", no `<button>`. Single column at every viewport.

    **`components/landing/featured-slot.tsx`** — export
    `function FeaturedSlot({ entry }: { entry: PostEntry | null })`, importing `PostEntry` as a
    type from `@/lib/content`.

    Guard first, like `LanguageSwitch`. When `entry` is null, return a fragment containing
    `<SmearTitle as="h3" className="text-heading">` holding the exact string
    `The case study is being written.` followed by
    `<p className="max-w-prose text-body">` holding the exact string
    `On the Mallorca piece: what was expected, what the data showed, and how the visual form changed in response.`
    **The interim headline is not a link and must not become one** — the case study does not exist
    and `/writing` is at n=0, so there is nowhere honest for it to point; a link to an empty index
    is a circular dead end. Comment that.

    When `entry` is non-null, return a fragment containing `<SmearTitle as="h3" className="text-heading">`
    wrapping `<a className="link-quiet" href={postPath("en", entry.slug)}>{entry.frontmatter.title}</a>`,
    then a `<div className="flex flex-col gap-md">` holding
    `<p className="max-w-prose text-standfirst">{entry.frontmatter.standfirst}</p>` and
    `<PostMeta locale="en" date={entry.frontmatter.date} switchHref={null} />`. The nested `gap-md`
    wrapper is the shape `app/(en)/writing/page.tsx:48-62` already uses, and it is what keeps the
    parent section's single `gap-lg` correct in both states.

    Add a comment recording that Phase 4 changes copy and adds one `<a>` — it changes no layout,
    no role, no gap and no order, which is the entire reason the slot is built now. Add a second
    comment recording why the headline is an `<h3>` beneath a `<h2>` section head even though it
    renders far larger: semantics follow structure, visual weight follows editorial hierarchy, and
    an `<h2>` there would put two `<h2>`s inside `<section id="case-study">` and silently break
    the outline `aria-labelledby` depends on.

    Do NOT add a `featured` boolean, a `FEATURED_ENABLED` env flag, or a front-matter `featured: true`.
    Do NOT call `loadPostModule` from anywhere in this plan.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint components/landing/work-list.tsx components/landing/featured-slot.tsx && node -e "const fs=require('fs'); const w=fs.readFileSync('components/landing/work-list.tsx','utf8'); if(w.includes('\"use client\"')) throw new Error('must be a Server Component'); if(!w.includes('role=\"list\"')) throw new Error('missing role=list'); if(w.includes('border-t') && !w.includes('border-rule')) throw new Error('Pitfall 1: border-t without border-rule'); if(/target=/.test(w)) throw new Error('no target attribute allowed'); if(/rounded|shadow|border-radius/.test(w)) throw new Error('no card treatment'); if(!w.includes('aria-hidden')) throw new Error('ordinals must be aria-hidden'); const f=fs.readFileSync('components/landing/featured-slot.tsx','utf8'); if(f.includes('\"use client\"')) throw new Error('must be a Server Component'); if(!f.includes('as=\"h3\"')) throw new Error('featured headline must be an h3'); if(!f.includes('The case study is being written.')) throw new Error('interim copy missing'); if(/FEATURED_ENABLED|featured:\s*true|loadPostModule/.test(f)) throw new Error('flagged state or loadPostModule'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0; `npx eslint` on both files reports 0 errors (scoped to these files only — the whole-repo `npm run lint` criterion is **no NEW errors**, never 0, because of the one deferred `use-prefers-reduced-motion.ts:23` finding).
    - Neither file contains `"use client"`.
    - `work-list.tsx` renders `<ol role="list">` with `list-none` and `gap-xl`, and each `<li>` after the first carries all three of `border-t`, `border-rule`, `pt-xl`.
    - `work-list.tsx` contains no `target=`, no `rel=`, no `rounded`, no `shadow`, no `border-radius`, and no `clsx`/`cn` import.
    - The ordinal `<p>` carries `aria-hidden="true"` and its value is derived from the map index, not hardcoded.
    - Exactly one `<a>` per `<li>`: `grep -c '<a ' components/landing/work-list.tsx` returns 1 (one JSX site inside the map).
    - `featured-slot.tsx` uses `SmearTitle as="h3"` in BOTH branches, and the interim branch's `<h3>` contains no `<a>`.
    - The interim strings are byte-exact: `The case study is being written.` and `On the Mallorca piece: what was expected, what the data showed, and how the visual form changed in response.`
    - `featured-slot.tsx` contains no `FEATURED_ENABLED`, no `featured: true`, and no `loadPostModule` call.
  </acceptance_criteria>
  <done>The work list and the two-state featured slot exist, type-check, and carry the hairline colour that Pitfall 1 exists to enforce.</done>
</task>

<task type="auto">
  <name>Task 3: Replace app/(en)/page.tsx with the de-cliented landing view and assert the A1 client boundary in source</name>
  <files>app/(en)/page.tsx, tests/unit/link-contract.test.ts</files>
  <read_first>
    - `app/(en)/page.tsx` (all 16 lines) — the file being replaced. Three things go: `"use client"` on line 1, the `useSmearHeading` import and ref on lines 3/6, and `min-h-screen … justify-center` on line 9.
    - `app/(en)/writing/[slug]/page.tsx:10-14` — the shipped de-clienting comment. Write a Phase-3 variant of it at the top of the new file.
    - `app/(en)/writing/[slug]/page.tsx:54-68` — the `await publishedFor` → `findBySlug` → branch shape. **Deviation for the landing: `null` is a valid state, not a 404. Never call `notFound()` and never call `loadPostModule`.**
    - `app/(en)/writing/[slug]/page.tsx:70-90` — the `<main>` class string and the nested one-flex-column-per-gap composition.
    - `app/(en)/layout.tsx:9-14` — the static `metadata` export form, and the fact that the layout owns `metadataBase`, `title`, `description` and `robots`.
    - `app/(en)/writing/page.tsx:10-23` — the `alternates.canonical` shape.
    - `tests/unit/link-contract.test.ts` — the Phase 3 source-fact gate this task appends to (Plan 03-02).
    - `.planning/phases/03-work-list-landing-skeleton/03-UI-SPEC.md` § *`/` — the landing view* — the normative section order, gaps and markup.
    - `.planning/phases/03-work-list-landing-skeleton/03-RESEARCH.md` § *C-2* — the measured metadata gap. `/` already inherits a title and a description; what it lacks is `rel="canonical"` and any route-level override.
  </read_first>
  <action>
    Replace `app/(en)/page.tsx` wholesale. It becomes an `async` Server Component with a static
    `metadata` export.

    Open with a Phase-3 variant of the `app/(en)/writing/[slug]/page.tsx:10-14` comment: this
    route carries no client directive; Phase 1 marked whole pages as Client Components to reach
    the scroll-trail hook, and doing that here would make the `metadata` export illegal and make
    `await publishedFor("en")` impossible, because the featured slot's state is resolved from the
    filesystem at build time. `SmearTitle` is the one client leaf that carries the trail instead.

    Imports, in house order: `import type { Metadata } from "next"`; `next/link` is not needed at
    this level; then `@/lib/content` (`findBySlug`, `publishedFor`), `@/lib/work`
    (`CASE_STUDY_SLUG`, `POSITIONING_PLACEHOLDER`); then `@/components/smear-title` and the four
    `@/components/landing/*` modules.

    Export `const metadata: Metadata` declaring exactly three things: `title: "Guillem Gelabert"`,
    `description: POSITIONING_PLACEHOLDER`, and `alternates: { canonical: "/" }`. Declare NOTHING
    else. Specifically: no `metadataBase` (the layout owns it via `lib/site.ts`), no `robots` (it
    is inherited from `app/(en)/layout.tsx:13`, and the string `robots` must stay confined to the
    two root layouts so Phase 6's `FIND-02` flips it in exactly two places), and no
    `alternates.languages`/`x-default` (the landing is English-only for v1, and advertising a
    language alternate that does not exist would be a lie in the markup). Comment the
    `description: POSITIONING_PLACEHOLDER` line naming Pitfall 6: one source for `HOME-01`, so
    the user's real sentence is a one-line change that updates the page and the share preview at
    once.

    Default export `async function Landing()`. First line of the body resolves the slot:
    `const caseStudy = findBySlug(await publishedFor("en"), CASE_STUDY_SLUG);`. Comment that a
    `null` result IS the interim state and there is no boolean to flip — and that this must
    tolerate `null` forever, not just this phase, so a renamed or re-drafted Phase 4 file returns
    the slot to interim rather than throwing.

    Render `<main className="flex flex-col gap-3xl px-lg py-3xl">` — verbatim the shipped shell,
    with `min-h-screen` and `justify-center` deliberately absent: the site is content-led, not
    hero-led, and the featured section must be reachable by a short scroll at 1440px rather than
    gated behind a viewport-height nameplate. Comment that removal so it is not restored.

    Inside `<main>`, in this exact order:

    1. `<header className="flex flex-col gap-lg">` containing `<SmearTitle as="h1" className="text-display">Guillem Gelabert</SmearTitle>`, then `<p className="max-w-prose text-standfirst">{POSITIONING_PLACEHOLDER}</p>`, then `<ContentsNav />`.
    2. `<section id="case-study" aria-labelledby="case-study-head" className="scroll-mt-xl flex flex-col gap-lg">` containing `<h2 id="case-study-head" className="section-head">Case study</h2>` then `<FeaturedSlot entry={caseStudy} />`.
    3. `<section id="work" aria-labelledby="work-head" className="scroll-mt-xl flex flex-col gap-lg">` containing `<h2 id="work-head" className="section-head">Work</h2>` then `<WorkList />`.
    4. `<section id="backlog" aria-labelledby="backlog-head" className="scroll-mt-xl flex flex-col gap-lg">` containing `<h2 id="backlog-head" className="section-head">Backlog</h2>` then `<SectionStub state="Nothing listed here yet." body="The current work is being written up." />`.
    5. `<section id="contact" aria-labelledby="contact-head" className="scroll-mt-xl flex flex-col gap-lg">` containing `<h2 id="contact-head" className="section-head">Contact</h2>` then `<SectionStub state="No contact details here yet." body="Email, GitHub and LinkedIn are being added." />`.

    Section order is evidence first (case study, then work), then work-in-progress, then the ask.
    Comment it. Every `section[id]` carries `scroll-mt-xl` so an anchor jump does not park the
    section head flush against the viewport edge. The heading outline is exactly one `<h1>`, four
    `<h2>`s, and `<h3>`s inside the featured slot and each work row — no level skipped, no `<h4>`.
    No skip link (there is no repeated chrome to skip). No `<button>`. No `<footer>`.

    Then append one test to `tests/unit/link-contract.test.ts`, which is the phase's source-fact
    gate: read `app/(en)/page.tsx` from disk with `readFileSync` and assert (i) it contains no
    `"use client"` directive, (ii) it does not import `useSmearHeading`, (iii) it contains
    `export const metadata`, (iv) it contains `canonical`, and (v) the string `robots` does not
    appear. Header the test with a comment naming amendment A1 and stating why a Playwright
    assertion cannot replace it: `/` inherits a title and a description from the layout today, so
    "a title appears on `/`" passes before the change and proves nothing — the client boundary is
    a source fact and must be asserted as one.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run test:unit && npx eslint "app/(en)/page.tsx" && npx playwright test tests/font-cls.spec.ts tests/deploy-smoke.spec.ts && npx playwright test</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `app/(en)/page.tsx` contains no `"use client"`, no `useSmearHeading`, no `min-h-screen`, no `justify-center`, no `notFound`, no `loadPostModule`, and no occurrence of the string `robots`.
    - `app/(en)/page.tsx` contains `export const metadata`, `description: POSITIONING_PLACEHOLDER`, and `alternates: { canonical: "/" }`. It contains no `metadataBase` and no `languages`.
    - `app/(en)/page.tsx` contains exactly four `id="` section attributes: `case-study`, `work`, `backlog`, `contact`, each paired with a matching `aria-labelledby` and each carrying `scroll-mt-xl`.
    - The rendered heading outline on `/` is one `<h1>`, four `<h2>`, and `<h3>`s only — assert by counting the JSX tags in source: one `as="h1"`, four `<h2`, no `<h4`.
    - `node --test tests/unit/link-contract.test.ts` passes, including the new A1 source-fact test.
    - `npx playwright test tests/font-cls.spec.ts` passes — `/` is now a much larger, overwhelmingly Newsreader page and this is Pitfall 4's gate; treat any non-zero cumulative shift as a real finding, and if it appears the remedy is a font-loader `adjustFontFallback` setting, never a looser threshold.
    - The full `npx playwright test` suite is green — no shipped spec regressed by the de-clienting.
    - `git grep -n 'Developer\.'` returns exactly two hits: `app/(en)/layout.tsx:12` (the layout default for any future EN route) and `lib/work.ts` (the single English statement). The old `app/(en)/page.tsx:13` hit is gone. Pitfall 6 closed.
  </acceptance_criteria>
  <done>`/` is an async Server Component with its own metadata and a `rel="canonical"`, composing all four landing components, and the whole shipped suite is still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time filesystem → prerendered HTML | `publishedFor("en")` reads `content/` at build time. No caller-supplied path reaches it; `CASE_STUDY_SLUG` is a module constant, not input. |
| site → two third-party origins | **The genuinely new surface in this phase.** `/` gains the site's first outbound `<a href="https://…">` links, to `ib-gdp.guillemgelabert.com` and `watchpeopledie.live`. |
| visitor → `/` | One-directional. `/` accepts no input: no form, no query param, no route param, no `searchParams`, no `<button>`, no visitor-changeable state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | The two outbound `<a>` in `components/landing/work-list.tsx` | mitigate | Reverse tabnabbing is **structurally absent**: same-tab links, no `target="_blank"`, therefore no `window.opener` for a destination to reach back through. Task 2's verify fails on any `target=` attribute. If a later change adds `target="_blank"` it must also add `rel="noopener noreferrer"` — recorded in a source comment beside the link. |
| T-03-08 | Information disclosure | `lib/work.ts` hrefs rendered on `/` | mitigate | The `ib-gdp-evolution` GitHub repo is private (D-06). No `github.com` href is rendered and the repo name appears nowhere in output; `tests/landing.spec.ts` (Plan 06) asserts absence from `body.innerText`, and Plan 01's unit gate asserts it at the data layer. |
| T-03-09 | Information disclosure | `app/(en)/page.tsx` metadata | mitigate | `robots: { index: false }` must stay in force and must stay stated in exactly two files (`app/(en)/layout.tsx:13`, `app/(de)/layout.tsx`). The landing declares no `robots` of its own — inheritance carries it. Acceptance criteria assert the string `robots` is absent from the page module (Pitfall 5). Phase 6's `FIND-02` owns the flip. |
| T-03-10 | Information disclosure | `lib/content.ts` access-control ordering (ASVS V4) | transfer | The shipped rule is that `findBySlug(await publishedFor(locale), slug)` must resolve before `loadPostModule` ever touches a slug (`lib/content.ts:215-219`). This plan calls only the first half, so the boundary is satisfied vacuously. Enforced by an acceptance criterion that `loadPostModule` appears nowhere in this plan's files. |
| T-03-11 | Tampering | React rendering of module data | accept | React escapes by default and this plan introduces no `dangerouslySetInnerHTML`. All rendered content is literal strings and typed module data authored in this repo. Rich-text backlog descriptions are Phase 5's problem and MDX is the answer there — not `dangerouslySetInnerHTML`. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | Phase 3 installs zero packages; `03-RESEARCH.md` § Package Legitimacy Audit records none to vet, so no `[ASSUMED]`/`[SUS]` entry and no blocking legitimacy checkpoint applies. An `npm install` in this plan is a scope violation. |

**ASVS L1 chapters explicitly not applicable**: V2 Authentication and V3 Session Management (no
accounts, no login, no session, no cookie), V6 Cryptography (no secrets in application code),
V13 API (no API route, no handler). V5 Input Validation is applicable in principle but has zero
surface here — the landing accepts no visitor input of any kind. V14 Configuration is
applicable and is covered by T-03-09.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npm run test:unit` reports 0 failures (5 files).
- `npx playwright test` — the whole shipped suite green, including `tests/font-cls.spec.ts` on the much larger `/`.
- `npm run lint` shows no NEW errors beyond the one known deferred finding in `components/smear-heading/use-prefers-reduced-motion.ts:23`.
- `git diff --stat HEAD -- components/smear-heading/` is empty — no second rAF driver, no retuned `MAX_TRAIL`/`MAX_SHADOWS`/`SCROLL_STOP_DELAY`/`HUE_SPEED`, and no viewport guard (`03-RESEARCH.md` C-3 measured the cost at 0.085 ms per heading per frame against an 8.33 ms budget; the guard is unnecessary at two headings).
- `git grep -c '"use client"' app components` returns hits only for `components/smear-title.tsx`, `components/smear-heading/use-smear-heading.ts`, `components/smear-heading/smear-heading-provider.tsx` and `app/(en)/type/page.tsx`. `/type` stays a Client Component — de-clienting it is out of scope and it is the phase's 5-heading trail calibration reference.
</verification>

<success_criteria>
- `/` is an `async` Server Component exporting `title`, `description` (sourced from `POSITIONING_PLACEHOLDER`) and `alternates.canonical`, and declaring no `robots` and no `metadataBase`.
- The landing renders, in order: nameplate, positioning sentence, five-link contents nav, Case study, Work, Backlog, Contact.
- The featured slot resolves from `findBySlug(await publishedFor("en"), CASE_STUDY_SLUG)` and renders its interim state; there is no boolean flag anywhere.
- The work list is a single-column `<ol role="list">` of two rows with a `--color-rule` hairline between them, plain same-tab outbound `<a>` links, and no card treatment.
- `"Developer."` appears in exactly two files after this plan.
</success_criteria>

<output>
Create `.planning/phases/03-work-list-landing-skeleton/03-03-SUMMARY.md` when done.
</output>
