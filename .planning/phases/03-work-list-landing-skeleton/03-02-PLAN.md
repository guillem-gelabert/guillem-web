---
phase: 03-work-list-landing-skeleton
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/css-source.ts
  - tests/unit/prose-contract.test.ts
  - app/globals.css
  - tests/unit/link-contract.test.ts
autonomous: true
requirements: [HOME-03, HOME-04]
must_haves:
  truths:
    - "A standalone link changes to accent and gains an underline on hover and on focus, and shows a 2px accent focus ring — on every surface, not just inside prose"
    - "Under prefers-reduced-motion the colour and underline change still happens; only the transition is removed"
    - "A landing section head reads as Label-role text over a 1px full-ink rule, with no prose margins"
    - "Introducing a fifth type size, a third weight, a literal hex colour, a fourth rule weight or an !important into the three new classes fails a test in under a second"
  artifacts:
    - path: "tests/unit/css-source.ts"
      provides: "The shared, nesting-aware globals.css parser — NOT named *.test.ts so npm run test:unit does not execute it as a zero-test suite"
      exports: ["css", "allBlocks", "allSelectors", "extractBlocks", "ownDeclarationText", "splitDeclarations", "declarationsOf", "valuesOf"]
    - path: "app/globals.css"
      provides: ".section-head, .link, .link-quiet"
      contains: ".link-quiet"
    - path: "tests/unit/link-contract.test.ts"
      provides: "The executable gate on the UI-SPEC's central promise — every declaration is a value already shipped"
      contains: "prefers-reduced-motion"
  key_links:
    - from: "tests/unit/link-contract.test.ts"
      to: "app/globals.css"
      via: "readFileSync + the shared parser from tests/unit/css-source.ts"
      pattern: "css-source"
    - from: "tests/unit/prose-contract.test.ts"
      to: "tests/unit/css-source.ts"
      via: "import — the parser is stated once, not copied"
      pattern: "css-source"
---

<objective>
Add the three CSS classes the whole phase's markup depends on — `.section-head`, `.link`,
`.link-quiet` — and the `node --test` gate that proves every declaration inside them is a
value already shipped in `app/globals.css`.

Purpose: Tailwind v4's preflight resets `a { color: inherit; text-decoration: inherit }`, so
every non-prose link on the shipped site currently has no accent hover and only a
browser-default focus ring. These three classes close that, and the gate is the cheapest
possible enforcement of the UI-SPEC's promise that Phase 3 introduces no new value.
Output: one new shared parser module, one new unit spec, one amended stylesheet, one
refactored (not rewritten) unit spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-work-list-landing-skeleton/03-UI-SPEC.md
@.planning/phases/03-work-list-landing-skeleton/03-PATTERNS.md
</context>

<interfaces>
<!-- The parser being extracted. Copied from tests/unit/prose-contract.test.ts, which is green today. -->

tests/unit/css-source.ts (new — extracted verbatim, NOT re-derived):
  export type Block = { selector: string; body: string }
  export const css: string                 // app/globals.css with /* */ comments stripped
  export const allBlocks: Block[]
  export const allSelectors: Set<string>   // every comma-split, trimmed selector in the file
  export function extractBlocks(text: string, parent?: string): Block[]
  export function ownDeclarationText(body: string): string
  export function splitDeclarations(text: string): string[]
  export function declarationsOf(block: Block): Array<[string, string]>
  export function valuesOf(property: string, blocks: Block[]): string[]

<!-- Parser behaviour that shapes the new assertions (read from the shipped source): -->
<!-- extractBlocks treats an at-rule as a CONTAINER, not a scope. For              -->
<!-- `@media (…) { .prose-site a { … } }` it pushes TWO blocks: one with selector  -->
<!-- `@media (prefers-reduced-motion: no-preference)`, and one with selector       -->
<!-- `.prose-site a` — NOT qualified by the media query. So "is this declaration   -->
<!-- inside the media query?" cannot be answered from a block's selector. Answer   -->
<!-- it from the @media block's BODY text instead (see Task 3).                    -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extract the globals.css parser into tests/unit/css-source.ts and rewire prose-contract.test.ts to import it</name>
  <files>tests/unit/css-source.ts, tests/unit/prose-contract.test.ts</files>
  <read_first>
    - `tests/unit/prose-contract.test.ts:1-168` in full — the header, the comment-stripping line, `Block`, `extractBlocks` (`:34-73`, nesting-aware, the WR-13 fix), `ownDeclarationText` (`:80-101`), `splitDeclarations` (`:108-137`, semicolon-in-value safe), `declarationsOf` (`:139-147`), `valuesOf` (`:149-157`), and the three derived constants (`:159-168`).
    - `package.json` `scripts.test:unit` — it is `node --test 'tests/unit/*.test.ts'`. A helper in that directory MUST NOT be named `*.test.ts` or Node executes it as a zero-test suite.
    - `tests/unit/dates.test.ts:1-20` — confirms the relative-import-with-`.ts`-extension form Node needs.
  </read_first>
  <action>
    Create `tests/unit/css-source.ts` — note the filename has NO `.test` segment, deliberately,
    because `npm run test:unit` globs `tests/unit/*.test.ts`. Put a file-header comment saying
    exactly that, so nobody renames it.

    Move — do not retype, do not "improve" — the following out of `tests/unit/prose-contract.test.ts`
    and into `tests/unit/css-source.ts`, adding `export` to each: the `Block` type; the `CSS_PATH`
    / `rawCss` / `css` constants including the comment-stripping regex and its comment; the five
    functions `extractBlocks`, `ownDeclarationText`, `splitDeclarations`, `declarationsOf`,
    `valuesOf`, each with its existing doc comment (the WR-13 nesting rationale on `extractBlocks`
    is load-bearing — carry it across verbatim); and the derived constants `allBlocks` and
    `allSelectors` with their comments. `CSS_PATH` and `rawCss` may stay module-local; `css`,
    `allBlocks` and `allSelectors` must be exported.

    Then edit `tests/unit/prose-contract.test.ts`: delete the moved code and replace it with a
    single import of `{ css, allBlocks, allSelectors, declarationsOf, valuesOf }` (and `Block` as
    a type import if still referenced) from `./css-source.ts` — relative, with the `.ts`
    extension. Keep `proseBlocks` (`:162`) where it is; it is prose-specific and stays. Keep all
    twelve tests `(a)`–`(l)` byte-identical. Add one line to the file's header comment noting
    that the parser now lives in `./css-source.ts` because `tests/unit/link-contract.test.ts`
    enforces the same budget over different selectors and the parser is stated once, not copied.

    This task changes zero assertions. If any of the twelve tests changes behaviour, the
    extraction is wrong.
  </action>
  <verify>
    <automated>node --test tests/unit/prose-contract.test.ts && npm run test:unit && npx tsc --noEmit && test ! -f tests/unit/css-source.test.ts && echo "helper correctly not named *.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/unit/prose-contract.test.ts` reports 12 passing tests, 0 failures — the same count as before the extraction.
    - `tests/unit/css-source.ts` exists; `tests/unit/css-source.test.ts` does NOT exist.
    - `tests/unit/prose-contract.test.ts` contains no `function extractBlocks` — `grep -c 'function extractBlocks' tests/unit/prose-contract.test.ts` returns 0.
    - `tests/unit/prose-contract.test.ts` imports from `./css-source.ts` and still defines `proseBlocks` locally.
    - `npm run test:unit` reports 0 failures and executes exactly the four `*.test.ts` files (`content`, `dates`, `prose-contract`, `work`), not five.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The nesting-aware CSS parser exists once and two suites import it.</done>
</task>

<task type="auto">
  <name>Task 2: Append .section-head, .link and .link-quiet to app/globals.css</name>
  <files>app/globals.css</files>
  <read_first>
    - `app/globals.css:4-22` — the `@theme` token block. This is the complete budget; nothing new may be added to it.
    - `app/globals.css:46-57` — `.prose-site h2`. `.section-head` is this rule minus its two prose margins.
    - `app/globals.css:70-93` — `.prose-site a` in full: rest, the `:hover, :focus-visible` pair, the `:focus-visible` outline, and the `@media (prefers-reduced-motion: no-preference)` transition block last, at file top level. `.link` is this verbatim.
    - `app/globals.css:294-304` — the `hr` rule and its comment. This is WR-06's scar tissue: Tailwind v4 preflight emits `border: 0 solid` with NO colour, so a bare border falls through to `currentColor` = full ink. Read the comment before writing any border declaration.
    - `app/globals.css:310-352` — the role classes and the `body` rule, which bracket the insertion point.
    - `.planning/phases/03-work-list-landing-skeleton/03-UI-SPEC.md` § *Link and section-head contract* — the normative declaration list for all three classes.
  </read_first>
  <action>
    Append the three classes to `app/globals.css` immediately after the `.text-standfirst` block
    (currently ending at line 347) and before the `body` rule (currently line 349). They are role
    classes and belong with the role classes, not inside the prose section. Do not touch any
    existing rule, any `@theme` token, or either `clamp()` curve.

    Write a leading comment for the group in the `app/globals.css:294-304` register — name the
    failure, not the code: these classes exist because Tailwind v4's preflight resets
    `a { color: inherit; text-decoration: inherit }`, so every non-prose link on the shipped site
    has no accent hover and only the browser-default focus ring; every declaration below is a
    value already shipped in this file.

    `.section-head` — eight declarations, all copied from `.prose-site h2` (`:46-57`) with its two
    prose margins dropped because on the landing the surrounding flex `gap` owns spacing:
    `font-family: var(--font-body)`, `font-size: 14px`, `font-weight: 400`, `line-height: 1.3`,
    `text-transform: uppercase`, `letter-spacing: 0.04em`, `padding-bottom: 8px`,
    `border-bottom: 1px solid var(--color-ink)`. The border colour is explicit — never a bare
    `border-bottom: 1px` (Pitfall 1). This is the "1px full ink = structural" weight; the phase's
    only other rule weight is 1px `--color-rule` on the work-list separator, and there is no third.

    `.link` — rest state copied verbatim from `.prose-site a` (`:70-76`): `color: inherit`,
    `text-decoration: underline`, `text-decoration-thickness: 1px`,
    `text-underline-offset: 0.12em`, `text-decoration-color: currentColor`.

    `.link:hover, .link:focus-visible` — `color: var(--color-accent)`,
    `text-decoration-color: var(--color-accent)`.

    `.link-quiet` — rest state: `color: inherit`, `text-decoration: none`. No underline at rest is
    deliberate and justified in the UI-SPEC: none of its uses sits inside a block of text, so
    WCAG 1.4.1 does not apply, and the hover state adds the underline back so the affordance is
    never colour-only. Put that justification in a comment.

    `.link-quiet:hover, .link-quiet:focus-visible` — `color: var(--color-accent)`,
    `text-decoration: underline`, `text-decoration-thickness: 1px`,
    `text-underline-offset: 0.12em`, `text-decoration-color: var(--color-accent)`.

    `.link:focus-visible, .link-quiet:focus-visible` — `outline: 2px solid var(--color-accent)`,
    `outline-offset: 2px`. Copied from `:84-87`. `outline: none` must not appear anywhere in this
    file.

    Last, a top-level `@media (prefers-reduced-motion: no-preference)` block containing exactly
    two rules: `.link { transition: color 120ms ease-out, text-decoration-color 120ms ease-out; }`
    and `.link-quiet { transition: color 120ms ease-out; }`. Comment it: the state change is NOT
    motion — under `prefers-reduced-motion: reduce` both classes keep the full colour and
    underline change and lose only the transition. Do not nest this media query inside a rule;
    match the shipped placement at `:89-93`.

    Introduce no new `@theme` token, no literal hex colour, no new duration, no new easing, no
    new underline offset, no fifth font-size, no third font-weight, no `border-radius`, no
    `!important`.
  </action>
  <verify>
    <automated>npm run test:unit && npx tsc --noEmit && node -e "const raw=require('fs').readFileSync('app/globals.css','utf8'); const c=raw.replace(/\/\*[\s\S]*?\*\//g,''); for (const s of ['.section-head','.link-quiet','.link:focus-visible','@media (prefers-reduced-motion: no-preference)']) if(!c.includes(s)) throw new Error('missing '+s); if(c.includes('!important')) throw new Error('!important present'); if(/outline\s*:\s*none/.test(c)) throw new Error('outline:none present'); if(/#[0-9a-fA-F]{3,8}/.test(c.split('@theme')[1].split('}')[1]||'')) console.log('note: check hex outside @theme'); const d=(n)=>c.split(n).length-1; if(d('clamp(3.5rem, 1.5rem + 8vw, 11.25rem)')!==1) throw new Error('Display curve count changed'); if(d('clamp(2rem, 1rem + 4vw, 4.5rem)')!==1) throw new Error('Heading curve count changed'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - With `/* */` comments stripped, `app/globals.css` contains the selectors `.section-head`, `.link`, `.link:hover`, `.link:focus-visible`, `.link-quiet`, `.link-quiet:hover`, `.link-quiet:focus-visible`.
    - `.section-head` declares `border-bottom: 1px solid var(--color-ink)` — a bare `border-bottom: 1px` fails (Pitfall 1 / WR-06).
    - `.link-quiet`'s rest block declares `text-decoration: none`; its `:hover, :focus-visible` block declares `text-decoration: underline`.
    - Both `transition` declarations appear only inside a `@media (prefers-reduced-motion: no-preference)` block; the colour and underline declarations appear outside it.
    - The string `!important` appears zero times in the stripped file; `outline: none` appears zero times.
    - `clamp(3.5rem, 1.5rem + 8vw, 11.25rem)` and `clamp(2rem, 1rem + 4vw, 4.5rem)` each appear exactly once (test `(j)` in `prose-contract.test.ts` already guards this and must stay green).
    - `npm run test:unit` reports 0 failures — the twelve prose-contract tests are unaffected by an additive edit.
    - `@theme` is byte-identical: `git diff app/globals.css` shows no change inside lines 4-22.
  </acceptance_criteria>
  <done>The three classes ship, every declaration is a value already in the file, and the prose gate is still green.</done>
</task>

<task type="auto">
  <name>Task 3: Write tests/unit/link-contract.test.ts and extend prose-contract's required-selector list</name>
  <files>tests/unit/link-contract.test.ts, tests/unit/prose-contract.test.ts</files>
  <read_first>
    - `tests/unit/css-source.ts` — the parser being imported (from Task 1).
    - `tests/unit/prose-contract.test.ts:170-179` (test `(a)`, the "only these values" register), `:215-239` (test `(f)`, the required-selector list to extend), `:271-277` (test `(j)`, the clamp-count invariant).
    - `app/globals.css` — the three new classes (from Task 2) and `@theme` at `:4-22`.
    - `.planning/phases/03-work-list-landing-skeleton/03-RESEARCH.md` § *Pattern 3 — the CSS contract as an executable gate* — the eight assertions this file must make.
  </read_first>
  <action>
    Create `tests/unit/link-contract.test.ts` using `node:test` + `node:assert/strict`, importing
    `{ css, allBlocks, allSelectors, declarationsOf, valuesOf }` from `./css-source.ts`.

    Header comment, in the house register: this file is the Phase 3 **source-fact gate**. Its
    first remit (below) covers HOME-03/HOME-04 by enforcing the UI-SPEC's central promise — every
    declaration inside `.section-head`, `.link` and `.link-quiet` is a value already shipped in
    `app/globals.css`. State in the same comment that Plan 03-03 appends a second remit to this
    file: the amendment-A1 assertion that `app/(en)/page.tsx` carries no `"use client"` directive
    and no `useSmearHeading` import, which is a source fact no browser assertion can replace.
    Also note the deliberate overlap with `prose-contract.test.ts` test `(f)`: presence is
    asserted in both, because a value assertion over an empty block set passes vacuously.

    Derive `linkBlocks` as every block whose selector, comma-split and trimmed, has any part
    starting with `.section-head`, `.link` or `.link-quiet`. Guard it: assert `linkBlocks.length`
    is at least 6, with a message saying the three classes are missing from `app/globals.css`.

    Write these tests:

    (a) All seven selectors are present in `allSelectors`: `.section-head`, `.link`,
    `.link-quiet`, and the hover/focus-visible pairs. Accept the shipped comma-grouped form by
    checking the comma-split set, which `allSelectors` already is.

    (b) Type budget: every `font-size` across `linkBlocks` is `14px` or `inherit`; every
    `font-weight` is `400`, `530` or `inherit`; every `letter-spacing` is `0.04em`, `0.035em`,
    `0` or `normal`. Message: the budget is four sizes and two weights, and Phase 3 adds none.

    (c) No literal colour: every `color`, `text-decoration-color`, `border-bottom-color`,
    `border-color`, `outline-color` and `background` value across `linkBlocks` is one of
    `inherit`, `currentColor`, `var(--color-accent)`, `var(--color-ink)`, `var(--color-rule)`,
    `var(--color-paper)`. Additionally assert no value in `linkBlocks` matches `/#[0-9a-fA-F]{3,8}\b/`
    and none matches `/\brgba?\(/`.

    (d) No fourth rule weight: every declaration in `linkBlocks` whose property matches
    `/^border(-[a-z]+)?(-width)?$/` or `/^outline(-width)?$/` has a length component of `1px` or
    `2px` only (the focus outline is the 2px), and any colour component is `var(--color-ink)`,
    `var(--color-rule)` or `var(--color-accent)`. A shorthand like
    `border-bottom: 1px solid var(--color-ink)` satisfies this; a bare `border-bottom: 1px` fails
    it because it has no colour component — that is Pitfall 1's gate.

    (e) `.link-quiet`'s REST block declares `text-decoration: none` (or
    `text-decoration-line: none`), and its `:hover`/`:focus-visible` block declares
    `underline`. Assert both directions, so removing the hover underline fails.

    (f) Both `.link` and `.link-quiet` have a `:focus-visible` rule declaring
    `outline: 2px solid var(--color-accent)` and `outline-offset: 2px`. Separately assert the
    string `outline: none` and `outline:none` appear nowhere in `css`.

    (g) Reduced-motion gating, asserted by TEXT RANGE, not by selector. The parser treats an
    at-rule as a container and does NOT qualify nested selectors with it, so a block's selector
    cannot tell you whether it sits inside the media query. Instead: collect every block from
    `allBlocks` whose selector starts with `@media` and whose selector text includes
    `prefers-reduced-motion: no-preference`; count `transition` occurrences inside the union of
    those blocks' bodies; count `transition` occurrences in the whole of `css`; assert the two
    counts are equal and greater than zero. Comment this: the state change is not motion — only
    the transition is gated — and a `transition` written outside the media query is exactly the
    regression this catches.

    (h) The existing invariants still hold, restated over the whole file so this suite fails
    independently of the prose one: `!important` appears zero times in `css`, and each of the two
    `clamp()` curves appears exactly once.

    Then edit `tests/unit/prose-contract.test.ts` test `(f)`: append `".section-head"`, `".link"`
    and `".link-quiet"` to the `required` array. Leave the test title and every other test alone.
  </action>
  <verify>
    <automated>node --test tests/unit/link-contract.test.ts && npm run test:unit && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/unit/link-contract.test.ts` reports 0 failures and at least 8 passing tests.
    - `npm run test:unit` reports 0 failures across all five `*.test.ts` files.
    - Negative check, performed by hand and reverted before commit: temporarily moving the `.link { transition: … }` declaration out of the `@media (prefers-reduced-motion: no-preference)` block makes test (g) fail; temporarily replacing `var(--color-accent)` with `#C1272D` in `.link:hover` makes test (c) fail; temporarily changing `.section-head`'s `border-bottom` to a bare `1px` makes test (d) fail.
    - `tests/unit/prose-contract.test.ts` test `(f)`'s `required` array contains all three new selectors and the file still reports 12 tests.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The three classes carry an executable budget gate that runs in well under a second.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stylesheet → rendered page | CSS is static, author-controlled, and inlined at build time. No visitor input reaches it. The boundary that matters here is *accessibility*, not confidentiality: a missing focus indicator is a real defect with a real gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Denial of service (accessibility) | `.link` / `.link-quiet` focus indicators | mitigate | Every link on every Phase 3 surface must show a visible focus ring. Task 2 declares `outline: 2px solid var(--color-accent)` with `outline-offset: 2px`; Task 3 test (f) asserts both rules exist AND that the string `outline: none` appears nowhere in `app/globals.css`. Accent on paper measures 5.84:1, past WCAG 1.4.11's 3:1 floor. |
| T-03-05 | Tampering | `app/globals.css` design budget | mitigate | An unreviewed value drift (a fifth size, a literal hex, a fourth rule weight, an `!important`) is silent and cumulative. Task 3 tests (b)-(d), (h) make it a build-time failure in under a second. Both `clamp()` curves are count-asserted so an accidental retune is caught. |
| T-03-06 | Tampering | `tests/unit/css-source.ts` naming | mitigate | A helper named `*.test.ts` in `tests/unit/` is executed by `npm run test:unit` as a zero-test suite, and Node exits non-zero on a suite with no tests — silently red, or silently masking. Task 1's verify asserts `tests/unit/css-source.test.ts` does not exist. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | Phase 3 installs zero packages. `03-RESEARCH.md` § Package Legitimacy Audit records no package to vet, so no `[ASSUMED]`/`[SUS]` entry and no blocking legitimacy checkpoint applies. An `npm install` in this plan is a scope violation. |

**ASVS L1 chapters explicitly not applicable to this plan**: V2 Authentication, V3 Session
Management, V4 Access Control, V5 Input Validation, V6 Cryptography, V12 File & Resource, V13
API. This plan writes CSS and reads it back from disk in a test. It accepts no visitor input,
performs no mutation, loads no third-party origin, and adds no dependency.
</threat_model>

<verification>
- `npm run test:unit` reports 0 failures across `content`, `dates`, `link-contract`, `prose-contract`, `work`.
- `npx tsc --noEmit` exits 0.
- `npm run lint` shows no NEW errors beyond the one known deferred finding in `components/smear-heading/use-prefers-reduced-motion.ts:23`.
- `git diff app/globals.css` shows an additive change only: no line inside `@theme` (`:4-22`) and no existing rule is modified or deleted.
</verification>

<success_criteria>
- `.section-head`, `.link` and `.link-quiet` exist in `app/globals.css` with every declaration copied from a value already in that file.
- Both transitions sit inside `@media (prefers-reduced-motion: no-preference)`; the colour and underline changes sit outside it.
- `tests/unit/link-contract.test.ts` fails on a literal hex, on a bare border width, on an ungated transition, on a missing hover underline and on a removed focus outline.
- The nesting-aware parser is stated once in `tests/unit/css-source.ts`; `prose-contract.test.ts` still reports its original 12 tests.
</success_criteria>

<output>
Create `.planning/phases/03-work-list-landing-skeleton/03-02-SUMMARY.md` when done.
</output>
