---
phase: 06-cv-contact-photo-discoverability
plan: 09
type: execute
wave: 4
depends_on: [06-01, 06-02, 06-04, 06-05, 06-06, 06-07]
files_modified:
  - tests/build/prerender.test.ts
autonomous: true
requirements: [PROF-01, PROF-02, PROF-03, PROF-04, FIND-01, FIND-02]

must_haves:
  truths:
    - "Every assertion this phase broke has been narrowed or repurposed by an owning change, never deleted and never left passing on a state that no longer exists"
    - "What production emits is asserted from real prerendered HTML: own title, own description, one canonical, the OG set, one icon link, and the sitemap and robots bodies"
    - "The launch gate's remaining rows are visible in the production tier, and the assertions the eventual FIND-02 flip must invert are named by test title"
  artifacts:
    - path: "tests/build/prerender.test.ts"
      provides: "The production-truth tier for this phase: five narrowed assertions, the FIND-01/FIND-02 additions, and the user-supplied gate rows"
  key_links:
    - from: "tests/build/prerender.test.ts"
      to: "app/sitemap.ts output"
      via: "set comparison against publishedFor() rather than a count"
      pattern: "publishedFor"
    - from: "tests/build/prerender.test.ts"
      to: "the og:image meta tag"
      via: "parse the tag and fetch the resolved URL — the route path carries a per-build hash"
      pattern: "og:image"
---

<objective>
The production tier. Every Playwright spec in this repo runs against `npm run dev`, where
`showDrafts()` is always true, so a browser test structurally cannot prove what a production build
ships. This file reads the real prerendered HTML that `next build` writes to `.next/server/app` — and
this plan is where the phase's changes are proven against it.

**Five existing assertions break because this phase ends the states they describe. Narrowing them is
the gate mechanism working, not damage.** Phase 4 narrowed the same launch-gate test when the
featured slot closed; Phase 5 narrowed it again when the backlog stub closed. The rule those two
phases set, and this plan follows: when an interim state ends, update the test that proved it was
interim — never delete it, and never leave it passing on a state that no longer exists.

Three of this phase's five user-supplied values still have no production surface, because the values
do not exist. Those assertions ship as explicitly-skipped tests naming their gate row as the
unblocker, exactly as plan 06-03 handled G12 — an absent assertion is invisible, a skipped one is a
standing instruction.

Purpose: production truth for everything plans 06-01 through 06-07 shipped, plus a durable record of
which assertions the eventual FIND-02 flip must invert.
Output: one heavily-extended file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-cv-contact-photo-discoverability/06-VALIDATION.md

<interfaces>
Line numbers verified against the current file (715 lines) AFTER Phase 5's plan 05-04 landed. The
numbers in 06-CONTEXT.md and 06-RESEARCH.md are both stale — re-grep before editing, do not trust
any recorded number blindly, including these.

BREAKS, must be narrowed or repurposed:

  :484  test "the contact stub ships its real, deliberately typeset copy"
        :491  for (const stub of ["No contact details here yet.", "Email, GitHub and LinkedIn are
              being added."]) assert.ok(root.includes(stub))          <- plan 06-04 deleted both
  :545  test "launch gate: the contact stub is still interim and three copy items are still
        unreviewed"
        :591  assert.ok(root.includes("No contact details here yet."))  <- same cause
  :532  test "the private repository stays private in production"
        :540  assert.doesNotMatch(root, /href="[^"]*github\.com[^"]*"/i)  <- PROF-04 breaks this
              deliberately; NARROW it, do not delete it
  :337  test "every (en) route's meta description is bound to POSITIONING_PLACEHOLDER by equality"
              loops over ["", "cv", "type"]                            <- plan 06-07 gave /cv and
              /type their own descriptions
  :393  test "the inherited noindex reaches both new surfaces — neither route restates robots"
              loops over ["", "cv"]                                    <- /type now declares its own

MUST NOT be inverted or weakened:

  :160-166  robots noindex survived the two-root-layout split          <- the flip inverts this, and
            the flip is NOT this phase's
  :250-258  /_not-found carries exactly one noindex robots meta        <- Next injects noindex for
            any status >= 400, including proxy-set ones. Measured.
  :541      assert.equal(root.includes("ib-gdp-evolution"), false)     <- private repo, stays
  :542      assert.doesNotMatch(root, /target="_blank"/)               <- stays

Helpers already in the file: walkHtmlRoutes / getRoutes at :39-79, DRAFT_ROUTE_KEYS and DRAFT_TITLES
at :96-100, backlogSource imported from ../unit/backlog-source.ts at :12.

Build-tier trap, load-bearing rather than hygiene: `next start` writes on-demand-rendered dynamic
responses into .next/server/app/, so curling a 404 once leaves a stale .html on disk that
walkHtmlRoutes then picks up, which can trip the draft-route assertion at :97. Always
`rm -rf .next && npm run build` before `npm run test:build`. `npm run test:all` already does.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: narrow the five assertions this phase broke</name>
  <files>tests/build/prerender.test.ts</files>
  <read_first>
    - `tests/build/prerender.test.ts` at the five sites listed in the interfaces block — re-grep each title first, because Phase 5's plan 05-04 already shifted them once and this plan's own edits shift them again as it goes
    - `.planning/phases/05-backlog/launch-gate.md` — the narrowing precedent and its exact wording
    - `.planning/phases/06-cv-contact-photo-discoverability/06-RESEARCH.md:550-592` — the full inversion table, including the three rows marked "must NOT invert"
    - `.planning/phases/06-cv-contact-photo-discoverability/06-04-SUMMARY.md` and `06-07-SUMMARY.md` — the captured failure output each handed forward
    - `.planning/phases/03-work-list-landing-skeleton/deferred-items.md` § 1 — the three consumption sites of `POSITIONING_PLACEHOLDER` and why the equality form matters
  </read_first>
  <action>
**The contact stub copy test (`:484`, loop at `:491`).** Move both strings — `"No contact details here
yet."` and `"Email, GitHub and LinkedIn are being added."` — out of the "must be present" loop and
into the "must be absent" loop that already sits below it for the backlog stubs. Retitle the test to
describe what it now proves. Following Phase 5's D-13 reasoning, keep the inversion rather than
deleting the strings: the difference between "not rendered" and "not present" is exactly the
dead-code branch a later reader would mistake for a supported state, and plan 06-04 deleted the
component, so absence is provable.

**The launch-gate test (`:545`, assertion at `:591`).** Remove
`assert.ok(root.includes("No contact details here yet."))` — the contact leg has closed. Do **not**
delete the test. Retitle it and rewrite its comment in the same register the Phase 4 and Phase 5
narrowings used, recording: the contact stub closed here, which surface remains interim (`/cv`,
while `EXPERIENCE` is empty), and which copy items remain unreviewed and still block FIND-02. Keep
the `COPY_REVIEWED === false` source-scrape against `backlogSource` exactly as it is — that is one of
D-14's three tripwire channels and it must survive. Add a positive assertion so the test is not
hollowed out: assert `/`'s production HTML contains the real GitHub profile URL, which is the
evidence the contact leg actually closed rather than merely stopped being asserted.

**The private-repo test (`:532`, assertion at `:540`).** PROF-04 deliberately breaks the blanket
`github.com` ban. Narrow it, do not delete it: assert that every `github.com` href in `/`'s
production HTML is the profile root `https://github.com/guillem-gelabert` and that none carries a
repository path segment beyond it. Keep `:541`'s `ib-gdp-evolution` absence assertion and `:542`'s
`target="_blank"` ban untouched — both still hold and both are the point of the test. Record in the
comment that the ban was narrowed rather than removed, and why.

**The description-equality test (`:337`).** Narrow the loop from `["", "cv", "type"]` to `[""]`. The
landing keeps `description: POSITIONING_PLACEHOLDER` — that is HOME-01's one-line-edit property, it
still compares against the imported constant rather than a literal, and it keeps passing when the
real sentence lands. Then add the assertions plan 06-07 makes true and which replace what the loop
used to guarantee: `/cv` and `/type` each carry their own non-empty description; neither equals
`POSITIONING_PLACEHOLDER`; and no `(en)` route serves the `(en)` layout's default description —
mirroring exactly the shape the German equivalent at `:369` already uses. Rewrite the comment: the
WR-06 fix it describes (three routes, two sources) was superseded by giving `/cv` and `/type` real
descriptions, which is what FIND-01 requires; the constant still has one rendered consumer and one
metadata consumer on `/`, and that is the property being locked.

**The inherited-noindex test (`:393`).** Add `"type"` to the loop so all three routes are asserted to
carry `noindex`, and retitle — "neither route restates robots" is no longer true, because plan 06-07
gave `/type` its own permanent declaration. Split the comment into two claims: `""` and `"cv"`
inherit from the root layout and are what a FIND-02 flip changes; `"type"` declares its own and must
stay `noindex` forever, so the flip must not touch it.

After every edit, re-grep the file. Each change shifts the line numbers of the ones below it.
  </action>
  <verify>
    <automated>rm -rf .next && npm run build && npm run test:build</automated>
  </verify>
  <acceptance_criteria>
    - `rm -rf .next && npm run build && npm run test:build` passes with zero failures
    - Both contact stub strings are asserted **absent** from `/`'s production HTML, and neither appears in a "must be present" assertion
    - The launch-gate test still exists, still asserts `COPY_REVIEWED === false` against `backlogSource`, no longer asserts the contact stub string, and now asserts the GitHub profile URL is present
    - The private-repo test still asserts `ib-gdp-evolution` is absent and `target="_blank"` is absent, and its `github.com` assertion permits only `https://github.com/guillem-gelabert` with no repository path
    - The description test loops over `[""]` only, still compares against the imported `POSITIONING_PLACEHOLDER`, and separately asserts `/cv` and `/type` carry their own distinct non-empty descriptions
    - A new assertion mirrors the German shape: no `(en)` route serves the `(en)` layout's default description
    - The inherited-noindex loop covers `["", "cv", "type"]` and all three pass
    - `tests/build/prerender.test.ts:160-166` and `:250-258` are unmodified — verify with `git diff` on those hunks
    - Every retitled test's title accurately describes what it now asserts; no test is named for a state it no longer proves
  </acceptance_criteria>
  <done>All five broken assertions are narrowed or repurposed with their comments rewritten to describe the current state, the three protected assertions are untouched, and the build tier is green.</done>
</task>

<task type="auto">
  <name>Task 2: FIND-01 and FIND-02 production assertions</name>
  <files>tests/build/prerender.test.ts</files>
  <read_first>
    - `.planning/phases/06-cv-contact-photo-discoverability/06-RESEARCH.md:441-472` — Q1: exactly which tags arrive free from the file convention and which need declaring, and the warning that the OG route path carries a per-build hash
    - `.planning/phases/06-cv-contact-photo-discoverability/06-RESEARCH.md:316-328` — the ordered pre-flip list; items 4 (canonical consistency), 6 (OG resolves), 7 (robots and sitemap agree), 8 (nothing crawlable that should not be)
    - `.planning/phases/06-cv-contact-photo-discoverability/06-RESEARCH.md:904-910` — Pitfalls 8 and 9
    - `.planning/phases/06-cv-contact-photo-discoverability/06-05-SUMMARY.md` — the sitemap and robots bodies as served and the trailing-slash spelling chosen
    - `.planning/phases/06-cv-contact-photo-discoverability/06-07-SUMMARY.md` — the head sweep for all six routes
    - `app/sitemap.ts`, `app/robots.ts`, `lib/site.ts` as shipped
    - `tests/build/prerender.test.ts:39-79` — `walkHtmlRoutes` and `getRoutes`, and how it normalises route keys
  </read_first>
  <action>
`sitemap.xml` and `robots.txt` are not `.html`, so `walkHtmlRoutes` does not see them. Read them from
`.next/server/app/` directly by their built filenames, or fall back to reading the route modules'
output — whichever the build actually produces; check `.next/server/app` before writing the reader.

**Sitemap.** Assert its post entries equal `publishedFor()`'s output for both locales, compared as
sets with a message naming the differing slug — not by count, because a count passes when one entry
is swapped for another. Assert the four static routes are present. Assert `/type` and both reserved
404 routes from plan 06-01 are absent, referencing `notFoundPath()` rather than literal strings.
Assert every `loc` has host `guillemgelabert.com`.

**Robots.** Assert the body contains `Allow: /`, `Disallow: /type`, and a `Sitemap:` line whose URL
is absolute and whose origin equals `SITE_URL`'s origin.

**Canonical and sitemap agree on the site root.** This is Pitfall 8 and it is the one assertion that
binds two files: extract `/`'s `rel="canonical"` href from the prerendered HTML and the site-root
`loc` from the sitemap, and assert the two strings are **identical**. Not merely same-host,
identical — the whole failure mode is two spellings of one page.

**OG tags.** For each of `/`, `/cv`, `/writing`, `/texte` and both post routes, assert the presence of
`og:title`, `og:description`, `og:url`, `og:type`, `og:site_name`, `og:locale` and `og:image`. Assert
every `og:url` and every `rel="canonical"` has host `guillemgelabert.com`. Assert `og:locale` is
`en_GB` on the English routes and `de_DE` on the German ones. Assert `twitter:card` is present with
value `summary_large_image` and note in a comment that it arrives from the `opengraph-image` file
convention rather than a hand-declaration.

**Never hardcode the OG image path** — it carries a per-build hash such as
`/opengraph-image-35z9bs?1d7b3ec36efef8be`. Parse `og:image`'s content from the HTML, assert it is
absolute with the right host, and assert the corresponding file exists in the build output. Assert
the English post's `og:image` differs from `/`'s and from the German post's, which is what proves the
per-post override is live rather than inherited.

**Exactly one icon link.** Assert `/`'s HTML contains exactly one `rel="icon"` and that
`app/favicon.ico` no longer exists on disk. Two `rel="icon"` tags is the measured symptom of shipping
`app/icon.png` without deleting the `.ico`, and one of them would still be the Next scaffold mark
that HOME-05 exists to avoid.

**Own titles.** Assert each of the six routes has a non-empty `<title>` and that no two of the six
share a description. Assert the string `Guillem Gelabert` appears at most once in any single title —
the template plus a hand-built suffix is how it ships twice.

**hreflang.** Assert `/writing` and `/texte` still emit `alternates` including `x-default`, and that
`/`, `/cv` and `/type` emit none. Write the second half as a positive statement with its reason, so
the audit reads a decision rather than an omission.
  </action>
  <verify>
    <automated>rm -rf .next && npm run build && npm run test:build</automated>
  </verify>
  <acceptance_criteria>
    - `rm -rf .next && npm run build && npm run test:build` passes with zero failures
    - The sitemap assertion compares post entries to `publishedFor()` as sets and reports the differing slug on failure
    - `/type`, `notFoundPath("en")` and `notFoundPath("de")` are asserted absent from the sitemap by constant reference, not by literal string
    - The site-root `rel="canonical"` href and the site-root sitemap `loc` are asserted **string-identical**
    - All six routes are asserted to carry the seven `og:*` tags, with `og:locale` matching the route's language
    - The `og:image` URL is parsed from the meta tag; no build-hash path appears as a literal anywhere in the file
    - The English post's `og:image` is asserted different from `/`'s and from the German post's
    - Exactly one `rel="icon"` is asserted, and `app/favicon.ico` is asserted absent from disk
    - No two of the six routes share a description value
    - `/writing` and `/texte` are asserted to emit `x-default`; `/`, `/cv` and `/type` are asserted to emit no `alternates`, with the reason in the comment
    - Temporarily changing `app/sitemap.ts`'s site-root spelling to add a trailing slash makes the canonical-agreement assertion fail — demonstrate once, then revert
  </acceptance_criteria>
  <done>Production output is asserted for the whole discoverability surface: sitemap contents bound to `publishedFor()`, robots contents, one spelling of the site root across two files, the full OG set per route with per-post overrides proven distinct, and exactly one icon link.</done>
</task>

<task type="auto">
  <name>Task 3: the user-supplied rows in the production tier, and naming the flip's inversion points</name>
  <files>tests/build/prerender.test.ts</files>
  <read_first>
    - `.planning/phases/06-cv-contact-photo-discoverability/06-RESEARCH.md:878-882` — Pitfall 3 and the exact build-tier assertion shape it prescribes for the email bytes
    - `.planning/phases/06-cv-contact-photo-discoverability/06-CONTEXT.md:384-441` — the gate table and the no-fabrication rule
    - `.planning/phases/06-cv-contact-photo-discoverability/06-VALIDATION.md` — the 13-row correction and the user's decision that the copy gate stays blocking
    - `tests/unit/launch-gate.test.ts` as written by plan 06-03 — the biconditional and its skipped G12 test, whose pattern this task reuses
    - `tests/build/prerender.test.ts:160-166` and `:250-258` — the two protected assertions this task documents but must not modify
  </read_first>
  <action>
Three of the five user-supplied values have no production surface because the values do not exist.
Assert what is provable today, and ship the populated assertion as an explicitly-skipped test naming
its gate row as the unblocker — the pattern plan 06-03 used for G12. A missing assertion is
invisible; a skipped one is a standing instruction with a name attached.

**Email (G4).** Assert unconditionally, across **every** prerendered route, that the string `&amp;#`
never appears. That is the exact signature of Pitfall 3's failure — React double-escaping the entity
so the page displays `&#64;` as literal text and the `mailto:` is broken — and it fires whether or
not `EMAIL` is set. Then assert the current absent state: zero `mailto:` occurrences in production
HTML. Then a skipped test for the populated state, naming G4: the served bytes contain `&#64;` and
`&#46;`, and the bare address with an unescaped `@` does not appear.

**Experience (G3).** Assert `/cv`'s production HTML currently contains `CV_STUB_BODY` and none of the
six banned marker words. Then a skipped test naming G3: `/cv`'s production HTML contains the first
`EXPERIENCE` row's `org`, and the stub line is absent.

**Portrait (G6).** Assert `/cv`'s production HTML currently contains zero `<img>` elements. Then a
skipped test naming G6: exactly one `<img>` with `width` and `height` attributes matching
`PORTRAIT`'s declared dimensions.

Each skipped test's name must contain its gate ID so `npm run test:build`'s output is itself a
readable gate report.

**Name the flip's inversion points, in the file, without performing the flip.** Add a comment block —
not a test — immediately above the protected assertion at `:160` recording, by test title rather than
by line number (because line numbers move):
- "robots noindex survived the two-root-layout split" — both assertions invert with the flip, in the
  same commit, and the test is renamed in that same commit. A test named "noindex survived" that
  asserts the opposite is worse than no test.
- "the inherited noindex reaches both new surfaces" — the `""` and `"cv"` rows invert; the `"type"`
  row does **not**, because `/type` declares its own permanent noindex.
- "/_not-found must carry exactly one noindex robots meta" — does **not** invert. Next injects
  `noindex` for any status at or above 400 including proxy-set ones, measured, so the reserved 404
  routes stay unindexed after the flip with no extra code.
- The flip itself is a two-file edit gated by `tests/unit/launch-gate.test.ts`, and it is the user's
  to perform after their own review. This phase does not perform it.

Extend the `/_not-found` assertion rather than duplicating it: assert both reserved 404 routes from
plan 06-01 also carry exactly one `noindex` in their prerendered HTML. That is the measured behaviour
and it is what makes them safe after the flip.

Finally, add a one-line note at the top of the file about the `.next/server/app/` pollution trap:
`next start` writes on-demand-rendered responses to disk and `walkHtmlRoutes` picks them up, so
`rm -rf .next && npm run build` before `npm run test:build` is load-bearing rather than hygiene.
  </action>
  <verify>
    <automated>rm -rf .next && npm run build && npm run test:build 2>&1 | tee /tmp/tb.log; /usr/bin/grep -c 'skipped' /tmp/tb.log; /usr/bin/grep -cE 'G3|G4|G6' /tmp/tb.log</automated>
  </verify>
  <acceptance_criteria>
    - `rm -rf .next && npm run build && npm run test:build` passes with zero failures
    - The output reports exactly three skipped tests, and each skipped test's name contains its gate ID (G3, G4, G6)
    - The `&amp;#` ban is asserted across every prerendered route unconditionally, not only on `/` and `/cv`
    - Zero `mailto:` and zero `<img>` are asserted on `/cv`'s production HTML in the current state
    - `/cv`'s production HTML is asserted to contain `CV_STUB_BODY` and none of the six banned marker words
    - Both reserved 404 routes are asserted to carry exactly one `noindex`, alongside the existing `/_not-found` assertion
    - The comment block above the protected assertion names all three inversion cases by test title and states the flip is not this phase's
    - `git diff` shows `:160-166`'s assertions and `:250-258`'s count assertion are unchanged in substance
    - The file's header carries the `.next/server/app/` pollution note
    - `npm run test:all` passes end to end
  </acceptance_criteria>
  <done>The three unfillable gate rows are visible in the production tier as named skipped tests, the double-escape failure mode is banned unconditionally across every route, the reserved 404s are proven noindex, and the flip's inversion points are documented by test title without the flip being performed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build output → the assertions that gate a release | This file is the only tier that sees what production actually emits |
| an ended interim state → a test still asserting it | A test left passing on a dead state is worse than no test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-50 | Repudiation | A broken assertion deleted rather than narrowed, losing the gate | mitigate | Task 1 narrows all five and retitles each; acceptance asserts the launch-gate test still exists with its `COPY_REVIEWED` scrape intact and gains a positive assertion so it is not hollowed out. |
| T-06-51 | Information Disclosure | The private `ib-gdp-evolution` repository linked once a GitHub URL is permitted | mitigate | The blanket `github.com` ban is narrowed to permit only the profile root with no repository path segment; `:541`'s explicit `ib-gdp-evolution` absence assertion is untouched. |
| T-06-52 | Spoofing | Two spellings of the site root split the eventual index | mitigate | Task 2 asserts `/`'s canonical href and the sitemap's site-root `loc` are string-identical, and demonstrates the assertion red by adding a trailing slash. |
| T-06-53 | Tampering | Entity-encoded email double-escaping to `&amp;#64;`, breaking the mailto silently | mitigate | Task 3 bans `&amp;#` across every prerendered route unconditionally, so the assertion fires whether or not `EMAIL` is set. |
| T-06-54 | Information Disclosure | Draft content, `/type` or a reserved 404 route enumerated in the sitemap | mitigate | Task 2 asserts the sitemap's post set equals `publishedFor()` and asserts the three excluded routes absent by constant reference. |
| T-06-55 | Repudiation | The user-supplied rows quietly disappearing because there is nothing to assert yet | mitigate | Three named skipped tests carrying their gate IDs, so `npm run test:build`'s own output is a gate report. |
| T-06-56 | Repudiation | A future flip inverting the wrong assertions, or missing one | mitigate | The comment block names all three cases by test title rather than line number, distinguishes `/type`'s permanent noindex, and records that `/_not-found` and the reserved 404s do not invert. |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs nothing. `06-RESEARCH.md` § Package Legitimacy Audit records zero packages for this phase. |
</threat_model>

<verification>
1. `rm -rf .next && npm run build && npm run test:build` — zero failures, exactly three skipped tests named for G3, G4 and G6.
2. `npm run test:all` — full suite green end to end.
3. The two induced-red demonstrations (the trailing-slash mismatch in task 2) captured and reverted.
4. `git diff` on the two protected assertion hunks — unchanged in substance.
5. `npm run lint` — no new errors. `npx tsc --noEmit` — exits 0.
6. `git diff app/(en)/layout.tsx app/(de)/layout.tsx` — empty. The flip is not performed.
7. `git diff --stat components/smear-heading/` — empty.
</verification>

<success_criteria>
- Every assertion this phase broke is narrowed or repurposed with an accurate title and a rewritten comment; none is deleted; the three protected assertions are untouched.
- The sitemap's contents are bound to `publishedFor()` by set comparison; robots' contents and origin are asserted; the site root has one spelling across both files.
- Every route is asserted to carry its own title, its own description and the full OG set, with the OG image resolved from the meta tag and per-post overrides proven distinct.
- Exactly one `rel="icon"` ships and the scaffold `.ico` is gone.
- The three unfillable gate rows are named skipped tests; the double-escape failure mode is banned unconditionally.
- The flip's inversion points are documented by test title and the flip is not performed.
</success_criteria>

<output>
Create `.planning/phases/06-cv-contact-photo-discoverability/06-09-SUMMARY.md` when done.
Record: a table of every assertion changed, with its test title, what it asserted before and what it
asserts now; the three skipped test names verbatim; the observed failure from the induced
trailing-slash mismatch; and the final `npm run test:all` counts for all three tiers.
</output>
</content>
