---
quick_id: 260903-e0l
description: Move the /style-playground conic-gradient pivot from the seam midpoint to the seam's viewport-boundary point nearest the bottom-left corner while preserving A/B corner alignment
date: 2026-09-03
---

# Quick Task Plan

## Task 1: Reposition the conic-gradient pivot along the seam

**Files:**
- `app/(en)/style-playground/seam-playground.tsx`
- `app/(en)/style-playground/style-playground.module.css`

**Action:** Extend the line through A's bottom-right and B's top-left corners to the scene bounds, choose the boundary intersection nearest the bottom-left corner, and use that point as the conic-gradient origin. Keep the existing corner anchors, responsive angle, box layout, and color treatment unchanged. Set the non-JavaScript fallback origin near the bottom-left.

**Verify:** Run focused ESLint, check the route on the existing development server, and complete a production build.

**Done:** The pivot is visibly down-left from the midpoint while the seam still passes exactly through both requested box corners at every observed size.
