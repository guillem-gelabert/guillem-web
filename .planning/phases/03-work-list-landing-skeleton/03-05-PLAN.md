---
phase: 03-work-list-landing-skeleton
plan: 05
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - app/(en)/writing/page.tsx
  - app/(de)/texte/page.tsx
  - app/(en)/writing/[slug]/page.tsx
  - app/(de)/texte/[slug]/page.tsx
  - app/(en)/writing/not-found.tsx
  - app/(de)/texte/not-found.tsx
  - app/not-found.tsx
  - components/language-switch.tsx
  - tests/writing-index.spec.ts
  - tests/writing-not-found.spec.ts
  - tests/writing-routing.spec.ts
autonomous: true
requirements: [HOME-03]
must_haves:
  truths:
    - "A visitor who follows the landing view's Writing link can get back to the site root from either index — the dead end this phase creates is closed in the same phase"
    - "A German reader on /texte reaches the site root through a link that declares it leads to an English page"
    - "Every non-prose link on the site changes to accent on hover and shows a 2px accent focus ring — no link is left on the browser default"
    - "Under prefers-reduced-motion those links still change colour and still gain their underline; only the transition is gone"
  artifacts:
    - path: "app/(en)/writing/page.tsx"
      provides: "Site-root back link (A2) and .link-quiet on the headline link (A3)"
      contains: "homeLink"
    - path: "app/(de)/texte/page.tsx"
      provides: "Site-root back link with hrefLang=en (A2) and .link-quiet on the headline link (A3)"
      contains: "hrefLang"
    - path: "components/language-switch.tsx"
      provides: ".link-quiet WITHOUT inline-block py-xs — WCAG 2.5.8's inline exception"
      contains: "link-quiet"
  key_links:
    - from: "app/(en)/page.tsx contents nav"
      to: "/writing"
      via: "the Writing entry — A2 closes the return path this link creates"
      pattern: "indexPath"
    - from: "app/(de)/texte/page.tsx back link"
      to: "/"
      via: "hrefLang=\"en\" — the one declared locale crossing"
      pattern: "hrefLang=\"en\""
---

<objective>
Close the dead end this phase creates and bring every shipped non-prose link into conformance
with the accent reservation: add a `← Guillem Gelabert` site-root back link to both writing
indexes (amendment A2) and `.link-quiet` to every non-prose link on the shipped surfaces
(amendment A3).

Purpose: Phase 3 sends visitors from the landing view to `/writing`, and the shipped index has
no route back. Separately, Tailwind v4's preflight resets `a { color: inherit; text-decoration:
inherit }`, so the index headline links, the post back links, the three not-found back links
and `LanguageSwitch` currently have no accent hover, no accent focus ring, and only the
browser-default outline. Phase 2's own Color section already reserves the accent for link
hover/focus on *any* link — so this is conformance with the shipped contract, not a change to it.
Output: two amended index routes, two amended post templates, three amended not-found
boundaries, one amended component, three extended specs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-work-list-landing-skeleton/03-UI-SPEC.md
@.planning/phases/03-work-list-landing-skeleton/03-PATTERNS.md
</context>

<scope_note>
**A3 covers more files than `03-PATTERNS.md`'s Modified-files table lists. Both extensions are
deliberate and are recorded here rather than made silently.**

1. **`app/not-found.tsx` is included.** The UI-SPEC's A3 text says "both `not-found` back links"
   — written when there were two. `app/not-found.tsx` shipped the same day under Phase 2 code
   review WR-14 and renders a bare `className="text-label"` link at `:36-38`, the exact defect
   A3 exists to close. `03-RESEARCH.md` Open Question 1 recommends including it: leaving one of
   three 404 pages with a different affordance is a worse outcome than a one-line overreach.

2. **The two `[slug]` post templates are included.** The UI-SPEC's A3 text names "both
   `← Writing` / `← Texte` back links". Those live at `app/(en)/writing/[slug]/page.tsx:73` and
   `app/(de)/texte/[slug]/page.tsx:76`, which `03-PATTERNS.md`'s table omits. The UI-SPEC is
   normative on the visual contract and the pattern map is a convenience index, so the UI-SPEC
   wins. Without them, the site would have five back links carrying `.link-quiet` and two
   carrying nothing.
</scope_note>

<interfaces>
<!-- Contracts consumed. Shipped or created in wave 1. -->

lib/locales.ts (+ homeLink from Plan 03-01):
  export const UI: Record<Locale, UiCopy>
  // UI.en.homeLink === UI.de.homeLink === "← Guillem Gelabert"   (proper noun, identical in both)
  // UI.en.backLink === "← Writing"  ;  UI.de.backLink === "← Texte"
  export function indexPath(locale: Locale): string
  export function otherLocale(locale: Locale): Locale

app/globals.css (Plan 03-02): .link-quiet
  // rest: color inherit, no underline
  // hover/focus-visible: accent colour + 1px accent underline at 0.12em offset
  // focus-visible: outline 2px solid accent, offset 2px
  // transition gated inside @media (prefers-reduced-motion: no-preference)

<!-- The shipped hrefLang spelling (components/language-switch.tsx:20): hrefLang={otherLocale(from)} -->
<!-- React SSR emits the DOM property spelling `hrefLang`, not the lowercase HTML attribute —      -->
<!-- match case-insensitively in any build-output assertion (tests/build/prerender.test.ts:184-187). -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Amendment A2 — site-root back links on both writing indexes, plus A3 on their headline links</name>
  <files>app/(en)/writing/page.tsx, app/(de)/texte/page.tsx</files>
  <read_first>
    - `app/(en)/writing/page.tsx:29-47` — the current `<main className="flex flex-col gap-2xl px-lg py-3xl">` whose first child is the kicker row `<div className="flex flex-row items-baseline gap-md">`. There is no `<header>` today.
    - `app/(de)/texte/page.tsx:29-47` — the same shape in German.
    - `app/(en)/writing/[slug]/page.tsx:72-75` — the exact back-link position the UI-SPEC says to match: on its own line, above the kicker row, as the first child of the header block.
    - `components/language-switch.tsx:20` — the shipped `hrefLang={otherLocale(from)}` attribute spelling, for the `/texte` → `/` crossing.
    - `tests/writing-index.spec.ts:21-33, 66-73, 85-106` — the shipped assertions this edit must not break: `page.locator("h1")` resolves to exactly one Label-role kicker; `article a` has count 1 on `/writing`; `main > hr` has count 1 on `/texte`.
    - `.planning/phases/03-work-list-landing-skeleton/03-UI-SPEC.md` § *Amendments to shipped surfaces* A2 and A3, and § *Localisation* for the one declared locale crossing.
  </read_first>
  <action>
    In both `app/(en)/writing/page.tsx` and `app/(de)/texte/page.tsx`, insert a site-root back
    link as the FIRST child of `<main>`, immediately above the existing kicker row `<div>`. Do not
    wrap it in a new `<header>` and do not change `<main>`'s className — its `gap-2xl` already
    produces the 48px the post template's `<header className="flex flex-col gap-2xl">` produces
    between its back link and what follows.

    The link is `<Link href="/" className="text-label link-quiet inline-block py-xs">` holding
    `{UI[locale].homeLink}`. On `/texte` it additionally carries `hrefLang="en"` — a literal
    `"en"`, not `otherLocale(locale)`, because this is a fixed crossing to an English-only page
    rather than a symmetric locale switch. Comment it: the landing genuinely only exists in
    English for v1 (`I18N-01` is scoped to writing and is complete), so a German reader following
    this link lands on an English page and the attribute says so honestly; the alternative was a
    dead end on `/texte` or a German landing whose largest single string is unwritten in either
    language.

    The `inline-block py-xs` is not decoration: a Label-role link's line box measures 18.2px,
    under WCAG 2.5.8's 24px floor; the 4px block padding takes it to 26.2px. Comment it naming
    WCAG 2.5.8 in the "comment the failure, not the code" register.

    Separately, in both files add `className="link-quiet"` to the entry headline `<Link>` inside
    the `<SmearTitle as="h2">` (currently `app/(en)/writing/page.tsx:46` and
    `app/(de)/texte/page.tsx:46`). It gets `.link-quiet` only — no `inline-block py-xs`, because
    a poster-scale headline clears the target floor by a wide margin. Comment that this is
    conformance with Phase 2's own Color section, which already reserves the accent for link
    hover/focus on any link, not a new visual decision.

    Change nothing else. Do not restructure the kicker row, do not touch `LanguageSwitch`'s call
    site, do not touch the `<hr>` separator, do not touch `generateMetadata`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint "app/(en)/writing/page.tsx" "app/(de)/texte/page.tsx" && npx playwright test tests/writing-index.spec.ts tests/i18n-routing.spec.ts && node -e "const fs=require('fs'); for (const f of ['app/(en)/writing/page.tsx','app/(de)/texte/page.tsx']) { const s=fs.readFileSync(f,'utf8'); if(!s.includes('homeLink')) throw new Error(f+': missing homeLink back link'); if((s.split('link-quiet').length-1) < 2) throw new Error(f+': expected link-quiet on both the back link and the headline link'); } const de=fs.readFileSync('app/(de)/texte/page.tsx','utf8'); if(!de.includes('hrefLang=\"en\"')) throw new Error('texte back link missing hrefLang=en'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0; `npx eslint` on both files reports 0 errors (scoped to these files only — the whole-repo `npm run lint` criterion is **no NEW errors**, never 0, because of the one deferred `use-prefers-reduced-motion.ts:23` finding).
    - Both index routes render a `<Link href="/">` whose text is `UI[locale].homeLink` and whose className carries all four of `text-label`, `link-quiet`, `inline-block`, `py-xs`, as the first child of `<main>`.
    - `app/(de)/texte/page.tsx`'s back link carries the literal `hrefLang="en"`; `app/(en)/writing/page.tsx`'s back link carries no `hrefLang`.
    - Both files' entry headline `<Link>` carries `className="link-quiet"` and no `inline-block`/`py-xs`.
    - `npx playwright test tests/writing-index.spec.ts` is green — in particular the shipped assertions that `page.locator("h1")` is a single Label-role kicker, that `article a` has count 1 on `/writing`, and that `main > hr` has count 1 with `borderTopColor` `rgba(0, 0, 0, 0.12)` on `/texte` all still pass.
    - Neither `<main>` className changed: `git diff` shows no edit to the `flex flex-col gap-2xl px-lg py-3xl` string.
  </acceptance_criteria>
  <done>Both writing indexes offer a route back to the site root, and their headline links carry the accent affordance.</done>
</task>

<task type="auto">
  <name>Task 2: Amendment A3 — .link-quiet on the two post back links, the three not-found back links, and LanguageSwitch</name>
  <files>app/(en)/writing/[slug]/page.tsx, app/(de)/texte/[slug]/page.tsx, app/(en)/writing/not-found.tsx, app/(de)/texte/not-found.tsx, app/not-found.tsx, components/language-switch.tsx</files>
  <read_first>
    - `app/(en)/writing/[slug]/page.tsx:72-75` and `app/(de)/texte/[slug]/page.tsx:75-78` — the two post-template back links, both `className="text-label"`.
    - `app/(en)/writing/not-found.tsx:18-20`, `app/(de)/texte/not-found.tsx:18-20`, `app/not-found.tsx:36-38` — the three not-found back links, all three `className="text-label"`.
    - `components/language-switch.tsx:19-22` — the switch link, `className="text-label"` with `hrefLang`.
    - `.planning/phases/03-work-list-landing-skeleton/03-UI-SPEC.md` § *Accessibility contract* — the `LanguageSwitch` exemption and its two reasons.
    - The `<scope_note>` at the top of this plan — why five of these six files are an explicit, recorded extension of A3's written list.
  </read_first>
  <action>
    Six one-line className changes. Nothing else in any of these files is touched — no
    restructuring, no copy change, no new element, no `<header>` introduction.

    **Five files take `text-label link-quiet inline-block py-xs`**: both post-template back links
    (`← Writing`, `← Texte`) and all three not-found back links. Each is a Label-role link on its
    own line, so each renders an 18.2px box under WCAG 2.5.8's 24px floor and each needs the 4px
    block padding to reach 26.2px. Add one comment at the first of these sites naming WCAG 2.5.8
    and the measured numbers; the rest are the same pattern and need no repetition.

    **`components/language-switch.tsx` takes `text-label link-quiet` and explicitly NOT
    `inline-block py-xs`.** Two reasons, both concrete, and both belong in a comment on that line:
    it sits inline inside `PostMeta`'s text line, which is WCAG 2.5.8's explicit inline exception;
    and adding `inline-block` padding there would change the shipped meta-line height on
    `/writing`, on `/texte` and on every post — a visual regression on shipped surfaces for no
    accessibility gain.

    Do not add `.link-quiet` to anything inside `.prose-site` — prose links are already covered by
    the shipped `.prose-site a` rule and stacking a second link class on them would double the
    declarations for no change. `.link` and `.link-quiet` plus `.prose-site a` is the complete set
    of link styling for this site; a later phase needing a third re-opens the UI-SPEC's *Link and
    section-head contract*.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint "app/(en)/writing/[slug]/page.tsx" "app/(de)/texte/[slug]/page.tsx" "app/(en)/writing/not-found.tsx" "app/(de)/texte/not-found.tsx" app/not-found.tsx components/language-switch.tsx && npx playwright test tests/writing-not-found.spec.ts tests/writing-routing.spec.ts tests/i18n-routing.spec.ts && node -e "const fs=require('fs'); const padded=['app/(en)/writing/[slug]/page.tsx','app/(de)/texte/[slug]/page.tsx','app/(en)/writing/not-found.tsx','app/(de)/texte/not-found.tsx','app/not-found.tsx']; for (const f of padded) { const s=fs.readFileSync(f,'utf8'); if(!s.includes('text-label link-quiet inline-block py-xs')) throw new Error(f+': back link missing the full A3 class list'); } const ls=fs.readFileSync('components/language-switch.tsx','utf8'); if(!ls.includes('link-quiet')) throw new Error('LanguageSwitch missing link-quiet'); if(/inline-block|py-xs/.test(ls)) throw new Error('LanguageSwitch must NOT take inline-block py-xs — WCAG 2.5.8 inline exception'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0; `npx eslint` on all six files reports 0 errors (scoped to these files only — the whole-repo `npm run lint` criterion is **no NEW errors**, never 0, because of the one deferred `use-prefers-reduced-motion.ts:23` finding).
    - All five standalone Label-role back links carry the exact className `text-label link-quiet inline-block py-xs`.
    - `components/language-switch.tsx` carries `link-quiet` and contains neither `inline-block` nor `py-xs`.
    - `git diff --stat` for these six files shows only className-string edits plus added comments — no element added, removed or reordered, and no copy string changed.
    - `npx playwright test tests/writing-not-found.spec.ts tests/writing-routing.spec.ts tests/i18n-routing.spec.ts` is green — in particular the three `javaScriptEnabled: false` server-render assertions in `writing-not-found.spec.ts` still pass.
    - No file under `components/mdx/` or `components/prose.tsx` was touched; `grep -rn 'link-quiet' components/mdx components/prose.tsx` returns nothing.
  </acceptance_criteria>
  <done>Every non-prose link on the shipped site carries the accent hover, the accent focus ring, and — where it is a standalone Label-role link — a 26.2px target box.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the three shipped specs to gate A2 and A3</name>
  <files>tests/writing-index.spec.ts, tests/writing-not-found.spec.ts, tests/writing-routing.spec.ts</files>
  <read_first>
    - `tests/writing-index.spec.ts:3-19` — the header-comment register and the `document.fonts.ready` beforeEach.
    - `tests/writing-not-found.spec.ts:12-29, 63-80` — the `LOCALE_CASES` table-driven shape ("a one-entry table today, looped over rather than hand-written once") and the back-link assertion inside the loop.
    - `tests/writing-routing.spec.ts:7-34, 47-56` — the `test.describe` shape and the routes it already visits (`/writing/fixture`; `/texte/musterseite` is the German counterpart in `content/`).
    - `.planning/phases/03-work-list-landing-skeleton/03-VALIDATION.md` § *Phase requirements → test map* rows A2 and A3.
  </read_first>
  <action>
    Extend three shipped specs. Add tests; do not modify existing ones.

    **`tests/writing-index.spec.ts`** — add two tests.

    (A2) On `/writing` and on `/texte`, a link whose trimmed text is `← Guillem Gelabert` exists
    exactly once and has `href="/"`. On `/texte` that link additionally has the `hreflang`
    attribute equal to `en` — read it with `getAttribute("hreflang")` and compare
    case-insensitively, because React SSR emits the DOM property spelling `hrefLang` and
    `tests/build/prerender.test.ts:184-187` already records that trap.

    (A3) On both indexes, the site-root back link and the entry headline link both carry the
    `link-quiet` class, and the back link's `getBoundingClientRect().height` is at least 24
    (WCAG 2.5.8). Assert the measured height, not the arithmetic — comment the 18.2px / 26.2px
    measured values from `03-VALIDATION.md`.

    **`tests/writing-not-found.spec.ts`** — grow `LOCALE_CASES` to cover all THREE not-found
    surfaces rather than the two it has today: add `/nope`, which reaches the root
    `app/not-found.tsx` boundary with `backLinkText: "← Writing"` and `backLinkHref: "/writing"`
    (that boundary renders `UI.en`). Then add one test inside the existing loop asserting the back
    link carries `link-quiet` and has a `getBoundingClientRect().height` of at least 24.

    Do NOT add a `javaScriptEnabled: false` assertion for `/writing/does-not-exist` or
    `/texte/gibt-es-nicht`. CR-01 is open and deferred to Phase 6: Next 16.3.3 seeds
    HTTP-access-fallback error responses with a hardcoded `<html id="__next_error__">`, so those
    two paths do not server-render a heading today. The file already carries a comment recording
    that trade — extend it rather than re-deriving it.

    **`tests/writing-routing.spec.ts`** — add one table-driven test covering the two post-template
    back links: `/writing/fixture` expecting text `← Writing` and href `/writing`, and
    `/texte/musterseite` expecting text `← Texte` and href `/texte`. Assert each carries
    `link-quiet` and each has a measured height of at least 24. Header the test with a comment
    noting this is amendment A3 extended to the post templates, which `03-PATTERNS.md`'s
    modified-file table omitted and the UI-SPEC's A3 text names explicitly.
  </action>
  <verify>
    <automated>npx playwright test tests/writing-index.spec.ts tests/writing-not-found.spec.ts tests/writing-routing.spec.ts && npx playwright test</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test tests/writing-index.spec.ts tests/writing-not-found.spec.ts tests/writing-routing.spec.ts` reports 0 failures.
    - `tests/writing-index.spec.ts` gained exactly two tests; its original six are unmodified.
    - `tests/writing-not-found.spec.ts`'s `LOCALE_CASES` has 3 entries including `/nope`, and the loop gained one `link-quiet` + target-size test.
    - `tests/writing-routing.spec.ts` gained one test covering both post-template back links.
    - The `hreflang` assertion on `/texte`'s back link compares case-insensitively.
    - No `javaScriptEnabled: false` assertion was added for `/writing/does-not-exist` or `/texte/gibt-es-nicht` — CR-01 is deferred and such an assertion would fail today.
    - The full `npx playwright test` suite is green.
  </acceptance_criteria>
  <done>A2 and A3 are gated on every surface they touch, including the two extensions this plan records.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor → shipped writing surfaces | Unchanged by this plan. `[slug]` routes still validate through `SAFE_SLUG` before any dynamic import; this plan edits only className strings and adds one static link per index. |
| locale boundary `/texte` → `/` | The one declared crossing. A German reader is sent to an English page, and the `hrefLang="en"` attribute declares it rather than hiding it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-16 | Denial of service (accessibility) | The five standalone Label-role back links | mitigate | Each renders an 18.2px line box, under WCAG 2.5.8's 24×24 CSS px floor. All five take `inline-block py-xs` → 26.2px measured. Task 3 asserts `getBoundingClientRect().height >= 24` on every one of them across the three specs. |
| T-03-17 | Denial of service (accessibility) | `components/language-switch.tsx` | accept | `LanguageSwitch` stays under the 24px floor and takes no padding. WCAG 2.5.8's inline exception applies verbatim: it sits inline inside `PostMeta`'s text line. Padding it would change the shipped meta-line height on `/writing`, `/texte` and every post — a visual regression on shipped surfaces for no accessibility gain. Recorded in a source comment so it is not "fixed". |
| T-03-18 | Denial of service (accessibility) | Focus indicators on every amended link | mitigate | Before this plan these links had only the browser-default outline. `.link-quiet:focus-visible` gives them `outline: 2px solid var(--color-accent)` at 5.84:1 on paper, past WCAG 1.4.11's 3:1 floor. `outline: none` appears nowhere (gated by `tests/unit/link-contract.test.ts`, Plan 03-02). |
| T-03-19 | Tampering | The three `not-found` boundaries | mitigate | These are error surfaces; a className edit that broke their server rendering would only show up with JavaScript disabled. Task 3 keeps the shipped `javaScriptEnabled: false` assertions green for the three paths that server-render today, and explicitly does NOT add them for the two paths CR-01 covers, which would fail. |
| T-03-20 | Information disclosure | `hrefLang="en"` on `/texte`'s back link | accept | Declaring the crossing is strictly better than hiding it. The alternative — a dead end on `/texte`, or a German landing whose positioning sentence is unwritten in either language — is worse for the reader. No data is disclosed; the attribute is an honesty signal. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | Phase 3 installs zero packages; `03-RESEARCH.md` § Package Legitimacy Audit records none to vet, so no `[ASSUMED]`/`[SUS]` entry and no blocking legitimacy checkpoint applies. An `npm install` in this plan is a scope violation. |

**ASVS L1 chapters explicitly not applicable**: V2 Authentication, V3 Session Management, V6
Cryptography, V13 API. V4 Access Control and V12 File & Resource are applicable but **unchanged**
— this plan edits className strings and adds one static `<Link href="/">` per index; it touches
no call to `publishedFor`, `findBySlug`, `loadPostModule`, `SAFE_SLUG` or `CONTENT_DIR`. V5
Input Validation is applicable but has no new surface: no link added here takes a parameter.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npx playwright test` — the whole suite green, including the three `javaScriptEnabled: false` not-found assertions.
- `npm run test:unit` reports 0 failures.
- `npm run lint` shows no NEW errors beyond the one known deferred finding in `components/smear-heading/use-prefers-reduced-motion.ts:23`.
- `git grep -n 'className="text-label"' app components` returns no back-link or switch-link site — every one now carries `link-quiet`. (Label-role headings and non-link `<p>`s legitimately keep the bare class.)
- CR-01 stays untouched and deferred to Phase 6: no middleware, no `dynamicParams = false`, no `headers()` in a not-found boundary.
</verification>

<success_criteria>
- `/writing` and `/texte` each render exactly one `← Guillem Gelabert` link to `/`, the `/texte` one carrying `hrefLang="en"`.
- Seven link sites — two index headlines, two post back links, three not-found back links — carry `.link-quiet`, and the five standalone Label-role ones also carry `inline-block py-xs`.
- `LanguageSwitch` carries `.link-quiet` and no padding, and the shipped meta-line height is unchanged.
- Three shipped specs gained A2/A3 coverage without any existing test being modified.
</success_criteria>

<output>
Create `.planning/phases/03-work-list-landing-skeleton/03-05-SUMMARY.md` when done.
</output>
