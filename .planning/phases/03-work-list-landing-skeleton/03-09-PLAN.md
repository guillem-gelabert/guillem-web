---
phase: 03-work-list-landing-skeleton
plan: 09
type: execute
wave: 5
depends_on: ["03-08"]
files_modified:
  - lib/work.ts
autonomous: false
requirements: [HOME-01, HOME-04, WORK-02]
must_haves:
  truths:
    - "A human has looked at / and /cv at 375px and at 1440px and confirmed the three failures no assertion can see"
    - "The work section reads as hierarchy rather than as two unrelated pages at 1440px"
    - "The two WORK-02 annotations have been surfaced to the user as editable drafts rather than shipped silently"
    - "HOME-01 has been named out loud as still outstanding at the moment the phase closes"
  artifacts:
    - path: ".planning/phases/03-work-list-landing-skeleton/deferred-items.md"
      provides: "The record the checkpoint reads from and, if the user edits an annotation, writes back to"
  key_links:
    - from: "the optical pass"
      to: "03-UI-SPEC.md § What this costs, stated plainly"
      via: "the two places the contract is deliberately thinner than the obvious alternative — the designated places to look"
      pattern: "designated optical checkpoint"
---

<objective>
The designated optical checkpoint: `/` and `/cv` at 375px and 1440px, plus surfacing the two
`WORK-02` annotations as editable drafts and naming `HOME-01` out loud as still outstanding.

Purpose: `03-UI-SPEC.md` designates this checkpoint and `03-VALIDATION.md` lists three
verifications that are structurally manual — they are optical judgements about whether a layout
reads as hierarchy, and no assertion can prove "reads as hierarchy". Everything automatable is
already green by the time this plan runs; this is the part that is not automatable.
Output: a recorded optical sign-off, and either confirmation of the two draft annotations or the
user's edits applied to `lib/work.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-work-list-landing-skeleton/03-UI-SPEC.md
@.planning/phases/03-work-list-landing-skeleton/03-VALIDATION.md
@.planning/phases/03-work-list-landing-skeleton/deferred-items.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Optical sign-off at 375px and 1440px, and the two draft annotations</name>
  <files>lib/work.ts</files>
  <read_first>
    - `.planning/phases/03-work-list-landing-skeleton/03-UI-SPEC.md` § *What this costs, stated plainly* — the two places the contract is deliberately thinner than the obvious alternative, named so the optical pass knows where to look, and the **designated optical checkpoint** paragraph naming the three specific 375px failures.
    - `.planning/phases/03-work-list-landing-skeleton/03-VALIDATION.md` § *Manual-only verifications* — the three rows and why each is manual.
    - `.planning/phases/03-work-list-landing-skeleton/deferred-items.md` — written in Plan 03-08; entries 1 and 2 are what this checkpoint surfaces.
    - `lib/work.ts` — the file the two annotations live in, in case the user edits them.
  </read_first>
  <action>
    Present the verification steps below to the user and wait for their response. Do not proceed
    past this checkpoint without one — `workflow.auto_advance` is `false` and this gate is
    `blocking`.

    Before presenting, start the dev server so the user can look immediately rather than being
    asked to set something up: run `npm run dev` in the background and confirm it is serving on
    `http://localhost:3000`.

    Present the five items in `<how-to-verify>` verbatim, in order, including the two `WORK-02`
    annotations quoted in full and the `HOME-01` paragraph. Do not summarise item 5 away — naming
    `HOME-01` out loud at the moment the phase closes is the third of three independent channels
    protecting the tripwire, and the first two are both silent by design.

    On the user's response:
    - `approved` with no changes → record the sign-off in the plan summary, naming which viewport
      passes were confirmed, and stop.
    - An edit to either annotation → apply it to the `annotation` field of the corresponding entry
      in `lib/work.ts` and nothing else, then re-run `node --test tests/unit/work.test.ts`. If the
      banned-tool-word test fails, report the failing token to the user rather than silently
      loosening the list.
    - A report that the work section reads thin at 1440px → fix it with spacing from the existing
      seven tokens. Do **not** add a type size, a font weight, a face, a colour or a rule weight.
      The UI-SPEC names this remedy in advance and `tests/unit/link-contract.test.ts` plus
      `tests/unit/prose-contract.test.ts` are the proof no budget was widened.
    - The positioning sentence, if the user writes it now → replace only the value of
      `POSITIONING_PLACEHOLDER` in `lib/work.ts`, rename nothing, and update the constant's doc
      comment to record that `HOME-01` is now satisfied. Then update entry 1 of
      `.planning/phases/03-work-list-landing-skeleton/deferred-items.md` and the `HOME-01` rows in
      `.planning/STATE.md` from deferred to closed. Both automated gates — the built meta
      description equalling the constant, and the rendered `<p>` equalling the meta description —
      must still pass, which is what proves the sentence landed in one place rather than two.

    After any change, re-run the full gate:
    `npm run test:unit && rm -rf .next && npm run build && npm run test:build && npx playwright test`,
    then `npm run lint` and confirm no NEW error beyond
    `components/smear-heading/use-prefers-reduced-motion.ts:23`.
  </action>
  <what-built>
    Everything in Phase 3 is built, gated and deployed. `tests/landing.spec.ts`,
    `tests/landing-viewport.spec.ts`, `tests/landing-trail.spec.ts`, `tests/cv.spec.ts`,
    `tests/unit/work.test.ts`, `tests/unit/link-contract.test.ts` and the extended
    `tests/build/prerender.test.ts` are all green on a clean-`.next` build, and the live Railway
    origin serves the new landing view with `/cv` returning 200 and the site still noindexed.

    What remains is the part no assertion can reach.
  </what-built>
  <how-to-verify>
    Start the dev server with `npm run dev` (or use the live Railway origin — both are the same
    build; drafts differ but no draft is on `/` or `/cv`).

    **1. `/` at 1440px.** The one risk at this width is whether the work section reads as
    hierarchy or as two unrelated pages. A 72px Humane featured headline sits roughly two hundred
    pixels above 18px Newsreader work titles, deliberately — face contrast, not size contrast, is
    what carries the distinction, and a fifth type size was considered and rejected on the record.
    Look at whether the work section reads thin.
    **If it does, the remedy is more space and the existing rules — NOT a fifth type size.**

    **2. `/` at 375px.** This is the pass that catches real failures. Three specific things:
    - Does the five-item contents list wrap to a readable two or three rows, or does it fall into
      a ragged single column?
    - Does the ordinal-above-title stack in each work row read as one row, or as four loose lines?
    - Does the nameplate at its 56px floor sit comfortably above the positioning sentence?

    **3. `/cv` at 375px and 1440px.** Does the stub read as an authored page rather than as an
    unfinished site? That judgement is the whole point of `D-02`.

    **4. The two `WORK-02` annotations are drafts, and they are yours to edit (`D-09`).** They
    live in `lib/work.ts`. Verbatim:
    - `01` — *The Balearics stopped gaining on Europe in 1993 — while tourist arrivals went on tripling.*
    - `02` — *Roughly two people die every second: where they are, when it happens, and who they were.*

    Each says what the piece is *about*, never what it was built with — that constraint is
    enforced by `tests/unit/work.test.ts`'s banned-tool-word list, so an edit naming a language,
    framework or library will fail the suite. If you want either line changed, say so and it will
    be edited in `lib/work.ts`; the annotations are the only copy in this phase you are being
    asked to review.

    **5. `HOME-01` is still outstanding, and this is being said out loud on purpose.** The
    positioning sentence — the one line on the landing view stating what you do — ships as
    `Developer.` behind the `POSITIONING_PLACEHOLDER` constant in `lib/work.ts`. That was the
    decision (`D-08`); it is not a gap. But the marker is in the source and never on the screen,
    so the landing looks finished while the site's most important sentence is unwritten, and
    nothing in steps 1-3 above can catch it. It is recorded in
    `.planning/phases/03-work-list-landing-skeleton/deferred-items.md` and in `.planning/STATE.md`
    so it carries into every subsequent phase, and it is a blocking pre-condition on Phase 6's
    `FIND-02` robots flip. **The site must not go indexable with `Developer.` in that slot.**
    Writing the sentence now is welcome but nothing is blocked on it.
  </how-to-verify>
  <verify>
    <automated>npm run test:unit && rm -rf .next && npm run build && npm run test:build && npx playwright test && npm run lint 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - The user has confirmed, or reported issues against, each of: `/` at 1440px, `/` at 375px, `/cv` at both widths.
    - If the work section was reported as reading thin, the fix applied is spacing using the existing tokens — **no fifth type size, no third font weight, no new face, no new colour**. `npm run test:unit` (`tests/unit/link-contract.test.ts` and `tests/unit/prose-contract.test.ts`) must still be green after any such fix, which is what proves no budget was widened.
    - The two `WORK-02` annotations have been shown to the user verbatim and either confirmed as-is or edited in `lib/work.ts`.
    - If an annotation was edited, `node --test tests/unit/work.test.ts` still passes — in particular the banned-tool-word test.
    - `HOME-01` has been named to the user as still outstanding, and its record in `deferred-items.md` and `STATE.md` is confirmed present.
    - After any change made in response to this checkpoint: `npm run test:unit && rm -rf .next && npm run build && npm run test:build && npx playwright test` is green, and `npm run lint` shows no NEW errors beyond `components/smear-heading/use-prefers-reduced-motion.ts:23`.
  </acceptance_criteria>
  <resume-signal>
    Reply `approved` to close the phase as-is, or describe what to change — including any edit to
    either work-list annotation, or the positioning sentence if you want to write it now.
  </resume-signal>
  <done>The optical pass is recorded, the two annotations have been through the user, and `HOME-01` has been named out loud at the moment the phase closes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| automated gates → human judgement | The only boundary in this plan. Everything mechanically checkable is already asserted; this checkpoint covers exactly the residue — three optical judgements plus two editorial ones. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-36 | Repudiation | The `HOME-01` tripwire | mitigate | The realistic failure is that `HOME-01` closes quietly because the page looks finished. This checkpoint names it out loud at the moment the phase closes, on top of the written record in `deferred-items.md` and `STATE.md` and the two automated gates that make a partial fix fail. Three independent channels, because a single one has already been shown to be missable. |
| T-03-37 | Tampering | The design budget, under optical pressure | mitigate | The realistic failure mode of an optical checkpoint is that "the work section reads thin" is answered by adding a type size. The UI-SPEC names the remedy in advance — more space and the existing rules — and `tests/unit/link-contract.test.ts` plus `tests/unit/prose-contract.test.ts` make a fifth size, a third weight, a literal hex or a fourth rule weight fail in under a second. The acceptance criteria require those to be green after any fix. |
| T-03-38 | Information disclosure | The two `WORK-02` annotations, on a live URL | mitigate | The annotations are drafts written by an agent after reading the live pieces, and they ship on a public (if noindexed) URL during a job hunt. Surfacing them for the user's edit is the mitigation; `tests/unit/work.test.ts`'s banned-tool-word list keeps any edit inside `PROJECT.md`'s allocation principle. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | Phase 3 installs zero packages. If the response to this checkpoint is "add an animation library / a component library / an icon set", that is a scope violation against `REQUIREMENTS.md` Out of Scope and `03-UI-SPEC.md` § Registry Safety, not a dependency discovery. `components.json` must not be created and the shadcn init gate must not be run. |

**ASVS L1 chapters explicitly not applicable**: V2, V3, V4, V5, V6, V12, V13, V14. This plan runs
a human verification and, at most, edits two string literals in `lib/work.ts`.
</threat_model>

<verification>
- The user has responded to the checkpoint.
- Any change made in response leaves `npm run test:unit`, `npm run test:build` and `npx playwright test` green on a clean-`.next` build.
- `npm run lint` shows no NEW errors beyond `components/smear-heading/use-prefers-reduced-motion.ts:23`.
- `git diff --stat HEAD -- components/smear-heading/` is empty.
- If any fix widened `app/globals.css`, `tests/unit/link-contract.test.ts` and `tests/unit/prose-contract.test.ts` both still pass — which is the proof no budget was widened.
</verification>

<success_criteria>
- `/` and `/cv` have been looked at by a human at 375px and 1440px, and the result is recorded.
- Any remedy applied to a thin-reading work section used spacing and the existing rules, not a new type role.
- The two `WORK-02` annotations went through the user rather than shipping silently.
- `HOME-01` was named out loud as outstanding, and its carry-forward record is confirmed in place.
</success_criteria>

<output>
Create `.planning/phases/03-work-list-landing-skeleton/03-09-SUMMARY.md` when done.
</output>
