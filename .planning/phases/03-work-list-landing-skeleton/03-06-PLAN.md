---
phase: 03-work-list-landing-skeleton
plan: 06
type: execute
wave: 3
depends_on: ["03-03"]
files_modified:
  - tests/landing.spec.ts
autonomous: true
requirements: [WORK-01, WORK-02, HOME-01, HOME-03, HOME-04]
must_haves:
  truths:
    - "The landing view's navigation surface, work list and featured slot are gated by assertions that read computed values from a real render, not from the plan's arithmetic"
    - "A card, a border box, a grid, a fill or a fourth rule weight appearing on the landing view fails a test"
    - "The featured slot's assertions are state-agnostic — they pass in the interim state and will still pass in Phase 4's published state, with no Phase 3 file changed"
    - "A rendered placeholder marker word on / fails a test"
    - "The private ib-gdp-evolution repo name appearing in rendered output fails a test"
  artifacts:
    - path: "tests/landing.spec.ts"
      provides: "HOME-01/03/04, WORK-01/02 and the CASE-03 slot structure, asserted against npm run dev"
      min_lines: 150
      contains: "aria-label=\"Sections\""
  key_links:
    - from: "tests/landing.spec.ts"
      to: "app/(en)/page.tsx and components/landing/*"
      via: "Playwright against npm run dev on :3000"
      pattern: "page.goto\\(\"/\"\\)"
    - from: "tests/landing.spec.ts HOME-01 assertion"
      to: "the <meta name=\"description\"> on /"
      via: "equality between the rendered <p> and the meta content — Pitfall 6's one-source property, asserted directly"
      pattern: "meta\\[name=\"description\"\\]"
---

<objective>
Write `tests/landing.spec.ts` — the integration gate for `HOME-01`, `HOME-03`, `HOME-04`,
`WORK-01`, `WORK-02` and the `CASE-03` slot's structure.

Purpose: `03-VALIDATION.md`'s requirement→test map assigns eleven rows to this file, and it is
the single largest ❌ in the Wave 0 list. Every assertion here reads a computed value from a
real render, because Phase 1's scar tissue is a plan-assumed value that was wrong by 40px, and
Phase 2's is a `toHaveCount(1)` that could not see a rule rendered 8× too dark.
Output: one new Playwright spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-work-list-landing-skeleton/03-VALIDATION.md
@.planning/phases/03-work-list-landing-skeleton/03-UI-SPEC.md
@.planning/phases/03-work-list-landing-skeleton/03-PATTERNS.md
</context>

<interfaces>
<!-- The rendered contract under test, from Plan 03-03. No exploration needed. -->

/ (app/(en)/page.tsx):
  main.flex.flex-col.gap-3xl.px-lg.py-3xl        (no min-h-screen, no justify-center)
    header.flex.flex-col.gap-lg
      h1.text-display                             "Guillem Gelabert"   (SmearTitle, trail)
      p.max-w-prose.text-standfirst               POSITIONING_PLACEHOLDER
      nav[aria-label="Sections"] > ul > li × 5
        a.text-label.link-quiet.inline-block.py-xs
          #work | /writing | #backlog | /cv | #contact   (that order)
    section#case-study[aria-labelledby=case-study-head].scroll-mt-xl
      h2#case-study-head.section-head             "Case study"
      h3.text-heading                             (SmearTitle as="h3", trail)
      p.max-w-prose.text-body                     (interim)  — or standfirst + PostMeta (Phase 4)
    section#work[aria-labelledby=work-head].scroll-mt-xl
      h2#work-head.section-head                   "Work"
      ol[role=list].flex.list-none.flex-col.gap-xl
        li.flex.flex-col.gap-sm       (li 2 also: border-t border-rule pt-xl)
          p.text-label[aria-hidden=true]          "01" / "02"
          h3.text-standfirst > a.link-quiet[href]
          p.max-w-prose.text-body                 the annotation
          p.text-label                            the host
    section#backlog[...] > h2.section-head "Backlog"  + div.flex.flex-col.gap-md > p.text-standfirst + p.text-body
    section#contact[...] > h2.section-head "Contact"  + same shape

<!-- The two locked destinations (D-06, verified HTTP 200 on 2026-08-29): -->
<!--   https://ib-gdp.guillemgelabert.com/everyone-in-mallorca-agrees-on-one-thing  host ib-gdp.guillemgelabert.com -->
<!--   https://watchpeopledie.live                                                  host watchpeopledie.live         -->

<!-- Measured values (03-VALIDATION.md assertion rule 1 — assert MEASURED, never arithmetic): -->
<!--   Label-role link line box 18.2px ; with py-xs 26.2px                                     -->
<!--   scroll-margin-top from scroll-mt-xl = 32px                                              -->
<!--   --color-rule computes to the string "rgba(0, 0, 0, 0.12)"                               -->
<!--   full-ink structural rule computes to "rgb(0, 0, 0)"                                     -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold tests/landing.spec.ts and assert HOME-03 (the navigation surface) and HOME-01 (the positioning sentence)</name>
  <files>tests/landing.spec.ts</files>
  <read_first>
    - `tests/writing-index.spec.ts:1-19` — the header-comment register (name the requirement AND the assertion-shape lesson) and the mandatory `await page.evaluate(() => document.fonts.ready)` in `beforeEach`, because trail registration is deferred until fonts resolve (`use-smear-heading.ts:30`).
    - `tests/writing-index.spec.ts:66-83` — the `getByRole("link", { name })` and computed-`fontWeight` assertion shapes.
    - `playwright.config.ts` — `testDir: ./tests`, chromium only, `webServer: npm run dev` on :3000. **Every spec in this repo runs against dev, where drafts are visible by design.**
    - `app/(en)/page.tsx` and `components/landing/contents-nav.tsx` (Plan 03-03) — the markup under test.
    - `.planning/phases/03-work-list-landing-skeleton/03-VALIDATION.md` § *Phase requirements → test map* — the four `HOME-03` rows and the first `HOME-01` row this task owns.
    - `.planning/phases/03-work-list-landing-skeleton/03-RESEARCH.md` § *C-2* — why "a title appears on `/`" is not a valid acceptance criterion: `/` already inherits a title and a description from the layout, so that assertion passes before the change and proves nothing.
  </read_first>
  <action>
    Create `tests/landing.spec.ts` importing only `{ expect, test }` from `@playwright/test`,
    matching every other spec in this repo.

    Header comment, in the house register — name the requirements (`HOME-01`, `HOME-03`,
    `HOME-04`, `WORK-01`, `WORK-02`, plus the `CASE-03` slot's structure) and then the three
    assertion-shape lessons this file is built around:
    (i) computed values are read from a real render, never assumed — Phase 1's `viewport.spec.ts`
    had to assert 139.2px where the plan said "≈180px";
    (ii) copy that depends on draft visibility is NOT asserted here, because this spec runs
    against `npm run dev` where `showDrafts()` is `true` — that split lives in
    `tests/build/prerender.test.ts` (Pitfall 2);
    (iii) "a title appears on `/`" is not a meaningful assertion, because `/` inherited one from
    the layout before this phase (C-2).

    Add a `test.beforeEach` that goes to `/` and awaits `document.fonts.ready`.

    **HOME-03 tests:**

    (a) `nav[aria-label="Sections"]` exists exactly once and holds exactly 5 links. Read all five
    `href` attributes in document order with `evaluateAll` and assert the array equals
    `["#work", "/writing", "#backlog", "/cv", "#contact"]`. Read all five trimmed texts and assert
    they equal `["Work", "Writing", "Backlog", "CV", "Contact"]`. Comment that the order is
    reader-importance, not page order, and that a sixth link to `#case-study` would point at what
    the reader is already looking at.

    (b) Target size, WCAG 2.5.8. For each of the five nav links read
    `getBoundingClientRect().height` and assert every value is at least 24. Comment the measured
    values from `03-VALIDATION.md`: a Label-role line box is 18.2px and `py-xs` takes it to
    26.2px, so this passes by 2.2px — assert the measurement, not the arithmetic, because the
    margin is small enough that a dropped `py-xs` would fail here and nowhere else.

    (c) Anchor landing position. Select every `section[id]` on the page, assert there are exactly
    four with ids `case-study`, `work`, `backlog`, `contact` in that order, and assert each
    computes `scroll-margin-top: 32px`. Comment that this is `scroll-mt-xl` and that it exists so
    an anchor jump does not park the section head flush against the viewport edge.

    (d) No smooth scrolling. Assert `getComputedStyle(document.documentElement).scrollBehavior`
    is not `smooth`. Comment the two concrete reasons from the Motion Contract: it is motion that
    would need its own reduced-motion branch, and a smooth scroll fires a long burst of `scroll`
    events, each of which advances the trail's `trailHue` — a single anchor click would spin the
    hue through a large arc.

    **HOME-01 tests:**

    (e) Exactly one `<h1>` on the page, its trimmed text is `Guillem Gelabert`, and it carries the
    class `text-display`.

    (f) The positioning sentence. Locate the `p.text-standfirst` inside the page `<header>`,
    assert there is exactly one, and assert its computed `font-weight` is the string `530`.

    (g) **The one-source property, asserted directly.** Read the `content` attribute of
    `meta[name="description"]` and assert it equals the positioning `<p>`'s trimmed `innerText`.
    Comment this at length, because it is the phase's most important assertion and the reason it
    is shaped this way is not obvious: Pitfall 6 is that the user writes the positioning sentence
    into the `<p>` and the `<meta name="description">` still says `Developer.` — which is what
    Slack, LinkedIn and eventually Google quote once Phase 6 flips `FIND-02`. Asserting equality
    rather than a literal string means this test proves the property that matters, keeps passing
    when the user's real sentence lands, and fails the moment the two drift. Do NOT hardcode
    `Developer.` here; the literal-value assertion against production HTML belongs in
    `tests/build/prerender.test.ts` (Plan 03-08).

    Do not assert `<link rel="canonical">` in this file — that is production truth and Plan 03-08
    owns it.
  </action>
  <verify>
    <automated>npx playwright test tests/landing.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test tests/landing.spec.ts` reports 0 failures and at least 7 passing tests.
    - Test (a) asserts the href array `["#work", "/writing", "#backlog", "/cv", "#contact"]` and the label array `["Work", "Writing", "Backlog", "CV", "Contact"]`, both in document order.
    - Test (b) reads `getBoundingClientRect().height` for all five nav links and asserts `>= 24` on each.
    - Test (c) asserts exactly four `section[id]` elements with the ids in order, each computing `scroll-margin-top` exactly `32px`.
    - Test (g) compares `meta[name="description"]`'s `content` against the rendered `<p>`'s text by equality and contains NO literal `Developer.`.
    - The spec imports only `@playwright/test` — `grep -c "^import" tests/landing.spec.ts` returns 1.
    - Negative check performed by hand and reverted: removing `py-xs` from one nav link makes test (b) fail; removing `scroll-mt-xl` from one section makes test (c) fail.
  </acceptance_criteria>
  <done>The navigation surface and the positioning sentence carry an automated gate, including the one-source property Pitfall 6 exists to protect.</done>
</task>

<task type="auto">
  <name>Task 2: Assert WORK-01, WORK-02 and HOME-04 — the work list's structure, destinations and non-card treatment</name>
  <files>tests/landing.spec.ts</files>
  <read_first>
    - `tests/writing-index.spec.ts:35-58` — the "not a card" computed-style assertion. Copy it wholesale, adapted: for the work list, row 2 legitimately has `borderTopWidth: 1px`, so assert the other three are `0px` and the top one is the hairline.
    - `tests/writing-index.spec.ts:95-106` — the hairline-colour assertion and its comment. `toHaveCount(1)` could not see a rule rendered 8× too dark; the computed colour can. This is Pitfall 1's direct gate.
    - `tests/writing-index.spec.ts:60-64` — the `body.innerText` absence-assertion shape.
    - `components/landing/work-list.tsx` (Plan 03-03) — the markup under test.
    - `.planning/phases/03-work-list-landing-skeleton/03-VALIDATION.md` — the three `WORK-01`, two `WORK-02` and two `HOME-04` rows this task owns.
  </read_first>
  <action>
    Add to `tests/landing.spec.ts`:

    (h) **WORK-01 structure.** `section#work` contains exactly one `ol[role="list"]`, and that
    `<ol>` has exactly 2 direct-child `<li>`. Assert `role="list"` is present as an attribute, and
    comment why it is not redundant: Safari drops list semantics when `list-style: none` is
    applied.

    (i) **WORK-01 destinations.** Each `<li>` contains exactly one `<a>`. Read both `href`s in
    order and assert they equal
    `["https://ib-gdp.guillemgelabert.com/everyone-in-mallorca-agrees-on-one-thing", "https://watchpeopledie.live"]`.
    Comment that hosting is per-project with no uniform pattern — one is an apex subdomain, the
    other a separate domain — so these are asserted as literals rather than derived from a rule
    (D-06). Also assert neither `<a>` carries a `target` attribute: same-tab links create no
    `window.opener`, so there is no reverse-tabnabbing surface, and adding `target="_blank"` later
    would require `rel="noopener noreferrer"`.

    (j) **The private repo stays private.** Assert `page.locator('a[href*="github.com"]')` has
    count 0 across the whole page, and assert `body.innerText` does not contain the string
    `ib-gdp-evolution` (D-06: the repo is private and the entry titles are the pieces' published
    headlines, never repository names).

    (k) **WORK-02 annotations.** Each `<li>` contains exactly one `p.text-body`, and each of those
    is non-empty and contains no newline. Also assert each `<li>`'s host line — the second
    `p.text-label` in the row — has trimmed text equal to `new URL(href).hostname` for that row's
    link, computed inside the browser from the row's own `<a>`. That proves the outbound marker
    names the real destination rather than a stale copy.

    (l) **The ordinals.** Each `<li>`'s first `p.text-label` carries `aria-hidden="true"` and reads
    `01` and `02` respectively. Comment that the `<ol>` already conveys order and count to
    assistive technology, and a visible `01` read aloud as "zero one" is noise.

    (m) **HOME-04 — not a card.** For the FIRST `<li>`: all four computed border widths are `0px`,
    `boxShadow` is `none`, `borderRadius` is `0px`, and `backgroundColor` is one of
    `rgba(0, 0, 0, 0)`, `transparent`, `rgb(255, 255, 255)`. Assert the same for the `<ol>` itself.

    (n) **HOME-04 — the separator is the hairline, not a fourth rule weight.** For the SECOND
    `<li>`: `borderTopWidth` is `1px`, `borderTopStyle` is `solid`, `borderTopColor` is exactly the
    string `rgba(0, 0, 0, 0.12)`, and the right, bottom and left border widths are all `0px`.
    Carry the `tests/writing-index.spec.ts:95-100` comment: Tailwind v4's preflight emits
    `border: 0 solid` with no colour, so `border-t` without `border-rule` falls through to
    `currentColor` — full-ink black, 8× darker than `--color-rule`, and a fourth rule weight the
    Prose Contract forbids. This is WR-06 recurring and it is exactly what a count assertion
    cannot see.

    (o) **HOME-04 — one column.** Both `<li>` have the same `getBoundingClientRect().x`, and the
    second's `y` is greater than the first's. That is a single-column vertical list at the default
    viewport; the 375px and 1440px passes are `tests/landing-viewport.spec.ts`'s (Plan 03-07).
  </action>
  <verify>
    <automated>npx playwright test tests/landing.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test tests/landing.spec.ts` reports 0 failures and at least 15 passing tests.
    - Test (i) asserts both absolute D-06 URLs as literals, in order, and asserts no `target` attribute on either link.
    - Test (j) asserts zero `a[href*="github.com"]` and that `body.innerText` excludes `ib-gdp-evolution`.
    - Test (k) derives the expected host from the row's own `<a>` via `new URL(...).hostname` rather than hardcoding it twice.
    - Test (n) asserts `borderTopColor` is exactly the string `rgba(0, 0, 0, 0.12)` — not a `toContain`, not a regex.
    - Negative check performed by hand and reverted: removing `border-rule` from the `<li>` className makes test (n) fail with a computed colour of `rgb(0, 0, 0)`.
  </acceptance_criteria>
  <done>The work list is gated on its structure, its two locked destinations, its annotation shape, its non-card treatment and Pitfall 1's hairline colour.</done>
</task>

<task type="auto">
  <name>Task 3: Assert the CASE-03 slot structure state-agnostically, the heading outline, and the no-placeholder-words rule</name>
  <files>tests/landing.spec.ts</files>
  <read_first>
    - `.planning/phases/03-work-list-landing-skeleton/03-RESEARCH.md` § *Pitfall 2* in full — why a Playwright assertion of the interim featured-slot COPY breaks in Phase 4 for a reason that is not a defect, and how the tier split fixes it.
    - `components/landing/featured-slot.tsx` and `components/landing/section-stub.tsx` (Plan 03-03) — the markup under test.
    - `app/globals.css` `.section-head` (Plan 03-02) — the 1px full-ink structural rule.
    - `tests/writing-index.spec.ts:60-64` — the `body.innerText` absence-assertion shape.
    - `.planning/phases/03-work-list-landing-skeleton/03-VALIDATION.md` — the `CASE-03 slot` and `HOME-01/D-02` rows this task owns.
  </read_first>
  <action>
    Add to `tests/landing.spec.ts`:

    (p) **The featured slot, state-agnostically.** `section#case-study` exists exactly once and
    contains exactly one `h2.section-head` and exactly one `h3.text-heading`. Do NOT assert the
    interim strings `The case study is being written.` or `On the Mallorca piece: …` here, and do
    NOT assert whether the `<h3>` contains an `<a>`. Comment the reason in full: every Playwright
    spec in this repo runs against `npm run dev`, where `showDrafts()` returns `true`, so the
    moment Phase 4 creates `content/the-chart-therefore-changes.mdx` with `draft: true` — the
    normal way to author — `findBySlug` starts returning an entry in dev only, and the slot
    renders its *published* state in dev while production still renders *interim*. A copy
    assertion here would turn red during Phase 4 authoring with no Phase 3 file changed. The
    interim copy is asserted against real production HTML in `tests/build/prerender.test.ts`
    instead. What is asserted here is what is true in BOTH states: one section head, one
    Heading-role `<h3>`, same roles, same gaps, same order.

    (q) **Section heads.** Each of the four `section[id]` contains exactly one `h2.section-head`,
    and their trimmed texts in order are `["Case study", "Work", "Backlog", "Contact"]`. Assert
    each `h2.section-head` computes `borderBottomWidth: 1px`, `borderBottomStyle: solid`,
    `borderBottomColor: rgb(0, 0, 0)` — the "1px full ink = structural" weight, distinct from the
    work-list separator's `rgba(0, 0, 0, 0.12)`. Two rule weights on this page and no third.

    (r) **The heading outline.** Exactly one `<h1>`, exactly four `<h2>`, exactly three `<h3>`
    (the featured headline plus the two work-list titles), and zero `<h4>`, `<h5>` and `<h6>`.
    Comment that the `<h3>` rendering far larger than its `<h2>` is deliberate and not an
    inversion to be "fixed": semantics follow structure, visual weight follows editorial
    hierarchy, and an `<h2>` in the featured slot would put two `<h2>`s inside
    `section#case-study` and silently break the outline `aria-labelledby` depends on. Also assert
    each `section[id]`'s `aria-labelledby` value matches the `id` of the `h2` it contains.

    (s) **`D-02` — nothing reads as unfinished.** Assert `body.innerText` on `/` contains none of
    `TODO`, `placeholder`, `Coming soon`, `Under construction`, `Lorem`, `TBD`, matched
    case-insensitively. Comment that `D-08` calls for a clearly-marked placeholder and `D-02` for
    deliberately typeset content, and they resolve exactly one way: **the placeholder is marked in
    the source, not on the screen.** A rendered `[positioning sentence goes here]` on a live URL
    during a job hunt is what `D-02` exists to prevent (Pitfall 7).

    (t) **No card idiom anywhere on the page.** Assert `page.locator("button")` has count 0,
    `page.locator("img")` has count 0, and `page.locator("main svg")` has count 0 — this phase
    ships no form, no toggle, no icon and no image (`PROF-02` is Phase 6). Assert every `<section>`
    and every `<div>` inside `<main>` computes `borderRadius: 0px` and `boxShadow: none`.

    (u) **Both stubs render.** `section#backlog` and `section#contact` each contain exactly one
    `p.text-standfirst` and exactly one `p.text-body` inside a single wrapper — the same shape the
    shipped `/writing` empty state uses. Assert the standfirst computes `font-weight: 530`. Do not
    assert the stub copy strings here; production truth for those is Plan 03-08's, and the
    absence assertion in (s) already covers the failure mode that matters.
  </action>
  <verify>
    <automated>npx playwright test tests/landing.spec.ts && npx playwright test</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test tests/landing.spec.ts` reports 0 failures and at least 21 passing tests total across all three tasks.
    - `tests/landing.spec.ts` contains NO occurrence of the string `The case study is being written.` and no occurrence of `On the Mallorca piece` — Pitfall 2's split is respected. Verify with `grep -c`.
    - Test (q) asserts `borderBottomColor: rgb(0, 0, 0)` on the section heads and test (n) asserts `rgba(0, 0, 0, 0.12)` on the work separator — the two rule weights are distinguished, and no third appears.
    - Test (r) asserts h1=1, h2=4, h3=3, h4=h5=h6=0, and that every `aria-labelledby` resolves to the id of the `h2` inside that section.
    - Test (s)'s word list contains all six of `TODO`, `placeholder`, `Coming soon`, `Under construction`, `Lorem`, `TBD` and matches case-insensitively.
    - The full `npx playwright test` suite is green.
  </acceptance_criteria>
  <done>The featured slot, the heading outline and the copy-discipline rule are gated by assertions that will still be true after Phase 4 lands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → dev server | `tests/landing.spec.ts` drives `npm run dev` on localhost:3000 through Chromium. No production surface, no secret, no network egress beyond localhost. |
| assertion tier → truth tier | The boundary that matters here. Playwright asserts DEV truth, where `showDrafts()` is `true`. Production truth belongs in `tests/build/prerender.test.ts`. Mixing them produces a suite that goes red in Phase 4 with no Phase 3 file changed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-21 | Information disclosure | The rendered work list | mitigate | The `ib-gdp-evolution` GitHub repo is private (D-06). Test (j) asserts zero `a[href*="github.com"]` on the page and that the repo name appears nowhere in `body.innerText`. This is the only place the *rendered* leak is caught; Plan 03-01 catches it at the data layer. |
| T-03-22 | Information disclosure | `/`'s rendered copy | mitigate | Rendering `TODO`/`placeholder`/`Coming soon` would disclose the site's unfinished state to any visitor during a job hunt, on a live URL. Test (s) asserts absence over `body.innerText` case-insensitively (Pitfall 7). |
| T-03-23 | Tampering | The two outbound `<a>` | mitigate | Test (i) asserts neither link carries a `target` attribute. Same-tab navigation creates no `window.opener`, so reverse tabnabbing is structurally absent rather than mitigated by an easily-dropped `rel`. If a future change adds `target="_blank"`, this test fails and forces `rel="noopener noreferrer"` to be considered. |
| T-03-24 | Repudiation (test integrity) | The featured-slot assertions | mitigate | A copy assertion here would go red during Phase 4 authoring for a non-defect, and the likely response is to weaken or delete the test — losing the structural gate entirely. Task 3 (p) is state-agnostic by construction, and the acceptance criteria `grep` for the interim strings to prove no copy assertion crept in. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | Phase 3 installs zero packages; `03-RESEARCH.md` § Package Legitimacy Audit records none to vet, so no `[ASSUMED]`/`[SUS]` entry and no blocking legitimacy checkpoint applies. Playwright 1.62.1 and `node:test` are already installed — `03-VALIDATION.md` records "Framework install needed: none." |

**ASVS L1 chapters explicitly not applicable**: V2 Authentication, V3 Session Management, V4
Access Control, V5 Input Validation, V6 Cryptography, V12 File & Resource, V13 API. This plan
adds one test file. It ships no application code, no route, no dependency and no configuration
change. V14 Configuration is touched only in that the spec deliberately does NOT assert
`robots`, leaving that to the production tier.
</threat_model>

<verification>
- `npx playwright test tests/landing.spec.ts` reports 0 failures, ≥21 tests.
- `npx playwright test` — the whole suite green.
- `npm run test:unit` reports 0 failures.
- `npx tsc --noEmit` exits 0.
- `npm run lint` shows no NEW errors beyond the one known deferred finding in `components/smear-heading/use-prefers-reduced-motion.ts:23`.
- `grep -c 'The case study is being written' tests/landing.spec.ts` returns 0 — the tier split holds.
- No `test.only`, no `test.skip`, no `--watch` flag anywhere in the file.
</verification>

<success_criteria>
- Every `HOME-01`, `HOME-03`, `HOME-04`, `WORK-01`, `WORK-02` and `CASE-03 slot` row in `03-VALIDATION.md`'s test map that names `tests/landing.spec.ts` has a passing assertion.
- The positioning sentence is gated by equality with `<meta name="description">`, not by a literal — so it keeps passing when the user's real sentence arrives and fails the moment the two drift.
- Two rule weights are distinguished by computed colour: `rgb(0, 0, 0)` structural, `rgba(0, 0, 0, 0.12)` separator.
- The featured slot's assertions pass in both the interim and the published state.
</success_criteria>

<output>
Create `.planning/phases/03-work-list-landing-skeleton/03-06-SUMMARY.md` when done.
</output>
