---
phase: 05-backlog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/backlog.tsx
  - components/landing/backlog-list.tsx
  - app/(en)/page.tsx
autonomous: true
requirements: [BACK-01, BACK-02]

must_haves:
  truths:
    - "A visitor scrolling to #backlog sees three named pieces of in-progress work, each with one paragraph saying what it is about and what question it pursues."
    - "A visitor sees one absolute 'Last touched' date above the list, before reading any item."
    - "The Phase 3 stub copy is gone from the backlog section — not hidden behind a branch, not kept as a fallback."
    - "A malformed LAST_TOUCHED or an empty item array fails `next build` with an error naming lib/backlog.tsx, proven by running it rather than assumed."
    - "The item copy is marked drafted-not-reviewed in source and nowhere on screen."
  artifacts:
    - path: "lib/backlog.tsx"
      provides: "BACKLOG (three items, name + description), LAST_TOUCHED, COPY_REVIEWED, and the fail-loud build-time validator"
      contains: "export const LAST_TOUCHED"
      min_lines: 60
    - path: "components/landing/backlog-list.tsx"
      provides: "The work list's row grammar minus ordinals, host line and link"
      exports: ["BacklogList"]
    - path: "app/(en)/page.tsx"
      provides: "The date line and the list mounted in Phase 3's <section id=\"backlog\">"
      contains: "BacklogList"
  key_links:
    - from: "app/(en)/page.tsx"
      to: "lib/backlog.tsx"
      via: "LAST_TOUCHED imported for the <time> element"
      pattern: "LAST_TOUCHED"
    - from: "components/landing/backlog-list.tsx"
      to: "lib/backlog.tsx"
      via: "BACKLOG imported and mapped"
      pattern: "BACKLOG"
    - from: "lib/backlog.tsx"
      to: "next build"
      via: "module-scope throw reached because app/(en)/page.tsx prerenders and imports it"
      pattern: "throw new Error"
---

<objective>
Ship the backlog section: the data module, the list component, and the one-line replacement in
`app/(en)/page.tsx` that deletes Phase 3's stub.

This is a content-and-curation plan wearing a thin layer of markup. Every class, token, component
shape and gate it needs is already shipped. The work is (1) writing three grounded item
descriptions, (2) mirroring `lib/work.ts` and `components/landing/work-list.tsx` with three
affordances subtracted, and (3) closing the one link in the design that research could not verify:
that a module-scope `throw` actually fails `next build` (Assumptions Log A1/A2).

The production diff for the whole phase is two new files and one changed line.

Purpose: BACK-01 and BACK-02's rendered half, plus D-09.1's build-time guard.
Output: `lib/backlog.tsx`, `components/landing/backlog-list.tsx`, one line of `app/(en)/page.tsx`.

**This plan ends with two known-red assertions in `tests/landing.spec.ts` — `(r)` at `:373`/`:389`
and `(u)` at `:444`. They are not regressions: each asserts an interim state this plan ends. Plan
03 owns them. Do not edit `tests/landing.spec.ts` here.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/05-backlog/05-CONTEXT.md
@.planning/phases/05-backlog/05-RESEARCH.md
@.planning/phases/05-backlog/05-VALIDATION.md
@.planning/phases/03-work-list-landing-skeleton/03-UI-SPEC.md
@lib/work.ts
@components/landing/work-list.tsx
@components/post-meta.tsx

<interfaces>
<!-- Everything below is shipped and read this session. Use it directly; do not go looking. -->

app/globals.css — the only type roles this section may use:
  .text-standfirst  font-size: 18px; font-weight: 530;  (item name)
  .text-body        font-size: 18px; font-weight: 400;  (description)
  .text-label       font-size: 14px; font-weight: 400; text-transform: uppercase;  (date line)
  .section-head     font-size: 14px; font-weight: 400;  (already on #backlog's <h2>, untouched)
  .link             underlined inline link outside .prose-site — shipped in Phase 3 naming this
                    phase as its consumer. NOT used in v1 (see Task 1).
Tailwind: max-w-prose → max-width: 65ch. gap-xl 32px, gap-lg 24px, gap-sm 8px, pt-xl 32px,
          border-rule → border-color: var(--color-rule) → rgba(0, 0, 0, 0.12).

lib/locales.ts:25-33
  export function formatPostDate(iso: string, locale: Locale): string
  UTC-pinned via `new Date(\`${iso}T00:00:00Z\`)` and timeZone: "UTC". Do not write a second
  formatter (Don't Hand-Roll).

components/post-meta.tsx:29 — the exact markup D-12 reuses:
  <time dateTime={date}>{formatPostDate(date, locale)}</time>

app/(en)/page.tsx — the mount point, already correct and not to be restructured:
  :8   import { SectionStub } from "@/components/landing/section-stub";   ← STAYS (#contact uses it)
  :75-84  <section id="backlog" aria-labelledby="backlog-head"
             className="scroll-mt-xl flex flex-col gap-lg">
            <h2 id="backlog-head" className="section-head">Backlog</h2>
            <SectionStub state="Nothing listed here yet."
                         body="The current work is being written up." />   ← THE ONLY LINE TO REPLACE
          </section>
  The id, aria-labelledby, scroll-mt-xl, the <h2>, and the contents-nav entry at
  components/landing/contents-nav.tsx:19 are all untouched. No navigation change is needed.

components/landing/work-list.tsx — the grammar being mirrored, verbatim shape:
  <ol role="list" className="flex list-none flex-col gap-xl">
    <li className={"flex flex-col gap-sm" + (index > 0 ? " border-t border-rule pt-xl" : "")}>
      <p className="text-label" aria-hidden="true">01</p>          ← SUBTRACTED (D-11.1)
      <h3 className="text-standfirst"><a className="link-quiet" …>  ← link SUBTRACTED (D-11.3)
      <p className="max-w-prose text-body">{annotation}</p>
      <p className="text-label">{host}</p>                          ← SUBTRACTED (D-11.2)

lib/content.ts:34-76 — assertFrontmatter's posture to mirror: collect problems[], then one
  `throw new Error(\`<file>: ${problems.join("; ")}\`)` naming every problem at once.

Standing gates this markup must satisfy (do not break them, do not edit them here):
  tests/landing.spec.ts:82-85    exactly 4 section[id] — do not add a section
  tests/landing.spec.ts:194      ZERO a[href*="github.com"] page-wide
  tests/landing.spec.ts:403-412  no todo/placeholder/coming soon/under construction/lorem/tbd in
                                 body.innerText — this is why the D-14 marker is source-only
  tests/landing.spec.ts:416-442  zero main button / img / main svg; every main section & main div
                                 computes border-radius 0px and box-shadow none — including the new
                                 date/list <div>
  tests/unit/link-contract.test.ts:270-291  app/(en)/page.tsx keeps no "use client", no
                                 useSmearHeading, no robots:
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author lib/backlog.tsx — three grounded items, the tripwire, the fail-loud validator</name>
  <files>lib/backlog.tsx</files>
  <read_first>
    - lib/work.ts in full — the data-module shape this file copies, including the comment register:
      why the type exists, why the tuple is closed, why POSITIONING_PLACEHOLDER is marked in source
    - lib/content.ts:34-76 — assertFrontmatter's collect-then-throw shape
    - .planning/phases/05-backlog/05-RESEARCH.md § "Q4 — What the three items actually are" — the
      evidence table, the three drafted descriptions, the accuracy caveats, and the recommended
      order. This is the source of the copy; do not re-derive it.
    - .planning/phases/05-backlog/05-RESEARCH.md § "Architecture Patterns → Pattern 1" — the
      validator block, including the +36h grace and why it is 36h and not 0
    - .planning/phases/05-backlog/05-RESEARCH.md § "Q3 — The .link class", the subsection
      "Honest finding: none of the three recommended items has a public URL to link to"
    - ~/vault/projects/personal/masterarbeit/CLAUDE.md and planning/research-notes.md
    - ~/vault/projects/personal/data-story-hausnamen/CLAUDE.md
    - ~/vault/projects/personal/pudding-pudding/README.md
  </read_first>
  <action>
    Create `lib/backlog.tsx`, mirroring `lib/work.ts` in structure and in comment register — every
    non-obvious choice carries a comment saying why, naming the decision ID.

    **The type.** `export type BacklogItem = { name: string; description: ReactNode }`. Exactly two
    fields, per D-06 — no date, no status, no tag, no href, no ordinal. `description` is a
    `ReactNode`, which is the entire reason the file is `.tsx` (D-05). Do not rename it to `.ts` and
    do not add JSX for the sake of earning the extension.

    **The tripwire.** `export const COPY_REVIEWED = false;` above the items, with a comment stating:
    the copy below is drafted from repository evidence and has NOT been reviewed by the author
    (D-14); the marker lives here in source and nowhere on screen because
    `tests/landing.spec.ts:403-412` bans marker words from rendered text and a visible "draft" badge
    on a live URL during a job hunt is exactly what D-02 exists to prevent; flip to `true` only
    after the author's editorial pass; must not reach Phase 6's FIND-02 robots flip while `false`.

    **The three items, in this order (D-04, widest-range-first), taking the drafted copy from
    RESEARCH § Q4 as the starting point:**

    1. `A data portrait of the Swiss commodity trade` —
       "The physical commodity trade runs through Switzerland in private partnerships with no
       disclosure duty and no regulator of their own. The question is what can actually be measured
       about a business whose defining feature is that it is not."
    2. `The house names of Zürich` —
       "Before street numbers, houses in Zürich were known by name. The question is how many of
       those names survived from the eighteenth century into the present — and whether what
       disappeared was the houses or only the naming."
    3. `The Pudding, read as a corpus` —
       "Two hundred-odd visual essays by one publication, read together instead of one at a time.
       The question is whether a house style is visible in the aggregate — which subjects recur,
       which forms get reused, and what the publication has quietly stopped doing."

    Verify each description against the three repo records listed in `read_first` before committing
    to it. You may tighten a clause; you may not change what it claims. Then check it against every
    one of these, all of which are hard constraints:

    - **Never say what it is built with.** No tool, language, framework or library name; no "built
      with" / "powered by". The banned-token list at `tests/unit/work.test.ts:56-58` is the standard
      and Plan 04 applies it to this copy against production HTML.
    - **State a question, never a finding (Assumptions Log A5).** Both `masterarbeit` and
      `data-story-hausnamen` record their research question as still open. Tightening the prose into
      a claim turns a drafted line into an unsourced assertion on a bylined page.
    - **Name no private individual** — not the thesis supervisor, not an interview contact, not any
      Pudding staff member.
    - **Name no real Zürich house.** Settled by the coordinator: omit. Do not recall one; a
      plausible-sounding invented name is precisely the fabrication D-02 forbids.
    - **Never describe `pudding-pudding` as a pitch.** It may be a live pitch and describing it as
      one publicly could cost it. It is a corpus study of one publication's body of work, and
      nothing else. Flag it in the SUMMARY as the one-edit veto item.
    - **No year on the *Rohstoff* book** — the repo's own records disagree (2011 vs 2012).
    - **No `<strong>` anywhere (Pitfall 1).** Tailwind v4 preflight ships
      `b,strong{font-weight:bolder}`, so a `<strong>` outside `.prose-site` renders **700** — a third
      weight on screen — while every `globals.css` source-budget test stays green. `<em>` is safe
      (preflight does not reset `font-style`, and Newsreader ships a real italic face) and is
      permitted where the prose genuinely calls for emphasis.
    - **No `<a>` in v1.** None of the three has a public artifact to link to; D-07's reasoning (a
      link on unfinished work invites a click that disappoints) argues against manufacturing one,
      and `.link` stays demonstrated on `/type`. Do not invent a link to justify the class.
    - **No `github.com` href, ever, and no `target="_blank"`** — page-wide gates, both tiers.
    - One paragraph, inline content only (D-08). No heading, list, figure, code block or second
      paragraph.

    **The date.** `export const LAST_TOUCHED = "2026-08-31";`. Confirm the value before writing it:
    run `find ~/vault/projects/personal/masterarbeit -type f -not -path '*/.git/*' -newermt
    '2026-08-30' | head` and check that item 1 really was touched on the 31st. If your calendar date
    has rolled past 2026-08-31, re-measure all three repos and set LAST_TOUCHED to the later of
    (max item last-touch, your commit date), and say which in the SUMMARY.

    Comment the constant with what it actually asserts: the honest reading is `max(item last-touch)`
    — measured as masterarbeit 2026-08-31, hausnamen 2026-08-26, pudding-pudding 2026-08-19 — not
    "when I last edited this file". On 2026-08-31 both readings coincide because item 1 was touched
    that day; if item 1 were ever dropped the honest value would fall back to 2026-08-26. Bump it
    when the WORK moves. Plan 02's guard enforces the floor: LAST_TOUCHED may never be earlier than
    the module's last change.

    **The validator (D-09.1), at module scope, using RESEARCH § Pattern 1 verbatim in shape:**
    collect a `problems[]` array, then one `throw new Error(\`lib/backlog.tsx: …\`)` naming every
    problem. Check: `BACKLOG.length === 0` (D-13 — there is no empty state);
    `BACKLOG.length > 4` (D-02's ceiling); `LAST_TOUCHED` matches `/^\d{4}-\d{2}-\d{2}$/`; it is a
    real calendar date, proven by the round-trip `new Date(\`${LAST_TOUCHED}T00:00:00Z\`)
    .toISOString().slice(0,10) === LAST_TOUCHED` (shape is not validity — `2026-02-31` passes the
    regex and silently rolls to March); and it is not in the future, with a **+36h grace, not zero**
    (Pitfall 4: the date is authored in CEST and the build runs in UTC, so a strict comparison fails
    a legitimate late-evening edit while 36h still rejects a date a month ahead).

    **Put no git call anywhere in this file.** Git in the build path is the one thing that would
    genuinely break Railway (RESEARCH Q1 §D). Freshness is Plan 02's tier.

    Run `npx tsc --noEmit` when done.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; /usr/bin/grep -c "export const LAST_TOUCHED" lib/backlog.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `lib/backlog.tsx` exists and `npx tsc --noEmit` is clean.
    - `/usr/bin/grep -E 'export const (BACKLOG|LAST_TOUCHED|COPY_REVIEWED)' lib/backlog.tsx` returns all three lines.
    - `/usr/bin/grep -c 'export const COPY_REVIEWED = false' lib/backlog.tsx` returns 1.
    - `/usr/bin/grep -Ec 'export const LAST_TOUCHED = "[0-9]{4}-[0-9]{2}-[0-9]{2}"' lib/backlog.tsx` returns 1.
    - `BACKLOG` holds exactly 3 items; every item has a non-empty `name` and a `description`, and no item has any third field.
    - `/usr/bin/grep -c '<strong' lib/backlog.tsx` returns 0. `/usr/bin/grep -c 'href=' lib/backlog.tsx` returns 0. `/usr/bin/grep -ci 'github.com' lib/backlog.tsx` returns 0.
    - `/usr/bin/grep -c 'spawnSync\|child_process\|git ' lib/backlog.tsx` returns 0 — no git in the build path.
    - The file contains exactly one `throw new Error(` and its message string begins with `lib/backlog.tsx:`.
    - The not-future check uses a `36` hour grace, not a bare `> Date.now()`.
    - No description contains a token from `["React","Next","D3","TypeScript","JavaScript","Svelte","WebGL","Python"]` (word-boundary, case-insensitive) or the phrases "built with" / "powered by".
    - No description names a private individual, states a research finding, names a Zürich house, describes item 3 as a pitch, or puts a year on *Rohstoff*.
    - The SUMMARY names item 3 as the one-edit veto item and records the LAST_TOUCHED measurement that was actually run.
  </acceptance_criteria>
  <done>The backlog's entire content is one typed module: three grounded items in editorial order, a source-only drafted-copy tripwire, a hand-set date carrying its own semantics in a comment, and a validator that will not let a malformed date or an empty list reach a build.</done>
</task>

<task type="auto">
  <name>Task 2: BacklogList, the date line, and the stub's deletion from app/(en)/page.tsx</name>
  <files>components/landing/backlog-list.tsx, app/(en)/page.tsx</files>
  <read_first>
    - components/landing/work-list.tsx in full — the row grammar, and specifically the two comments
      at `:11-13` (why `border-rule` is not optional) and `:20-22` (why `role="list"` is not
      redundant). Both reasons apply here unchanged.
    - components/post-meta.tsx:29 — the `<time dateTime>` line to reproduce
    - app/(en)/page.tsx:1-20 and :75-95 — the import block and the two stub call sites
    - .planning/phases/05-backlog/05-RESEARCH.md § "Architecture Patterns → Pattern 2 and Pattern 3"
    - .planning/phases/05-backlog/05-CONTEXT.md § D-10, D-11, D-12
  </read_first>
  <action>
    **Create `components/landing/backlog-list.tsx`** exporting `BacklogList`, importing `BACKLOG`
    from `@/lib/backlog`.

    Structure: `<ul role="list" className="flex list-none flex-col gap-xl">`, one `<li>` per item
    with `className={"flex flex-col gap-sm" + (index > 0 ? " border-t border-rule pt-xl" : "")}`,
    keyed on `item.name`. Inside each `<li>`, exactly two elements:
    `<h3 className="text-standfirst">{item.name}</h3>` and
    `<p className="max-w-prose text-body">{item.description}</p>`.

    `<ul>`, not `<ol>` — the backlog is unranked (D-11.1). `role="list"` is required and not
    redundant: Safari drops list semantics when `list-style: none` is applied.

    `<h3>`, not `<p>`, for the name. This is settled: D-10 says "the work list's row grammar" and
    D-11 names exactly three subtractions, of which element type is not one; downgrading to `<p>`
    would be an unstated fourth subtraction and would cost screen-reader users a navigable outline
    in a section that is otherwise pure prose. It takes the document from three `<h3>` to six —
    Plan 03 owns that count.

    `border-t` and `border-rule` always travel together. Tailwind v4's preflight emits
    `border: 0 solid` with **no colour**, so a bare `border-t` falls through to `currentColor` and
    renders full ink — 8× darker than `--color-rule` and a fourth rule weight the Prose Contract
    forbids. Carry `work-list.tsx:11-13`'s comment across; this is a recurring defect in this repo.

    Add comments marking each of D-11's three subtractions at the point where the work list has the
    thing and this list does not: no ordinal `<p className="text-label">`, no
    `<a className="link-quiet">` on the name, no host `<p className="text-label">`. The absence is
    the marker, and a later reader must not "restore" them.

    No `clsx`/`cn`. The repo has no class-composition helper and must not gain one
    (`work-list.tsx:11-13`).

    **Edit `app/(en)/page.tsx`.** Replace the single `<SectionStub state="Nothing listed here yet."
    body="The current work is being written up." />` line inside `<section id="backlog">` with:

      a `<div className="flex flex-col gap-lg">` holding, in order, a
      `<p className="text-label">` reading `Last touched ` followed by
      `<time dateTime={LAST_TOUCHED}>{formatPostDate(LAST_TOUCHED, "en")}</time>`, and then
      `<BacklogList />`.

    Per D-12: the section keeps its `<h2>` and its own `gap-lg`, and this one `<div>` carries date
    and list, so head→date and date→list are both `lg`. No new token. The date is **above** the
    list, not below — BACK-02 is the mitigation the user accepted in place of per-item dates, and a
    freshness signal read after the list has mitigated nothing.

    Add the two imports (`LAST_TOUCHED` from `@/lib/backlog`, `formatPostDate` from `@/lib/locales`,
    `BacklogList` from `@/components/landing/backlog-list`). **Keep the `SectionStub` import** — the
    contact section still uses it until Phase 6. Delete nothing else: the `id`, `aria-labelledby`,
    `scroll-mt-xl flex flex-col gap-lg`, the `<h2>` and the contents-nav entry are all untouched, and
    no navigation change is needed.

    Do not add `"use client"`, do not add a `robots:` key, do not touch `content/` or `lib/content.ts`.

    Verify by eye in the dev server that three rows render with a rule above rows 2 and 3 and none
    above row 1, and that the date line sits between the section head and the first row. Then run
    `npx tsc --noEmit` and `npm run lint` (the criterion is no NEW errors — the repo carries exactly
    one known deferred error at `components/smear-heading/use-prefers-reduced-motion.ts:23`, which
    must not be fixed here).
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx playwright test tests/landing.spec.ts --reporter=line -g "(t) no card idiom" &amp;&amp; npx playwright test tests/landing.spec.ts --reporter=line -g "(s) D-02"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` is clean; `npm run lint` reports exactly one error and it is `use-prefers-reduced-motion.ts:23`.
    - `/usr/bin/grep -c "Nothing listed here yet." 'app/(en)/page.tsx'` returns 0 and `/usr/bin/grep -c "The current work is being written up." 'app/(en)/page.tsx'` returns 0.
    - `/usr/bin/grep -c "SectionStub" 'app/(en)/page.tsx'` returns at least 2 — the import and the `#contact` call site both survive.
    - `/usr/bin/grep -c "No contact details here yet." 'app/(en)/page.tsx'` returns 1 — the contact stub is untouched.
    - `components/landing/backlog-list.tsx` contains `<ul role="list"`, contains `border-t border-rule pt-xl`, and `/usr/bin/grep -c "link-quiet\|text-label\|aria-hidden" components/landing/backlog-list.tsx` returns 0 — all three D-11 subtractions hold.
    - `/usr/bin/grep -c "use client" 'app/(en)/page.tsx'` returns 0 and `/usr/bin/grep -c "robots" 'app/(en)/page.tsx'` returns 0.
    - Playwright `(t)` and `(s)` pass unchanged: the new date/list `<div>` computes `border-radius: 0px` and `box-shadow: none`, and no banned marker word reaches `body.innerText`.
    - `npx playwright test tests/landing.spec.ts --reporter=line` fails on exactly two tests — `(r)` and `(u)` — and no others. Record their names in the SUMMARY as expected-red, owned by Plan 03.
    - No file under `content/`, `lib/content.ts` or `components/landing/contents-nav.tsx` changed.
  </acceptance_criteria>
  <done>The backlog section renders three real rows under one absolute date, the Phase 3 stub copy is gone from the page rather than branched around, and the only Playwright failures are the two assertions that exist to notice exactly this change.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the build-time validator actually fails `next build`</name>
  <files>lib/backlog.tsx</files>
  <read_first>
    - .planning/phases/05-backlog/05-RESEARCH.md § "Assumptions Log" rows A1 and A2 — the two
      unverified claims this task closes, and the probe research recommends
    - .planning/phases/05-backlog/05-RESEARCH.md § "Q1 §I — Two independent gates on the same fact"
    - lib/backlog.tsx as written in Task 1
    - package.json — `build` is exactly `next build`; `test:unit` is never invoked by it
  </read_first>
  <action>
    D-09.1 is only a guard if it fires. Research could not verify that a module-scope `throw` fails
    `next build` (A1) or that the validation block survives tree-shaking (A2) — both are inference
    from bundler defaults, and an unfired guard is worse than no guard because it reads as
    protection. Task 2 made the probe meaningful: `app/(en)/page.tsx` now imports the module, so
    prerendering `/` evaluates it.

    Run three builds and record the exact exit code and the first line of stderr for each.

    1. **Baseline.** `rm -rf .next && npm run build`. Must exit 0. This is the control.
    2. **Invalid date.** Temporarily change `LAST_TOUCHED` to `"2026-02-31"` — a string that passes
       the regex and is not a real calendar date, which is the specific case the round-trip check
       exists for. `rm -rf .next && npm run build` must exit **non-zero**, and the output must
       contain `lib/backlog.tsx:` and name the problem. Restore the real value.
    3. **Empty list.** Temporarily change `BACKLOG` to `[]`. `rm -rf .next && npm run build` must
       exit **non-zero** and name the D-13 problem. Restore the three items.

    Then `rm -rf .next && npm run build` once more and confirm exit 0 with the real values restored,
    and confirm `.next/server/app/index.html` contains the three item names and a
    `dateTime="<LAST_TOUCHED>"` attribute — note that React 19 emits the JSX prop name **camelCase**
    in the raw file, so grep for `dateTime=`, not `datetime=` (Pitfall 2).

    If a broken build exits **0**, A1 or A2 is false and D-09.1 is a no-op. Do not paper over it:
    stop, record the measured behaviour, and say so plainly in the SUMMARY — the correct response is
    a plan revision, not a workaround, because the repo-tier guard in Plan 02 would then be the only
    thing standing.

    `git status --porcelain lib/backlog.tsx` must be clean of the probe edits before you finish.
    Commit the three files from Tasks 1 and 2 together.
  </action>
  <verify>
    <automated>rm -rf .next &amp;&amp; npm run build &amp;&amp; /usr/bin/grep -c 'dateTime="' .next/server/app/index.html</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records three measured results in a table: baseline exit code, invalid-date exit code and error excerpt, empty-list exit code and error excerpt.
    - The invalid-date build and the empty-list build both exited non-zero, and both error messages contained the literal `lib/backlog.tsx:`.
    - The final `rm -rf .next && npm run build` exits 0.
    - `/usr/bin/grep -c 'dateTime="' .next/server/app/index.html` returns at least 1.
    - `.next/server/app/index.html` contains all three item names.
    - `git diff --stat lib/backlog.tsx` shows no probe edits remaining; `LAST_TOUCHED` and `BACKLOG` hold their real values.
    - If either broken build exited 0, the SUMMARY says so explicitly under a heading naming A1/A2 as falsified, and no workaround was added.
  </acceptance_criteria>
  <done>The build-time half of D-09's two-guard design is measured rather than assumed: a malformed date and an empty list each fail `next build` with an error naming the file, and the restored module prerenders three items and a date into real HTML.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author-authored constants → build | `LAST_TOUCHED` is the only value in this phase that is validated rather than trusted. It crosses into `next build` and into rendered HTML. |
| repository evidence → public bylined copy | The three descriptions assert things about real projects and, potentially, about real third parties. This is the boundary that matters most in this plan. |

There is no untrusted runtime input anywhere in this plan. The section takes no user input, ships
no JS, has no route handler, no form and no fetch.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | `description: ReactNode` rendered into the landing page | mitigate | Descriptions are compile-time JSX; React escapes text children. `dangerouslySetInnerHTML` and `innerHTML` appear nowhere in this plan and must not be introduced. ASVS V5. |
| T-05-02 | Information Disclosure | Item copy naming private third parties | mitigate | Task 1 forbids naming the thesis supervisor, any interview contact and any Pudding staff member, and forbids naming a real Zürich house. The `masterarbeit` evidence directory contains all of these. |
| T-05-03 | Information Disclosure | A backlog description linking to a private repository | mitigate | Zero `<a>` ships in v1 (no item has a public artifact). `tests/landing.spec.ts:194` and the production-tier equivalent assert zero `github.com` hrefs page-wide; the acceptance criteria grep the module directly. |
| T-05-04 | Tampering | Reverse tabnabbing via `target="_blank"` | mitigate | No `target="_blank"` ships; no `<a>` ships at all. The existing production assertion `doesNotMatch(root, /target="_blank"/)` covers the new copy. |
| T-05-05 | Denial of Service | A malformed `LAST_TOUCHED` reaching a visitor | mitigate | D-09.1 fails at build, not at request time — there is no runtime error state on the landing view. Task 3 proves the failure is real rather than assuming it. ASVS V5/V7. |
| T-05-06 | Repudiation | A stale freshness claim shipping unnoticed | transfer | Deliberately out of this plan's build path: git in `next build` is the one thing that would break Railway. The floor is enforced at repo tier by Plan 02. |
| T-05-SC | Tampering | npm / pip / cargo installs | accept | **This plan installs nothing.** RESEARCH's Package Legitimacy Gate was not run because there is no package to check; `package.json` already carries every capability. If an agent proposes a TS/TSX loader dependency, it has taken the wrong turn — see Plan 02. |

ASVS V2 (Authentication), V3 (Session Management), V4 (Access Control) and V6 (Cryptography) are
**inapplicable**: this is a static, public, unauthenticated page with no session, no credential and
nothing cryptographic. V14 (Configuration) is unchanged — `robots: { index: false }` stays on until
Phase 6's FIND-02, and `app/(en)/page.tsx` must continue to declare no `robots:` of its own.
</threat_model>

<verification>
- `npx tsc --noEmit` clean.
- `npm run lint` reports exactly one error, at `components/smear-heading/use-prefers-reduced-motion.ts:23`.
- `rm -rf .next && npm run build` exits 0, and `.next/server/app/index.html` carries three item names and a `dateTime="` attribute.
- A build with `LAST_TOUCHED = "2026-02-31"` exits non-zero naming `lib/backlog.tsx:` — measured, in the SUMMARY.
- `npx playwright test tests/landing.spec.ts` fails on exactly `(r)` and `(u)`, both named in the SUMMARY as Plan 03's.
- `git diff --stat` touches exactly three files: `lib/backlog.tsx`, `components/landing/backlog-list.tsx`, `app/(en)/page.tsx`.
</verification>

<success_criteria>
- Three grounded items ship, in D-04's order, each one paragraph, each saying what the work is about and what question it pursues.
- The section-level date renders above the list as one `.text-label` line with a `<time dateTime>` carrying the ISO value.
- Phase 3's backlog stub copy is deleted from the page; `SectionStub` still serves `#contact`.
- `COPY_REVIEWED = false` is exported and marked in source, and appears nowhere on screen.
- D-09.1's build-time guard is proven to fire, closing Assumptions Log A1 and A2.
</success_criteria>

<output>
Create `.planning/phases/05-backlog/05-01-SUMMARY.md` when done. Record: the final copy of all three
descriptions and the repo file each clause traces to; the measurement behind `LAST_TOUCHED`; the
three-row build-probe table with exit codes and error excerpts; item 3 named as the one-edit veto
item; and the two expected-red Playwright tests by name and line.
</output>
