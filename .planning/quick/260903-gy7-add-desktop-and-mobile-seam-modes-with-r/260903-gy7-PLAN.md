---
quick_id: 260903-gy7
description: Add desktop and mobile seam modes with rotated mobile landscape behavior and more dynamic desktop angles
date: 2026-09-03
---

# Quick Task Plan

## Task 1: Split the seam system into device-width and orientation modes

**Files:**
- `components/seam/use-seam-alignment.ts`
- `components/landing/landing-seam.module.css`
- `app/(en)/style-playground/style-playground.module.css`

**Action:** Define desktop as wider than 64rem and keep its two-corner geometry active at every aspect ratio, so the seam angle continues to react to the rendered frame positions even in tall desktop windows. For mobile portrait, retain the stacked layout and bottom-left pivot. For mobile landscape, rotate that composition clockwise: place the boxes side by side with the same tight edge-sized gap, move the pivot to the bottom-right, and aim the seam through the inter-box gap. Keep the landing and playground CSS modes identical and let their shared hook select the correct pivot logic.

**Verify:** Lint the shared hook, request `/` and `/style-playground` from the development server, run `git diff --check`, and complete a production build.

**Done:** Desktop behavior no longer switches based on aspect ratio, mobile has distinct portrait and landscape arrangements, and the desktop seam remains responsive to viewport geometry.
