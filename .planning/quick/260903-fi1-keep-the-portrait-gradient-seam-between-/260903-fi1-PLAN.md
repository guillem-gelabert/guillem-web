---
quick_id: 260903-fi1
description: Keep the portrait gradient seam between the stacked boxes by moving its conic center to their midpoint
date: 2026-09-03
---

# Quick Task Plan

## Task 1: Derive a portrait conic center between the boxes

**Files:**
- `app/(en)/style-playground/seam-playground.tsx`
- `app/(en)/style-playground/style-playground.module.css`

**Action:** Separate the landscape conic-center inputs from the resolved gradient-center variables. Preserve the Leva-controlled center in landscape, but at the existing `1:1` breakpoint resolve the conic center and rotation pivot to the stacked boxes' shared midpoint (`50% 50%`). Keep the seam angle measured from A's bottom-right to B's top-left so the seam crosses the gap and both anchors.

**Verify:** Run focused ESLint, request `/style-playground` from the existing development server, run `git diff --check`, and complete a production build.

**Done:** The portrait seam lies between the stacked boxes, its conic center and pivot coincide on the corner-to-corner line, and landscape center controls behave as before.
