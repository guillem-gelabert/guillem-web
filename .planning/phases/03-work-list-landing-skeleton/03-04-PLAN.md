---
phase: 03-work-list-landing-skeleton
plan: 04
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - app/(en)/cv/page.tsx
  - tests/cv.spec.ts
  - app/(en)/type/page.tsx
  - tests/type-specimen.spec.ts
autonomous: true
requirements: [HOME-03]
must_haves:
  truths:
    - "A visitor following the CV link from the landing view reaches a real page that returns 200 and reads as authored, not as an unfinished site"
    - "The CV page offers a route back to the site root"
    - "The type specimen carries a reference rendering of every new typographic class, so the next phase checks against it rather than inventing"
    - "Neither /cv nor /type renders the words TODO, placeholder, Coming soon, Under construction or Lorem"
  artifacts:
    - path: "app/(en)/cv/page.tsx"
      provides: "The /cv stub route — real page, real metadata, deliberately typeset placeholder (D-02)"
      contains: "export const metadata"
    - path: "tests/cv.spec.ts"
      provides: "200, an h1, the site-root back link, target size, and the no-placeholder-words gate"
    - path: "app/(en)/type/page.tsx"
      provides: "One specimen section for .section-head, .link and .link-quiet (amendment A4)"
      contains: "section-head"
  key_links:
    - from: "components/landing/contents-nav.tsx"
      to: "/cv"
      via: "the CV entry in the five-item contents list — HOME-03 requires the destination to resolve"
      pattern: "/cv"
    - from: "app/(en)/cv/page.tsx"
      to: "lib/locales.ts UI.en.homeLink"
      via: "the shared ← Guillem Gelabert back-link string added in Plan 03-01"
      pattern: "homeLink"
---

<objective>
Ship the `/cv` stub route and extend the `/type` specimen with a reference rendering of the
three new classes.

Purpose: `HOME-03` requires the CV to be reachable from the landing view, and `D-02` requires
unbuilt sections to ship as real surfaces rather than 404s or greyed-out affordances — a
visitor in a ninety-second scan reads disabled navigation as an unfinished site. `/cv` 404s
today. Amendment A4 keeps Phase 1's standing instruction that the specimen route is the
reference every later phase's new typographic component is checked against, rather than a
one-time scaffold.
Output: one new route, one new spec, one amended shipped route, one extended shipped spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-work-list-landing-skeleton/03-UI-SPEC.md
@.planning/phases/03-work-list-landing-skeleton/03-PATTERNS.md
</context>

<interfaces>
<!-- Contracts consumed. Shipped or created in wave 1. -->

lib/locales.ts (+ homeLink from Plan 03-01):
  export const UI: Record<Locale, UiCopy>   // UI.en.homeLink === "← Guillem Gelabert"

components/smear-title.tsx (widened in Plan 03-01):
  export function SmearTitle(props: { as?: "h1" | "h2" | "h3"; className?: string;
                                      children: React.ReactNode }): JSX.Element

app/globals.css (Plan 03-02): .section-head, .link, .link-quiet

<!-- Measured values these specs must assert (03-VALIDATION.md assertion rule 1). -->
<!-- Assert what was MEASURED, never the arithmetic:                              -->
<!--   Label-role link line box  = 18.2px ; with py-xs = 26.2px                    -->
<!--   Heading role DOES saturate its 72px ceiling by 1440px                       -->
<!--   Display role is 139.2px at 1440px, NOT 180px — reuse viewport.spec.ts's clampPx() -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create app/(en)/cv/page.tsx — the /cv stub route</name>
  <files>app/(en)/cv/page.tsx</files>
  <read_first>
    - `app/(en)/writing/[slug]/page.tsx:70-90` — the page shell this route traces: `<main className="flex flex-col gap-3xl px-lg py-3xl">`, a `<header className="flex flex-col gap-2xl">` holding the back `<Link>`, then a nested `<div className="flex flex-col gap-lg">` holding the `SmearTitle` and the body copy. One flex column per gap value, never a margin.
    - `app/(en)/layout.tsx:9-14` — the static `metadata` export form, and the fact that the layout owns `metadataBase`, `title`, `description` and `robots`.
    - `app/(en)/writing/page.tsx:10-23` — the `alternates.canonical` shape.
    - `.planning/phases/03-work-list-landing-skeleton/03-RESEARCH.md` § *Pitfall 5* — `/cv` inherits `robots: { index: false }` from the layout. Adding it here would create a second statement of a rule Phase 6's `FIND-02` must flip, and the string `robots` appearing outside the two root layouts is the warning sign.
    - `.planning/phases/03-work-list-landing-skeleton/03-UI-SPEC.md` § *Stub sections and `/cv`* — the normative markup and copy.
  </read_first>
  <action>
    Create `app/(en)/cv/page.tsx` as a Server Component. No `"use client"`, no hook, no `async`
    (it reads nothing).

    Imports in house order: `import type { Metadata } from "next"`, `next/link`, `@/lib/locales`
    (`UI`), `@/components/smear-title` (`SmearTitle`).

    Export `const metadata: Metadata` declaring exactly two things: `title: "CV — Guillem Gelabert"`
    and `alternates: { canonical: "/cv" }`. Declare NOTHING else — no `robots` (inherited from
    `app/(en)/layout.tsx:13`; Pitfall 5), no `metadataBase` (the layout owns it), no `description`
    (the layout's default is correct for a stub). Comment the absence of `robots` so a later
    reader does not "fix" the inheritance by restating it. The `canonical` is a consistency
    recommendation from research assumption A4 rather than a UI-SPEC requirement; note that
    inline.

    Render `<main className="flex flex-col gap-3xl px-lg py-3xl">` containing a
    `<header className="flex flex-col gap-2xl">` which holds, in order:

    1. `<Link href="/" className="text-label link-quiet inline-block py-xs">{UI.en.homeLink}</Link>`
       — the site-root back link. The `inline-block py-xs` clears WCAG 2.5.8's 24px target floor
       (a Label-role line box measures 18.2px; the 4px block padding takes it to 26.2px). Comment
       it naming WCAG 2.5.8.
    2. `<div className="flex flex-col gap-lg">` holding
       `<SmearTitle as="h1" className="text-heading">CV</SmearTitle>` and then
       `<p className="max-w-prose text-body">The CV is being written up as a page.</p>`.

    The heading takes the Heading role (Humane, trail), not Display — this page has one heading
    and it is a two-letter word. The body copy is exactly `The CV is being written up as a page.`
    — present tense, no date promise, no "coming soon", no badge, no adjective. `D-02` requires
    placeholder content to be deliberately typeset because the site is on a live URL during a job
    hunt; the marker for what is unwritten lives in source, never on screen (Pitfall 7).

    No `<button>`, no icon, no SVG, no card, no border box, no `min-h-screen`, no `justify-center`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx eslint "app/(en)/cv/page.tsx" && node -e "const s=require('fs').readFileSync('app/(en)/cv/page.tsx','utf8'); if(s.includes('\"use client\"')) throw new Error('must be a Server Component'); if(s.includes('robots')) throw new Error('Pitfall 5: robots must stay in the two root layouts only'); if(s.includes('metadataBase')) throw new Error('layout owns metadataBase'); if(!s.includes('CV — Guillem Gelabert')) throw new Error('title missing'); if(!s.includes('homeLink')) throw new Error('back link must use UI.en.homeLink'); if(!/link-quiet[\s\S]*inline-block[\s\S]*py-xs|text-label link-quiet inline-block py-xs/.test(s)) throw new Error('back link missing target-size classes'); const body=s.replace(/\/\/.*|\/\\*[\s\S]*?\\*\//g,''); for (const w of ['TODO','placeholder','Coming soon','Under construction','Lorem','TBD']) if(body.includes(w)) throw new Error('rendered placeholder marker: '+w); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0; `npx eslint "app/(en)/cv/page.tsx"` reports 0 errors.
    - The file contains no `"use client"`, no `robots`, no `metadataBase`, no `min-h-screen`, no `justify-center`.
    - `metadata.title` is exactly `CV — Guillem Gelabert` and `alternates.canonical` is exactly `/cv`.
    - The back link uses `UI.en.homeLink`, targets `/`, and carries all four of `text-label`, `link-quiet`, `inline-block`, `py-xs`.
    - The body copy is exactly `The CV is being written up as a page.`
    - Outside source comments, the file contains none of `TODO`, `placeholder`, `TBD`, `Coming soon`, `Under construction`, `Lorem`.
  </acceptance_criteria>
  <done>`/cv` is a real route with real metadata and a route back to the site root.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/cv.spec.ts</name>
  <files>tests/cv.spec.ts</files>
  <read_first>
    - `tests/type-specimen.spec.ts` (all of it) — the closest analog: a short single-route Playwright spec with a `// Covers …` header.
    - `tests/writing-index.spec.ts:3-19` — the header-comment register (name the requirement AND the assertion-shape lesson) and the mandatory `await page.evaluate(() => document.fonts.ready)` after `goto`, because trail registration is deferred until fonts resolve (`use-smear-heading.ts:30`).
    - `tests/writing-index.spec.ts:60-64` — the `body.innerText` absence-assertion shape.
    - `tests/writing-not-found.spec.ts:12-29` — the back-link assertion shape (text and href together).
    - `playwright.config.ts:6-27` — `testDir: ./tests`, `testMatch: **/*.spec.ts`, chromium only, `webServer: npm run dev` on :3000.
    - `.planning/phases/03-work-list-landing-skeleton/03-VALIDATION.md` § *Phase requirements → test map* — the two `HOME-03` rows this file owns.
  </read_first>
  <action>
    Create `tests/cv.spec.ts`. Header comment: `// Covers HOME-03 / D-02:` — `/cv` is one of the
    five destinations the landing's contents list names, it 404'd before this phase, and `D-02`
    requires it to read as authored rather than as an unfinished site; the placeholder-word
    absence assertion is the automated half of that, because "reads as authored" is otherwise
    only checkable optically.

    Use a `test.beforeEach` that navigates to `/cv` and then awaits `document.fonts.ready`.

    Write these tests:

    (a) `/cv` responds 200. Capture the response from `page.goto("/cv")` inside this test (not
    from the shared `beforeEach`) and assert `response?.status()` is `200`. This is the assertion
    that would have failed before this phase.

    (b) The page renders exactly one `<h1>` and its text is `CV`. Assert `page.locator("h1")`
    has count 1 and its `innerText` trimmed equals `CV`.

    (c) The `<h1>` carries `text-heading`, and its computed `font-family` resolves to the Humane
    stack — assert `getComputedStyle(el).fontFamily` contains the substring `Humane`
    case-insensitively. This proves the Heading role rather than a bare browser default.

    (d) The site-root back link exists exactly once, its trimmed text is `← Guillem Gelabert`
    (the arrow is the literal U+2190 character), and its `href` resolves to the site root — assert
    the `href` attribute is `/`.

    (e) Target size, WCAG 2.5.8. Read the back link's `getBoundingClientRect().height` and assert
    it is at least 24. Assert the MEASURED height, not the arithmetic: `03-VALIDATION.md` records
    a Label-role line box at 18.2px and 26.2px with `py-xs`, and Phase 1's scar tissue is that a
    computed value assumed from a plan was wrong by 40px. Comment that.

    (f) `body.innerText` on `/cv` contains none of `TODO`, `placeholder`, `Coming soon`,
    `Under construction`, `Lorem`, `TBD` — matched case-insensitively. This is `D-02`'s gate and
    Pitfall 7's warning sign.

    Do not assert `<meta name="robots">` here. Playwright runs against `npm run dev`; the
    production-truth assertions for `/cv` belong in `tests/build/prerender.test.ts` and are
    Plan 08's.
  </action>
  <verify>
    <automated>npx playwright test tests/cv.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test tests/cv.spec.ts` reports 0 failures and at least 6 passing tests.
    - The spec asserts a literal `200` status from `page.goto("/cv")`.
    - The spec asserts the back-link text `← Guillem Gelabert` and `href="/"` in the same test.
    - The target-size assertion reads `getBoundingClientRect().height` and compares against `24`, with a comment recording the measured 18.2px / 26.2px values.
    - The placeholder-word list in the spec contains all six of `TODO`, `placeholder`, `Coming soon`, `Under construction`, `Lorem`, `TBD`.
    - The spec contains no assertion about `<meta name="robots">` (that is production truth, Plan 08's).
  </acceptance_criteria>
  <done>`/cv` has an automated gate covering its status, its heading, its back link, its target size and its copy discipline.</done>
</task>

<task type="auto">
  <name>Task 3: Amendment A4 — add a specimen section to /type for the three new classes and extend its spec</name>
  <files>app/(en)/type/page.tsx, tests/type-specimen.spec.ts</files>
  <read_first>
    - `app/(en)/type/page.tsx` in full, and especially `:14-24` — the repeated specimen shape: a `<section className="flex flex-col gap-sm">`, a Label-role `<p>` caption, the specimen itself, then a `max-w-prose text-body` sentence explaining it.
    - `tests/type-specimen.spec.ts` (all of it) — the single existing test, `type specimen route renders all four type-scale roles`.
    - `app/globals.css` — the three new classes (Plan 03-02), so the specimen demonstrates them accurately.
    - `.planning/phases/03-work-list-landing-skeleton/03-UI-SPEC.md` § *Amendments to shipped surfaces* item A4 — placement and wording are `planner may override`; the requirement that the three classes appear there does not move.
  </read_first>
  <action>
    Append ONE new `<section className="flex flex-col gap-sm">` to `app/(en)/type/page.tsx`,
    after the existing four role specimens, following the shipped section shape exactly.

    **`/type` stays a Client Component.** Do not remove its `"use client"` directive and do not
    de-client it — that is out of scope, has no requirement behind it, and `/type` is this
    project's trail calibration reference at five registered headings, which the phase falls back
    on if the landing's two-heading measurement is ever questioned. The new section is static JSX
    inside the existing file; it registers no sixth trail heading and must not call
    `useSmearHeading`.

    The new section contains, in order:

    1. A Label-role caption `<p className="text-label">Section head and links</p>`.
    2. `<h2 className="section-head">Section head</h2>` — the `.section-head` specimen.
    3. A `<p className="max-w-prose text-body">` containing a sentence with exactly one inline
       `<a className="link" href="/">` — the `.link` specimen, demonstrating a link inside a block
       of body copy outside `.prose-site`. Word the sentence so the link is the only anchor in it.
    4. `<a className="text-label link-quiet inline-block py-xs" href="/">Guillem Gelabert</a>` —
       the `.link-quiet` specimen, a standalone link that is not inside a block of text, with the
       `inline-block py-xs` that clears WCAG 2.5.8's 24px target floor.
    5. A closing `<p className="max-w-prose text-body">` naming the behaviour that cannot be seen
       at rest: both classes move to the accent colour on hover and on focus, `.link-quiet` adds
       its underline back on hover so the affordance is never colour-only, both show a 2px accent
       focus ring on `:focus-visible`, and under `prefers-reduced-motion: reduce` the colour and
       underline change still happens — only the 120ms transition is removed, because the state
       change is not motion.

    Exactly one element with class `section-head`, exactly one with class `link`, exactly one with
    class `link-quiet` on the whole page. Introduce no fifth type size, no new class and no icon.

    Then extend `tests/type-specimen.spec.ts` with one new test asserting `/type` renders exactly
    one `.section-head`, exactly one `.link` and exactly one `.link-quiet`, and that the
    `.section-head` element's computed `borderBottomColor` is `rgb(0, 0, 0)` with
    `borderBottomWidth` `1px` and `borderBottomStyle` `solid` — the "1px full ink = structural"
    weight. Assert the computed colour string exactly, in the shape `tests/writing-index.spec.ts:104`
    already uses; a `toHaveCount` alone cannot see a wrong rule weight. Leave the existing test
    untouched.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx playwright test tests/type-specimen.spec.ts && node -e "const s=require('fs').readFileSync('app/(en)/type/page.tsx','utf8'); if(!s.includes('\"use client\"')) throw new Error('/type must stay a Client Component'); for (const c of ['section-head','\"link\"','link-quiet']) if(!s.includes(c.replace(/\"/g,''))) throw new Error('missing specimen for '+c); const refs=(s.match(/useSmearHeading<HTMLHeadingElement>\(\)/g)||[]).length; if(refs!==5) throw new Error('trail heading count changed from 5 to '+refs); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test tests/type-specimen.spec.ts` reports 0 failures and 2 passing tests (the original plus the new one).
    - `app/(en)/type/page.tsx` still contains `"use client"` and still calls `useSmearHeading` exactly five times — `/type` remains the 5-heading trail calibration reference.
    - `/type` renders exactly one element with class `section-head`, exactly one with class `link`, exactly one with class `link-quiet`.
    - The `.section-head` specimen computes `borderBottomWidth: 1px`, `borderBottomStyle: solid`, `borderBottomColor: rgb(0, 0, 0)`.
    - The `.link-quiet` specimen carries `inline-block` and `py-xs`; the `.link` specimen sits inside a `<p>` of body copy and does not.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>The specimen route demonstrates all three new classes and the extended spec catches a wrong rule weight.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor → `/cv`, `/type` | One-directional. Both routes are statically prerendered, accept no input — no form, no query param, no route param, no `searchParams` — and perform no mutation. |
| route metadata → crawler | `robots: { index: false }` must remain in force and remain stated in exactly two files until Phase 6's `FIND-02`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-12 | Information disclosure | `app/(en)/cv/page.tsx` metadata | mitigate | `/cv` must inherit `robots: { index: false }` from `app/(en)/layout.tsx:13` rather than restate it. A second statement is a rule Phase 6 must then flip in three places instead of two, with the failure mode being an indexed stub CV during a job hunt. Task 1's verify fails on the string `robots` appearing in the route file (Pitfall 5). Plan 08 asserts the inherited noindex against production HTML. |
| T-03-13 | Denial of service (accessibility) | `/cv` back link, `/type` `.link-quiet` specimen | mitigate | Both are Label-role standalone links whose 18.2px line box is under WCAG 2.5.8's 24px floor. Both carry `inline-block py-xs` → 26.2px measured. `tests/cv.spec.ts` (e) asserts the measured `getBoundingClientRect().height` ≥ 24. |
| T-03-14 | Information disclosure | `/cv` placeholder copy | mitigate | The site is on a live URL during a job hunt. Rendering `TODO`/`placeholder`/`Coming soon` would disclose the site's unfinished state to any visitor and is exactly what `D-02` exists to prevent. `tests/cv.spec.ts` (f) asserts absence over `body.innerText`. |
| T-03-15 | Tampering | `app/(en)/type/page.tsx` | mitigate | `/type` is the trail's 5-heading calibration reference. An amendment that silently changed its registered-heading count would invalidate the one measurement this phase can fall back on. Task 3's verify asserts `useSmearHeading` is still called exactly five times and `"use client"` is still present. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | Phase 3 installs zero packages; `03-RESEARCH.md` § Package Legitimacy Audit records none to vet, so no `[ASSUMED]`/`[SUS]` entry and no blocking legitimacy checkpoint applies. An `npm install` in this plan is a scope violation. |

**ASVS L1 chapters explicitly not applicable**: V2 Authentication, V3 Session Management, V4
Access Control, V6 Cryptography, V12 File & Resource (neither route reads the filesystem), V13
API. V5 Input Validation has zero surface — both routes are static and take no parameter. V14
Configuration is applicable and is covered by T-03-12.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npx playwright test tests/cv.spec.ts tests/type-specimen.spec.ts` reports 0 failures.
- `npx playwright test` — the whole suite green.
- `npm run lint` shows no NEW errors beyond the one known deferred finding in `components/smear-heading/use-prefers-reduced-motion.ts:23`.
- `git grep -n 'robots' app/` returns exactly two files: `app/(en)/layout.tsx` and `app/(de)/layout.tsx`.
</verification>

<success_criteria>
- `/cv` returns 200, renders an `<h1>` reading `CV`, offers a `← Guillem Gelabert` link to `/`, and declares only `title` and `alternates.canonical`.
- Neither `/cv` nor `/type` renders any placeholder marker word.
- `/type` demonstrates `.section-head`, `.link` and `.link-quiet` exactly once each, and still registers exactly five trail headings.
- `tests/cv.spec.ts` exists and gates status, heading, back link, target size and copy discipline.
</success_criteria>

<output>
Create `.planning/phases/03-work-list-landing-skeleton/03-04-SUMMARY.md` when done.
</output>
