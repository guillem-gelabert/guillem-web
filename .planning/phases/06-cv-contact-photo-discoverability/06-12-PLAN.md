---
phase: 06-cv-contact-photo-discoverability
plan: 12
type: execute
wave: 6
depends_on: [06-11]
files_modified:
  - .planning/phases/06-cv-contact-photo-discoverability/audit.md
  - .planning/phases/06-cv-contact-photo-discoverability/launch-gate.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: false
requirements: [PROF-02, PROF-03, FIND-01]

must_haves:
  truths:
    - "The three checks no automated tier can make — the Slack unfurl, the screen-reader email pass, and the optical sign-off — have been performed by a human and recorded with a verdict"
    - "The milestone's tracking surfaces reflect what actually shipped, including what remains open and who owns it"
  artifacts:
    - path: ".planning/phases/06-cv-contact-photo-discoverability/audit.md"
      provides: "Extended with the recorded human verdicts for the three manual rows"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 6 marked complete with its plan list"
  key_links:
    - from: "audit.md manual rows"
      to: "launch-gate.md G7 and G12"
      via: "the recorded verdict and date"
      pattern: "G12"
---

<objective>
Three checks in this phase cannot be automated, and every one of them guards a requirement.

PITFALLS #8 exists because nobody pastes their own localhost link into Slack — a link preview can be
correct in every assertion and still look wrong. PITFALLS #5's acceptance for the obfuscated email is
three-part, and the third leg is reading it with a screen reader, which no Playwright assertion
substitutes for. And every prior phase in this milestone closed with a designated optical sign-off at
375px and 1440px, because "reads as authored" is only checkable by looking.

Everything automatable has already been automated across plans 06-01 to 06-11. This plan asks the
user once, at the end, for the three things only a person can confirm — and then closes the
milestone's tracking surfaces honestly, including what remains open.

Purpose: the human half of the audit, batched into a single request, and the milestone close.
Output: recorded verdicts and updated tracking.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-cv-contact-photo-discoverability/06-VALIDATION.md
@.planning/phases/06-cv-contact-photo-discoverability/launch-gate.md
@.planning/phases/06-cv-contact-photo-discoverability/HANDOFF-user-supplied.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: the three manual rows — unfurl, screen reader, optical pass</name>
  <files>.planning/phases/06-cv-contact-photo-discoverability/audit.md</files>
  <action>
Pause and ask the user, once, for the three verdicts described below. Everything automatable was
already automated across plans 06-01 to 06-11; this task adds no code and fixes nothing. Capture each
verdict verbatim with its date, and hand any reported issue to Task 2 for triage rather than acting
on it here.
  </action>
  <read_first>
    - `.planning/phases/06-cv-contact-photo-discoverability/audit.md` — sections 1 to 6 as written by plan 06-11, so the request cites what has already been proven
    - `.planning/phases/06-cv-contact-photo-discoverability/06-11-SUMMARY.md` — the live verification output, including the resolved OG image URLs
    - `.planning/research/PITFALLS.md` #5 and #8
  </read_first>
  <what-built>
Everything in this phase, deployed and verified against the live origin. Specifically: `/cv` with the
portrait slot and the contact block; the landing view's real `#contact`; the six security headers with
the delivered CSP byte-identical to its unit-tested string; `sitemap.xml` and `robots.txt`; the
committed OG cards including a per-post card for each case study; the replaced favicon; localised
server-rendered 404s for unmatched post slugs in both locales; and every route carrying its own
title, description and canonical on `guillemgelabert.com`.

The site is still `noindex` on every route, deliberately. The FIND-02 flip is documented in
`launch-gate.md` as a gated one-commit procedure for you to perform after your own review.
  </what-built>
  <how-to-verify>
Three things, in any order. Each takes a minute.

**1. The Slack unfurl (FIND-01, gate G7).** Paste `https://guillemgelabert.com` into Slack, a DM, or
any link-preview debugger, and look at the card. Then paste
`https://guillemgelabert.com/writing/the-chart-therefore-changes` and look at that one. Expected: a
title, a readable description, and a 1200x630 ink-on-paper card with the name in Humane. The case
study's card should differ from the site card.
  Report: does each unfurl show a title, a description and an image, and does the card look right?

**2. The screen-reader email pass (PROF-03).** This is the one leg of PITFALLS #5's three-part
acceptance that cannot be automated; the keyboard and copyable legs are already asserted in
`tests/contact.spec.ts`. Note that `EMAIL` is currently `null`, so there is nothing to read yet — this
row is expected to be recorded as **deferred until the address is supplied**, not as a pass. If you
would rather supply the address first and then do this pass, say so and it will be recorded as
blocked on `HANDOFF-user-supplied.md` row 1.
  Report: deferred, or — if you supplied an address — does VoiceOver read the address correctly and
  announce the link?

**3. The optical sign-off at 375px and 1440px.** Open `https://guillemgelabert.com/` and
`https://guillemgelabert.com/cv` at both widths. This is the same designated pass Phases 2, 3, 4 and 5
each closed with, applied to the two surfaces this phase changed.
  Look for: the contact rows sitting correctly in the landing's rhythm; `/cv` reading as authored
  rather than unfinished while the CV body is still its stub line; nothing rendering a visible
  placeholder anywhere; and no third font weight, no icon, no rounded corner appearing where the
  automated sweep would not have looked.
  Report: approved, or describe what is wrong.

For reference while you look: `.planning/phases/06-cv-contact-photo-discoverability/audit.md` holds
the automated verdicts, and `HANDOFF-user-supplied.md` lists the five facts that are still yours to
supply — the CV body being a stub is expected, not a defect.
  </how-to-verify>
  <acceptance_criteria>
    - A verdict is recorded for each of the three rows, with a date
    - Row 2's verdict is either a pass against a supplied address or an explicit deferral naming `HANDOFF-user-supplied.md` row 1 as its blocker
    - Any issue the user describes is captured verbatim before any fix is attempted
    - No fix is made during this task — issues are recorded and triaged in Task 2
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/06-cv-contact-photo-discoverability/audit.md && /usr/bin/grep -cE 'Slack unfurl|screen reader|optical' .planning/phases/06-cv-contact-photo-discoverability/audit.md</automated>
  </verify>
  <resume-signal>Reply with a verdict for each of the three rows — for example "1 approved, 2 deferred, 3 approved" — or describe what is wrong.</resume-signal>
  <done>The three checks no automated tier can make have a human verdict and a date on the record.</done>
</task>

<task type="auto">
  <name>Task 2: record the verdicts and close the milestone's tracking surfaces</name>
  <files>.planning/phases/06-cv-contact-photo-discoverability/audit.md, .planning/phases/06-cv-contact-photo-discoverability/launch-gate.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>
    - Task 1's recorded verdicts
    - `.planning/phases/06-cv-contact-photo-discoverability/launch-gate.md` — the 13 rows written by plan 06-11
    - `.planning/ROADMAP.md` § Phase 6 and the Progress table
    - `.planning/REQUIREMENTS.md` § Traceability — the eight rows marked Pending against Phase 6
    - `.planning/STATE.md` — Current Position, Accumulated Context, Deferred Items, and the three carried blockers
    - `.planning/phases/05-backlog/05-04-SUMMARY.md` — how the previous phase closed its tracking
  </read_first>
  <action>
Write Task 1's three verdicts into `audit.md` section 5, each with its date, and mirror the unfurl
verdict into `launch-gate.md`'s G7 row. If the user reported an issue, record it verbatim and triage
it: fix it now only if it is a one-line correction within this phase's own files; otherwise add it to
`deferred-items.md` with an owner and stop rather than expanding scope at milestone close.

Update `REQUIREMENTS.md`'s traceability table. Mark **BUILD-04**, **FIND-01**, **PROF-04** as
Complete. Mark **PROF-01**, **PROF-02**, **PROF-03**, **PROF-05** as *Complete (surface shipped;
value user-supplied)* with a pointer to `HANDOFF-user-supplied.md` — the requirement's mechanism is
built and proven, and only the personal fact is outstanding. Mark **FIND-02** as *Blocked on the
launch gate* with a pointer to `launch-gate.md`, because the sitemap and robots file ship but the
site is deliberately not indexable. Do not mark anything Complete that is not, and do not mark
anything Pending that shipped — the traceability table is the milestone's own claim about itself.

Update the checkbox list in the Profile and Contact, Foundation and Deployment, and Discoverability
sections to match, using the same qualifier wording so the two views of the same fact agree.

Update `ROADMAP.md`: mark Phase 6 complete with its date, replace `**Plans:** TBD` with the real plan
count and the wave-grouped plan list, and update the Progress table's Phase 6 row.

Update `STATE.md`:
- Current Position: Phase 6 complete; milestone v1.0 complete except the launch gate.
- Progress counters: total plans and completed plans across all six phases.
- Accumulated Context / Decisions: add this phase's decisions — the canonical host moved to the apex
  and why the code fallback plus the Railway variable both exist; `proxy.ts` rather than
  `middleware.ts` and that it exists solely for CR-01 rather than riding along with the headers;
  committed OG PNGs because `next/og` cannot load any variable font; `'unsafe-inline'` accepted for
  both `script-src` and `style-src` with the two named consumers; the `documentTop` gauge-invariance
  correction and that `components/smear-heading/` came out of the phase with an empty diff.
- Deferred Items: CR-01 moves from Open to **Closed by Phase 6** with the mechanism named. Add
  BUILD-07's remaining scope, PROF-06, the nonce CSP, the re-deferred lint debt, and the
  `_pm/kanban.md` discrepancy.
- Blockers/Concerns: re-assert the three carried copy tripwires per HOME-01's carry-forward rule —
  they are not closed and the user chose to keep all three blocking — and add the five user-supplied
  values as a single blocker pointing at `HANDOFF-user-supplied.md`. State plainly that the site is
  live and complete but deliberately unindexed, and that the flip is the user's to perform.

Do not flip robots. Do not edit `app/(en)/layout.tsx` or `app/(de)/layout.tsx`. Confirm both diffs are
empty as the last step.
  </action>
  <verify>
    <automated>npm run test:all && git diff --stat "app/(en)/layout.tsx" "app/(de)/layout.tsx" components/smear-heading/ && /usr/bin/grep -c 'Phase 6' .planning/ROADMAP.md && /usr/bin/grep -c 'HANDOFF-user-supplied' .planning/REQUIREMENTS.md .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:all` passes end to end
    - All three human verdicts are recorded in `audit.md` with dates, and G7's verdict is mirrored into `launch-gate.md`
    - `REQUIREMENTS.md`'s traceability table has no row still reading Pending for Phase 6, and every qualifier matches the corresponding checkbox line
    - FIND-02 is marked blocked on the launch gate with a pointer to `launch-gate.md`
    - PROF-01, PROF-02, PROF-03 and PROF-05 carry the user-supplied qualifier with a pointer to `HANDOFF-user-supplied.md`
    - `ROADMAP.md` marks Phase 6 complete with a date, a real plan count, and the wave-grouped plan list; the Progress table row matches
    - `STATE.md` records this phase's decisions, moves CR-01 to closed with its mechanism named, and re-asserts the three carried copy tripwires plus the five user-supplied values as blockers
    - `git diff --stat` on both root layouts and on `components/smear-heading/` is empty
    - No `_pm/` directory exists
    - Any issue the user reported is either fixed as a one-line correction or recorded in `deferred-items.md` with an owner
  </acceptance_criteria>
  <done>The three human verdicts are on the record, every requirement carries an accurate status with its qualifier, and the milestone's tracking surfaces state honestly what shipped, what is open, and who owns each open item.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human verdict → the project record | The three checks no automated tier can make |
| tracking surfaces → the milestone's claim about itself | A requirement marked Complete that is not is a false record |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-70 | Repudiation | A requirement marked Complete while its user-supplied value is missing | mitigate | PROF-01, PROF-02, PROF-03 and PROF-05 carry an explicit qualifier pointing at `HANDOFF-user-supplied.md`; FIND-02 is marked blocked on the launch gate. Acceptance asserts every qualifier matches its checkbox line. |
| T-06-71 | Repudiation | The three carried copy tripwires dropping at milestone close | mitigate | Task 2 re-asserts all three in `STATE.md`'s blockers per HOME-01's carry-forward rule, and `tests/unit/launch-gate.test.ts` holds them independently of any document. |
| T-06-72 | Tampering | Scope expanding at milestone close to fix a reported issue | mitigate | Task 1 forbids fixes during the checkpoint; Task 2 fixes only one-line corrections within this phase's files and defers anything larger with an owner. |
| T-06-73 | Repudiation | Flipping robots as part of "closing the milestone" | mitigate | Task 2 forbids editing either root layout and asserts both diffs are empty as its last step; the gate test independently enforces the biconditional. |
| T-06-SC | Tampering | npm/pip/cargo installs | mitigate | This plan installs nothing. `06-RESEARCH.md` § Package Legitimacy Audit records zero packages for this phase. |
</threat_model>

<verification>
1. The checkpoint returned three verdicts with dates.
2. `npm run test:all` — unit, build and Playwright tiers all green.
3. `git diff --stat` on both root layouts and `components/smear-heading/` — empty.
4. `ROADMAP.md`, `REQUIREMENTS.md` and `STATE.md` all agree on what shipped and what is open.
5. `npm run lint` — exactly one error, the re-deferred one.
6. No `_pm/` directory created.
</verification>

<success_criteria>
- The Slack unfurl, the screen-reader pass and the optical sign-off each carry a human verdict and a date.
- Every Phase 6 requirement has an accurate status with an honest qualifier where the mechanism shipped but the value did not.
- CR-01 is recorded closed with its mechanism named.
- The three carried copy tripwires and the five user-supplied values survive into the milestone close as named blockers.
- The site is live, complete and deliberately unindexed; the flip is documented and unperformed.
</success_criteria>

<output>
Create `.planning/phases/06-cv-contact-photo-discoverability/06-12-SUMMARY.md` when done.
Record: the three human verdicts verbatim with their dates; the final requirement statuses with their
qualifiers; the final suite counts across all three tiers; and a one-paragraph statement of what the
user must do to finish the milestone, pointing at `HANDOFF-user-supplied.md` and `launch-gate.md`.
</output>
</content>
