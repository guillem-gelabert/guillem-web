---
phase: 03-work-list-landing-skeleton
plan: 07
type: execute
wave: 3
depends_on: ["03-03"]
files_modified:
  - tests/landing-viewport.spec.ts
  - tests/landing-trail.spec.ts
  - tests/smear-heading.spec.ts
  - tests/reduced-motion.spec.ts
autonomous: true
requirements: [HOME-04, HOME-01]
must_haves:
  truths:
    - "The landing view stays a single column with no horizontal overflow at 375px and at 1440px"
    - "The nameplate genuinely responds to viewport width, asserted against the real clamp() formula rather than an assumed ceiling"
    - "Both Humane headings on / grow a multi-layer text-shadow mid-scroll and settle back to none when scrolling stops"
    - "A visitor arriving with prefers-reduced-motion set sees no trail at all on / — asserted for a visitor who already has the preference, not one who changes it mid-session"
    - "The landing registers exactly two trail headings, not three and not five"
  artifacts:
    - path: "tests/landing-viewport.spec.ts"
      provides: "375/1440 no-overflow, single-column and measured clamp() assertions for /"
      contains: "clampPx"
    - path: "tests/landing-trail.spec.ts"
      provides: "HOME-06 and BUILD-05 regression coverage on the landing's two registered headings"
      contains: "emulateMedia"
  key_links:
    - from: "tests/landing-trail.spec.ts"
      to: "components/smear-heading/smear-heading-provider.tsx"
      via: "computed text-shadow read off both registered headings during a scripted scroll"
      pattern: "textShadow"
    - from: "tests/landing-viewport.spec.ts"
      to: "app/globals.css clamp() curves"
      via: "clampPx() reimplementing the real formula, copied from tests/viewport.spec.ts"
      pattern: "clampPx"
---

<objective>
Write the landing view's two measurement specs — the 375/1440 viewport pass and the trail /
reduced-motion pass — and correct two stale comments in shipped specs that assert `/` has
nothing to scroll.

Purpose: `HOME-04` is a legibility requirement and the only honest gate for it is a real render
at two widths. `HOME-06` and `BUILD-05` are Phase 1 requirements that this phase puts real
content underneath for the first time — `/` went from two elements to a nameplate, a five-item
nav, four section heads, a poster `<h3>`, two work rows and two stubs, and it now registers two
trail headings where it registered one.
Output: two new Playwright specs, two comment-only corrections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-work-list-landing-skeleton/03-VALIDATION.md
@.planning/phases/03-work-list-landing-skeleton/03-UI-SPEC.md
@.planning/phases/03-work-list-landing-skeleton/03-PATTERNS.md
</context>

<interfaces>
<!-- Measured facts these specs must assert. 03-VALIDATION.md assertion rule 1: assert MEASURED. -->

Display role  clamp(3.5rem, 1.5rem + 8vw, 11.25rem)
  at 375px  → 3.5rem floor = 56px
  at 1440px → 1.5rem + 8vw = 24 + 115.2 = 139.2px   ← NOT the 180px ceiling.
              The Display curve does not reach 11.25rem until ~1950px viewport width.
Heading role  clamp(2rem, 1rem + 4vw, 4.5rem)
  at 1440px → 1rem + 4vw = 16 + 57.6 = 73.6 > 72px  ← DOES saturate its 72px ceiling.

tests/viewport.spec.ts:23-53 already defines the helper to copy:
  const ROOT_PX = 16; const TOLERANCE_PX = 4;
  function clampPx(minRem, preferredRem, preferredVw, maxRem, viewportWidth): number
  const VIEWPORTS = [{ width: 375, height: 812 }, { width: 1440, height: 900 }]

tests/fixture-viewport.spec.ts:61-62 — the two values to compare for overflow:
  pageScrollWidth: document.documentElement.scrollWidth
  innerWidth:      window.innerWidth

<!-- The two trail-carrying headings on / (03-UI-SPEC.md Motion Contract):        -->
<!--   h1.text-display  "Guillem Gelabert"     — Display role, Humane, trail       -->
<!--   h3.text-heading  (inside section#case-study) — Heading role, Humane, trail  -->
<!-- Newsreader NEVER trails. Section heads, work titles, nav links, annotations,  -->
<!-- host labels and stub copy carry no trail. Exactly two registered, not three.  -->

<!-- Trail constants, read-only — do NOT retune (03-RESEARCH.md C-3 measured the   -->
<!-- cost at 0.085ms per heading per frame against an 8.33ms budget):              -->
<!--   MAX_TRAIL, MAX_SHADOWS (240-layer clamp), SCROLL_STOP_DELAY, HUE_SPEED      -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/landing-viewport.spec.ts — 375px and 1440px, no overflow, one column, measured clamp values</name>
  <files>tests/landing-viewport.spec.ts</files>
  <read_first>
    - `tests/viewport.spec.ts:1-53` in full — the deviation-note header comment recording why the real `clamp()` output is asserted instead of the plan's "≈180px" assumption, the `ROOT_PX`/`TOLERANCE_PX` constants, the `clampPx()` helper, and the `VIEWPORTS` array. Copy `clampPx` rather than reimplementing it.
    - `tests/fixture-viewport.spec.ts:40-70` — the single-`page.evaluate` "collect everything in one round trip, then assert" shape, and the `pageScrollWidth` / `innerWidth` overflow pair.
    - `tests/fixture-viewport.spec.ts:69-80` — the `for (const viewport of VIEWPORTS) { test.describe(...) }` parameterisation shape.
    - `app/(en)/page.tsx` and `components/landing/work-list.tsx` (Plan 03-03) — the markup under test.
    - `.planning/phases/03-work-list-landing-skeleton/03-VALIDATION.md` § *Manual-only verifications* — the three 375px failures that are optical and are NOT this spec's job.
  </read_first>
  <action>
    Create `tests/landing-viewport.spec.ts`, importing only `{ expect, test }` from
    `@playwright/test`.

    Header comment: covers `HOME-04` — the landing stays legible with only two work items, lists
    and prose, no card grids and no three-across rows — measured at a mobile-width and a
    desktop-width viewport. State the assertion-shape lesson explicitly: values are asserted
    against the real `clamp()` formula via `clampPx()`, not against a ceiling assumed from a
    plan; `tests/viewport.spec.ts` had to be corrected from "≈180px near-ceiling" to the real
    139.2px, and the Display curve does not reach its 180px ceiling until roughly a 1950px
    viewport. Also state what this spec deliberately does NOT cover: whether the five-item
    contents list wraps to a readable two or three rows rather than a ragged column, whether the
    ordinal-above-title stack reads as one row rather than four loose lines, and whether the work
    section reads as hierarchy at 1440px are all optical and are the designated manual checkpoint.

    Copy `ROOT_PX`, `TOLERANCE_PX`, `clampPx` and `VIEWPORTS` from `tests/viewport.spec.ts`
    verbatim. Do not hardcode 139.2 or 56 anywhere — derive them from `clampPx`.

    Parameterise over `VIEWPORTS` with a `test.describe(\`at ${viewport.width}px\`)`. In each
    describe, use a `beforeEach` that sets the viewport size, navigates to `/`, and awaits
    `document.fonts.ready`.

    Write these tests at each width:

    (a) **No horizontal page overflow.** Read `document.documentElement.scrollWidth` and
    `window.innerWidth` in one `page.evaluate` and assert `scrollWidth <= innerWidth`. Comment
    that a poster-scale Humane nameplate at a 375px viewport is the realistic way this breaks.

    (b) **The nameplate tracks the Display curve.** Read the `<h1>`'s computed `font-size` and
    assert it is within `TOLERANCE_PX` of `clampPx(3.5, 1.5, 8, 11.25, viewport.width)`. At 375px
    that is the 56px floor; at 1440px it is 139.2px, not 180px.

    (c) **The featured headline tracks the Heading curve and saturates at 1440px.** Read the
    `h3.text-heading` inside `section#case-study` and assert its computed `font-size` is within
    `TOLERANCE_PX` of `clampPx(2, 1, 4, 4.5, viewport.width)`. Comment that this role DOES reach
    its 72px ceiling by 1440px — the opposite of the Display curve — so the two assertions are
    deliberately not symmetric.

    (d) **The work list is one column.** Read every `#work li`'s `getBoundingClientRect()` in one
    evaluate; assert there are exactly 2, that both have the same `x` (within 1px), and that the
    second's `y` is greater than the first's plus its height. That is a vertical list, not a row
    and not a grid — `PROJECT.md` Out of Scope names card grids and three-across feature rows.

    (e) **Nothing inside `<main>` scrolls internally.** Count elements inside `<main>` whose
    `scrollWidth` exceeds their `clientWidth` and assert the count is 0. This is the
    `fixture-viewport.spec.ts:49-51` idiom adapted: at 375px a long absolute URL or a long
    annotation is the realistic overflow source, and it would be invisible to (a) if the
    container clipped it.

    (f) **The measure holds.** Assert every `.max-w-prose` element inside `<main>` computes
    `max-width: 65ch`. At 375px `65ch` exceeds the viewport, which is correct and is why (a) and
    (e) exist alongside it.

    Do not set `deviceScaleFactor`, do not take a screenshot, and do not assert anything about
    the trail here — that is Task 2's.
  </action>
  <verify>
    <automated>npx playwright test tests/landing-viewport.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test tests/landing-viewport.spec.ts` reports 0 failures and at least 12 passing tests (6 per viewport).
    - The spec defines `clampPx` with the same signature as `tests/viewport.spec.ts:26-31` and calls it for both the Display and Heading assertions.
    - `grep -c '139.2\|180' tests/landing-viewport.spec.ts` returns 0 outside comments — no measured value is hardcoded.
    - Test (a) compares `document.documentElement.scrollWidth` against `window.innerWidth` at both 375 and 1440.
    - Test (d) asserts exactly 2 `#work li`, equal `x` within 1px, and strictly increasing `y`.
    - The spec contains no `screenshot`, no `deviceScaleFactor` and no `test.only`.
  </acceptance_criteria>
  <done>The landing view is gated at both widths for overflow, single-column layout and the real clamp() curves.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/landing-trail.spec.ts — the two registered headings smear and settle</name>
  <files>tests/landing-trail.spec.ts</files>
  <read_first>
    - `tests/smear-heading.spec.ts:41-82` — the poll-then-settle assertion shape AND the layer-count trap: `getComputedStyle` normalises the trail hue to `rgb(r, g, b)`, which carries two commas of its own, so a naive `split(",")` reports 3 for a single layer and any `> 1` assertion passes trivially. Count colour functions with `/rgba?\(/g` instead — one per layer.
    - `tests/smear-heading.spec.ts:15-30` — the "must actually overflow the viewport so a visitor can really scroll" guard, and why an injected spacer was rejected: it proved the algorithm ran, not that the criterion was met.
    - `components/smear-heading/use-smear-heading.ts:30` — registration is deferred until `document.fonts.ready` resolves.
    - `.planning/phases/03-work-list-landing-skeleton/03-UI-SPEC.md` § *Motion Contract* — the trail table: Humane gets the trail, Newsreader never does; `/` registers exactly two.
    - `.planning/phases/03-work-list-landing-skeleton/03-RESEARCH.md` § *C-3* and § *The smear trail at the landing view's heading count* — the measurement that says **do not add a viewport guard**: the shipped 5-heading `/type` holds 120fps, `draw()` at the 240-layer clamp costs 0.40ms at two headings, and saturation needs roughly 95 headings.
  </read_first>
  <action>
    Create `tests/landing-trail.spec.ts`, importing only `{ expect, test }` from `@playwright/test`.

    Header comment: covers `HOME-06` as a Phase 3 regression — Phase 1 shipped the trail against
    a specimen route, and `/` is now the first real page carrying it. State the two lessons this
    file is shaped by: count colour functions rather than commas when reading a `text-shadow`
    layer count, and assert against a page that genuinely overflows the viewport rather than an
    injected spacer.

    Write these tests:

    (a) **`/` genuinely scrolls.** Navigate to `/`, await `document.fonts.ready`, and assert
    `document.documentElement.scrollHeight` exceeds `window.innerHeight`. Comment that this used
    to be false — the Phase 1 holding page was name-only and had nothing to scroll — and that
    without this guard the rest of the file could pass while a visitor saw nothing.

    (b) **Exactly two trail-carrying headings are registered.** Assert `h1.text-display` has count
    1 and `section#case-study h3.text-heading` has count 1, and that no `.text-display` or
    `.text-heading` element exists anywhere else on the page. Comment the rule: Humane gets the
    trail, Newsreader never does — a 240-layer stacked shadow on a 14px uppercase section head
    would be visual noise at reading scale. Comment also that cost is linear in *registered*
    headings rather than visible ones, that capping the landing at two is what keeps it bounded,
    and that **no viewport guard is to be added**: research measured `draw()` at the 240-layer
    clamp costing 0.40ms at two headings against an 8.33ms budget, and the shipped 5-heading
    `/type` holds 120fps.

    (c) **Both headings smear mid-scroll and settle to none.** Write a `readShadows()` helper that
    returns the computed `textShadow` of BOTH headings in one `page.evaluate` — generalise
    `tests/smear-heading.spec.ts`'s `document.querySelector("h1")` to an array of the two
    selectors. Assert both are `none` at rest. Scroll with `window.scrollBy(0, 1200)`. Poll up to
    10 times at 16ms intervals until both values are non-`none`. For each, count layers with
    `(value.match(/rgba?\(/g) ?? []).length` and assert the count is greater than 10 — copy the
    comment explaining why counting commas is wrong. Then wait 1500ms (past `SCROLL_STOP_DELAY`)
    and assert both are back to `none`.

    (d) **Newsreader does not trail.** After the same scroll, assert the computed `textShadow` of
    a `h2.section-head`, a work-list `h3.text-standfirst` and a nav link is `none` at every point.
    This is the assertion that catches a future change registering the wrong headings.

    Touch nothing under `components/smear-heading/`. Do not add a second `requestAnimationFrame`
    driver. Do not retune `MAX_TRAIL`, `MAX_SHADOWS`, `SCROLL_STOP_DELAY` or `HUE_SPEED`. Do not
    add the off-screen `draw()` skip guard — it is `planner may override` in the UI-SPEC and the
    measurement says do not.
  </action>
  <verify>
    <automated>npx playwright test tests/landing-trail.spec.ts && git diff --stat HEAD -- components/smear-heading/ | tee /dev/stderr | test ! -s /dev/stdin</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test tests/landing-trail.spec.ts` reports 0 failures and at least 4 passing tests.
    - Test (a) asserts `scrollHeight > innerHeight` on `/` and the spec injects no spacer element — `grep -ci 'spacer\|3000px' tests/landing-trail.spec.ts` returns 0.
    - The layer count is derived with a `/rgba?\(/g` match, not with `split(",")` — verify by `grep`.
    - Test (c) reads both headings' shadows in ONE `page.evaluate` per sample, not two round trips.
    - Test (d) asserts `textShadow === "none"` on a `.section-head`, a work-list title and a nav link.
    - `git diff --stat HEAD -- components/smear-heading/` is empty.
  </acceptance_criteria>
  <done>Both Humane headings on `/` are proven to smear and settle, and Newsreader is proven not to.</done>
</task>

<task type="auto">
  <name>Task 3: Add the BUILD-05 reduced-motion case to tests/landing-trail.spec.ts and correct the two stale comments in shipped specs</name>
  <files>tests/landing-trail.spec.ts, tests/smear-heading.spec.ts, tests/reduced-motion.spec.ts</files>
  <read_first>
    - `tests/reduced-motion.spec.ts:1-30` in full — the header comment, and specifically the ordering rule: `page.emulateMedia({ reducedMotion: "reduce" })` is called BEFORE `page.goto()`, because the app reads `matchMedia(...).matches` at mount. Emulation applied after navigation tests the `change`-listener path rather than the visitor-arrives-with-the-preference path. Playwright's `reducedMotion` context/test option was tried and found unreliable for `matchMedia` in this environment (1.62.1 / Chromium).
    - `tests/reduced-motion.spec.ts:21-25` — the stale comment claiming `/` is capped at name-only copy and does not overflow the viewport.
    - `tests/smear-heading.spec.ts:5-9` — the same stale claim, in the file header.
    - `03-VALIDATION.md` § *Assertion-style rules* rule 2 — the same ordering rule, restated as load-bearing.
  </read_first>
  <action>
    **First, add the `BUILD-05` case to `tests/landing-trail.spec.ts`.**

    A new test asserting that under `page.emulateMedia({ reducedMotion: "reduce" })` — called
    BEFORE `page.goto("/")`, and this ordering is load-bearing — both `/` headings' computed
    `textShadow` stays `none` across a full scroll. Await `document.fonts.ready`, sample both
    headings at rest, scroll `window.scrollBy(0, 1200)`, poll at 16ms intervals for at least 10
    attempts confirming both stay `none`, scroll again to the bottom, and confirm again. Copy the
    ordering rationale from `tests/reduced-motion.spec.ts:8-16` into a comment on the
    `emulateMedia` line so it is not reordered by a future edit.

    Add a second assertion in the same test, or a sibling test, proving the CSS half of the
    reduced-motion contract: under the same emulation, a nav link's computed `transition-duration`
    is `0s` while its rest `color` is unchanged. The state change is not motion — under
    `prefers-reduced-motion: reduce` a link still changes to accent and still gains its hover
    underline; only the transition is removed. Comment that distinction, because it is the thing
    most likely to be "simplified" away.

    **Then correct the two stale comments. These are comment-only edits — change no assertion,
    no selector, no route, no timing.**

    `tests/smear-heading.spec.ts:5-9` currently says this spec runs against `/type` rather than
    `/` because the holding page is capped at name-only copy by Phase 1 `D-06`, so it does not
    overflow the viewport and a visitor has nothing to scroll there "until Phase 3 gives it
    content." That is now false. Rewrite it to say: `/` now carries real content and does
    overflow — `tests/landing-trail.spec.ts` covers it — and this spec deliberately stays on
    `/type` because that route registers five trail headings and is the project's calibration
    reference for the effect at a higher heading count than any real page.

    `tests/reduced-motion.spec.ts:21-25` carries the same stale claim inline. Rewrite it the same
    way, pointing at `tests/landing-trail.spec.ts`'s reduced-motion case for the landing view and
    keeping this one on `/type` as the 5-heading calibration reference.

    Preserve both files' still-true content in place: the "an earlier version injected a 3000px
    spacer, which proved the algorithm ran rather than that the criterion was met" note in
    `smear-heading.spec.ts`, and the entire `emulateMedia`-before-`goto` rationale in
    `reduced-motion.spec.ts:8-16`. Neither is stale and both are load-bearing.
  </action>
  <verify>
    <automated>npx playwright test tests/landing-trail.spec.ts tests/smear-heading.spec.ts tests/reduced-motion.spec.ts && npx playwright test && node -e "const fs=require('fs'); for (const f of ['tests/smear-heading.spec.ts','tests/reduced-motion.spec.ts']) { const s=fs.readFileSync(f,'utf8'); if(/nothing to scroll|does not overflow the viewport/.test(s)) throw new Error(f+': stale comment still present'); if(!s.includes('landing-trail')) throw new Error(f+': corrected comment must point at tests/landing-trail.spec.ts'); } const t=fs.readFileSync('tests/landing-trail.spec.ts','utf8'); const i=t.indexOf('emulateMedia'); const g=t.indexOf('goto(\"/\")', i); if(i<0||g<0||g<i) throw new Error('emulateMedia must precede goto'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test tests/landing-trail.spec.ts` reports 0 failures and at least 6 passing tests.
    - In the reduced-motion test, `page.emulateMedia({ reducedMotion: "reduce" })` appears strictly before `page.goto("/")` in source order, with a comment recording why.
    - The reduced-motion test asserts both headings stay `textShadow: "none"` across two separate scrolls, sampled at least 10 times.
    - A nav link's computed `transition-duration` is asserted to be `0s` under reduced-motion emulation, with a comment stating that the colour and underline state change is preserved.
    - `grep -c 'nothing to scroll' tests/smear-heading.spec.ts tests/reduced-motion.spec.ts` returns 0 for both.
    - `git diff tests/smear-heading.spec.ts tests/reduced-motion.spec.ts` shows comment lines only — no changed `test(`, `expect(`, `page.goto(`, `waitForTimeout(` or selector.
    - The full `npx playwright test` suite is green.
  </acceptance_criteria>
  <done>`BUILD-05` is proven on the landing view for a visitor who arrives with the preference set, and no shipped comment still claims `/` has nothing to scroll.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → dev server | Both specs drive `npm run dev` on localhost:3000 through Chromium. No production surface, no secret, no network egress beyond localhost. |
| OS accessibility preference → application behaviour | `prefers-reduced-motion` is a user preference the site must honour from the first frame, not after a `change` event. Emulation ordering is what makes the assertion about the real visitor rather than about a code path they never take. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-25 | Denial of service (accessibility, vestibular) | The scroll-driven trail on `/` | mitigate | A visitor with `prefers-reduced-motion: reduce` must see no trail at all. Task 3 asserts both headings stay `textShadow: "none"` across a full scroll under emulation applied BEFORE navigation, so the assertion covers a visitor who already has the OS preference set rather than the `change`-listener path. |
| T-03-26 | Denial of service (performance) | `frame()` iterating the trail registry every animation frame | accept | `frame()` calls `draw()` for every registered heading regardless of viewport position, so cost is linear in registered headings. **Measured**: 0.40ms at two headings against an 8.33ms budget at 120Hz; the shipped 5-heading `/type` holds 120fps; saturation needs roughly 95 headings. Accepted without mitigation, and Task 2 asserts the count stays at two so the assumption stays true. The off-screen `draw()` skip guard is explicitly NOT added. |
| T-03-27 | Tampering | `components/smear-heading/` | mitigate | This plan measures the trail and must not modify it. Task 2's verify asserts `git diff --stat HEAD -- components/smear-heading/` is empty — no second rAF driver, no retuned constant, no added guard. |
| T-03-28 | Repudiation (test integrity) | The layer-count assertion | mitigate | `getComputedStyle` normalises the hue to `rgb(r, g, b)`, which carries two commas, so `split(",")` reports 3 for a single layer and any `> 1` assertion passes trivially — a test that looks green while proving nothing. Colour functions are counted with `/rgba?\(/g` instead, and the acceptance criteria `grep` for the wrong form. |
| T-03-29 | Repudiation (test integrity) | Stale comments in shipped specs | mitigate | `tests/smear-heading.spec.ts:5-9` and `tests/reduced-motion.spec.ts:21-25` assert in prose that `/` has nothing to scroll. Left uncorrected, a future reader concludes the landing has no trail coverage and either duplicates it or deletes it. Task 3 corrects both and the verify greps for the stale phrasing. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | Phase 3 installs zero packages; `03-RESEARCH.md` § Package Legitimacy Audit records none to vet, so no `[ASSUMED]`/`[SUS]` entry and no blocking legitimacy checkpoint applies. |

**ASVS L1 chapters explicitly not applicable**: V2 Authentication, V3 Session Management, V4
Access Control, V5 Input Validation, V6 Cryptography, V12 File & Resource, V13 API, V14
Configuration. This plan adds two test files and edits two comments. It ships no application
code, no route and no dependency. The applicable risk here is accessibility (WCAG 2.3.3 /
`prefers-reduced-motion`), covered by T-03-25.
</threat_model>

<verification>
- `npx playwright test tests/landing-viewport.spec.ts tests/landing-trail.spec.ts` reports 0 failures.
- `npx playwright test` — the whole suite green.
- `npx tsc --noEmit` exits 0.
- `npm run lint` shows no NEW errors beyond the one known deferred finding in `components/smear-heading/use-prefers-reduced-motion.ts:23`.
- `git diff --stat HEAD -- components/smear-heading/` is empty.
- `git diff tests/smear-heading.spec.ts tests/reduced-motion.spec.ts` touches comment lines only.
- No `test.only`, no `test.skip`, no watch-mode flag anywhere.
</verification>

<success_criteria>
- `/` has no horizontal overflow and stays a single column at 375px and 1440px, with the nameplate and featured headline asserted against the real `clamp()` curves through `clampPx()`.
- `/` registers exactly two trail-carrying headings; both smear mid-scroll and settle to `none`; no Newsreader element ever does.
- Under `prefers-reduced-motion: reduce` applied before navigation, neither heading ever leaves `none`, while a link's colour state change is preserved and only its transition duration goes to `0s`.
- Neither shipped spec still claims `/` has nothing to scroll, and both now point at `tests/landing-trail.spec.ts`.
</success_criteria>

<output>
Create `.planning/phases/03-work-list-landing-skeleton/03-07-SUMMARY.md` when done.
</output>
