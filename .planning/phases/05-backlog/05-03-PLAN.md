---
phase: 05-backlog
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - tests/landing.spec.ts
autonomous: true
requirements: [BACK-01, BACK-02]

must_haves:
  truths:
    - "The two assertions that existed to notice the backlog stub ending have been updated to prove the new true state, not deleted and not loosened."
    - "A real render proves the backlog is three rows of name-plus-description with no ordinal, no host line and no link."
    - "A real render proves the row separator is the 1px rgba(0, 0, 0, 0.12) hairline on rows 2 and 3 and absent on row 1 — measured, not counted."
    - "A real render proves the date line sits above the first item, geometrically."
    - "A real render proves no third font weight and no fifth type size entered the section — the check the globals.css source budget structurally cannot make."
  artifacts:
    - path: "tests/landing.spec.ts"
      provides: "The dev-tier proof of BACK-01's structure and BACK-02's placement, and the two updated regression assertions"
      contains: "section#backlog ul[role='list']"
  key_links:
    - from: "tests/landing.spec.ts"
      to: "section#backlog"
      via: "getComputedStyle on a real render, never derived arithmetic"
      pattern: "getComputedStyle"
    - from: "tests/landing.spec.ts"
      to: "the date line's placement"
      via: "getBoundingClientRect().y ordering against the first <li>"
      pattern: "getBoundingClientRect"
---

<objective>
Own every Playwright assertion the backlog section breaks, and add the rendered proof that BACK-01
and BACK-02 are actually true on screen.

Research located both breakages by line and Phase 4 called its equivalent step the most
under-estimated item in that phase. Neither is a regression: each asserts an interim state Plan 01
ended. **Update them to prove the new true state — do not delete them and do not loosen them into
vacuity.** Phase 3's own review found gates that passed while proving nothing.

Two of the new assertions exist because a source-level gate structurally cannot make them.
`tests/unit/prose-contract.test.ts` reads `app/globals.css` from disk, and Tailwind v4's preflight
is not in that file — so a `<strong>` in a backlog description would render at **700**, a third
weight on screen, with every budget test green. Only a computed-value assertion catches it. The same
is true of the row separator: a bare `border-t` without `border-rule` falls through to
`currentColor` and renders full ink, and `toHaveCount()` cannot see it.

Purpose: BACK-01's structure and D-11's three subtractions; BACK-02's placement.
Output: one test file, edited — two assertions updated, three tests added.

This plan runs against `npm run dev`. **Production truth is Plan 04's.** Do not put a copy
assertion or a `<time dateTime>` string match here — Playwright cannot prove what a production build
emits, and the split is deliberate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-backlog/05-VALIDATION.md
@.planning/phases/05-backlog/05-01-SUMMARY.md
@components/landing/backlog-list.tsx
@app/(en)/page.tsx

<interfaces>
<!-- Located by research and re-confirmed by grep this session. Line numbers are exact as of the
     pre-Plan-01 tree; locate by test name if an edit has shifted them. -->

THE TWO BREAKAGES

tests/landing.spec.ts:373  test("(r) the heading outline is h1=1, h2=4, h3=3, h4/h5/h6=0, …")
                     :389  expect(counts).toEqual({ h1: 1, h2: 4, h3: 3, h4: 0, h5: 0, h6: 0 });
  Item names render as <h3>, taking the document from three to six. BOTH the count at :389 AND the
  h3=3 in the test NAME at :373 must change to 6. The comment at :382-386 stays — its point (that
  the featured <h3> renders larger than its <h2> deliberately) still holds.

tests/landing.spec.ts:444  test("(u) both stubs render one standfirst and one body line, standfirst
                                 at weight 530")
                     :446  for (const id of ["backlog", "contact"]) {
  Asserts p.text-standfirst count 1 and p.text-body count 1 per section. #backlog now has three
  <h3 class="text-standfirst"> names (not <p>) and three .text-body descriptions. Narrow the loop
  to ["contact"] and retitle.

THE SHIPPED MEASURED IDIOM TO MIRROR

tests/landing.spec.ts:277-305  test("(n) HOME-04: the second row's separator is the hairline, not a
                                    fourth rule weight")
  reads getComputedStyle on `section#work ol[role='list'] > li` .nth(1) and asserts:
    borderTopWidth "1px" / borderTopStyle "solid" / borderTopColor "rgba(0, 0, 0, 0.12)"
    borderRight/Bottom/Left width all "0px"
  and :250-275 asserts the same six-plus-three on the list and the FIRST item with borderTopWidth
  "0px". Copy this shape exactly for #backlog.

THE SHIPPED TYPE ROLES (app/globals.css) — the complete set reachable inside section#backlog
  .section-head     14px / 400   (the <h2>)
  .text-label       14px / 400   (the date line)
  .text-standfirst  18px / 530   (item names)
  .text-body        18px / 400   (descriptions)
  → every computed font-size in the section must be in {14px, 18px}; every computed font-weight in
    {400, 530}. Predicted by research; MEASURE first and assert the measured set.

Tailwind: gap-xl 32px, gap-lg 24px, gap-sm 8px, max-w-prose → max-width: 65ch (resolves to a px
value in getComputedStyle — read it, do not assume the number).

STANDING GATES IN THIS FILE THAT MUST STAY GREEN, UNEDITED
  :82-85    exactly 4 section[id], ids ["case-study","work","backlog","contact"]
  :194      ZERO a[href*="github.com"] page-wide
  :344-371  (q) four h2.section-head, text ["Case study","Work","Backlog","Contact"], each with a
            1px solid rgb(0, 0, 0) bottom rule
  :403-412  (s) no todo/placeholder/coming soon/under construction/lorem/tbd in body.innerText —
            this is what binds the D-14 tripwire to source-only marking
  :416-442  (t) zero main button / img / main svg; every main section & main div computes
            border-radius 0px and box-shadow none — the new date/list <div> is now inside this set

The file's tests are lettered (a)…(u). New tests continue at (v).
The file's beforeEach already does `page.goto("/")` then `document.fonts.ready`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update the two breaking assertions to the new true state</name>
  <files>tests/landing.spec.ts</files>
  <read_first>
    - tests/landing.spec.ts:1-30 — the three assertion-shape lessons this file is built around,
      especially (i) computed values from a real render and (ii) why copy that depends on draft
      visibility is not asserted here
    - tests/landing.spec.ts:373-402 — test (r) in full, including the comment at :382-386
    - tests/landing.spec.ts:444-460 — test (u) in full, including its closing comment about where
      production truth for the stub copy lives
    - components/landing/backlog-list.tsx — confirm the element actually chosen for item names
    - .planning/phases/05-backlog/05-RESEARCH.md § "Pitfall 3 — the <h3> decision silently breaks
      the heading-outline test"
  </read_first>
  <action>
    **Test (r), the heading outline.** Change `h3: 3` to `h3: 6` at `:389` **and** change `h3=3` to
    `h3=6` in the test name at `:373` — the name states the assertion, and a name that disagrees
    with its body is how the next reader is misled. Keep the comment at `:382-386` unchanged; its
    point still holds. Add one sentence to that comment recording that the three new `<h3>` are the
    backlog item names, that `<h3>` was chosen deliberately over `<p>` because D-11 names exactly
    three subtractions and element type is not one of them, and that downgrading them would cost
    screen-reader users a navigable outline in a section that is otherwise pure prose.

    Before editing, run the test and read the actual failing count. If it is not 6, the component
    does not render what Plan 01's SUMMARY says it does — investigate the component, do not fit the
    number to the failure.

    **Test (u), the stub shape.** Narrow the loop from `["backlog", "contact"]` to `["contact"]` and
    retitle to name only the contact stub. Keep every assertion inside it intact — the count of 1,
    the count of 1, and the computed weight of 530. Add a dated comment recording that the backlog
    half was removed on 2026-08-31 because Phase 5 filled that stub with real items, that
    `SectionStub` still serves `#contact` until Phase 6, and that the backlog's own structure is now
    asserted by the tests added below. Narrowing an assertion when the state it described ends is
    the gate mechanism working — but only if the replacement exists, which is Tasks 2 and 3.

    Do not touch any other test in this file in this task.
  </action>
  <verify>
    <automated>npx playwright test tests/landing.spec.ts --reporter=line -g "heading outline" &amp;&amp; npx playwright test tests/landing.spec.ts --reporter=line -g "contact stub"</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test tests/landing.spec.ts --reporter=line` passes every test in the file.
    - `/usr/bin/grep -c "h3: 3" tests/landing.spec.ts` returns 0 and `/usr/bin/grep -c "h3: 6" tests/landing.spec.ts` returns 1.
    - `/usr/bin/grep -c "h3=3" tests/landing.spec.ts` returns 0 — the test name was updated too.
    - `/usr/bin/grep -c '\["backlog", "contact"\]' tests/landing.spec.ts` returns 0.
    - Test (u) still asserts `toHaveCount(1)` twice and `fontWeight` `"530"` — no assertion inside it was removed or weakened.
    - The measured `h3` count read from the failing run before the edit was 6. If it was not, the SUMMARY records what it was and what the component actually renders.
    - Both edited tests carry a dated comment naming what changed and why.
  </acceptance_criteria>
  <done>Both located breakages are owned and updated to assert the post-Phase-5 truth, with the reason recorded in the file where the next reader will find it.</done>
</task>

<task type="auto">
  <name>Task 2: (v) BACK-01's structure and D-11's three subtractions, (w) BACK-02's placement</name>
  <files>tests/landing.spec.ts</files>
  <read_first>
    - tests/landing.spec.ts:150-250 — the work-list structural tests, for the locator style and the
      counting idiom this file uses
    - components/landing/backlog-list.tsx and app/(en)/page.tsx's backlog section — the exact
      markup being asserted
    - .planning/phases/05-backlog/05-VALIDATION.md § "Phase Requirements → Test Map" — the six
      BACK-01 rows and the two BACK-02 rows assigned to the e2e tier
    - .planning/phases/05-backlog/05-CONTEXT.md § D-11 and § D-12
  </read_first>
  <action>
    Add two tests at the end of the file, continuing the letter sequence at `(v)` and `(w)`.

    **`(v)` BACK-01: the backlog is three rows of name and description, with the work list's
    affordances subtracted.**

    - `section#backlog ul[role='list']` has count 1, and `section#backlog ol` has count 0 — a `<ul>`
      not an `<ol>`, because the backlog is unranked (D-11.1).
    - `section#backlog ul[role='list'] > li` has count 3.
    - Each `<li>` contains exactly one `h3.text-standfirst` and exactly one `p.max-w-prose.text-body`,
      and exactly two element children in that order.
    - **D-11.1, no ordinals:** `section#backlog ul p.text-label` has count 0, and no text in the list
      matches `/^\s*0\d\s*$/`.
    - **D-11.2, no host line:** the same `p.text-label` count of 0 covers it; additionally assert no
      text node inside the `<ul>` matches a bare hostname pattern (`/\.[a-z]{2,}\b/` on a whole
      standalone line).
    - **D-11.3, the name is not a link:** `section#backlog a` has count 0. Not "the `h3` has no `a`"
      — zero anchors in the whole section, because v1 ships no inline `.link` either and a stray one
      would be a decision, not a detail.
    - Assert the three item names read from the page match, in order, the three names shipped in
      `lib/backlog.tsx`. State them as a literal array in the test with a comment pointing at the
      module; do not import the `.tsx` module into Playwright.

    **`(w)` BACK-02: the section date is one Label line carrying a `<time>`, above the first item.**

    - `section#backlog p.text-label` has count 1, and it is **not** a descendant of the `<ul>`.
    - It contains exactly one `<time>`, whose `datetime` attribute (read with
      `getAttribute("datetime")` — browsers ASCII-lowercase attribute names during parsing, so this
      works even though React emits `dateTime` in the raw file) matches `/^\d{4}-\d{2}-\d{2}$/`.
    - Its text content starts with `Last touched ` and its rendered `<time>` text is a non-empty
      absolute date containing a four-digit year and a month name — not a relative string. Assert
      it does **not** match `/\bago\b|\byesterday\b|\btoday\b/i`.
    - **Placement, geometrically:** `getBoundingClientRect().y` of the date line is strictly less
      than `getBoundingClientRect().y` of the first `<li>`. Comment why this is asserted by geometry
      rather than by DOM order: BACK-02 is the mitigation the user accepted in place of per-item
      dates, and a freshness signal read after the list has mitigated nothing — so "above" is the
      requirement, and only the rendered box proves it.
    - `text-transform` on the date line computes `uppercase` (the Label role), read from
      `getComputedStyle`.

    Do **not** assert the date's literal value against `lib/backlog.tsx` here. That equality binding
    is production truth and belongs to Plan 04.
  </action>
  <verify>
    <automated>npx playwright test tests/landing.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test tests/landing.spec.ts --reporter=line` passes every test in the file.
    - Tests `(v)` and `(w)` exist and are the last two in the file.
    - `(v)` asserts: `ul[role='list']` count 1, `ol` count 0, `li` count 3, one `h3.text-standfirst` and one `p.max-w-prose.text-body` per row, `p.text-label` count 0 inside the `ul`, and `section#backlog a` count 0.
    - `(v)` asserts the three item names in order and its comment points at `lib/backlog.tsx` as the source.
    - `(w)` asserts `p.text-label` count 1, that it is outside the `ul`, one `<time>` with an ISO `datetime` attribute, text beginning `Last touched `, no relative-date word, computed `text-transform: uppercase`, and the `y` ordering against the first `<li>`.
    - `/usr/bin/grep -c "getBoundingClientRect" tests/landing.spec.ts` increased — the placement assertion is geometric, not DOM-order.
    - Neither new test imports from `lib/backlog.tsx`, and neither asserts a literal date value.
    - No test outside `(v)` and `(w)` changed in this task.
  </acceptance_criteria>
  <done>A real render proves the backlog is three name-and-description rows with all three D-11 affordances absent, and that the freshness date is read before the list rather than discovered after it.</done>
</task>

<task type="auto">
  <name>Task 3: (x) the measured separator, the measured geometry, and the type budget on screen</name>
  <files>tests/landing.spec.ts</files>
  <read_first>
    - tests/landing.spec.ts:250-305 — tests (m) and (n) in full: the exact computed-style shape to
      copy, and the comment at :294-298 explaining why a `toHaveCount()` assertion cannot see this
    - .planning/phases/05-backlog/05-RESEARCH.md § "Pitfall 1" and § "Pitfall 7" — the two defects
      only a computed-value assertion can catch
    - app/globals.css:305-370 — the four type roles and `.section-head`, with their exact px sizes
      and weights
    - .planning/phases/05-backlog/05-VALIDATION.md § "Three rules that carry, restated", rule 1
  </read_first>
  <action>
    Add one test at the end of the file, `(x)`.

    **Measure before you assert.** Run a throwaway `page.evaluate` that dumps every computed
    `fontSize`, `fontWeight`, `borderTopWidth`, `borderTopColor`, `rowGap` and `maxWidth` inside
    `section#backlog`, and read the real values. Phase 1 learned this the expensive way: the Display
    clamp reads 139.2px at 1440px, not the plan's assumed ≈180px. Assert what you measured; use the
    predictions below as the expectation to check, not as the value to write blind.

    **The separator (Pitfall 7 / WR-06 recurring).** Mirroring `(n)` at `:277-305` exactly:
    - The first `<li>` computes `borderTopWidth` `"0px"`.
    - The second and third `<li>` each compute `borderTopWidth` `"1px"`, `borderTopStyle` `"solid"`,
      `borderTopColor` `"rgba(0, 0, 0, 0.12)"`, and `borderRightWidth` / `borderBottomWidth` /
      `borderLeftWidth` all `"0px"`.
    - Carry `(n)`'s comment across: Tailwind v4's preflight emits `border: 0 solid` with no colour,
      so `border-t` without `border-rule` falls through to `currentColor` — full-ink black, 8× darker
      than `--color-rule`, and a fourth rule weight the Prose Contract forbids. `toHaveCount()`
      cannot see this; only the computed colour can.
    - Also assert the `<ul>` itself computes all four border widths `"0px"`, `boxShadow` `"none"` and
      `borderRadius` `"0px"` — the not-a-card property, mirroring `(m)` at `:250-275`.

    **The geometry (D-10, D-12).**
    - The `<ul>` computes `rowGap` `"32px"` (`gap-xl`).
    - Each `<li>` computes `rowGap` `"8px"` (`gap-sm`).
    - The date/list `<div>` computes `rowGap` `"24px"` (`gap-lg`), and the `<section>` computes
      `rowGap` `"24px"` — head→date and date→list are both `lg`, per D-12, with no new token.
    - Each description computes a `maxWidth` equal to the measured resolution of `65ch`. Read the px
      value from the render and assert equality against the same value read from a work-list
      `p.max-w-prose` in the same run — the point is that the backlog uses the shipped measure, not
      a new one, and comparing two rendered values proves that without hardcoding a font-dependent
      number.

    **The type budget on screen (Pitfall 1) — the assertion no source gate can make.**
    - Collect the computed `fontWeight` of every element inside `section#backlog` that has a
      non-empty direct text node, and assert every value is in `{"400", "530"}`.
    - Collect the computed `fontSize` of the same set and assert every value is in `{"14px", "18px"}`.
    - Comment why this test exists at this tier: `tests/unit/prose-contract.test.ts` reads
      `app/globals.css` from disk and Tailwind v4's preflight ships `b,strong{font-weight:bolder}`
      in the compiled CSS, not in that file. A `<strong>` in a backlog description therefore renders
      at **700** — a third weight on screen — with the entire source budget green. This is the only
      place that catches it. If a future description genuinely needs `<strong>`, the fix is one
      budget-legal line (`.text-body strong { font-weight: 530 }`) — not weakening this assertion.

    Then run the whole Playwright suite, not just this file, and the unit tier: the new markup is on
    the landing page, which several other specs also load.
  </action>
  <verify>
    <automated>npx playwright test --reporter=line &amp;&amp; npm run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - `npx playwright test --reporter=line` passes the whole suite, not just `tests/landing.spec.ts`.
    - `npm run test:unit` exits 0 and `npx tsc --noEmit` is clean.
    - `(x)` asserts `borderTopWidth` `"0px"` on row 1 and `"1px"` / `"solid"` / `"rgba(0, 0, 0, 0.12)"` plus three `"0px"` sides on rows 2 and 3.
    - `(x)` asserts `rowGap` `"32px"` on the `<ul>`, `"8px"` on each `<li>`, and `"24px"` on both the date/list `<div>` and the `<section>`.
    - `(x)` asserts the description `maxWidth` by comparing against a work-list `p.max-w-prose` measured in the same run, with no hardcoded px number.
    - `(x)` asserts every computed `font-weight` in `section#backlog` is in `{"400","530"}` and every computed `font-size` is in `{"14px","18px"}`, and carries the comment explaining why the `globals.css` source budget cannot make this check.
    - The SUMMARY records the measured dump: the actual set of font sizes, weights and gaps found in `section#backlog`, and whether they matched the prediction.
    - The SUMMARY records the Playwright spec count against the 124-spec pre-phase baseline.
    - `npm run lint` reports exactly one error, at `use-prefers-reduced-motion.ts:23`.
  </acceptance_criteria>
  <done>The rendered section is measured rather than assumed: the hairline is the hairline, the spacing is on the shipped grid, the measure is the shipped measure, and no third weight or fifth size reached the screen behind a green source budget.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source CSS budget → rendered CSS | The one real boundary in this plan. `globals.css` is not the whole stylesheet: Tailwind v4 preflight is compiled in and is invisible to every source-reading gate in `tests/unit/`. |
| dev render → production render | Playwright runs against `npm run dev`. It cannot observe what a production build omits, so no copy or draft-visibility assertion belongs here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Tampering | Weakening a breaking assertion to make the suite green | mitigate | Task 1 states the rule: an assertion whose interim state ended is updated to prove the new state and carries a dated comment saying so. Test (u) must keep all three of its inner assertions; the acceptance criteria grep for them. |
| T-05-12 | Spoofing | A green source budget hiding a third font weight on screen | mitigate | Test `(x)` reads computed `font-weight` and `font-size` from a real render. This is the only tier that can see preflight's `b,strong{font-weight:bolder}`. |
| T-05-13 | Spoofing | A green count hiding a full-ink separator | mitigate | Test `(x)` asserts `borderTopColor` `"rgba(0, 0, 0, 0.12)"`, mirroring the shipped `(n)` that already caught this exact defect once. |
| T-05-14 | Information Disclosure | A link into a private repository entering the new copy | mitigate | Existing `:194` (zero `a[href*="github.com"]` page-wide) stays green, and `(v)` additionally asserts zero `<a>` anywhere inside `section#backlog`. |
| T-05-15 | Repudiation | A visible draft marker reaching a live URL | mitigate | Existing `(s)` at `:403-412` bans `todo`/`placeholder`/`coming soon`/`under construction`/`lorem`/`tbd` from `body.innerText`. It stays unedited and now covers the new item copy — which is the mechanism that keeps the D-14 tripwire source-only. |
| T-05-SC | Tampering | npm / pip / cargo installs | accept | No package is installed. This plan edits one existing test file. |

ASVS V2, V3, V4 and V6 are **inapplicable** — no auth, session, access control or cryptography
exists anywhere on this static site. V14 (Configuration) is untouched: this plan changes no
metadata and no `robots:` setting.
</threat_model>

<verification>
- `npx playwright test --reporter=line` passes the entire suite.
- `npm run test:unit` exits 0; `npx tsc --noEmit` clean; `npm run lint` at exactly the one known error.
- `/usr/bin/grep -c "h3: 6" tests/landing.spec.ts` returns 1 and `/usr/bin/grep -c "h3: 3" tests/landing.spec.ts` returns 0.
- Tests `(v)`, `(w)` and `(x)` exist; `(u)` covers only `["contact"]` with all its inner assertions intact.
- `git diff --stat` touches exactly one file: `tests/landing.spec.ts`.
</verification>

<success_criteria>
- Both located Playwright breakages are updated to prove the new true state, with a dated reason in the file.
- BACK-01 is proven on a real render: three rows, name plus description, zero ordinals, zero host lines, zero links.
- BACK-02 is proven on a real render: one Label-role date line with a `<time>`, geometrically above the first item, absolute rather than relative.
- The separator hairline, the three gaps and the prose measure are all asserted from measured computed values.
- The two-weight, four-size budget is asserted on screen, where the source budget cannot reach.
</success_criteria>

<output>
Create `.planning/phases/05-backlog/05-03-SUMMARY.md` when done. Record: the measured computed-value
dump for `section#backlog` and whether it matched research's prediction of `{14px, 18px}` /
`{400, 530}`; the measured `h3` count before the edit; the resolved px value of `65ch` on this
machine; and the Playwright spec count against the 124-spec baseline.
</output>
