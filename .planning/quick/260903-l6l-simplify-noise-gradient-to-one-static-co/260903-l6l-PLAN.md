---
quick_id: 260903-l6l
description: Simplify /noise-gradient to one static conic gradient, one SVG noise texture, and pink colorization with no controls or masks.
date: 2026-09-03
---

# Quick Task Plan

Collapse the experimental multi-element blend stack into a single square with
three CSS background layers: solid pink colorization, one embedded SVG
`feTurbulence` texture, and one continuous grayscale conic gradient. Remove all
React state, controls, masks, duplicated noise fields, and guard elements.

Use a symmetric gray → black → gray → white → gray conic so the bottom-center
returns naturally to pink while the right and left move toward black and white
without a discontinuity. Update the focused browser test to protect the simple
single-element implementation.
