---
quick_id: 260903-gn5
description: Update the main page to use the style playground design and responsive seam logic
date: 2026-09-03
---

# Quick Task Plan

## Task 1: Share the playground seam geometry

**Files:**
- `components/seam/use-seam-alignment.ts`
- `app/(en)/style-playground/seam-playground.tsx`

**Action:** Extract the responsive conic-seam alignment calculation into a small client hook and keep the playground using it unchanged.

**Verify:** Lint the hook and playground component and complete a production build.

**Done:** The seam angle and portrait pivot behavior have one implementation shared by the playground and landing page.

## Task 2: Apply the playground composition to the landing page

**Files:**
- `components/landing/landing-seam.tsx`
- `components/landing/landing-seam.module.css`
- `app/(en)/page.tsx`
- `app/globals.css`

**Action:** Place the existing nameplate and featured case-study content inside transparent top-left and bottom-right frames over the same black-to-white conic gradient as the playground. Preserve the page as a Server Component by passing its rendered content into a focused client layout boundary. Stack the frames at square and portrait aspect ratios, retain minimum dimensions, inverse-blend the frame content, and remove the superseded static landing gradient.

**Verify:** Lint all changed TypeScript files, request `/` and `/style-playground` from the existing development server, run `git diff --check`, and complete a production build.

**Done:** `/` uses the playground's visual system and exact responsive seam logic without losing its server-rendered metadata or content.
