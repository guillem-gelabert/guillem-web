---
phase: 01-deploy-foundation-design-system
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - app/layout.tsx
  - app/page.tsx
  - app/type/page.tsx
  - tests/font-cls.spec.ts
  - tests/viewport.spec.ts
  - tests/type-specimen.spec.ts
autonomous: true
requirements: [HOME-05, BUILD-03, BUILD-06]

must_haves:
  truths:
    - "Visitor sees a deliberate, authored typographic system (type scale, self-hosted fonts) rather than framework-default styling, correctly on both a mobile-width and a desktop-width viewport"
    - "Visitor loading any page sees no layout shift as fonts load"
  artifacts:
    - path: "app/layout.tsx"
      provides: "font variable wiring + robots: { index: false } metadata"
      contains: "robots"
    - path: "app/page.tsx"
      provides: "the holding page (D-05/D-06): Display-scale h1 + one Body paragraph"
    - path: "app/type/page.tsx"
      provides: "the /type specimen route demonstrating all four type roles (D-05)"
    - path: "tests/font-cls.spec.ts"
      provides: "automated CLS assertion across the font-load window"
    - path: "tests/viewport.spec.ts"
      provides: "automated clamp()-bounds assertion at 375px and 1440px"
    - path: "tests/type-specimen.spec.ts"
      provides: "automated presence/visibility assertion for all four type roles"
  key_links:
    - from: "app/page.tsx <h1>"
      to: "app/globals.css .text-display"
      via: "className"
      pattern: "text-display"
    - from: "app/layout.tsx <html>"
      to: "app/fonts/humane.ts and app/fonts/newsreader.ts"
      via: "humane.variable + newsreader.variable className"
      pattern: "humane.variable"
---

<objective>
Wire the root layout's font variables and `robots: { index: false }` metadata, then build the two
routes this phase ships: `/` (a name-only holding page) and `/type` (a non-indexed type specimen
that is the checkable proof of "a deliberate, authored typographic system").

Purpose: This is where HOME-05, BUILD-03, and BUILD-06 become observable, testable behaviors —
Plan 02 built the mechanism (fonts, tokens); this plan renders it on real routes and proves it
with Playwright specs (CLS ≈ 0, correct `clamp()` bounds at mobile/desktop, all four roles present).
Output: `app/layout.tsx`, `app/page.tsx`, `app/type/page.tsx`, and three new Playwright specs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-deploy-foundation-design-system/01-CONTEXT.md
@.planning/phases/01-deploy-foundation-design-system/01-RESEARCH.md
@.planning/phases/01-deploy-foundation-design-system/01-UI-SPEC.md
@.planning/phases/01-deploy-foundation-design-system/01-VALIDATION.md
@.planning/phases/01-deploy-foundation-design-system/01-02-SUMMARY.md
@BRIEF.md
</context>

<interfaces>
<!-- Consumed from Plan 02 (already built): -->

From `app/fonts/humane.ts`: `export const humane` — a `next/font/local` object exposing `.variable` (`'--font-humane'`) and `.className`.
From `app/fonts/newsreader.ts`: `export const newsreader` — a `next/font/google` object exposing `.variable` (`'--font-newsreader'`) and `.className`.
From `app/globals.css`: the four type-scale classes `.text-display`, `.text-heading`, `.text-body`, `.text-label` (exact rules per Plan 02's Interfaces contract) — use these class names directly, do not invent new ones.

<!-- This plan CREATES the route markup Plan 04 will wire the heading trail onto: -->

`app/page.tsx` renders exactly one Humane-set element: `<h1 className="text-display">Guillem Gelabert</h1>`. Plan 04 attaches its ref to this exact element — do not wrap it in an extra intermediate element that would change what `getBoundingClientRect()` measures.

`app/type/page.tsx` renders one `.text-display` sample and one `.text-heading` sample as top-level heading elements (not nested inside a wrapper that adds its own padding/transform) — Plan 04 attaches its hook to both.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire the root layout — fonts, robots noindex, globals import</name>
  <files>app/layout.tsx, tests/font-cls.spec.ts</files>
  <read_first>app/layout.tsx (current create-next-app scaffold, to know exactly what is being replaced), app/fonts/humane.ts, app/fonts/newsreader.ts (Plan 02 output — the exact exports to import), .planning/phases/01-deploy-foundation-design-system/01-CONTEXT.md (D-06, D-07), .planning/phases/01-deploy-foundation-design-system/01-RESEARCH.md (Code Examples "robots metadata, root layout", Pitfall 7)</read_first>
  <action>
    Replace `app/layout.tsx`: import `humane` from `./fonts/humane` and `newsreader` from `./fonts/newsreader`. Apply `className={\`${humane.variable} ${newsreader.variable}\`}` on the `<html lang="en">` element so both CSS custom properties are available document-wide; import `./globals.css` at the top of the file.

    Export `export const metadata: Metadata = { title: 'Guillem Gelabert', description: 'Developer.', robots: { index: false } }` (D-07 — the site must not be crawlable through Phases 1-5). This is the ONLY place `robots` is set anywhere in this phase — per Pitfall 7, Next.js's metadata merge does NOT deep-merge the `robots` field across route segments, so `app/type/page.tsx` (Task 2) must export no `metadata` object of its own at all, letting it inherit root's `noindex` correctly.

    Create `tests/font-cls.spec.ts`: navigate to `/`, inject a `PerformanceObserver` for `layout-shift` entries before navigation (via `page.addInitScript`), wait for the page to settle (`document.fonts.ready` plus one `requestAnimationFrame`), sum every entry's `value` where `hadRecentInput` is false, and assert the cumulative total is below `0.1` — a clearly-above-zero total on this two-element page would indicate a real font-load shift, not measurement noise.
  </action>
  <verify>
    <automated>npx playwright test tests/font-cls.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - `app/layout.tsx` imports both `humane` and `newsreader` and applies `humane.variable`/`newsreader.variable` on `<html>`
    - `app/layout.tsx` exports `metadata` containing `robots: { index: false }`
    - `app/type/page.tsx` (once Task 2 creates it) contains no `export const metadata` or `export function generateMetadata`
    - `npx playwright test tests/font-cls.spec.ts --project=chromium` exits 0
  </acceptance_criteria>
  <done>`app/layout.tsx` wires both font variables and `robots: { index: false }`; `tests/font-cls.spec.ts` passes, confirming near-zero cumulative layout shift on `/`.</done>
</task>

<task type="auto">
  <name>Task 2: Build the holding page and the /type specimen route; add viewport + specimen tests</name>
  <files>app/page.tsx, app/type/page.tsx, tests/viewport.spec.ts, tests/type-specimen.spec.ts</files>
  <read_first>app/page.tsx (current create-next-app scaffold, to know what is being replaced), app/globals.css (Plan 02 output — exact class names and their `clamp()` bounds), .planning/phases/01-deploy-foundation-design-system/01-UI-SPEC.md (Routes & Layout section, full — "/" and "/type"), .planning/phases/01-deploy-foundation-design-system/01-CONTEXT.md (D-05, D-06)</read_first>
  <action>
    Replace `app/page.tsx` with a server component rendering `<h1 className="text-display">Guillem Gelabert</h1>` followed by `<p className="text-body">Developer.</p>` — no navigation, no links, no CTA, no work list, no backlog (D-06; all explicitly out of this phase's boundary, Phase 3's job).

    Create `app/type/page.tsx`: a server component demonstrating all four roles — one `.text-display` element, one `.text-heading` element, one `.text-label` element (short uppercase caption text), and a `.text-body` block of 2+ paragraphs long enough to show line-height/measure in context (placeholder editorial-register copy is acceptable here — this route is a reference artifact per D-05, not audience-facing copy). Export no `metadata` from this file at all (Pitfall 7 — `noindex` inherits from root via Task 1's export).

    Create `tests/viewport.spec.ts`: for `{width:375,height:812}` and `{width:1440,height:900}`, load `/type`, read `getComputedStyle(...).fontSize` on the `.text-display` and `.text-heading` elements, and assert: at 375px both are near their `clamp()` floor (Display ≈56px, Heading ≈32px, allow ±4px), and at 1440px both are near their `clamp()` ceiling (Display ≈180px, Heading ≈72px, allow ±4px) — proving the scale actually responds to viewport width rather than rendering a fixed size.

    Create `tests/type-specimen.spec.ts`: load `/type`, assert one visible element each with class `text-display`, `text-heading`, `text-body`, `text-label`, and that the `.text-label` element's computed `textTransform` is `uppercase`.
  </action>
  <verify>
    <automated>npx playwright test tests/viewport.spec.ts tests/type-specimen.spec.ts --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - `/` renders exactly the D-06 copy: heading "Guillem Gelabert", body "Developer.", no nav/links/CTA
    - `/type` renders all four type-scale classes with no `metadata` export in the file
    - `npx playwright test tests/viewport.spec.ts --project=chromium` exits 0 at both 375px and 1440px
    - `npx playwright test tests/type-specimen.spec.ts --project=chromium` exits 0
  </acceptance_criteria>
  <done>`/` renders the holding page per D-05/D-06; `/type` renders every declared type role; both automated specs pass at mobile and desktop widths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Visitor's browser → `/` and `/type` | Both are public, unauthenticated, static-content routes — no forms, no query-param handling, no interactive elements |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-01-07 | Information Disclosure | Holding-page copy (`app/page.tsx`) | accept | Copy is name + single role-noun only (D-06) — no PII beyond the visitor's own public identity, which is the point of a personal site. |
| T-01-08 | Tampering (config drift) | `robots` metadata silently un-inheriting on a future route (Pitfall 7) | mitigate | `app/type/page.tsx` exports no `metadata` object at all in this plan — verified by acceptance criterion. Any future page under this phase's routes that adds its own `metadata` must either omit `robots` entirely or explicitly re-declare `{ index: false }`. |
| T-01-09 | N/A this plan (no new HTTP-serving mechanism) | accept | Real HTTP security response headers remain Phase 6's job (BUILD-04); `robots` metadata is a crawl-directive, not a security control, and is not conflated with `next.config.ts` `headers()` here. |
</threat_model>

<verification>
- `npx playwright test tests/font-cls.spec.ts tests/viewport.spec.ts tests/type-specimen.spec.ts --project=chromium` exits 0
- `/` and `/type` both render with `next dev` running locally
</verification>

<success_criteria>
Visitor sees a deliberate, authored typographic system rather than framework-default styling, correctly on both a mobile-width and a desktop-width viewport, with no layout shift as fonts load — all three proven by automated Playwright specs.
</success_criteria>

<output>
Create `.planning/phases/01-deploy-foundation-design-system/01-03-SUMMARY.md` when done
</output>
