---
phase: 02-content-pipeline
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - app/globals.css
  - components/prose.tsx
  - tests/unit/prose-contract.test.ts
  - components/mdx/figure.tsx
  - components/mdx/aside.tsx
  - mdx-components.tsx
  - components/smear-title.tsx
  - components/post-meta.tsx
  - components/language-switch.tsx
autonomous: true
requirements: [WRIT-01, I18N-01]

must_haves:
  truths:
    - "Rendered prose reads Phase 1's four type roles — no fifth size, no third weight, no fourth face"
    - "Code blocks sit on the ink tint with square corners and no theme background of their own"
    - "A blockquote is italic between two hairlines, and an `<em>` inside it renders upright"
    - "A bare Markdown image fails the build with a message pointing the author at `<Figure>`"
    - "A Humane title can carry the scroll trail without turning its page into a Client Component"
    - "The language switch is absent from the DOM entirely when no translation exists"
  artifacts:
    - path: "app/globals.css"
      provides: "--font-mono, the two ink tints, the unlayered .prose-site block, and .text-standfirst"
      contains: ".prose-site"
    - path: "components/prose.tsx"
      provides: "The single typographic wrapper around compiled MDX output"
      exports: ["Prose"]
    - path: "components/smear-title.tsx"
      provides: "The only new client boundary in this phase"
      exports: ["SmearTitle"]
    - path: "components/post-meta.tsx"
      provides: "Date, language switch and dev-only draft marker on one Label-role line"
      exports: ["PostMeta"]
    - path: "components/language-switch.tsx"
      provides: "Target-language text link, rendered only when a translation exists"
      exports: ["LanguageSwitch"]
    - path: "mdx-components.tsx"
      provides: "pre, table and img overrides plus the Figure/Aside component map"
      contains: "Figure"
    - path: "tests/unit/prose-contract.test.ts"
      provides: "Automated UI-SPEC conformance gate over the .prose-site block"
  key_links:
    - from: "components/prose.tsx"
      to: "app/globals.css .prose-site"
      via: "className on the wrapper div"
      pattern: "prose prose-neutral max-w-none prose-site"
    - from: "components/smear-title.tsx"
      to: "components/smear-heading/use-smear-heading.ts"
      via: "useSmearHeading ref attached to the heading element"
      pattern: "useSmearHeading"
    - from: "mdx-components.tsx"
      to: "components/mdx/figure.tsx"
      via: "component map entry consumed by MDX"
      pattern: "Figure"
---

<objective>
Build the prose layer: the `.prose-site` CSS that overrides `@tailwindcss/typography` with Phase
1's type system, the `Prose` wrapper, the two MDX components, and the three small Server/Client
components the post and index templates compose from.

Purpose: this is success criteria 3 and 4 in code. Everything here is a contract the routes in
Plans 04 and 05 consume, so it is built and type-checked before any route exists — the executor
receives the contracts rather than going looking for them.

Output: `app/globals.css` extended additively, `components/prose.tsx`, `components/mdx/figure.tsx`,
`components/mdx/aside.tsx`, an extended `mdx-components.tsx`, `components/smear-title.tsx`,
`components/post-meta.tsx`, `components/language-switch.tsx`, and an automated conformance test
over the CSS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-content-pipeline/02-CONTEXT.md
@.planning/phases/02-content-pipeline/02-RESEARCH.md
@.planning/phases/02-content-pipeline/02-PATTERNS.md
@.planning/phases/02-content-pipeline/02-UI-SPEC.md
</context>

<interfaces>
<!-- From Plan 02, already built. Import these; do not re-derive them. -->

`lib/content.ts` — `Locale`, `PostFrontmatter`, `PostEntry`
`lib/locales.ts` — `formatPostDate(iso, locale)`, `otherLocale(locale)`, `UI[locale].switchLabel`,
`UI[locale].draftMarker`

<!-- Contracts this plan establishes for Plans 04 and 05. -->

`Prose` — `({ children }: { children: React.ReactNode })`, Server Component
`SmearTitle` — `({ as?: "h1" | "h2", className?: string, children })`, Client Component
`PostMeta` — `({ locale, date, switchHref, draft }: { locale: Locale; date: string; switchHref: string | null; draft?: boolean })`, Server Component
`LanguageSwitch` — `({ from, href }: { from: Locale; href: string | null })`, Server Component, returns `null` when `href` is `null`
`Figure` — `({ src, alt, width, height, wide?, children? })`, MDX component
`Aside` — `({ kicker?, children })`, MDX component
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: The .prose-site layer and its conformance test</name>
  <files>app/globals.css, components/prose.tsx, tests/unit/prose-contract.test.ts</files>
  <read_first>
    - app/globals.css (the whole 56-line file: `@theme` on lines 4-19 holds tokens only; the unlayered `.text-*` classes on lines 21-51 are the override mechanism `.prose-site` copies, and lines 21-51 are a locked Phase 1 interface that `tests/viewport.spec.ts` asserts against)
    - .planning/phases/02-content-pipeline/02-UI-SPEC.md § Prose Contract (the element table — this is the source of truth for every value below) and § Spacing Scale (the seven tokens plus the three declared exceptions) and § Typography (the four roles and their variants)
    - .planning/phases/02-content-pipeline/02-RESEARCH.md § Pattern 8 (why unlayered CSS beats the typography plugin without `!important`, with the citation into the plugin source)
    - .planning/phases/02-content-pipeline/02-PATTERNS.md § `app/globals.css` (EDIT) (the two-layer split and the "additive only" rule)
    - tests/viewport.spec.ts (lines 1-30: the spec that will break if lines 21-51 of globals.css are touched)
  </read_first>
  <action>
Edit `app/globals.css` **additively only**. Do not modify lines 21-51 — the `.text-display`,
`.text-heading`, `.text-body` and `.text-label` rules are Phase 1's locked interface and
`tests/viewport.spec.ts` asserts against those exact `clamp()` curves.

Into the existing `@theme` block add three tokens and nothing else:
`--font-mono: var(--font-ibm-plex-mono), ui-monospace, monospace;`,
`--color-surface-code: rgba(0, 0, 0, 0.04);`, `--color-rule: rgba(0, 0, 0, 0.12);`. The two-level
font indirection mirrors Phase 1's `--font-humane` → `--font-display` pattern; UI-SPEC names the
loader variable `--font-mono` directly, but routing it through `--font-ibm-plex-mono` matches the
shipped convention and the rendered result is identical.

Beneath `@theme`, in the same unlayered plain-CSS style as `.text-*`, add the `.prose-site` block.
Unlayered rules beat `@tailwindcss/typography`'s `addComponents` output regardless of specificity,
so **no `!important` is needed anywhere** — if you find yourself reaching for one, the rule is in
the wrong place. Every value must read a `var(--…)` token or be one of the two fixed type sizes.
Write these selectors with these values:

- `.prose-site` — `color: var(--color-ink)`, `max-width: 65ch`
- `.prose-site p` — `var(--font-body)`, `18px`, weight `400`, `line-height: 1.6`, `margin-top: 16px`
- `.prose-site h2` — `var(--font-body)`, `14px`, weight `400`, `line-height: 1.3`, `text-transform: uppercase`, `letter-spacing: 0.04em`, `margin-top: 48px`, `margin-bottom: 16px`, `padding-bottom: 8px`, `border-bottom: 1px solid var(--color-ink)`
- `.prose-site h3` — identical type to `h2` but **no border**, `margin-top: 32px`, `margin-bottom: 8px`. The rule and the 48px-vs-32px top margin are the only things separating the two levels; do not add a size, a weight or a second tracking value
- `.prose-site a` — `color: inherit`, `text-decoration: underline`, `text-decoration-thickness: 1px`, `text-underline-offset: 0.12em`, `text-decoration-color: currentColor`
- `.prose-site a:hover`, `.prose-site a:focus-visible` — `color: var(--color-accent)`, `text-decoration-color: var(--color-accent)`
- `.prose-site a:focus-visible` — `outline: 2px solid var(--color-accent)`, `outline-offset: 2px`
- the link transition — `transition: color 120ms ease-out, text-decoration-color 120ms ease-out` wrapped in `@media (prefers-reduced-motion: no-preference)`. Wrap it at the moment you write it, not later. The colour change itself must survive under `reduce`; only the transition is removed
- `.prose-site strong` — `font-weight: 530`
- `.prose-site em` — `font-style: italic`, `font-weight: 400`
- `.prose-site ul`, `.prose-site ol` — `margin: 24px 0`, `padding-left: 24px`
- `.prose-site li + li` — `margin-top: 24px`; `.prose-site li ul`, `.prose-site li ol` — `margin: 8px 0`; `.prose-site li li + li` — `margin-top: 8px`
- `.prose-site ul > li::marker` — `content: "– "` (en-dash U+2013 plus a space), not a filled disc; `.prose-site ol > li::marker` — `color: var(--color-ink)`
- `.prose-site blockquote` — `font-style: italic`, `font-weight: 400`, `border: 0`, `border-top: 1px solid var(--color-rule)`, `border-bottom: 1px solid var(--color-rule)`, `padding: 24px 0`, `margin: 32px 0`. No left bar, no quotation glyph
- `.prose-site blockquote p:first-of-type::before`, `.prose-site blockquote p:last-of-type::after` — `content: none`. The typography plugin injects curly quotes here; the contract says no quotation glyph
- `.prose-site blockquote em` — `font-style: normal`. Required reset: without it an emphasised phrase inside an already-italic pull quote is invisible
- `.prose-site code` — `var(--font-mono)`, `18px`, `color: var(--color-ink)`, `background: var(--color-surface-code)`, `padding: 2px 4px`, `border-radius: 0`. No syntax colouring on inline code
- `.prose-site code::before`, `.prose-site code::after` — `content: none`. The typography plugin injects literal backticks
- `.prose-site pre` — `var(--font-mono)`, `18px`, `line-height: 1.5`, `color: var(--color-ink)`, `background: var(--color-surface-code)`, `border-radius: 0`, `padding: 16px 24px`, `margin: 32px 0`, `overflow-x: auto`, `white-space: pre`. Code lines must not soft-wrap
- `.prose-site pre code` — `background: none`, `padding: 0`, `font-size: inherit`, `color: inherit`
- `.prose-site .prose-table` — `overflow-x: auto`, `margin: 32px 0` (the scroll wrapper the `table` override adds)
- `.prose-site table` — `border-collapse: collapse`, `width: 100%`, `margin: 0`
- `.prose-site th` — `var(--font-body)`, `14px`, weight `400`, `line-height: 1.3`, uppercase, `letter-spacing: 0.04em`, `text-align: left`, `padding: 8px 16px`, `border-bottom: 1px solid var(--color-ink)`
- `.prose-site td` — `var(--font-body)`, `14px`, weight `400`, `line-height: 1.4`, `text-transform: none`, `letter-spacing: 0`, `padding: 8px 16px`, `border-bottom: 1px solid var(--color-rule)`. No zebra striping, no vertical rules, no outer border
- `.prose-site th`, `.prose-site td` — `font-variant-numeric: tabular-nums`. Column alignment comes from the inline `style="text-align:right"` that `remark-gfm` already emits on aligned columns; do not add a selector for it
- `.prose-site th:first-child`, `.prose-site td:first-child` — `padding-left: 0`; `:last-child` — `padding-right: 0`, so table type aligns with the prose measure
- `.prose-site figure` — `margin: 48px 0`
- `.prose-site figure img` — `display: block`, `width: 100%`, `height: auto`, `border-radius: 0`, `border: 0`, `box-shadow: none`
- `.prose-site figcaption` — `var(--font-body)`, `14px`, weight `400`, `line-height: 1.3`, uppercase, `letter-spacing: 0.04em`, `margin-top: 8px`
- `.prose-site figure[data-wide]` — `width: min(52rem, calc(100vw - 48px))`, `max-width: none`, `margin-left: 50%`, `transform: translateX(-50%)`. The 52rem wide measure applies to `<Figure wide>` only; `<pre>` and tables stay on the reading measure and scroll internally, which UI-SPEC permits ("may widen") and which keeps the 375px overflow behaviour the one it explicitly accepts
- `.prose-site aside` — `border-left: 4px solid var(--color-ink)`, `padding-left: 24px`, `margin: 32px 0`, `background: none`. The only left-bar element
- `.prose-site aside > .aside-kicker` — `var(--font-body)`, `14px`, weight `400`, `line-height: 1.3`, uppercase, `letter-spacing: 0.04em`, `margin: 0`
- `.prose-site hr` — `border: 0`, `border-top: 1px solid var(--color-rule)`, `margin: 48px 0`

Add one further unlayered class **outside** the `.prose-site` block, beside the `.text-*` roles:
`.text-standfirst` — `font-family: var(--font-body)`, `font-size: 18px`, `font-weight: 530`,
`line-height: 1.5`. The standfirst is emitted by the page templates above the `<Prose>` boundary,
so it cannot live inside `.prose-site`. It is a *variant* of the Body role, not a fifth type
role: same face, same 18px size, and weight 530 is already in the system. Do not give it 24px, and
do not express it as a Tailwind utility stack on the element.

Do not style `h4`, `h5` or `h6` — the contract stops at `h3`. Do not add a rounded corner
anywhere. Do not add a third ink tint or a muted text colour.

Create `components/prose.tsx` as a Server Component (no `"use client"`) exporting `Prose`,
rendering `<div className="prose prose-neutral max-w-none prose-site">{children}</div>`. The
plugin stays as the element-selection engine; `.prose-site` supplies every visible value, and
`max-w-none` cancels the plugin's own measure so `.prose-site`'s `65ch` governs.

Create `tests/unit/prose-contract.test.ts` — a `node:test` suite that reads `app/globals.css` from
disk and enforces the contract as source assertions. Open with the mandatory
`// Covers WRIT-01 (SC3, SC4): …` header block. Extract every rule block whose selector starts
with `.prose-site` and assert:
(a) the only `font-size` values used are `14px`, `18px` and `inherit` — a fifth size anywhere in
that block is a UI-SPEC violation, and this test is the gate that catches it;
(b) the only `font-weight` values are `400`, `530` and `inherit`;
(c) the only `letter-spacing` values are `0.04em` and `0`;
(d) every `border-radius` value is `0`;
(e) the string `!important` does not appear anywhere in the file;
(f) each of these selectors is present: `.prose-site p`, `.prose-site h2`, `.prose-site h3`,
`.prose-site a`, `.prose-site strong`, `.prose-site em`, `.prose-site blockquote`,
`.prose-site blockquote em`, `.prose-site code`, `.prose-site pre`, `.prose-site table`,
`.prose-site th`, `.prose-site td`, `.prose-site figure`, `.prose-site figcaption`,
`.prose-site aside`, `.prose-site hr`, and `.text-standfirst`;
(g) `h4`, `h5` and `h6` are not styled under `.prose-site`;
(h) `--font-mono`, `--color-surface-code` and `--color-rule` are defined inside `@theme`;
(i) `.text-standfirst` declares `font-size: 18px` and `font-weight: 530` and no other size;
(j) the two Phase 1 `clamp()` curves `clamp(3.5rem, 1.5rem + 8vw, 11.25rem)` and
`clamp(2rem, 1rem + 4vw, 4.5rem)` each still appear exactly once, proving the edit was additive.
Strip CSS comments before matching so a comment cannot satisfy or break an assertion.
  </action>
  <verify>
    <automated>npm run test:unit && npx tsc --noEmit && rm -rf .next && npm run build && npx playwright test</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:unit` exits 0 with all three unit suites passing, including every assertion in (a)-(j)
    - `grep -c '.text-standfirst' app/globals.css` is 1
    - `grep -c '!important' app/globals.css` is 0
    - `grep -c 'border-radius' app/globals.css` is at least 3 and every occurrence is followed by `0`
    - `grep -c 'prefers-reduced-motion: no-preference' app/globals.css` is 1
    - `grep -c 'prose prose-neutral max-w-none prose-site' components/prose.tsx` is 1
    - `grep -c '"use client"' components/prose.tsx` is 0
    - `npx playwright test` still reports 9 passed — proving the Phase 1 type scale was not disturbed
    - `rm -rf .next && npm run build` exits 0
  </acceptance_criteria>
  <done>The Prose Contract exists as unlayered CSS with no `!important`, the `Prose` wrapper applies it, and an automated test fails the build if a fifth type size, a third weight, a rounded corner or an `!important` ever appears in that block.</done>
</task>

<task type="auto">
  <name>Task 2: MDX components — Figure, Aside, the table wrapper, and the bare-image guard</name>
  <files>components/mdx/figure.tsx, components/mdx/aside.tsx, mdx-components.tsx</files>
  <read_first>
    - mdx-components.tsx (the file being extended — the `pre` override written in Plan 01 must survive unchanged)
    - .planning/phases/02-content-pipeline/02-UI-SPEC.md § Prose Contract, the `<img>`/`<Figure>`, `<Aside>` and `<table>` rows
    - .planning/phases/02-content-pipeline/02-UI-SPEC.md § Component inventory for this phase (Figure and Aside are the entire shipped MDX component map)
    - .planning/phases/02-content-pipeline/02-RESEARCH.md § Pattern 9 (the verified `pre` override) and § Open Questions item 3 (why bare Markdown images are forbidden in v1)
    - app/type/page.tsx (lines 12-24: the repo's convention of applying type roles as bare CSS classes, never as Tailwind utility stacks)
  </read_first>
  <action>
Create `components/mdx/figure.tsx` — a Server Component exporting `Figure` with props
`src: string`, `alt: string`, `width: number`, `height: number`, `wide?: boolean`, and optional
`children` used as the caption. Render a `<figure>` carrying `data-wide` (present when `wide` is
true, absent otherwise — use `undefined`, not `false`, so the attribute is omitted), containing a
plain `<img>` with `src`, `alt`, the explicit `width` and `height` attributes,
`loading="lazy"` and `decoding="async"`, followed by a `<figcaption>` only when `children` is
present.

Use a plain `<img>`, not `next/image`. Rationale to write into the file as a comment: production
image optimization in Next 16 requires `sharp` as a runtime dependency, and this phase's stated
outcome is zero new runtime dependencies. Explicit `width`/`height` attributes plus
`height: auto` in `.prose-site` give the browser an intrinsic aspect ratio and reserve the space
before load, which is what BUILD-06's no-layout-shift posture actually requires. Add a targeted
`// eslint-disable-next-line @next/next/no-img-element` with that rationale rather than disabling
the rule project-wide.

Create `components/mdx/aside.tsx` — a Server Component exporting `Aside` with props
`kicker?: string` and `children`. Render an `<aside>` containing, when `kicker` is present, a
`<p className="aside-kicker text-label">` holding it, followed by `children`. No fill, no icon, no
rounded corner; the 4px left bar comes from `.prose-site aside`.

Extend `mdx-components.tsx`'s component map with four entries, leaving the `pre` override from
Plan 01 exactly as written:

- `Figure` and `Aside`, imported through the `@/` alias. These two are the entire shipped MDX
  component map. D-08's "MDX may import arbitrary React components" stays true — an author can
  `import` any component inside an `.mdx` file — but the default prose path requires none, so
  reaching for React in Phase 4 is a deliberate act rather than the baseline.
- `table` — wrap the incoming `<table>` in `<div className="prose-table">` so it can scroll
  horizontally without the page doing so. Spread props onto the inner `<table>`.
- `img` — a function that **throws** an `Error` reading
  `Bare Markdown images are not supported. Use <Figure src alt width height /> so the image reserves its space before it loads (BUILD-06).`
  This runs at prerender, so a bare `![](…)` fails `next build` rather than shipping a silent
  layout shift. Note in a comment that v2's archive migration is where this is revisited, since
  the legacy posts are full of bare Markdown images and will want a build-time dimension probe.
  </action>
  <verify>
    <automated>npx tsc --noEmit && rm -rf .next && npm run build && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'data-wide' components/mdx/figure.tsx` is at least 1
    - `grep -c 'next/image' components/mdx/figure.tsx` is 0 and `grep -c 'loading="lazy"' components/mdx/figure.tsx` is 1
    - `grep -c 'width' components/mdx/figure.tsx` is at least 2 and `grep -c 'height' components/mdx/figure.tsx` is at least 2
    - `grep -c 'aside-kicker' components/mdx/aside.tsx` is 1
    - `grep -v '^\s*//' mdx-components.tsx | grep -c 'prose-table'` is 1
    - `grep -v '^\s*//' mdx-components.tsx | grep -c 'throw new Error'` is 1
    - `grep -v '^\s*//' mdx-components.tsx | grep -c 'aria-label="Code sample"'` is still 1 (the Plan 01 `pre` override survived)
    - `grep -c '"use client"' components/mdx/figure.tsx components/mdx/aside.tsx` reports 0 for both
    - `rm -rf .next && npm run build` exits 0 and `npm run lint` exits 0
  </acceptance_criteria>
  <done>The two MDX components exist, tables scroll inside their own wrapper, and a bare Markdown image is a build failure with an actionable message instead of a silent CLS regression.</done>
</task>

<task type="auto">
  <name>Task 3: SmearTitle, PostMeta and LanguageSwitch</name>
  <files>components/smear-title.tsx, components/post-meta.tsx, components/language-switch.tsx</files>
  <read_first>
    - app/page.tsx — now `app/(en)/page.tsx` (lines 1-16: the repo's only client boundary, and precisely the shape to relocate rather than repeat)
    - components/smear-heading/use-smear-heading.ts (lines 1-45: the hook's contract and its header comment stating that measurement is deferred to `document.fonts.ready` and no inline `text-shadow` is ever set from server markup, which is what makes this boundary safe across RSC)
    - components/smear-heading/smear-heading-provider.tsx (lines 14-18 for the locked constants, and 40-60 for the error thrown when the provider is absent)
    - lib/locales.ts (`formatPostDate`, `UI[locale].switchLabel`, `UI[locale].draftMarker`)
    - .planning/phases/02-content-pipeline/02-UI-SPEC.md § Motion Contract → "The heading trail — where it applies on writing surfaces" (the table of which titles carry the trail) and § The language switcher and § Date formats
    - .planning/phases/02-content-pipeline/02-RESEARCH.md § Pattern 5 (the verified `SmearTitle`) and § Code Examples → "The language switcher"
  </read_first>
  <action>
Create `components/smear-title.tsx` — the **only** new file in this phase that may carry
`"use client"`. Export `SmearTitle` taking `as?: "h1" | "h2"` (defaulting to `h1`),
`className?: string` and `children`. Call `useSmearHeading<HTMLHeadingElement>()` and attach the
returned ref to the rendered tag. That is the whole component.

The point of this file is what it prevents: Phase 1 put `"use client"` on `app/page.tsx` and
STATE.md records that as cost-free there because no server-only data fetching existed. On a post
route it is not cost-free — it would push the compiled MDX module, its component imports, every
Shiki token span and `fs` into the client bundle. Do not convert any route file to a Client
Component to reach the hook. Do not re-implement the hook, do not add a second
`requestAnimationFrame` driver, and do not retune `MAX_TRAIL`, `MAX_SHADOWS`, `SCROLL_STOP_DELAY`,
`HUE_SPEED` or `INITIAL_HUE`. Do not touch `components/smear-heading/` at all.

Create `components/language-switch.tsx` — a Server Component exporting `LanguageSwitch` with props
`from: Locale` and `href: string | null`. When `href` is `null` it returns `null`: the control is
**absent from the DOM entirely**, not greyed out, not `aria-disabled`, not a tooltip (D-07 — a
dead affordance advertises something that does not exist and is a known screen-reader trap).
Otherwise render a `next/link` `<Link>` with `className="text-label"`, `hrefLang` set to the
*target* locale (the opposite of `from`), and `UI[from].switchLabel` as its text — which is
written in the target language, so the English page carries the German sentence. No flag, no globe
icon, no `EN | DE` toggle, no dropdown.

Create `components/post-meta.tsx` — a Server Component exporting `PostMeta` with props
`locale: Locale`, `date: string` (the ISO front-matter value), `switchHref: string | null` and
`draft?: boolean`. Render a single `<p className="text-label">` containing, in order: a
`<time dateTime={date}>` holding `formatPostDate(date, locale)`; then, when `switchHref` is
non-null, a ` · ` separator followed by `<LanguageSwitch from={locale} href={switchHref} />`;
then, when `draft` is true **and** `process.env.NODE_ENV === "development"`, a ` · ` separator
followed by `UI[locale].draftMarker`. The draft marker renders at full ink in the Label role and
is never accent-coloured — the accent reservation is closed to focus rings and link states only.
It must never appear in a production build.

All three files use double quotes and semicolons, kebab-case filenames with PascalCase named
exports, and apply type roles as bare CSS classes (`text-label`), never as Tailwind utility stacks.
  </action>
  <verify>
    <automated>npx tsc --noEmit && rm -rf .next && npm run build && npx playwright test && npm run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"use client"' components/smear-title.tsx` is 1
    - `grep -c '"use client"' components/post-meta.tsx components/language-switch.tsx` reports 0 for both
    - `grep -c 'useSmearHeading' components/smear-title.tsx` is 1
    - `git diff --stat HEAD -- components/smear-heading/` is empty (the shipped trail system was not touched)
    - `grep -v '^\s*//' components/language-switch.tsx | grep -c 'return null'` is at least 1
    - `grep -c 'aria-disabled' components/language-switch.tsx` is 0
    - `grep -c 'hrefLang' components/language-switch.tsx` is 1
    - `grep -v '^\s*//' components/post-meta.tsx | grep -c 'dateTime'` is 1
    - `grep -v '^\s*//' components/post-meta.tsx | grep -c 'NODE_ENV === "development"'` is 1
    - `rm -rf .next && npm run build` exits 0 and `npx playwright test` still reports 9 passed
  </acceptance_criteria>
  <done>A Humane title can carry the trail from inside a Server-Component page, the meta line renders a localised `<time>` plus a target-language switch, and the switch simply does not exist when there is nothing to switch to.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MDX author → rendered DOM | Component props (`src`, `alt`, `kicker`) become attributes and text in prerendered HTML |
| Server Component tree → client bundle | The `SmearTitle` boundary decides how much of the page ships to the browser |
| `NODE_ENV` → visible chrome | The draft marker must never reach a production render |

ASVS V2, V3, V6 and V13 remain inapplicable. V7 applies only in the negative: UI-SPEC is explicit
that there is **no runtime error state** in this phase — a malformed post or a broken MDX import
fails `next build`, not a visitor's request — so no error boundary may be added that would swallow
one.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Information disclosure | `"use client"` creeping onto a route file to reach `useSmearHeading` | mitigate | The trail is reachable only through `components/smear-title.tsx`; every other new component and every route file stays server-side. Task 3's acceptance criteria assert the directive appears in exactly one new file. A wrongly-placed directive would ship the compiled MDX module and `fs`-adjacent code to the browser. |
| T-02-12 | Information disclosure | The dev-only draft marker rendering in production | mitigate | Gated on `process.env.NODE_ENV === "development"` inside `PostMeta`, the same predicate `isVisible` uses. Plan 06's production build-output test asserts the string is absent from prerendered HTML. |
| T-02-13 | Tampering | Bare Markdown `![](…)` bypassing the space-reservation rule | mitigate | The `img` override throws at prerender with an actionable message. Forbidding it in v1 (rather than probing dimensions at build time) also avoids adding `image-size`, an unexercised dependency. Revisit in v2 when the archive lands. |
| T-02-14 | Availability | `remark-gfm`'s inline `style="text-align:right"` on aligned table cells vs a future CSP `style-src` | transfer | Same class as Shiki's inline token styles (T-02-04). Recorded for Phase 6's BUILD-04; both are inline style *attributes*, so one decision covers both. |
| T-02-15 | Elevation of privilege | An MDX author importing an arbitrary component (D-08) | accept | Deliberate and locked: content is repo-committed and code-reviewed (D-11), and the shipped map is only `Figure` and `Aside` so the default prose path needs no React at all. Never render MDX from a fetched or user-supplied source. |
</threat_model>

<verification>
- `npm run test:unit` exits 0 including the new `.prose-site` conformance suite
- `npx tsc --noEmit` exits 0
- `rm -rf .next && npm run build` exits 0
- `npm run lint` exits 0
- `npx playwright test` still reports 9 passed
- `git diff --stat HEAD -- components/smear-heading/` is empty
</verification>

<success_criteria>
The Prose Contract is expressed as unlayered CSS that beats `@tailwindcss/typography` without
`!important` and is machine-checked against UI-SPEC's four-role, two-weight, three-face caps; the
MDX map holds exactly `Figure` and `Aside` plus the `pre`/`table`/`img` overrides; and the trail,
the localised date and the conditional language switch are available to the routes as typed
components.
</success_criteria>

<output>
Create `.planning/phases/02-content-pipeline/02-03-SUMMARY.md` when done
</output>
</content>
