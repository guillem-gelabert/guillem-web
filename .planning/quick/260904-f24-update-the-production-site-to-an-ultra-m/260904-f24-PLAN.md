---
quick_id: 260904-f24
description: Update the production site to an ultra-minimal page with name, descriptor, two described project links, and an obfuscated email link; preserve a Railway staging deployment
date: 2026-09-04
files_modified:
  - app/(en)/page.tsx
  - components/contact-block.tsx
  - lib/contact.ts
  - lib/work.ts
  - tests/landing.spec.ts
  - tests/build/prerender.test.ts
autonomous: true
---

# Quick Task Plan

Replace only the English landing route at `/` with the requested concise identity and
work surface. Keep every other route and the `guillem-edge` service untouched. Create
and prove a separate Railway `staging` environment before changing production, deploy
the verified candidate there first, and only then deploy it to the production `web`
service that the existing edge service fronts at `guillemgelabert.com`.

## Task 1: Preserve the current site in a Railway staging environment

**Files:** None (Railway environment state only)

**Action:** Before editing or deploying application code, preflight the authenticated
Railway CLI and resolve the linked `guillem-web` project (`f6be1197-bc33-469e-8f16-d528e44c9f0f`),
production environment (`d6c7e845-df3b-496c-b061-4976fbaf5084`), and `web` service
(`1ee326d6-e9fd-4087-8a01-da28701501a2`) by read-back rather than by name alone. Use
one stable `RAILWAY_AGENT_SESSION` and the `RAILWAY_CALLER=skill:use-railway@1.2.3`
prefix for all Railway CLI calls. If no `staging` environment exists, create it with
`railway environment new staging --duplicate production`; if it already exists, reuse
it only after confirming it belongs to this project. Confirm that the duplicated `web`
service has a successful running deployment of the current production revision. Generate
or read its Railway-provided service domain with `railway domain --service web
--environment staging --json`, and record the returned URL for later smoke tests. Do not
attach either custom domain to staging, do not move or alter `guillemgelabert.com`, and do
not mutate the `guillem-edge` or Postgres services.

**Verify:** `railway environment list --json` contains distinct `production` and
`staging` environment IDs; the staging `web` service reports a successful deployment;
`curl -fsS https://<staging-web-domain>/` returns HTTP 200 before any production change;
the staging domain is a Railway-provided `*.up.railway.app` hostname and no custom domain
is attached to staging.

**Done:** A separately addressable staging deployment exists at a Railway subdomain and
the production environment and edge-owned custom domains remain unchanged.

## Task 2: Replace the homepage and rewrite its contracts around the new surface

**Files:**
- `app/(en)/page.tsx`
- `components/contact-block.tsx`
- `lib/contact.ts`
- `lib/work.ts`
- `tests/landing.spec.ts`
- `tests/build/prerender.test.ts`

**Action:** Set `POSITIONING_PLACEHOLDER` to the exact visible and metadata description
`Data - Visualisation - Journalism`, and set `EMAIL` to the owner-provided address
`gelabert.guillem+github@gmail.com`. Keep `WORK` as the single source for the two existing
titles, annotations, and exact destinations; do not duplicate those strings in the page.
Extract/export the existing entity-encoded mail anchor from `components/contact-block.tsx`
as a focused reusable component, and make `EmailRow` delegate to it so `/cv` keeps the
same behavior. Its HTML may be produced with `dangerouslySetInnerHTML` only from the
trusted `EMAIL` constant/test literals through the existing `entityEncode()` path; do not
accept URL, query, form, or database input, and do not add a second encoder.

Rewrite `app/(en)/page.tsx` as a synchronous Server Component with one semantic `main`:
one `h1` reading `Guillem Gelabert`, one descriptor line, a semantic two-item list whose
linked titles and adjacent short descriptions come from `WORK`, and one visible
obfuscated mail link with no extra visible label. Remove the landing seam, smear title,
featured case study lookup, navigation, backlog, CV/GitHub/LinkedIn affordances, images,
and all other visible homepage copy from the render tree, but delete no route or shared
component merely because `/` no longer imports it. Preserve the route metadata export,
bare site title, canonical `/`, and description-to-visible-copy equality. Use the existing
font roles, link/focus rules, spacing tokens, and responsive Tailwind utilities already
available through `app/globals.css`; do not add global CSS or a dependency for this page.
The layout must have no horizontal overflow at 320px, provide at least 24px link targets,
retain a logical heading/list structure, and work without client JavaScript.

Replace `tests/landing.spec.ts`'s superseded seam/navigation/case-study/backlog contracts
with focused browser assertions for the exact ordered visible text, exactly one `h1`, the
exact descriptor and matching meta description, the two `WORK` destinations and
annotations, the one readable/focusable mail link, same-tab project links, keyboard focus,
24px target floors, and no horizontal overflow at phone and desktop widths. Update every
homepage-specific assertion in `tests/build/prerender.test.ts` instead of merely appending
new tests: bind expected work content to imported `WORK`, require only the two requested
HTTP(S) destinations plus one `mailto:` on `/`, prove the raw prerendered root contains
`&#64;` and `&#46;` but neither the bare address nor `&amp;#`, prove the removed homepage
sections/copy and GitHub link are absent, and retain all non-home-route, metadata,
discoverability, draft, locale, `/cv`, and contact encoding coverage that remains true.

**Verify:** Run `npm run lint -- 'app/(en)/page.tsx' components/contact-block.tsx
lib/contact.ts lib/work.ts tests/landing.spec.ts tests/build/prerender.test.ts`,
`node --test tests/unit/contact.test.ts tests/unit/work.test.ts`,
`npm exec playwright test tests/landing.spec.ts`, then `rm -rf .next && npm run build &&
npm run test:build`, followed by `git diff --check`. The focused suites and clean build
must pass. Do not use the already-known unrelated `npm run test:unit` failures as a reason
to weaken or skip these targeted checks, and do not touch the user's existing
`next-env.d.ts` modification.

**Done:** `/` visibly contains only the requested identity line, descriptor, two linked
works with their existing short descriptions, and one readable entity-obfuscated email
link; its metadata, semantics, responsive behavior, raw HTML, and absence of retired
homepage content are covered, while all other routes still build.

## Task 3: Promote the verified candidate through staging to production

**Files:** None (Railway deployments only)

**Action:** After Task 2 is committed and all checks pass, use explicit project,
environment, and service targets for both deploys. First run `railway up --project
f6be1197-bc33-469e-8f16-d528e44c9f0f --environment staging --service web --ci -m
"Deploy ultra-minimal landing to staging"`; wait for success and exercise the Railway
subdomain with the same content/absence checks as the browser contract. Only after that
passes, run the equivalent `railway up` against `--environment production --service web`
with an auditable production message. Never deploy or reconfigure `guillem-edge`: it
already owns `guillemgelabert.com` and proxies the production `web` service. Read back both
deployment IDs/statuses and domains after mutation. Leave staging running at its Railway
subdomain as the persistent testing environment.

**Verify:** Both targeted `railway deployment list --service web --environment <env>
--limit 1 --json` reads report successful deployments of the verified revision. The
staging Railway URL and `https://guillemgelabert.com/` each return HTTP 200 and contain the
exact name, descriptor, both requested hrefs/descriptions, and entity-encoded mail link;
neither response contains the retired featured-case-study, backlog, navigation, GitHub,
LinkedIn, or placeholder homepage copy. Confirm the two deployment IDs and environment
IDs differ, and that the custom domains remain attached only to the production
`guillem-edge` service.

**Done:** The ultra-minimal page is live on the custom production domain, an independently
deployed copy remains available at the staging Railway subdomain for testing, and no edge
routing or unrelated service was changed.

## Threat Model

| Threat | Disposition | Mitigation |
|---|---|---|
| Email HTML injection through `dangerouslySetInnerHTML` | Mitigate | Keep input restricted to the source-controlled `EMAIL` value/test literals and reuse the single character encoder; raw-build tests reject double escaping and bare `@` output. |
| Reverse tabnabbing from outbound project links | Accept/remove surface | Keep links in the same tab with no `target="_blank"`; tests pin the absence of new-window behavior. |
| Production deployment sent to the wrong Railway resource | Mitigate | Resolve and pass explicit project/environment/service identifiers, stage first, and read back deployment/domain ownership after each mutation. |
| Public staging URL exposes private or secret material | Mitigate | Publish only the same public static surface as production; render no environment variables, credentials, private repository URL, or admin affordance. |

## Source Coverage Audit

| Source | ID | Feature / Requirement | Task | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Ultra-minimal production landing and preserved Railway testing deployment | 1–3 | COVERED | Staging is established first; implementation is verified before promotion. |
| REQ | — | No ROADMAP requirement IDs are assigned to this post-milestone quick task | — | N/A | The user request is the authoritative scope. |
| RESEARCH | — | No research phase for this quick task | — | N/A | Existing project and Next.js/Railway patterns are sufficient. |
| CONTEXT | — | Exact name and `Data - Visualisation - Journalism` descriptor | 2 | COVERED | Visible copy and metadata share the source constant. |
| CONTEXT | — | Mallorca and Watch People Die Live links with short descriptions | 2 | COVERED | Reuses the existing locked `WORK` tuple. |
| CONTEXT | — | Obfuscated owner email link | 2 | COVERED | Reuses the proven entity-encoding path with the supplied address. |
| CONTEXT | — | Homepage visibly contains no other content; other routes remain | 2 | COVERED | Tests assert both absence and continued build reachability. |
| CONTEXT | — | Testing deployment remains at a Railway subdomain | 1, 3 | COVERED | Separate staging environment, staged promotion, persistent domain. |

