---
quick_id: 260903-hb4
description: Set the featured slot in the body serif and drop its meta line
date: 2026-09-03
---

# Quick Task Plan

## Task 1: Move the featured headline to the serif Heading role

**Files:**
- `components/landing/featured-slot.tsx`

**Action:** Swap `.text-heading` for `.text-heading-serif` on both `SmearTitle`
elements — the interim state and the published state. The class already exists
in `app/globals.css`, added by `6cc7c43`, and its own comment names this file
as the intended caller: identical clamp, weight and letter-spacing, body family
instead of Humane, so the case-study headline stops competing with the
nameplate directly above it for the one display voice.

**Verify:** Both states render at the Heading curve in the body family.

**Done:** Neither `SmearTitle` in the file carries `.text-heading`.

## Task 2: Remove PostMeta from the published state

**Files:**
- `components/landing/featured-slot.tsx`

**Action:** Delete the `PostMeta` call and the `gap-md` wrapper it shared with
the standfirst, leaving the standfirst as a direct child so the parent
section's single `gap-lg` stays correct in both states. Drop the now-unused
import. Record in a comment what goes with it — the featured entry's draft
marker — and where the answer still lives.

**Verify:** The landing renders no date and no draft marker in the case-study
section; `/writing` and both `[slug]` templates are untouched.

**Done:** `components/landing/featured-slot.tsx` imports and renders no
`PostMeta`.

## Task 3: Stage the noise tile

**Files:**
- `public/noise.svg`

**Action:** Commit the 200×200 `feTurbulence` fractal-noise tile at 5% alpha.
Nothing references it yet; it is staged for upcoming texture work.

**Done:** The file is tracked.
