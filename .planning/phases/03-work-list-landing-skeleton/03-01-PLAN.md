---
phase: 03-work-list-landing-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/work.ts
  - tests/unit/work.test.ts
  - components/smear-title.tsx
  - lib/locales.ts
  - tests/unit/dates.test.ts
autonomous: true
requirements: [WORK-01, WORK-02, HOME-01]
must_haves:
  truths:
    - "The two work entries, their annotations, their absolute destinations and their host labels exist as typed data — adding a third entry is a change to one array and nothing else"
    - "The English positioning sentence has exactly one statement in the codebase, and it is marked in source as HOME-01 awaiting the user"
    - "A trail-carrying heading can be declared as an <h3> without a type error"
    - "Both locales can render a site-root back link from one shared string"
    - "No annotation names a language, framework, library or technique"
  artifacts:
    - path: "lib/work.ts"
      provides: "WorkEntry type, the two-entry WORK tuple, CASE_STUDY_SLUG, POSITIONING_PLACEHOLDER"
      exports: ["WorkEntry", "WORK", "CASE_STUDY_SLUG", "POSITIONING_PLACEHOLDER"]
      contains: "export const WORK"
    - path: "tests/unit/work.test.ts"
      provides: "tuple shape, absolute-https hrefs, host/href agreement, banned-tool-word absence"
      contains: "node:test"
    - path: "components/smear-title.tsx"
      provides: "as?: \"h1\" | \"h2\" | \"h3\""
      contains: "\"h3\""
    - path: "lib/locales.ts"
      provides: "homeLink in UiCopy and in both UI entries"
      contains: "homeLink"
  key_links:
    - from: "lib/work.ts"
      to: "app/(en)/page.tsx metadata.description and the rendered positioning <p>"
      via: "POSITIONING_PLACEHOLDER — one export, two consumers (Pitfall 6)"
      pattern: "POSITIONING_PLACEHOLDER"
    - from: "lib/work.ts"
      to: "lib/content.ts findBySlug"
      via: "CASE_STUDY_SLUG resolved against publishedFor(\"en\") — derived state, never a flag"
      pattern: "CASE_STUDY_SLUG"
---

<objective>
Ship the phase's data layer and the three one-line primitive amendments every later plan
depends on: `lib/work.ts` (the work-list entries, the case-study slug, the positioning
placeholder), its `node --test` gate, `SmearTitle`'s widened `as` union, and `homeLink` in
`lib/locales.ts`.

Purpose: `D-05` requires work entries to be data, not markup. Pitfall 6 requires the
positioning sentence to have exactly one English statement so `HOME-01` arriving is a
one-line edit. Plans 03, 04 and 05 all block on these four files.
Output: one new data module, one new unit spec, two amended shipped modules, one amended
shipped unit spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-work-list-landing-skeleton/03-UI-SPEC.md
@.planning/phases/03-work-list-landing-skeleton/03-PATTERNS.md
</context>

<interfaces>
<!-- Contracts this plan CREATES. Plans 03, 04 and 05 consume them directly; no exploration needed. -->

lib/work.ts (new):
  export type WorkEntry = { title: string; annotation: string; href: string; host: string }
  export const WORK: readonly [WorkEntry, WorkEntry]
  export const CASE_STUDY_SLUG: string   // "the-chart-therefore-changes"
  export const POSITIONING_PLACEHOLDER: string   // "Developer."

components/smear-title.tsx (amended, shipped today at :5-9):
  type SmearTitleProps = { as?: "h1" | "h2"; className?: string; children: React.ReactNode }
  -> becomes  as?: "h1" | "h2" | "h3"
  export function SmearTitle({ as: Tag = "h1", className, children }): JSX.Element

lib/locales.ts (amended, shipped today at :35-77):
  type UiCopy = { indexKicker; indexDescription; backLink; switchLabel; emptyHeading;
                  emptyBody; notFoundHeading; notFoundBody; draftMarker }  // 9 keys today
  -> gains a 10th key: homeLink
  export const UI: Record<Locale, UiCopy>
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create lib/work.ts — the work entries, the case-study slug and the positioning placeholder</name>
  <files>lib/work.ts</files>
  <read_first>
    - `lib/locales.ts:35-77` — the shipped "local type + single exported const, per-field doc comment" data-module shape this file copies. Note `UiCopy` is NOT exported.
    - `lib/site.ts:1-17` — the repo's "one place a value is written down" register. `POSITIONING_PLACEHOLDER` gets exactly this doc-comment treatment.
    - `lib/content.ts:8-16` — the per-field trailing-comment style for a data type.
    - `lib/content.ts:164-178` — `showDrafts` / `isVisible`. Read to confirm the featured slot's state is derived, so this module carries NO boolean flag.
    - `.planning/phases/03-work-list-landing-skeleton/03-UI-SPEC.md` § *Work list contract* — the two entries, their exact titles, annotations, hrefs and hosts.
  </read_first>
  <action>
    Create `lib/work.ts` following the `lib/locales.ts` module shape: a file-level comment, an
    exported type, then the exported constants. Import order rule does not apply — this module
    imports nothing.

    Export `type WorkEntry` with exactly four `string` fields in this order: `title`,
    `annotation`, `href`, `host`. Unlike `UiCopy` in `lib/locales.ts`, `WorkEntry` IS exported
    (the landing components import it for their props type) — state that deviation in a comment.
    Give each field a trailing comment in the `lib/content.ts:8-16` register: `title` is the
    piece's PUBLISHED headline, never a repo name (D-06); `annotation` is one line saying what
    the piece is ABOUT, never what it was built with (WORK-02, D-09); `href` is the absolute URL
    to the live piece (D-06); `host` is the destination host rendered as the outbound marker.

    Export `const WORK` typed `readonly [WorkEntry, WorkEntry]` (a two-tuple, not `WorkEntry[]`)
    with `as const`. Carry a comment stating that an empty work list is a build error, not a UI
    state (D-03) — that is what the tuple type buys. The two entries, verbatim:

    Entry 1 — title `Everyone in Mallorca Knows It`; annotation `The Balearics stopped gaining
    on Europe in 1993 — while tourist arrivals went on tripling.` (em dash U+2014); href
    `https://ib-gdp.guillemgelabert.com/everyone-in-mallorca-agrees-on-one-thing`; host
    `ib-gdp.guillemgelabert.com`.

    Entry 2 — title `Watch People Die Live`; annotation `Roughly two people die every second:
    where they are, when it happens, and who they were.`; href `https://watchpeopledie.live`;
    host `watchpeopledie.live`.

    Export `const CASE_STUDY_SLUG = "the-chart-therefore-changes"`. Comment it as locked by
    Phase 4 D-15, resolved through `publishedFor("en")` by the featured slot, and note that a
    `null` result IS the interim state — there is no boolean to flip.

    Export `const POSITIONING_PLACEHOLDER = "Developer."` with a `lib/site.ts`-register doc
    comment naming HOME-01 explicitly: it is the user's sentence to write (D-08), it is marked
    HERE in source and never on screen (D-02, Pitfall 7), and it also serves as
    `app/(en)/page.tsx`'s `metadata.description` so the real sentence is a one-line change when
    it arrives (Pitfall 6).

    Do NOT add: a `featured` boolean, a `FEATURED_ENABLED` flag, a `github` field, any
    `ib-gdp-evolution` string (the repo is private, D-06), or any import.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node -e "const w=require('fs').readFileSync('lib/work.ts','utf8'); const n=(s)=>w.split(s).length-1; if(n('ib-gdp-evolution')!==0) throw new Error('private repo name present'); if(n('github.com')!==0) throw new Error('github link present'); for (const k of ['export type WorkEntry','export const WORK','export const CASE_STUDY_SLUG','export const POSITIONING_PLACEHOLDER']) if(!w.includes(k)) throw new Error('missing '+k); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0.
    - `lib/work.ts` exports exactly these four names: `WorkEntry`, `WORK`, `CASE_STUDY_SLUG`, `POSITIONING_PLACEHOLDER`.
    - `WORK` is annotated `readonly [WorkEntry, WorkEntry]` — source contains the substring `readonly [WorkEntry, WorkEntry]`.
    - `CASE_STUDY_SLUG` is exactly `"the-chart-therefore-changes"`.
    - `POSITIONING_PLACEHOLDER` is exactly `"Developer."`.
    - The strings `ib-gdp-evolution` and `github.com` appear zero times in the file.
    - `lib/work.ts` contains no `import` statement.
    - The file contains no boolean export and no occurrence of `NODE_ENV`.
  </acceptance_criteria>
  <done>`lib/work.ts` exists, type-checks, and is the single English statement of the positioning sentence.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/unit/work.test.ts — the WORK-01/WORK-02/D-05 data gate</name>
  <files>tests/unit/work.test.ts</files>
  <read_first>
    - `tests/unit/dates.test.ts:1-20` — the `node:test` header comment register and the `../../lib/*.ts` relative-import form (`@/` is a bundler alias Node cannot resolve).
    - `tests/unit/content.test.ts:1-11, 26-32` — the static-import form and the convention of stating in a comment what is deliberately NOT exercised here.
    - `lib/work.ts` — the module under test (from Task 1).
    - `.planning/phases/03-work-list-landing-skeleton/03-VALIDATION.md` § *Phase requirements → test map* — the four WORK rows this file must satisfy.
  </read_first>
  <action>
    Create `tests/unit/work.test.ts` using `node:test` + `node:assert/strict`, importing from
    `../../lib/work.ts` (relative, with the `.ts` extension — never `@/lib/work`).

    Open with a `// Covers WORK-01 / WORK-02 / D-05:` header comment stating why the assertions
    are shaped the way they are: the banned-tool-word list is an editorial rule (PROJECT.md's
    allocation principle — engineering is demonstrated, never claimed) that would otherwise have
    no gate at all, and the host/href agreement catches a copy-paste that would silently mislabel
    a destination.

    Write these tests:

    (a) `WORK` has exactly 2 entries, and every entry has all four fields as non-empty strings.

    (b) Every `href` parses via `new URL(...)` and its `protocol` is exactly `https:`. Assert on
    the parsed protocol, not on a `startsWith("https://")` string check.

    (c) `new URL(entry.href).hostname === entry.host` for every entry — host and href cannot drift.

    (d) No `href` hostname is or ends with `github.com` — the `ib-gdp-evolution` repo is private
    (D-06). Also assert the literal string `ib-gdp-evolution` appears in no field of any entry.

    (e) Every `annotation` is a single line: it contains no `\n` and no `\r`.

    (f) No annotation names a tool. Build a banned list of single tokens — `React`, `Next`, `D3`,
    `TypeScript`, `JavaScript`, `Svelte`, `WebGL`, `Python` — matched case-insensitively with
    word boundaries (`new RegExp("\\b" + token + "\\b", "iu")`), plus two phrases — `built with`,
    `powered by` — matched as case-insensitive substrings. Assert no match in any `annotation`.
    Include the failure message naming WORK-02 and D-09.

    (g) `CASE_STUDY_SLUG` matches `lib/content.ts`'s `SAFE_SLUG` shape
    `/^[a-z0-9]+(?:-[a-z0-9]+)*$/` — restate the regex literally in this file rather than
    importing it (`SAFE_SLUG` is module-local in `lib/content.ts`), with a comment saying so.

    (h) `POSITIONING_PLACEHOLDER` is a non-empty string containing none of the rendered-marker
    words `TODO`, `placeholder`, `TBD`, `Coming soon`, `Under construction`, `Lorem`
    (case-insensitive) — Pitfall 7: the marker lives in a source comment, never in the value.

    Close with a comment in the `tests/unit/content.test.ts:26-32` register noting that the
    rendered output of these entries is deliberately NOT asserted here — that is
    `tests/landing.spec.ts`'s job in Plan 06.
  </action>
  <verify>
    <automated>node --test tests/unit/work.test.ts && npm run test:unit</automated>
  </verify>
  <acceptance_criteria>
    - `node --test tests/unit/work.test.ts` reports 0 failures and at least 8 passing tests.
    - `npm run test:unit` (which globs `tests/unit/*.test.ts`) reports 0 failures across all four unit files.
    - The spec imports from `../../lib/work.ts` — `grep -c '@/lib/work' tests/unit/work.test.ts` returns 0.
    - Deliberately mutating `WORK[0].annotation` to include the word `React` makes test (f) fail (spot-check by hand, then revert; do not commit the mutation).
  </acceptance_criteria>
  <done>The work-list data has an executable editorial and structural gate that runs in under a second.</done>
</task>

<task type="auto">
  <name>Task 3: Widen SmearTitle's `as` union to h3, add `homeLink` to lib/locales.ts, and grow the dates.test.ts key list to ten</name>
  <files>components/smear-title.tsx, lib/locales.ts, tests/unit/dates.test.ts</files>
  <read_first>
    - `components/smear-title.tsx:5-9` — the props type. The entire change is on line 6.
    - `lib/locales.ts:35-77` — `UiCopy` and both `UI` entries. `:37-43` (`indexDescription`) is the doc-comment register to match.
    - `tests/unit/dates.test.ts:43-62` — the test asserting "all nine copy keys" by name. Adding a key without growing this array leaves it untested.
    - `.planning/phases/03-work-list-landing-skeleton/03-RESEARCH.md` § *`SmearTitle`'s `as` union* — the verified 7-call-site impact analysis.
  </read_first>
  <action>
    Three small edits, one commit each is fine.

    (1) `components/smear-title.tsx` line 6: change `as?: "h1" | "h2";` to
    `as?: "h1" | "h2" | "h3";`. That is the whole change. Do NOT add a prop, a runtime branch, a
    second rAF driver, or any change to `components/smear-heading/`. Research verified all 7
    existing call sites are source-compatible and no test asserts the prop type.

    (2) `lib/locales.ts`: add a `homeLink: string` key to `UiCopy` and a `homeLink` entry to both
    `UI.en` and `UI.de`. The value is identical in both locales: `← Guillem Gelabert` (the arrow
    is the literal character U+2190, the precedent Phase 2 set for `← Writing` — not an icon,
    not an SVG). Because every other key differs between locales, give `homeLink` a doc comment
    in the `indexDescription` register explaining the deviation: it is a proper noun, a translated
    variant would be a different affordance for no gain, and on `/texte` the link carries
    `hrefLang="en"` because the landing genuinely only exists in English (the one declared locale
    crossing, UI-SPEC § *Localisation*).

    (3) `tests/unit/dates.test.ts:43-62`: add `"homeLink"` to the `keys` array and change the
    test title from `all nine copy keys` to `all ten copy keys`. Do not restructure the test.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run test:unit && node -e "const s=require('fs').readFileSync('components/smear-title.tsx','utf8'); if(!s.includes('\"h1\" | \"h2\" | \"h3\"')) throw new Error('as union not widened'); const l=require('fs').readFileSync('lib/locales.ts','utf8'); if((l.split('homeLink').length-1) < 3) throw new Error('homeLink missing from UiCopy or a UI entry'); const d=require('fs').readFileSync('tests/unit/dates.test.ts','utf8'); if(!d.includes('homeLink')) throw new Error('dates.test.ts key list not grown'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 with the widened union in place.
    - `components/smear-title.tsx` contains the exact substring `as?: "h1" | "h2" | "h3";` and the file's line count is unchanged from 25.
    - `git diff --stat HEAD -- components/smear-heading/` is empty — the smear system itself is untouched.
    - `homeLink` appears at least 3 times in `lib/locales.ts` (the `UiCopy` key plus both `UI` entries) and `UI.en.homeLink === UI.de.homeLink === "← Guillem Gelabert"`.
    - `tests/unit/dates.test.ts` names `homeLink` in its keys array and its test title reads `all ten copy keys`.
    - `npm run test:unit` reports 0 failures.
  </acceptance_criteria>
  <done>A trail-carrying `<h3>` type-checks, and both locales share one site-root back-link string with a gate that sees it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo source → build output | The only boundary this plan touches. `lib/work.ts` values are inlined into statically prerendered HTML at build time. No visitor input crosses any boundary in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information disclosure | `lib/work.ts` `href` fields | mitigate | The `ib-gdp-evolution` GitHub repo is private (D-06). Task 2 test (d) asserts no `href` hostname is `github.com` and the literal string `ib-gdp-evolution` appears in no entry field; Task 1's verify greps the source for both. |
| T-03-02 | Information disclosure | `lib/work.ts` outbound `href` values | accept | Both destinations are the user's own published properties, verified HTTP 200 on 2026-08-29 (D-06). A visitor following them sends a `Referer` naming a `noindex` in-progress site. No PII, no credential, no session. |
| T-03-03 | Tampering | `lib/locales.ts`, `components/smear-title.tsx` | mitigate | Both edits are additive and type-gated. `npx tsc --noEmit` plus `npm run test:unit` (including the grown `dates.test.ts` key list) is the gate. `git diff --stat HEAD -- components/smear-heading/` must stay empty. |
| T-03-SC | Tampering | npm/pip/cargo installs | mitigate | **Phase 3 installs zero packages** (`03-RESEARCH.md` § Package Legitimacy Audit; `03-UI-SPEC.md` § Registry Safety). Any `npm install` in this plan is a scope violation, not a dependency discovery. No `[ASSUMED]`/`[SUS]` package exists, so no legitimacy checkpoint is required. |

**ASVS L1 chapters explicitly not applicable to this plan**, stated rather than padded: V2
Authentication and V3 Session Management (no accounts, no login, no session), V6 Cryptography
(no secrets in application code), V13 API (no API). V5 Input Validation is applicable in
principle but has no surface here — this plan accepts zero visitor input: no forms, no query
params, no route params, no `searchParams`.
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npm run test:unit` reports 0 failures across `content.test.ts`, `dates.test.ts`, `prose-contract.test.ts`, `work.test.ts`.
- `npm run lint` shows no NEW errors beyond the one known deferred finding in `components/smear-heading/use-prefers-reduced-motion.ts:23` (see `.planning/phases/02-content-pipeline/deferred-items.md`). Never write a criterion of "0 errors".
- `git grep -n 'Developer\.'` returns `app/(en)/layout.tsx:12` and `lib/work.ts` only — the rendered `<p>` in `app/(en)/page.tsx:13` is removed by Plan 03, which closes Pitfall 6.
</verification>

<success_criteria>
- `lib/work.ts` exports `WorkEntry`, a `readonly [WorkEntry, WorkEntry]` tuple, `CASE_STUDY_SLUG` and `POSITIONING_PLACEHOLDER`, with no boolean flag anywhere.
- `tests/unit/work.test.ts` gates tuple shape, https-absolute hrefs, host/href agreement, the private-repo absence rule, single-line annotations and the banned-tool-word list.
- `SmearTitle` accepts `as="h3"`; `components/smear-heading/` is byte-identical.
- `lib/locales.ts` carries `homeLink` in `UiCopy` and both locales, and `tests/unit/dates.test.ts` asserts ten keys.
</success_criteria>

<output>
Create `.planning/phases/03-work-list-landing-skeleton/03-01-SUMMARY.md` when done.
</output>
